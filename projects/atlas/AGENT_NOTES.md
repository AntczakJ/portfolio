# atlas — Agent Notes

> Append-only cross-agent context. Every subagent reads on start, appends on end.

## Gotchas

- **Keyless map is a HARD GATE (ADR-001, ADR-006).** The committed repo MUST render the map with NO paid secret. A fresh checkout with no `.env` renders the keyless fallback basemap and the fleet on it. Any tile/style key is an OPTIONAL env var (`NEXT_PUBLIC_MAP_TILE_KEY` or equivalent) documented in `.env.example`, never committed in `.env`, and only enriches the basemap when supplied. Do NOT introduce Mapbox GL JS (paid token + non-open license) — MapLibre GL JS only. Verify the keyless render under a prod build before claiming it works.
- **MapLibre + strict CSP (ADR-006, the meld/razors-edge/pulse lesson).** MapLibre uses a Web Worker for tile parsing, so the CSP must allow `worker-src 'self' blob:` (and `blob:` where the worker is created from a blob, plus the tile host in `connect-src`/`img-src` for raster). This must be done WITHOUT opening `unsafe-eval`. Verify the exact policy against a `next build && next start` PROD build, not `next dev` (HMR uses runtime eval that a no-`unsafe-eval` policy flags — a dev-only artifact). Also watch faker (bake seed to a static module so it is build-only) and Zod 4's JIT probe (`z.config({ jitless: true })` as a global early side-effect) — both bite the runtime-validation paths (the WS frame validation validates at runtime here, so jitless Zod matters).
- **The streaming + interpolation surface is NOT Motion (ADR-001).** Marker interpolation between authoritative ticks is a hand-rolled `requestAnimationFrame` tween driving MapLibre's imperative API OFF the React render path (an off-render store — Zustand/ref — holding last+next positions, NOT React state per tick per marker). Camera fly-to is MapLibre native (`flyTo`/`easeTo`). Trails/zone-pulse are MapLibre paint updates. Motion is for React-state UI chrome ONLY (detail-panel slide, events-feed `AnimatePresence`, fleet selection, toasts, theme crossfade). Per-marker per-frame animation through Motion/React state is a frame-budget disaster — the exact mistake pulse's "streaming surface is not Motion" and tape's "Canvas2D is not Motion" rules guard against. Do not propose a Motion-driven live map in review; it is considered and rejected.
- **Server-authoritative sim, client-interpolated motion — the credibility line.** Positions/headings/speeds/ETAs/route-progress/geofence-transitions are computed SERVER-SIDE by the seeded engine. The on-screen smoothness is interpolation between authoritative 1 Hz (or whatever ADR-003 picks) ticks, NOT faked client motion. Document this honestly. A faked "live" marker that moves without server backing would gut the showcase the way a faked probe would gut pulse.
- **The simulation engine is a PURE tick reducer (ADR-002).** `tick(state, dt) -> { state, events }`, no wall-clock inside, time supplied. This is what makes it deterministic (same seed + tick count → same world), unit-testable, and seekable (replay to tick T → reproducible wow beats). Do NOT put `Date.now()` or IO inside the reducer; the IO shell (loop/worker) owns time and side effects.
- **Geofence detection must debounce (ADR-004).** Point-in-polygon transition tracking needs hysteresis so a vehicle skimming a boundary does not flap enter/exit/enter. One `enter` per genuine crossing, one `exit` later. This is a unit-tested invariant (a boundary-skimming fixture must not spawn duplicate events).
- **WS frame contract is the FE/BE integration boundary (conventions § 5).** Zod schemas in `src/lib/schemas/ws/` are the single source for the snapshot frame, the tick/delta frame, event frames, and client→server control frames. Both the Fastify gateway and the Next WS client import from the same file (types-only on the web side — mirror the pulse/tape `exports` pattern). Validate frames at the boundary (drop malformed control frames; never trust client input — the WS control channel is an input surface, Zod-validated AND rate-limited).
- **Port hygiene (confirm on scaffold).** Proposed: Fastify server **3090**, Next web **3091**, Postgres **5438**. Non-colliding with tape (3000-3002/3055-3061, pg 5435), meld (3002, pg 5436), razors-edge (3070), pulse (api 3080 / web 3081 / pg 5437 / redis 6381), Mila (pg 5434). Record the final picks here on scaffold. No Redis is planned (no queue) — if ADR-007 introduces one, pick a non-colliding port (6379/6380/6381 taken-or-system).
- **Node 22 LTS pin — `.nvmrc` committed.** Fastify / `@fastify/websocket` (`ws`) / Drizzle (`postgres-js`) / turf are best-paved on Node. Do not bump to Bun (Elysia/Bun is tape's slot; Atlas's budget goes to the engine + stream + interpolation, not runtime compat).

## Decisions to revisit

These are assumptions the planner left implicit or under-determined in PLAN.md / ADR-001 that downstream agents (especially the architect in Phase 0) should actively challenge rather than inherit:

- **Tick cadence + encoding (ADR-003).** PLAN.md assumes ~1 Hz authoritative ticks with 60 fps client interpolation. The architect should pin the actual tick rate against the fleet size and the smoothness/bandwidth trade-off, and decide JSON vs a compact binary/MessagePack tick (binary is more production-faithful and lighter for the per-tick broadcast, but JSON is simpler and the fleet is small — weigh against the "high-frequency telemetry" framing in the WS-vs-SSE justification). If the cadence ends up much faster than 1 Hz, the interpolation story changes (less to interpolate) — keep the 1 Hz-data / 60 fps-motion gap as the wow, which argues for a moderate, not high, tick rate.
- **In-process engine loop vs split worker (ADR-002 + ADR-007).** PLAN.md leaves the topology to the architect (the pure-reducer boundary makes it orthogonal to the domain logic). A split worker is more production-faithful (mirrors pulse's web/worker split) but adds deploy surface and needs a worker→WS bridge (pulse used Redis pub/sub; Atlas has no Redis planned — an in-process loop avoids that entirely). Weigh: in-process is simpler and Redis-free; split is the stronger systems signal but reintroduces a broker. Decide ADR-002 and ADR-007 together.
- **PostGIS vs plain Postgres (ADR-005).** PLAN.md recommends plain Postgres + `jsonb` GeoJSON + turf-in-engine for deploy simplicity, with PostGIS as v2. But a "real geo DB" (PostGIS, spatial indexes, real spatial queries) is itself a wow for a geospatial product and a strong backend signal. The architect should explicitly weigh "deploy-lean + turf-in-engine" against "PostGIS earns a genuine spatial-systems wow" — and if PostGIS is adopted, find a real query for it to do (server-side viewport culling via a spatial index, vehicles-within-radius) so it is not decorative. Default leans lean (matches the Fly posture); overturn only if a prototype shows it earns its keep.
- **Keyless tile source shape (ADR-006).** PLAN.md offers keyless raster (zero-dep floor, but no bearing-rotation, less control-room) vs self-hosted Protomaps `.pmtiles` (best look, fully keyless, more setup) vs an optional free-tier key (simpler, depends on the owner supplying a key). The architect prototypes and pins. The keyless FALLBACK is mandatory regardless; the question is whether the default keyless path is raster or self-hosted vector. Self-hosted Protomaps is the planner's lean (premium look + truly keyless + no third-party at demo time), but it adds a tile-serving concern on Fly — weigh it.
- **MapLibre marker rendering approach (ADR-006).** HTML markers (easy, but DOM cost + harder to rotate smoothly at scale) vs a GeoJSON symbol layer (GPU-rendered, rotatable via `icon-rotate`, scales to many vehicles, but updated via `setData`) vs a custom WebGL layer (max control, most code). The smooth-interpolation requirement and the fleet size (~12–30) push toward the GeoJSON symbol layer updated off the render path, but the architect should prototype the smoothness of each against the rAF tween before committing.
- **Replay/seek vs live-only demo affordance (ADR-002, Task 5.4).** PLAN.md offers two shapes for the reproducible wow beat: (a) simply focus a vehicle approaching a zone (live, no scrub), or (b) a scrub/seek-to-tick replay control leveraging the deterministic engine. Replay is the stronger systems flex (it proves determinism) but is more UI + WS-contract surface (the client must drive the engine to tick T and the snapshot must reconstruct that state). Decide whether v1 ships the full scrub or just a curated "watch this delivery" focus that happens to approach a zone. Do not let a full replay UI balloon scope if a focus affordance delivers the beat.
- **Demo city + route authoring (ADR-005, Task 3.3).** PLAN.md says "one demo city" with ~6–12 curated routes and ~4–8 zones, but does not pick the city or how routes are authored (hand-drawn polylines vs extracted from OSM). The keyless tiles must cover the chosen city. Routes are predefined polylines (no routing engine in v1, see out-of-scope) — decide whether they are hand-authored GeoJSON (full control, looks deliberate) or sampled from real road geometry (more realistic, more setup). Document the choice + the seed.
- **Accessibility of a WebGL map — the non-map table is first-class, not a stub.** The fleet table (the no-WebGL fallback) IS the accessible keyboard/SR path and must be built first-class (Task 5.1 builds it early for exactly this reason). The architect/frontend-engineer must not treat it as an afterthought — it is graded in its own right by the designer-critic and asserted by the test-engineer. Do not ship a map that has no keyboard/SR equivalent.
- **WS over the Fly edge (ADR-007).** Long-lived WebSocket + the Fly edge idle timeout needs a heartbeat (the pulse SSE-heartbeat precedent). Also: a single live engine means a single source of truth — v1 likely runs single-instance (or sticky) so every client sees the same fleet; multi-instance would need the engine state shared (a broker), which argues again for in-process + single-instance v1. Pin the warm-floor posture so the moving fleet does not cold-start in front of a recruiter (tape/pulse `min_machines_running=1` / `auto_stop=off` precedent).

## Cross-cutting concerns

- **Sovereign design tokens — NO reuse from tape, meld, razors-edge, pulse, or apex** (`docs/conventions.md` § 14, ADR-001). Atlas is a fresh operations/control-room identity — a dark control-room register and a clean light register, BOTH intentional, with **matching dark + light MapLibre basemap styles** (the theme toggle switches the map style, not just the chrome). Do NOT import tape's slate/cyan, meld's warm-paper/violet, razors-edge's brass/charcoal, pulse's indigo clean-SaaS, or apex's palette. Build Atlas's palette from scratch and document it here at the design-system task. The control-room register (dense, dark, deliberate, data-forward) is judged against Vercel + Linear.
- **Dark + light both intentional** (CLAUDE.md § 4). Ship both via CSS variables + `next-themes`, respect `prefers-color-scheme`, no FOUC. The map basemap has a dark and a light style; both are reviewed in their own right.
- **The map is genuinely pushed over WebSocket, not polled.** DevTools must show exactly ONE WebSocket connection carrying telemetry frames; no polling XHR loop drives the fleet. This is a success criterion and the proof viewer 1 looks for. Do not "simplify" to a TanStack Query polling interval — the WS push + the rAF interpolation IS the showcase.
- **Performance budget is sacred** on two axes: the SEO-bearing landing/public page (Lighthouse ≥ 95 all four categories, SSR/static first paint, no client-only data dependency) AND the live map's frame budget (60 fps interpolation, off-render-path, coalesce-to-latest-tick backpressure). The map dashboard is documented exempt from the Lighthouse-SEO target (client-only live-data first paint), but it must still feel fast and never jank.
- **Security posture.** CSP strict, no `unsafe-eval`, with the MapLibre `worker-src 'self' blob:` + tile-host allowances. Rate limit the public WS (connection rate + per-connection message rate via `@fastify/rate-limit` or equivalent) and the public REST read endpoints. All input (WS control frames, REST) validated by shared Zod schemas (`src/lib/schemas/`, the single source imported by both `server/` and `web/`). `.env.example` committed (never `.env`). NOTE: Atlas has NO user-supplied-URL / SSRF surface (unlike pulse — the sim targets nothing external); the input surface is the WS control channel, which is Zod-validated + rate-limited.
- **Accessibility: status is never color-alone.** Every vehicle status carries a text label / icon and an accessible name. Map markers have accessible names. The non-map fleet table is the first-class keyboard/SR path. `prefers-reduced-motion` collapses the interpolation tween (markers snap to ticks), the camera fly-to (cut), trails, and zone pulse to instant — the fleet stays fully legible and live. Live event announcements go to an `aria-live="polite"` region without flooding. WCAG 2.2 AA, contrast ≥ 4.5:1. MapLibre keyboard nav enabled.
- **Determinism for tests + screenshots.** `faker.seed(<n>)` for the fleet/route assignment + a fixed tick `dt` + a deterministic speed-modulation fn → a reproducible world, shared by the seed generator, the engine, and the Playwright/Lighthouse runs (same discipline as tape/meld/razors-edge/pulse). Tests that assert on "live" behaviour drive the deterministic engine (seek to a known tick / focus a known approaching vehicle), NEVER wait on a fixed sleep — the geofence beat is reproducible because the engine is deterministic.
- **English-only in all files** (source, comments, ADRs, README, UI copy). Polish is for owner chat only (CLAUDE.md § 2).
- **No emojis anywhere** (source, commits, README, UI). Icons from `lucide-react` / `phosphor-icons` / `heroicons` (CLAUDE.md § 2).

## References

- **PLAN.md** — the full surface/flow spec, the wow + mandatory fallback, the simulation/mock-data shape, the phased task list (Phase 0–9), success criteria, out-of-scope, and the deferred-to-architect ADR list.
- **DECISIONS.md ADR-001** — api-heavy + Fastify + `@fastify/websocket` + MapLibre-keyless + turf/plain-Postgres + the pure-reducer sim + rAF-interpolation/Motion split + sovereign tokens ratified; ADR-002..007 deferred to the architect with scoped boundaries.
- **`docs/conventions.md`** — § 5 (shared Zod schemas as the contract), § 6 (seeded determinism), § 10 (api-heavy triggers — three fire), § 11 (backend choice — Fastify is the unused framework), § 12 (portfolio composition — Atlas completes the four-backend story), § 14 (sovereign tokens, no reuse), § 15 (single declarative animation library — Motion; the map motion is hand-rolled and outside it).
- **`docs/inspirations.md`** — Linear (alive / micro-interaction + easing register), Vercel (dense-dark control-room restraint), Stripe (premium finish) — the references the designer-critic gates the map, panels, and events feed against (at least two by name, zero pochwał).
- **pulse `AGENT_NOTES.md` + `DECISIONS.md`** — the closest in-repo analogue: the "real-time channel is justified not defaulted" frame, the "streaming surface is not Motion" frame-budget discipline (reused for the rAF interpolation), the seed-vs-live credibility discipline, the CSP-under-prod-build verification recipe (faker baked to static seed; `z.config({ jitless: true })`), the web/worker-split-and-warm-floor deploy precedent, and the cross-package types-only `exports` pattern.
- **meld `DECISIONS.md`** — the Node WS gateway + upgrade-routing + WS frame-contract precedent (a Node WebSocket pattern Atlas's `@fastify/websocket` gateway can learn from), and the proxy/deploy topology precedent.
- **tape `AGENT_NOTES.md`** — "Canvas2D is not Motion", the shared-Zod-frame-contract-as-integration-boundary, the Postgres-port-collision hygiene, and the warm-floor Fly precedent.

## Architect Phase 0 decisions (2026-06-06) — ADR-002..007

> The six deferred ADRs are now ACCEPTED in DECISIONS.md. Build directly from them. Where a decision changed a planner assumption, it is flagged CHANGED.

- **ADR-002 (Simulation engine).** Pure reducer `tick(state, dt, rng) -> { state, events }`, no wall-clock/IO inside (time + seeded PRNG injected by the shell). Determinism is two-phase: `faker.seed(n)` builds a frozen static baseline once (kept OUT of the hot path — bake to a static module), a seeded **mulberry32 PRNG carried in `WorldState`** drives bounded per-tick jitter (folds identically on replay). Cadence **fixed `dt = 1000 ms` (1 Hz)** — moderate by design to maximise the 1 Hz-data / 60 fps-motion interpolation gap (the wow). Shell uses a **fixed-dt accumulator** (late wakeups advance whole ticks; demo speed scales ticks-per-real-second, NEVER the reducer dt) with a **max-catch-up cap**. Projection: **precomputed per-route cumulative-segment-length table** + per-tick binary-search + within-segment lerp + segment-bearing heading (turf `along` is the test oracle only). Speed model: base + braking taper into stops + dwell timer + bounded jitter. Route-end: **loop (cyclical) or ping-pong (linear) per a `loop_mode` field** — fleet always moving (CHANGED: planner left route-end open between loop/reverse/reassign; reassignment rejected as dispatch-scope). Topology: **in-process, single instance, NO broker** (decided jointly with ADR-007) — CHANGED the "split worker" assumption: rejected for v1 (reintroduces a broker Atlas avoids, no scaling benefit since the engine must stay single-instance anyway; pure-reducer keeps split-worker a clean v2 shell change).
- **ADR-003 (WS contract).** **Single tagged-union JSON envelope** `{ t, seq, ... }`. Server→client: `snapshot` (full world + zones + tick index + `protocolVersion`), `tick` (per-vehicle delta — **ETA + status FOLDED IN**, not separate frames), `event` (SimEvent for the feed), `heartbeat` (every 20 s, carries `serverTick`). Client→server: `subscribe`/`unsubscribe` (`{ vehicleIds?, bbox? }`), `snapshot.request`, `sim.control`. **Encoding = JSON** (CHANGED the "weigh binary/MessagePack" assumption: rejected for v1 — fleet is tiny so the wire saving is nil, AND binary makes the DevTools recruiter-tell LESS legible, weakening the signal; contract is `protocolVersion`-versioned so binary is a clean v2 lever). **Server-side subscription scoping**, whole-fleet default (first paint populated). Heartbeat under Fly ~60 s edge idle; client reconnects after ~2 missed beats. Reconnect = `snapshot` then deltas; **monotonic `seq` gap-detection** triggers `snapshot.request`. **Backpressure = coalesce to latest tick per vehicle per connection** (never replay stale motion — the honest "no stale-as-live" rule). Frames Zod-validated at the boundary, malformed control frames dropped + rate-limited. **Zod `jitless` early side-effect** (runtime WS validation under no-`unsafe-eval` CSP).
- **ADR-004 (Geo + geofence).** **Scoped `@turf/*` modules** (NOT `@turf/turf` mega-bundle — keeps the web bundle lean for the Lighthouse budget) in `src/lib/geo/`, shared FE+BE. **Geofence = per-(vehicle, zone) hysteresis state machine**: enter/exit needs `>= CONFIRM` consecutive ticks inside/outside OR a boundary `margin` crossing; the CONFIRMED state (not raw point-in-polygon) drives exactly one `enter`/one `exit` — the unit-tested no-flap invariant (a boundary-skim fixture → zero spurious events). **ETA = remaining-distance-to-next-stop / rolling-average speed over last K ticks** (CHANGED/pinned: rolling-avg NOT instantaneous — the braking taper makes instantaneous speed spike ETA to infinity near stops; clamped at a floor; dwell-remaining folded in while at a stop). SHARP EDGE: pin turf units explicitly everywhere (turf defaults to km; the cumulative table + geofence margin must all be in the same unit) — reviewer checks this.
- **ADR-005 (Data + persistence).** **Plain Postgres + GeoJSON in `jsonb`; PostGIS REJECTED for v1** (CHALLENGED and confirmed lean: surveyed every v1 read — snapshot/feed/definitions are all ordinary indexed id/time reads; NO spatial query exists; viewport culling is WS-subscription bbox scoping against in-memory state, not a DB query — so PostGIS would be DECORATIVE. Clean v2 upgrade since `jsonb` GeoJSON is PostGIS-indexable later.) Schema: `vehicles`, `routes` (geometry jsonb LineString, length_m, `loop_mode`), `route_stops` (point jsonb, seq, dwell_seconds), `zones` (geometry jsonb Polygon, kind), `telemetry_snapshots` (upserted latest per-vehicle), `events` (at, type, vehicle_id, zone_id?, payload jsonb). Indexes: `events(at desc)`, `events(vehicle_id, at desc)`, `route_stops(route_id, seq)`. **Bounded retention** (periodic snapshot upsert + capped events window — NOT the per-tick firehose). **Demo city = a real compact downtown core, routes HAND-AUTHORED as GeoJSON LineStrings** (CHANGED: planner left city + authoring open; picked real-city + hand-authored over OSM-sampled — OSM/road-snapping is out-of-scope; hand-authored looks deliberate, crosses zones where the wow needs them, sits on a real keyless basemap). City + small bbox PINNED JOINTLY with ADR-006 (keyless `.pmtiles` must cover it). `postgres-js` single small pool + Drizzle; engine writes via a queued async sink off the tick hot path.
- **ADR-006 (Map + tiles).** Markers = **single GeoJSON source + symbol layer** (`icon-rotate` ← per-feature `heading`), updated via `setData()` off the React render path by the rAF loop at 60 fps (CHOSEN over HTML markers [DOM thrash] and a custom WebGL layer [over-scope for 12–30 vehicles]). Trails/remaining-route = `line` layers; zones = `fill`+`line` — all off-render-path. **Keyless default = self-hosted Protomaps `.pmtiles`** of the city bbox, same-origin static asset via the pmtiles plugin (CHOSEN over keyless raster [no bearing-rotation, third-party host risk] and optional-key-primary [fresh checkout would look lesser]) — a real keyless VECTOR basemap, NO third-party tile dependency at demo time. Keyless raster retained only as a last-resort fallback. Optional `NEXT_PUBLIC_MAP_TILE_KEY` is purely additive enrichment. Dark + light = two intentional style JSONs over one source, swapped by `next-themes`. **CSP (no `unsafe-eval`, prod-build-verified):** `worker-src 'self' blob:`, `img-src 'self' data: blob:`, `connect-src 'self'` (same-origin `.pmtiles` + WS; tile host added ONLY when the optional key is set), `script-src 'self'`, `style-src 'self' 'unsafe-inline'` (MapLibre injects inline styles — style-src only, script stays locked; documented so the reviewer does not flag it). Self-hosting same-origin deliberately SHRINKS the CSP surface.
- **ADR-007 (Deploy).** Fly.io: **Fly Postgres + a SINGLE Fastify machine** (WS gateway + in-process engine + REST, single instance — the engine is the one source of truth, every client connects to it, tick→broadcast is an in-memory emitter, NO broker) + **Next web**. WS survives the Fly edge via the 20 s app heartbeat (ADR-003); single-instance makes stickiness moot. **Warm floor on the engine machine** (`min_machines_running = 1`, `auto_stop_machines = off`) — the fleet must be already moving on first paint, no cold-start in front of a recruiter (tape/pulse precedent). Seed-vs-live: `faker.seed(n)` frozen baseline = the world definition; engine ticks forward (live) + persists a bounded window; seek = reducer fold. `.pmtiles` = committed same-origin static, served with HTTP range support (verify at deploy); optional tile key = a Fly secret at the owner's deploy, never committed. **NO Redis.**
- **PORTS (CONFIRMED — corrects the planner).** Fastify **3092**, Next **3093**, Postgres **5438**. **CHANGED: the planner's 3090/3091 COLLIDE with apex (slot 5)** — apex-web binds 3090 (dev/start) and 3091 (Playwright E2E); the AGENT_NOTES port table omitted apex. 3092/3093 confirmed free (no repo ref, nothing listening); 5438 free. In-use dev ports at audit: `3000, 3001, 3055, 3061, 3070, 3080, 3081, 3090, 3091, 3099, 3210, 3999`; PG `5434–5437`. Engineers MUST use 3092 / 3093 / 5438.

## Backend Phase 1 scaffold (backend-engineer, 2026-06-06) — Tasks 1.1–1.4

> Phase 1 backend is built, typechecks, lints, tests pass (14 geo smoke tests), the server boots and serves `GET /health` live, and the Drizzle migration generates. The main thread commits. Notes for the frontend-engineer + the next backend phase (engine + WS gateway):

- **PACKAGE LAYOUT (DIVERGES FROM PULSE — read this).** Atlas does NOT fold the shared schemas into the server package the way pulse does (pulse exposes types via `pulse-server`'s `exports`). Atlas has a **dedicated shared package at the atlas ROOT**: `atlas-shared` (`projects/atlas/package.json`), holding BOTH `src/lib/schemas/` and `src/lib/geo/`. Reason: the geo module ships **implementation** (not just types) to both server and web (the client needs the same projection/lineSlice/ETA math, ADR-004) — so it must be a real workspace package both `dependsOn`, not a types-only re-export. This matches the PLAN's explicit `projects/atlas/src/lib/...` location and conventions section 13 Trigger-B (a project pair sharing code, no `packages/*` promotion). The three workspace packages are: **`atlas-shared`** (root), **`atlas-server`** (`server/`), **`atlas-web`** (`web/`, frontend-engineer).
- **THE CROSS-PACKAGE IMPORT SEAM.** `atlas-shared` `exports` map (uses a `default` condition, NOT `import`, so drizzle-kit's CJS resolver finds the `.ts` source — an `import`-only condition fails drizzle-kit with ERR_PACKAGE_PATH_NOT_EXPORTED; this bit me, the fix is `default`):
  - `atlas-shared` — the whole surface (schemas + geo).
  - `atlas-shared/schemas` — every Zod schema + inferred type (server: impl for runtime validation; web: types-only).
  - **`atlas-shared/schemas/ws`** — the WS frame contract (ADR-003). The server gateway (Task 4.1) validates inbound control frames against it; the web WS client (Task 4.2) imports its **types only** (`import type`, so Zod is erased from the browser bundle via verbatimModuleSyntax).
  - **`atlas-shared/geo`** — the pure turf functions (ADR-004). Both server (engine, authoritative) and web (remaining-route slice + interpolation geometry) import the IMPL.
  - The web's `package.json` must declare `"atlas-shared": "workspace:*"` (the pulse-web `"pulse-server": "workspace:*"` precedent). FRONTEND-ENGINEER: add this dependency to import the contract.
- **JITLESS ZOD (ADR-003/006 gotcha — DONE on the server side).** `z.config({ jitless: true })` runs as an early side-effect in `src/lib/schemas/zod-config.ts`, imported FIRST by the schemas barrel AND explicitly as the first import of the server `main.ts`. Any consumer importing a schema from `atlas-shared/schemas` gets jitless Zod transitively. FRONTEND-ENGINEER: the web only imports WS frame TYPES (Zod erased), so the browser bundle never runs Zod and does not need this — but if the web ever runs a shared Zod validator at runtime under the strict CSP, import `atlas-shared/schemas` (or replicate the side-effect) first. (Confirmed the web already has its own `src/lib/zod-config.ts` — keep them consistent.)
- **ZOD 4 API NOTE.** `z.string().datetime()` is DEPRECATED in Zod 4 (the strictTypeChecked lint flags `@typescript-eslint/no-deprecated`). Use **`z.iso.datetime()`** (used in `event.ts` for the `at` ISO timestamp). Same for any future date/uuid string formats — prefer the `z.iso.*` / top-level format helpers.
- **WS CONTRACT SHAPE (the FE/BE boundary, ADR-003).** Envelope `{ t, seq, ... }`. Server→client union `serverFrameSchema` = `snapshot | tick | event | heartbeat` (`server-frames.ts`); client→server union `clientFrameSchema` = `subscribe | unsubscribe | snapshot.request | sim.control` (`client-frames.ts`). `PROTOCOL_VERSION = 1`, `HEARTBEAT_INTERVAL_MS = 20_000` in `protocol.ts`. The per-vehicle telemetry (`vehicleTelemetrySchema`, ETA + status folded in) is the payload of both `snapshot.telemetry[]` and `tick.telemetry[]` — the web interpolates between consecutive same-vehicle ticks. `sim.control` is a discriminated union on `action` (pause/resume/setSpeed/seek) — Task 5.4's demo control speaks this.
- **GEO MODULE STATE SHAPES (for the Phase 3 engine reducer).** `atlas-shared/geo` already exports the pure pieces the WorldState will carry and fold: `RouteProjector` (build ONCE per route at load via `buildRouteProjector`, then `projectAlongRoute(projector, s)` per tick — O(log segments)); `GeofenceTracker` (`{ state, pendingTicks }`, one per (vehicle, zone), `stepGeofence(tracker, rawInside)` returns `{ tracker, transition }` — `CONFIRM_TICKS = 2`); the rolling speed buffer helpers (`pushSpeedSample`, `rollingAverageSpeed`, `estimateEtaSeconds` with `MIN_ETA_SPEED_MPS` floor + dwell folding). All pure, no wall-clock — they fold identically on a seek. The reducer (Task 3.1) wires these; it does NOT re-implement the math.
- **UNITS PINNED (ADR-004 sharp edge — DONE).** Every `@turf/*` call in `geo/route.ts` passes `{ units: 'meters' }`. `RouteProjector.cumulative` + `totalLengthM`, telemetry `distanceAlongRouteM`, ETA distances — all METRES; `speedMps` m/s; `headingDeg` degrees [0,360); `etaSeconds` seconds. Do not mix a turf default-km value in.
- **DB / MIGRATION.** `docker compose -f projects/atlas/docker-compose.yml up -d` brings up Postgres on 5438 (atlas/atlas/atlas). `pnpm -F atlas-server db:generate` regenerates from the schema (drizzle-kit auto-loads `server/.env`); `db:migrate` applies. Migration `0000_*.sql` is committed: 6 tables, GeoJSON in `jsonb`, the ADR-005 indexes (`events(at desc)`, `events(vehicle_id, at desc)`, `route_stops(route_id, seq)`, `vehicles(route_id)`), enum types built from the SHARED const tuples (`server/src/db/schema/enums.ts` imports `VEHICLE_TYPES` etc. from `atlas-shared/schemas` so the DB enum cannot drift from the Zod enum). Ids are **stable hand-authored strings** (e.g. `route-downtown-loop`, `truck-7`), not random UUIDs — the seed (Task 3.3) and the WS contract reference legible ids.
- **`.env.example` — committed, `.env` never.** `server/.env.example` declares PORT=3092, NODE_ENV, DATABASE_URL (5438), CORS_ORIGINS (=http://localhost:3093). No new secret introduced (Atlas has no auth/SSRF surface; the optional `NEXT_PUBLIC_MAP_TILE_KEY` is a WEB env, ADR-006, owned by the frontend-engineer — not in the server `.env.example`).
- **CROSS-PACKAGE PRETTIER TOUCH (heads-up for frontend-engineer).** Running the repo prettier glob over `projects/atlas/**` reflowed a handful of in-progress `web/` files (whitespace only — the same normalization lint-staged applies on commit). No logic changed. If your editor shows churn on those files, it is cosmetic prettier alignment, safe to keep.
- **VERIFIED.** `pnpm -F atlas-shared {typecheck,lint,test}` and `pnpm -F atlas-server {typecheck,lint,build}` all clean; `GET /health` returns `{ status:'ok', commit, ts }` (200, Zod-serialized) on `0.0.0.0:3092`; the migration generates. NOTE: the live DB connection was not exercised (no local Postgres up during scaffold) — `db:migrate` against a running 5438 is the next backend session's first smoke step before the seed (Task 3.3).

## Phase 2 frontend close-out (frontend-engineer, 2026-06-06) — Tasks 2.1 + 2.2

> The Next.js web client scaffold + the MapLibre integration boundary are built under `web/` and verified under a PROD build. Build directly on these; the imperative map contract below is what Phase 4 (live WS + rAF interpolation) plugs into.

### Sovereign token palette (Task 2.1) — operations / control-room, NO sibling reuse

A fresh identity built from scratch in `web/src/app/globals.css` (Tailwind v4 CSS-first `@theme`; no `tailwind.config.js`). **DARK is canonical** (the control-room register — viewer 1's lead surface); LIGHT is the intentional clean-dispatcher counterpart. NO value reused from tape (slate/cyan), meld (warm-paper/violet), razors-edge (brass/charcoal), pulse (indigo), apex (EV green-cyan on `#0a0e14`).

- **Dark stage:** deep **petrol-navy** — bg `#0b1018`, surfaces `#121a26` / `#18222f` / `#212d3c` (backlit glass), ink `#e6ecf3` (16:1), muted `#97a4b6`, subtle `#6f7e93`.
- **Light field:** cool **paper** — bg `#eef1f5`, surface `#ffffff`, ink `#141a22` (15.4:1), muted `#4b5566`, subtle `#636c7c` (4.7:1).
- **Single brand accent = SIGNAL AMBER** `#f6a821` (the "live / active / dispatch" indicator; re-keyed to amber-bronze `#9a6300` as text on light for AA). NOT a green/cyan/indigo/violet — deliberately distinct from every sibling.
- **Fleet-STATUS semantic palette** (separate from the brand accent so the map reads as instrumentation; NEVER colour-alone — always paired with a text label): en_route teal-green `#36d399`, at_stop amber `#f6a821`, idle grey `#8a97a9`, returning periwinkle `#8b9dff`, alert rose `#ff6b81`. **Zone tints:** depot periwinkle, delivery teal-green, restricted rose.
- **Motifs:** `.live-grid` (faint coordinate graticule) + `.sweep-line` / `--gradient-sweep` (an amber "radar sweep" on the top bar underline + panel dividers).
- **Type:** IBM Plex Sans (UI/body, console grotesk) + IBM Plex Mono (telemetry numerals; tabular figures via `.font-mono`/`.tabular`). Self-hosted via next/font (no third-party `font-src`). Distinct from apex's Inter/Space Grotesk.
- Contrast verified by ratio (in the globals.css header comment). All TEXT pairs ≥ 4.5:1 in both themes; status colours are graphical-object fills judged at the 3:1 non-text bar AND label-paired.

### CSP / keyless verification (the HARD GATES) — both PASS under a prod build

- **Exact CSP** (in `web/next.config.ts`, verified live via `curl -I` on `next start`): `default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob:; font-src 'self'; connect-src 'self'; worker-src 'self' blob:; child-src 'self' blob:; frame-ancestors 'none'; base-uri 'self'; form-action 'self'`. **NO `unsafe-eval`** (the load-bearing guarantee). `worker-src 'self' blob:` is THE MapLibre allowance (its tile-parse worker is a same-origin blob). `connect-src 'self'` only — keyless `.pmtiles` + the same-origin WS; NO third-party tile host with no key.
  - NOTE vs ADR-006: ADR-006 wrote `script-src 'self'`. Next 15 emits UNHASHED inline hydration/next-themes bootstrap scripts that a bare `script-src 'self'` blocks outright (kills hydration) — so `'unsafe-inline'` for SCRIPTS is carried (the meld/razors-edge/apex precedent), with the `'unsafe-eval'` ban kept absolute (that is the part ADR-006 actually gates on). v1.1 hardening = nonce middleware to drop script `'unsafe-inline'` (debt, same as apex). `style-src 'unsafe-inline'` is required (Tailwind v4 + MapLibre inline styles) and documented so the reviewer does not flag it.
  - The optional tile-host enrichment is additive only: `NEXT_PUBLIC_MAP_TILE_HOST` (set alongside `NEXT_PUBLIC_MAP_TILE_KEY`) appends the host to `connect-src`/`img-src`. With no env, the policy stays same-origin — the committed repo is on the tightest CSP.
- **Keyless render verified with Playwright against the prod build:** 1 `.maplibregl-map` container + 1 WebGL canvas mount; **ZERO `securitypolicyviolation` events** with the live map rendered; no third-party network requests; works with **no `.env`**. The only console line is the expected `404` for the not-yet-committed `/map/porto.pmtiles` extract — the keyless **graceful fallback**: the map still renders (painted basemap background + the fleet/route/zone app layers on top); the `.pmtiles` is a Phase 9 deploy artifact (gitignored; see `web/public/map/README.md`).

### Demo city + bbox PINNED (ADR-005/ADR-006 coupling — web side resolved)

- **City: Porto** downtown core. Centre `[-8.6109, 41.1496]`, initial zoom `14.2`, **`DEMO_CITY_BBOX = [-8.645, 41.135, -8.585, 41.165]`** (`web/src/lib/fleet/demo-city.ts`). The keyless `.pmtiles` extract (Phase 9) is generated from this bbox.
- **ACTION for backend-engineer (Task 3.3 seed):** author the seeded routes/zones inside THIS bbox so the keyless basemap covers them and the FE/BE worlds align. The web static fixture (`web/src/mocks/static-fleet.ts`, 4 hand-authored Porto routes + 3 zones + ~10 vehicles, `faker.seed`-deterministic) is a Phase-2 placeholder ONLY — the backend's seeded snapshot replaces it at Phase 4.

### Map-wrapper imperative contract — what Phase 4 (WS + rAF interpolation) plugs into

`web/src/lib/map/map-controller.ts` — `AtlasMapController`, a PLAIN class (no React) owning the MapLibre instance off the render path; `web/src/components/map/map-canvas.tsx` is the `'use client'` wrapper that mounts it once in a ref (dynamic-imported `ssr:false`, so MapLibre is never in the server bundle; `/` First Load JS = 116 kB). The Phase 4 interpolation loop drives the map ONLY through these imperative methods — NEVER React state per frame (the hard gate):

- `controller.setVehiclesGeoJSON(featureCollection)` — call PER ANIMATION FRAME with the interpolated vehicle positions/headings for that frame (lerped between the last+next authoritative 1 Hz ticks). One `GeoJSONSource.setData()` on `SOURCE_VEHICLES` — the cheapest GPU-side path, no reconciliation. The dot/heading-wedge/label layers already exist; Phase 4 only swaps the data. The FeatureCollection builder + the per-feature `heading` (drives `icon-rotate`) are in `web/src/lib/map/fleet-layers.ts`.
- `controller.setSnapshot(snapshot)` — full re-derive of all three sources (vehicles/routes/zones). Use on WS connect/reconnect (a `snapshot` frame); per-frame motion uses `setVehiclesGeoJSON` instead.
- `controller.focusVehicle(id)` — camera `flyTo` (or `jumpTo` under reduced-motion). MapLibre native, NOT Motion.
- `controller.setReducedMotion(bool)` — flips focus between fly (animated) and cut. Phase 4's snap-mode (markers snap to ticks) is also a controller flag, not a re-implementation. The wrapper already bridges the `prefers-reduced-motion` media query to this.
- Theme switch is wired: `next-themes resolvedTheme` → `controller.setTheme('dark'|'light')` → `map.setStyle()` swaps the basemap JSON and re-adds the app sources/layers on `style.load` (the fleet never vanishes across a theme flip). Two intentional style JSONs in `web/src/lib/map/basemap-style.ts`.
- MapLibre **keyboard nav enabled** (`keyboard: true`; canvas `tabIndex=0`, `role="application"` with an instructive `aria-label`). The first-class non-map fleet table (the SR/keyboard path + no-WebGL fallback) is Phase 5/6 (the fleet panel is scaffolded as the seed of it).
- Trails / remaining-route (Phase 4.3) attach as additional `line` layers on `SOURCE_ROUTES` (or a new trail source) updated off the render path; zone-pulse is a paint update on `LAYER_ZONE_FILL`/`LAYER_ZONE_LINE`. Nothing in this surface goes through Motion (Motion = UI chrome only).

### Local placeholder types — Phase 4 swap

`web/src/lib/fleet/types.ts` is a LOCAL placeholder (Vehicle/Route/Zone/FleetSnapshot) marked `// TODO: import from atlas-shared once consumed`. The backend has since landed **`atlas-shared`** with `exports` `.` / `/schemas` / `/schemas/ws` / `/geo`. Phase 4 (Task 4.2): replace the local WS-frame shapes with `atlas-shared/schemas/ws` (types-only on the web side) and the entity shapes with `atlas-shared/schemas`; pull client-side geo (remaining-route slice, interpolation geometry) from `atlas-shared/geo` (impl). The static fixture's lightweight inline geometry helpers (`web/src/mocks/static-fleet.ts`) are a Phase-2-only stand-in and are dropped then.

### tsconfig seam

`web/tsconfig.json` extends `../tsconfig.base.json` (the atlas project base the backend-engineer created — identical to the root base; repointed once it existed). `paths` `@/*` → `./src/*`. Vitest mirrors the alias.

### Deploy follow-up the Phase 9 (deploy) agent must do

1. Generate the Porto `.pmtiles` extract from `DEMO_CITY_BBOX` and place it at `web/public/map/porto.pmtiles` (gitignored — see `web/public/map/README.md` for the `pmtiles extract` command). Verify the keyless vector basemap renders under the prod build with HTTP range support.
2. `output: 'standalone'` is set; the Windows local build emits EPERM symlink warnings for the standalone trace (a Windows symlink-privilege artifact, non-fatal — the route output is clean). On Linux/Fly the standalone emit works (the apex precedent). `outputFileTracingRoot` is pinned to `projects/atlas/` (one hop up from `web/`).
3. The optional tile key (`NEXT_PUBLIC_MAP_TILE_KEY` + `NEXT_PUBLIC_MAP_TILE_HOST`) is a Fly secret at the owner's discretion — never committed; the keyless default stands without it.

## Lint — root strictTypeChecked gate (frontend-engineer, Phase 2 close)

atlas/web is gated by the **ROOT** ESLint config (`C:portfolioeslint.config.mjs`: typescript-eslint strictTypeChecked + stylisticTypeChecked + jsx-a11y/recommended, `projectService: true`) via the pre-commit hook — NOT just the per-project `next/typescript`. The per-project `next lint` passing is necessary but NOT sufficient: the hook lints staged TS/TSX with the far stricter root config, and the commit blocks on it. Future web work must satisfy strictTypeChecked + jsx-a11y here. Concrete rules that bit the Phase 2 scaffold and how they are honestly satisfied (no config-weakening, no file-level disables): (1) the root config has NO `react-hooks` plugin, so an inline `// eslint-disable react-hooks/exhaustive-deps` directive ITSELF errors ("rule not found") — do not write that directive; instead make mount-once effects genuinely dependency-free by reading live values (theme, Zustand actions) through refs updated on each render. (2) `jsx-a11y/no-noninteractive-tabindex`: `role="application"` is NOT classified interactive by the rule, and MapLibre already sets `tabindex="0"`+aria-label on its OWN canvas when `keyboard:true` — so the wrapper div needs no `tabIndex` (a redundant double tab-stop); the container carries `role="region"`+aria-label for SR context only. (3) `restrict-template-expressions`: numbers in template literals need explicit `String(n)`/`.toLocaleString()`. (4) `getSource` is generic (`getSource<GeoJSONSource>(id)`) — use it instead of `as GeoJSONSource` casts (which trip `no-unnecessary-type-assertion`); `[number,number]` is assignable to `LngLatLike` with no cast. (5) MapGeoJSONFeature.properties is `{[k]:any}` (non-null) — read into an `unknown` then narrow (avoids `no-unsafe-assignment`/`no-unnecessary-condition` from `?.`/`any`). (6) `no-non-null-assertion` is RELAXED only for `*.test.ts`/`*.spec.ts` — `src/mocks/static-fleet.ts` is a fixture imported by app code, NOT a test, so its `coords[i]!`/`RECORD[id]!` must use real narrowing (guard + `continue`, or `?? fallback`) because `noUncheckedIndexedAccess` makes indexed access `T|undefined`. All 4 target files + the full `projects/atlas/web/src/**/*.{ts,tsx}` glob now pass the root gate (exit 0); tsc --noEmit, per-project lint, and prod build all still green.

## Phase 3 simulation engine close-out (backend-engineer, 2026-06-06; trackers finalised by main thread after a 529 cut the agent's wrap-up)

The engine spine is built and verified. Module layout under `server/src/engine/`:

- `reducer/` — the PURE core (ADR-002): `tick.ts` (`tick(state, dt) -> { state, events }`, no wall-clock/IO), `prng.ts` (mulberry32 carried IN state, not Math.random), `route-progress.ts` (s -> lat/lng/heading via the shared `atlas-shared/geo` cumulative table), `speed-model.ts` (speed-modulation + dwell-at-stop), `events.ts`, `world-state.ts`.
- `shell/` — `engine.ts` (`SimulationEngine`): the IO shell, fixed-dt accumulator loop at 1 Hz, owns wall-clock, `onTick` fan-out, `snapshot()`, control surface (`pause`/`resume`/`setSpeed`/`seek`). Loop and reducer are cleanly separated (the pure boundary).
- `persistence/event-sink.ts` — queued async write sink (ADR-005); the tick loop never blocks on the DB.
- `baseline/` — `build-baseline.ts` + `types.ts`: the frozen baseline (ADR-002 two-phase determinism).
- `engine-service.ts` — the COMPOSITION ROOT the Fastify app registers. Builds the baseline from the Porto fixture, owns engine + sink, wires `onTick -> sink.ingest + drain`.

**Engine→WS seam (what Phase 4.1 plugs into):** `EngineService` is the integration point. The WS gateway:

- subscribes per-tick: `const off = engineService.onTick((output: EngineTickOutput) => broadcast(output))` — `output` carries `serverTick`, `telemetry[]` (per-vehicle authoritative), and the tick's `events[]`;
- builds the snapshot-on-connect frame from `engineService.snapshot()` (EngineSnapshot — current full-world telemetry) + `engineService.definitions` (routes/stops/zones/vehicles, so a cold client renders the map with no REST round trip);
- routes the `sim.control` client frame to `pause()`/`resume()`/`setSpeed(mult)`/`seek(tick)` (the last returns a fresh snapshot for deterministic replay);
- calls `engineService.start()` at boot and `await engineService.stop()` on shutdown (flushes the sink).
  The telemetry + event shapes are the `atlas-shared/schemas` telemetry + event Zod types; the WS frame envelope is `atlas-shared/schemas/ws`. Validate every client frame at the boundary (jitless Zod), drop malformed.

**Seed / demo city:** `server/src/seed/porto-fixture.ts` + `porto-geometry.ts` hand-author the Porto routes/stops/zones INSIDE `DEMO_CITY_BBOX` (matches `web/src/lib/fleet/demo-city.ts`). `server/scripts/seed.ts` (`pnpm --filter atlas-server seed`) writes the frozen `faker.seed(n)` baseline, idempotent. The engine boots from the fixture directly (DB is for the feed/snapshot reads, NOT the engine's source of truth — ADR-005), so the fleet moves even before the seed runs.

**Verified:** `tsc --noEmit` clean; root strictTypeChecked eslint clean over `server/src/**/*.ts` + `src/**/*.ts`; 14 engine tests green — determinism (same baseline + N ticks → identical world; `seek == replay`), purity (no input mutation), motion (telemetry inside Porto bbox, heading 0–360, progress 0–1), geofence (enter/exit fires deterministically; NO duplicate consecutive enters for the same vehicle/zone — the no-flap invariant). **NOT yet run:** the DB seed end-to-end against Postgres on 5438 (Docker not started this session). Deferred to test/deploy: `docker compose up -d` (in `projects/atlas`) then `pnpm --filter atlas-server db:migrate && pnpm --filter atlas-server seed`.

## Phase 4.1 WS gateway close-out (backend-engineer, 2026-06-06) — Task 4.1

> The `@fastify/websocket` telemetry gateway is built and verified LIVE. This is the FE/BE seam the Phase 4.2 client connects to. Everything the frontend needs to connect, interpolate, reconnect, and drive the demo is here.

**ENDPOINT.** One WebSocket at **`/ws`** (dev: `ws://localhost:3092/ws`). Same-origin in prod (the CSP `connect-src 'self'` covers it — the frontend-engineer's Phase 2 note). Genuinely PUSHED — no polling fallback exists; the gateway IS the DevTools "one WebSocket, telemetry frames" proof (success criterion).

**MODULE LAYOUT** under `server/src/gateway/`: `ws-gateway.ts` (the Fastify plugin: registers `@fastify/websocket`, the `/ws` route, the per-tick fan-out, the heartbeat timer, inbound frame routing); `connection.ts` (the `Connection` class — per-socket state); `frames.ts` (PURE outbound frame builders, schema-shaped); `scope.ts` (server-side subscription filtering); `rate-limiter.ts` (per-connection token bucket). Wired in `app.ts` (instantiates `EngineService`, registers the gateway, returns `engineService` in `BuiltApp`) and `main.ts` (`engineService.start()` after `listen`; `await engineService.stop()` is the FIRST shutdown step, before `app.close()` so the sink flushes before the pool ends).

**FRAME FLOW (server→client, the contract is `atlas-shared/schemas/ws`).**

1. **On connect → `snapshot`** immediately: `{ t:'snapshot', seq:0, protocolVersion:1, serverTick, ts, vehicles[], routes[], stops[], zones[], telemetry[] }`. The static definitions (vehicles/routes/stops/zones) come from `engineService.definitions` so a COLD client renders the whole map with NO REST round trip; `telemetry[]` is the current authoritative position of every in-scope vehicle. → `controller.setSnapshot(...)`.
2. **Per engine tick → `tick`**: `{ t:'tick', seq, serverTick, ts, telemetry[] }` carrying the in-scope vehicles. The client interpolates between consecutive same-`vehicleId` ticks (lerp lat/lng along route geom, shortest-arc heading) and drives `controller.setVehiclesGeoJSON(fc)` per rAF frame. NOTE: at 1x speed the engine ticks ~1 Hz, so `tick` frames arrive ~once/sec — THE interpolation gap is the wow.
3. **Tick events → `event`** (one frame each): `{ t:'event', seq, serverTick, event:SimEvent }` (geofence.enter/exit, status.change, arrived/departed) → events feed + zone-pulse paint update.
4. **Every 20 s → `heartbeat`**: `{ t:'heartbeat', seq, serverTick, ts }`. ALSO a ws-protocol ping each beat; the gateway reaps a socket that did not pong since the last beat. `HEARTBEAT_INTERVAL_MS = 20_000` (from `atlas-shared/schemas/ws` — under the Fly ~60 s edge idle timeout, ADR-007). **Client rule:** treat ~2 missed beats (~45 s) as dead → reconnect.

**SUBSCRIPTION SCOPING (server-side, never client-trusted).** Default = WHOLE FLEET (first paint populated). Narrow with `{ t:'subscribe', vehicleIds?, bbox? }` — `vehicleIds` = focused subset, `bbox` = `[west, south, east, north]` viewport cull, both → intersection. Enforced SERVER-SIDE in `scope.ts` (`filterTelemetry`); the client frame only expresses intent. A scope change (`subscribe`/`unsubscribe`) triggers a fresh `snapshot` so the client reconciles immediately. `{ t:'unsubscribe' }` resets to whole-fleet. (v1 client can ignore scoping entirely — whole-fleet default at 18 vehicles is fine; scoping is there for the viewport-cull showcase.)

**RECONNECT / SEQ-RESUME.** Every outbound frame carries a per-connection MONOTONIC `seq` (starts at 0 on connect, increments per frame sent to THAT connection). On reconnect the new connection gets a fresh `snapshot` (seq resets to 0). If the client detects a `seq` GAP (a missed frame), send `{ t:'snapshot.request' }` to force a fresh snapshot and reconcile — NEVER show stale-as-live. The server DEBOUNCES snapshot requests to 1/sec per connection (a flapping client cannot snapshot-storm). `seq` is per-connection, NOT a global stream id — do not assume two connections share a sequence.

**BACKPRESSURE = coalesce-to-latest-per-vehicle (ADR-003, implemented in `Connection`).** When a socket's send buffer is backed up (`bufferedAmount > 1 MiB`), a new tick does NOT queue behind the old one — it REPLACES the pending telemetry per vehicle (newest wins), so a slow consumer gets the LATEST authoritative position, never a replayed backlog of stale motion. Memory is bounded by FLEET SIZE, not tick count. The held buffer flushes (with the newest serverTick/ts) on the next heartbeat drain pass once the socket recovers. The frontend does not need to do anything for this — it just never receives stale-as-live.

**DEMO CONTROL.** `{ t:'sim.control', action }`: `pause` / `resume` / `setSpeed` (`{ multiplier }`, positive, schema-capped at 16, gateway re-caps defence-in-depth) / `seek` (`{ tick }`, nonneg int → deterministic re-fold from the baseline; the gateway replies with a fresh `snapshot` at that tick). These hit the single in-process engine — they affect the WORLD every client sees (single source of truth, ADR-007), not a per-connection view. Task 5.4's demo control speaks this.

**RATE-LIMIT POSTURE (the WS control channel is a public input surface).** Two layers: (1) `@fastify/rate-limit` (the app-global 120/min from Task 1.1) guards the HTTP UPGRADE = connection rate; (2) each connection carries a TOKEN BUCKET (`rate-limiter.ts`, default 10 frames/sec steady, burst 20) throttling INBOUND control frames — `@fastify/rate-limit` does not see individual WS frames once the socket is open, so the per-socket bucket covers that. Over-rate frames are DROPPED (not queued, not a disconnect). EVERY inbound frame is `clientFrameSchema.safeParse`'d (jitless Zod) at the boundary; non-JSON or schema-invalid frames are dropped silently, never crashed on. A handler exception is caught and logged, never takes down the socket or the fan-out.

**INBOUND VALIDATION IS JITLESS.** The gateway imports `clientFrameSchema` from `atlas-shared/schemas/ws`, which transitively runs the `z.config({ jitless: true })` side-effect (the schemas barrel) — so the runtime WS-frame validation is CSP-safe (no JIT eval) on the server. The web client imports the WS frame TYPES ONLY (Zod erased), so it does not validate at runtime and does not need this — but if 4.2 ever runs a shared Zod validator in the browser, import `atlas-shared/schemas` first (its own `web/src/lib/zod-config.ts` already mirrors the side-effect).

**OUTBOUND CONTRACT SELF-CHECK.** In dev/test (`NODE_ENV !== 'production'`) the gateway validates every OUTBOUND frame against `serverFrameSchema` before sending (a guard so the pure builders cannot drift from the shared contract); off in prod (cost). A frame that fails is logged + skipped, never crashes the fan-out. The frame builders in `frames.ts` are pure and unit-tested to pass the shared schema.

**DB-LESS RESILIENCE (confirmed live).** The engine boots from the frozen Porto baseline directly (NOT the DB — ADR-005), and the persistence sink (`event-sink.ts`, Phase 3) catches + LOGS every DB failure (`event sink drain failed (engine state is unaffected)`) and never propagates it into the tick loop. So the live WS channel works WITHOUT Postgres — verified by running `server/scripts/ws-smoke.ts` with an unreachable 5438 (snapshot + ticks + control + heartbeat all PASS while the sink logs ECONNREFUSED off the hot path). No change was needed to make this true; it was already the sink's design.

**DEPS ADDED.** `ws` + `@types/ws` to `server/package.json` — the canonical `WebSocket` type source for the handler/connection. `@fastify/websocket` carries `ws` transitively but does not surface its types for a direct `import type { WebSocket } from 'ws'`, so `ws` is now a direct dep (`pnpm install` run). NO new env var; NO `.env.example` change (the WS endpoint is same-origin, CORS_ORIGINS already covers the dev cross-origin loop).

**SMOKE SCRIPT.** `server/scripts/ws-smoke.ts` (gitignored-from-lint by the root `**/scripts/**` ignore) boots the real app DB-less, opens one WS, and asserts: snapshot-on-connect (18 vehicles), ticks stream + vehicles move, pause halts the tick index + resume advances it, heartbeat arrives (interval shortened via the test-only `buildApp(env, { heartbeatIntervalMs })` seam — prod stays 20 s), malformed frames dropped. Run: `node --import tsx projects/atlas/server/scripts/ws-smoke.ts`. Re-run it after any gateway change. **VERIFIED:** tsc clean, root strictTypeChecked eslint clean, per-package lint clean, 27 tests (14 engine + 13 `gateway.test.ts`), live smoke PASS.

## Phase 4.2/4.3 close-out (frontend-engineer, 2026-06-06) — the WS client + the rAF interpolation WOW + trails/remaining/zone-pulse

> The live channel is wired end-to-end and verified live under a prod build. This is the centerpiece. What Phase 5 builds on, plus the gotchas it must not re-trip.

**MODULE LAYOUT (web).** Two new dirs under `web/src/lib/`:

- `ws/` — `ws-client.ts` (`TelemetryWsClient`, the single connection + reconnect/seq/heartbeat state machine), `frames.ts` (TYPES-ONLY frame imports from `atlas-shared/schemas/ws` + Zod-free structural narrowing — `parseServerFrame`), `ws-url.ts` (env/origin derivation), `snapshot-adapter.ts` (WS `SnapshotFrame` → the controller's `FleetSnapshot` view model).
- `interp/` — `interpolation.ts` (PURE math: `interpolatePose` lerps the scalar `s` then projects via the shared `atlas-shared/geo` projector so the marker follows the ROAD not the chord; `lerpHeading` is shortest-arc across the 0/360 seam; `tickProgress` clamps at 1 so a late tick FREEZES, never dead-reckons), `interp-store.ts` (the OFF-render store), `raf-loop.ts` (the single loop), `stop-index.ts` (stop→`s`), `use-live-telemetry.ts` (the glue hook).
- `store/connection-store.ts` (Zustand, low-frequency WS status for the pill).

**THE OFF-RENDER STORE + rAF LOOP SHAPE (the hard gate, honored).** `InterpStore` is a PLAIN class (NOT React state, NOT Zustand) holding per-vehicle `{ last, next, nextAtMs, snap }` + cached route projectors (built ONCE per route — the cumulative-length table is expensive). The WS `onTick` writes it (`applyTick`: current `next`→`last`, incoming→`next`, clock resets); a SINGLE `RafLoop.start()` reads it every frame and calls `controller.setVehiclesGeoJSON(fc)` ONCE per frame (one `GeoJSONSource.setData`, no React). NOTHING per-frame goes through React. Only the LOW-frequency connection status (a few times a session) + the zone-pulse paint (per geofence event) touch React. This is the pulse "streaming surface is not Motion" discipline applied: per-marker per-frame through React/Motion would be the frame-budget disaster.

**THE INTERPOLATION MATH (the wow).** Between two authoritative ticks the loop computes `t = clamp01((now - nextAtMs) / intervalMs)`, lerps `s = lastS + (nextS-lastS)*t`, projects `s`→lat/lng via the shared projector, and shortest-arc-blends the heading. `intervalMs` defaults to 1000 (the 1 Hz engine cadence). A WRAP (loop/ping-pong: new `s` drops by > half the route length) or a snapshot reseed sets `snap=true` so the loop holds at the newest authoritative point rather than tweening a giant rewind. Reduced-motion or a frozen engine (socket down) → snap mode (markers step to ticks, fleet stays live). All pure + unit-tested (`interpolation.test.ts`, `raf-loop.test.ts`: the lng STRICTLY advances across sub-tick frames = interpolation is live, not stepped).

**THE WS CLIENT + RECONNECT MODEL.** ONE `WebSocket` to `/ws` (the DevTools proof — no polling, no second socket). `connect()` is idempotent. Reconnect = capped exponential backoff (500 ms → 10 s) + 30% jitter. `seq` is per-connection (resets to -1 on each open; a `snapshot` rebaselines it); a detected gap (`seq > lastSeq + 1`) sends a debounced (1 s) `{ t:'snapshot.request' }` and reconciles from the fresh snapshot — never applies a delta on a hole. Heartbeat liveness: ANY frame re-arms a timer (`HEARTBEAT_INTERVAL_MS*2 + 5 s` ≈ 45 s); silence past it force-closes → reconnect. On a drop the InterpStore is set `frozen` (the loop snaps/holds at the last authoritative position — the "never stale-as-live / no drift" rule); a reconnect's fresh snapshot reseeds cleanly. Framework-free + injectable `socketFactory` → fully mock-socket-tested (`ws-client.test.ts`).

**TRAILS / REMAINING / ZONE-PULSE (all off-render-path, NOT Motion).** Added `SOURCE_TRAILS` (with `lineMetrics: true` for a `line-gradient` fade tail) + `SOURCE_REMAINING` (signal-amber planned path) to `fleet-layers.ts`, and `setTrailsGeoJSON`/`setRemainingGeoJSON`/`pulseZone` to the controller. The rAF loop refreshes the lines THROTTLED to 200 ms (they move slowly vs the 60 fps marker glide): trail = `sliceRouteGeometry(0..current, bounded to a 350 m tail)`, remaining = `sliceRouteGeometry(current..nextStopS)`. `pulseZone(zoneId)` is a `setPaintProperty` `case`-expression raise on the geofence zone fill+line opacity, eased back over ~1.4 s — under reduced-motion the easing transition is skipped (a static raise that clears), no pulse animation. NOTE: I ADDED `sliceRouteGeometry` to the SHARED `atlas-shared/geo/route.ts` (pure, off the cumulative table, no turf round trip so it shares the exact geometry the engine projects from — no FE/BE drift). Shared geo tests still 14/14.

**WIRE→VIEW BOUNDARY.** The controller renders a web-local `FleetSnapshot` VIEW MODEL (vehicles carry a live position; routes nest stops; zones carry a flat ring) — deliberately distinct from the shared WIRE contract. `snapshot-adapter.ts` is the ONE place that maps `SnapshotFrame` (static defs + `telemetry[]`) → that view model. The interp path imports the shared telemetry/route types + geo IMPL directly (no adapter). `web/src/lib/fleet/types.ts` is now documented as the view model, not a placeholder. The browser bundle stays Zod-FREE (types-only imports + structural narrowing — the frames come from our own trusted gateway).

**CSP `connect-src` CHANGE (verified both ways).** `next.config.ts` now appends the dev WS origin to `connect-src` IN NON-PRODUCTION ONLY (`wsConnectSrc()` returns `''` when `NODE_ENV === 'production'`, else `ws://localhost:3092` or the `NEXT_PUBLIC_WS_URL` origin). PROD stays `connect-src 'self'` (same-origin WS, proxied) — NO `unsafe-eval` in either case. Verified live: `curl -I` on `next start` (prod) → `connect-src 'self'`; on `next dev` → `connect-src 'self' ws://localhost:3092`. The same-origin deploy posture (ADR-007) needs no override; LOCAL dev needs `NEXT_PUBLIC_WS_URL=ws://localhost:3092/ws` in `.env.local` (the web is :3093, the gateway :3092 — cross-origin in dev).

**THE DEV-vs-PROD VERIFICATION GOTCHA (important for the next agent).** Under `next dev` the strict no-`unsafe-eval` CSP BLOCKS React Refresh's `eval`, which aborts the `ssr:false` map chunk evaluation → the map never mounts and the WS never opens. This is the documented dev-only HMR artifact (it is NOT a code bug). The live channel MUST be verified against a PROD build (`next build && next start`). Because the prod CSP is `connect-src 'self'`, a local prod verify needs the web + WS on ONE origin — I used a throwaway same-origin proxy (`atlas-proxy.mjs`, web→3097 / ws→3092 under :3096) + Playwright. RESULT: ONE WebSocket, all four frame types in-browser, MapLibre canvas mounts, ZERO CSP violations, pill "Live". The proxy + the server `.env` were removed after; re-create `server/.env` from `.env.example` (gitignored) to run locally.

**WHAT I VERIFIED LIVE vs DEFERRED.**

- VERIFIED: one socket (browser + raw probe); all frame types flow; authoritative `distanceAlongRouteM` advances across ticks (the interpolation INPUT is genuinely live — server DB-less probe); geofence `enter`+`exit` events fire with valid `zoneId`s (the zone-pulse trigger); zero CSP violations under prod; the pill reflects the real WS state; the interpolation math + the loop's moves-between-ticks + snap-under-reduced-motion are unit-pinned.
- DEFERRED (Phase 5 / test / deploy): a pixel-level in-browser assert that a SPECIFIC marker's screen position changes between two ticks (covered transitively by the unit test + the live position-advances probe, but a Playwright pixel-diff is a Phase 8 E2E job); the DB seed end-to-end against Postgres on 5438 (the engine + WS run DB-less, so the wow does not need it, but the events FEED + the SSR floor + reconnect-from-persisted-snapshot do — Phase 5/9); the geofence-beat watched on-screen with the zone visibly pulsing (the trigger + the paint call are wired + the events fire live; the visual beat is a Phase 5/designer-critic review item).

**WHAT PHASE 5 NEEDS (the seam).** The panels (fleet list, detail, events feed) are LOW-frequency React UI — they must NOT subscribe to the rAF loop. Add a small telemetry store (Zustand) updated on each `onTick` (1 Hz is fine for React) ALONGSIDE the off-render InterpStore, and an events store updated on each `onEvent`. Extend the EXISTING `use-live-telemetry.ts` callbacks — do NOT open a second socket. The events feed's geofence beat: the zone-pulse is already wired (`pulseZone` on geofence events); Phase 5 adds the event ROW (`AnimatePresence`, `aria-live`) + the status flip. Motion IS allowed in the panels (they are React-state chrome) — just not in the streaming/map surface.

## Phase 5 close-out (frontend-engineer, 2026-06-06) -- fleet panel + detail + events feed + demo affordance

> The operations panels are built and verified LIVE under a prod build. The hard gate held: the panels are React-state UI reading NEW low-frequency stores fed from the ONE existing socket -- they do NOT subscribe to the rAF interpolation loop. What Phase 6 builds on, plus the contracts it must reuse.

**THE DATA LAYER (the seam, built first per the handoff).** Two new Zustand stores fed from the EXISTING use-live-telemetry.ts callbacks (extended, not a second socket):

- web/src/lib/store/telemetry-store.ts -- 1 Hz. applySnapshot seeds the static world DEFINITIONS (vehicle identity, routes with nested ordered stops, zones with name+kind) + the first per-vehicle telemetry; applyTick REPLACES only the vehicles in that tick (unchanged keep their reference -- a row that did not change does not re-render). vehicleIds is a stable natural-numeric label order (Unit 2 before Unit 10). 1 Hz React re-renders are fine for a table; per-frame motion stays in the off-render InterpStore.
- web/src/lib/store/events-store.ts -- bounded 60-event newest-first ring, dedup by id (reconnect-snapshot-replay safety: a dup neither re-lists nor re-announces), lastAnnounced+announceSeq for the coalesced aria-live. selectVehicleEvents is the pure per-vehicle filter the detail panel uses.
- Two bridge stores: sim-control-store.ts (the demo UI send is published on onStatus===live and cleared otherwise -- null until live, so the demo controls disable rather than queue into the void; tracks paused/speed) and controller-store.ts (the MapCanvas publishes its AtlasMapController so SIBLING panels -- fleet/detail, outside the map subtree -- can drive the camera; null when no map = the no-WebGL fallback, where selection still works).

**STATUS-NEVER-COLOUR-ALONE = ONE SOURCE (reuse everywhere).** status-descriptor.ts maps each VehicleStatus to {label, icon, textClass, dotClass, description}. Token keys are status-enroute/status-atstop/status-idle/status-returning (NO underscore -- they map to the --color-status-\* vars; the enum is en_route/at_stop). status-badge.tsx renders dot + icon + label together -- colour is reinforcement only. EVERY status surface (fleet rows, detail header, event-copy) goes through these -- new surfaces MUST reuse them, no colour-only chip.

**THE FLEET-PANEL-AS-A11Y-PATH CONTRACT (Task 5.1).** fleet-panel.tsx is a use-client component reading the telemetry store. A genuine sortable table, not a stub: role=grid with an accessible name, each row a button inside li role=row (Tab between vehicles, Enter/Space focuses), sortable by unit/status/speed/progress/ETA (aria-sort headers + sort chips), each row a full descriptive aria-label. This IS the no-WebGL fallback + the keyboard/SR path -- Phase 6.1 only renders it full-width instead of the map when WebGL is absent (same data, same socket). DetailPanel + EventsPanel are now their own files (split from the Phase-2 placeholder).

**SELECTION + FOCUS FLOW.** use-focus-vehicle.ts is the single action both the row click and the marker click converge on: selects in ops-store (pins detail, highlights via aria-pressed) AND flies the camera to the LIVE position via controller.focusVehicleAt([lng,lat]) (from the telemetry store -- fresher than the controller last snapshot). ADDED focusVehicleAt(position) + private flyOrCut to map-controller.ts: the old focusVehicle(id) reads this.snapshot.vehicles (only updated on setSnapshot) so it would fly to a STALE position; focusVehicleAt flies to the live point. Controller null (no-WebGL) -> step 2 skipped, selection still works.

**DETAIL / FEED LAYOUT (Motion allowed here -- UI chrome).** detail-panel.tsx = AnimatePresence mode=wait slide-in keyed on selected id (Linear easing [0.16,1,0.3,1]); route, live metrics (ETA/speed/% tabular), ordered stops (next highlighted via nextStopId), recent events. events-panel.tsx = AnimatePresence row enter (y+height)/exit; a SEPARATE sr-only aria-live=polite aria-atomic region announces ONLY the latest event, throttled+coalesced to 1.5s (the visible ul is aria-hidden so the SR is not double-announced and a burst does not flood). BOTH use useReducedMotion() -> under reduced motion slide/height/y collapse to opacity-only. The geofence beat is wired THREE ways: pulseZone (controller, 4.3) + the event row + the status flip (next tick telemetry -> fleet/detail re-render).

**DEMO AFFORDANCE (5.4 -- kept tight).** demo-control.tsx floats bottom-left over the map. Play geofence beat picks an en_route vehicle with currentZoneId===null (heading toward a zone), focuses it, resumes if paused, setSpeed 4x via sim.control so the crossing fires within seconds; plus pause/resume + 1/2/4/8x chips. NO full scrub UI (PLAN explicit -- deterministic seek stays a v2 lever). Frames typed against atlas-shared/schemas/ws SimControlFrame. Disabled until live.

**use-live-telemetry.ts EXTENSIONS (one socket).** Inside the existing callbacks: onSnapshot->applySnapshot, onTick->applyTick, onEvent->pushEvent, onStatus->simControl.setSend(live ? client.send : null). Teardown clears setSend(null). NO new socket, no polling.

**VERIFIED LIVE vs DEFERRED.**

- VERIFIED (prod build, same-origin proxy :3096 web->next start :3093 + ws->gateway :3092, Playwright headless): fleet panel lists the live fleet (18 vehicles; test saw 36 row-buttons because BOTH the lg:flex rail and the lg:hidden reflow copy mount at default viewport -- expected, 18x2); rows update between ticks; exactly ONE WebSocket; snapshot+tick+event frames parsed in-browser; row click sets aria-pressed + pins the detail h3; keyboard Enter focuses; the demo beat drives the feed 44->112 rows; the map canvas screenshot DIFFERS between two different focuses (camera fly-to genuinely moves to the live position); ZERO CSP violations. A raw ws probe independently confirmed snapshot(18/6/5/13) + ~1 Hz ticks + geofence/status/arrived/departed events with real zone names. Unit: 46 web tests.
- DEFERRED (Phase 6 / test / deploy): the no-WebGL toggle swapping map for full-width fleet panel (6.1 -- the panel is built + IS the fallback); a Playwright assert of the throttled aria-live text (the throttle is wired + the region is in the DOM, but SR-announcement asserts are Phase-8 a11y E2E); the DB seed against Postgres 5438 (the feed shows LIVE events without it; persisted history + SSR floor + reconnect-from-persisted-snapshot want it); a precise ETA-decrement pixel-diff (covered by the row-changes check + unit formatters; Phase 8).

**NOTE FOR designer-critic + reviewer.** (1) Dashboard / First Load JS rose to 164 kB (from 116) -- panels + Motion; the dashboard is exempt from the Lighthouse-SEO budget, the landing is Phase 6. Re-examine Motion code-split / the feed layout animation cost. (2) The fleet panel renders TWICE in the DOM (rail + reflow copy both mounted, shown/hidden by CSS) -> two store subscribers + two sort states; acceptable at this fleet size but a critic may prefer one instance moved by CSS -- flagged. (3) The demo speed bump affects the WORLD every client sees (single in-process engine, ADR-007) -- intended for a single-viewer demo; the README should note the demo controls are global, not per-session.

## Phase 6 public read REST endpoints close-out (backend-engineer, 2026-06-06) -- Task 6.3 backend portion

> The rate-limited public read REST surface is built and verified LIVE DB-less. This is what the web SSR floor / no-WebGL initial paint / external consumers read. The frontend-engineer building the Phase 6 SSR floor + landing can fetch these OR use the static shared seed fixture -- both are valid; the response TYPES are exported from `atlas-shared/schemas` so the SSR side can consume them types-only.

**ENDPOINTS (all GET, public, rate-limited, served DB-LESS from the in-memory `EngineService`).** Registered in `server/src/routes/public-read.ts`, wired in `app.ts` right after the `EngineService` is constructed (so they read `engineService.snapshot()` + `engineService.definitions`). They do NOT touch the DB -- the engine is the live source of truth (ADR-005), so the read surface is available without a reachable Postgres, exactly like the `/ws` channel. `/health` and `/ws` are unaffected.

- **`GET /api/fleet/snapshot`** -> `{ serverTick, ts, vehicles[], telemetry[] }`. The current authoritative per-vehicle telemetry (from `engineService.snapshot()`) joined with the static vehicle metadata. THE read surface for the SSR floor table + the no-WebGL initial paint. `vehicles.length === telemetry.length` (one telemetry per fleet member). LIVE: `serverTick` + `distanceAlongRouteM` advance across calls (verified).
- **`GET /api/routes`** -> `{ routes[], stops[] }`. Route GeoJSON LineStrings (geometry + `lengthM` + `loopMode`) + the ordered `route_stops` (with `point`, `seq`, `dwellSeconds`). Static for a run.
- **`GET /api/zones`** -> `{ zones[] }`. The geofence Polygon definitions (`kind`: depot/delivery/restricted). Static for a run.
- **`GET /api/vehicles`** -> `{ vehicles[] }`. The static fleet roster (label/type/routeId/baseSpeedMps/status). The LIVE position/status/ETA is the snapshot telemetry, NOT here.

**SHARED RESPONSE CONTRACT (single source, conventions section 5).** Added `src/lib/schemas/rest.ts` to `atlas-shared` (`fleetSnapshotResponseSchema`, `routesResponseSchema`, `zonesResponseSchema`, `vehiclesResponseSchema` + inferred types `FleetSnapshotResponse` etc.), re-exported from the schemas barrel (`atlas-shared/schemas`). The server uses the SCHEMAS as `fastify-type-provider-zod` serializer compilers (so the response is Zod-shaped + the handler return type is inferred); the web SSR floor imports the TYPES only (types-only -> Zod erased from the browser bundle, the verbatimModuleSyntax discipline). NO duplicated DTO. The snapshot response reuses `vehicleSchema` + `vehicleTelemetrySchema` but deliberately DROPS the WS envelope (`t`/`seq`/`protocolVersion`) -- a REST consumer does not need the frame-stream metadata, and reusing the WS frame verbatim would leak the transport into the read contract. (It also omits routes/zones, which have their own endpoints; the snapshot stays the fleet read.)

**RATE-LIMIT POSTURE (the security criterion's public-REST limits -- distinct from the WS limits).** The app-global `@fastify/rate-limit` (120/min from Task 1.1) is the floor; each REST route overrides it with a per-route `config.rateLimit`: `/api/fleet/snapshot` = **60/min/IP** (a no-WebGL client may POLL it), the three static-definition routes = **30/min/IP** (they do not change for a run). `trustProxy: true` (set in `app.ts`) means the limiter keys on the real client IP behind the Fly edge / web proxy. Verified: `x-ratelimit-limit` is 60 on the snapshot, 30 on the definitions.

**CACHE-CONTROL POSTURE.** Static definitions (`/api/routes`, `/api/zones`, `/api/vehicles`) -> `cache-control: public, max-age=60` (stable for a run, a short shared cache is fine). The live snapshot (`/api/fleet/snapshot`) -> `cache-control: no-store` -- NEVER serve a cached snapshot as current (the stale-as-live rule the credibility line forbids). Headers set via `reply.header(...)` in the handler.

**CORS.** Unchanged -- the existing `@fastify/cors` (`app.ts`, `origin: env.CORS_ORIGINS`) already allows the web origin (`http://localhost:3093` dev default). Verified: a GET with `Origin: http://localhost:3093` returns `access-control-allow-origin: http://localhost:3093`. So the GETs are reachable from the Next server (SSR fetch) + the browser. No `.env.example` change; no new env var.

**VERIFIED.** `pnpm --filter atlas-shared typecheck` + `pnpm --filter atlas-server build` (tsc) clean; root strictTypeChecked eslint over `server/src/**` + `src/**` EXIT 0; **32 server tests green** (27 prior + 5 new `server/src/routes/public-read.test.ts` -- integration via `app.inject()` against a REAL built app [the Zod serializer + the rate-limit plugin + the engine composition root], NOT mocks: each body re-parses against the shared schema, snapshot has live positions, routes have LineString geometry, zones have Polygons, rate-limit headers present + per-route budgets [60 vs 30], cache-control posture). **LIVE smoke DB-less** (`node --import tsx src/main.ts`, no Postgres on 5438): `/api/fleet/snapshot` 18 vehicles + 18 telemetry with finite lat/lng/heading, two calls 2 s apart show tick 138->141 + veh-1 dist 1347.21->1374.79 m (genuinely live), `no-store`, limit 60; `/api/routes` 6 routes + 13 stops, `/api/zones` 5 polygons, `/api/vehicles` 18, all `public, max-age=60` + limit 30. `/health` still `{status:ok}`; `/ws` untouched.

**FOR THE FRONTEND-ENGINEER (the SSR floor).** The 1 Hz `telemetry-store` shape is SSR-friendly: seed it from `GET /api/fleet/snapshot` for the no-JS table (vehicles + telemetry), and `GET /api/routes` + `/api/zones` for the static map/route/zone reference. Import the response types from `atlas-shared/schemas` (`FleetSnapshotResponse`, `RoutesResponse`, `ZonesResponse`, `VehiclesResponse`) -- types-only, Zod stays out of the browser. Either the REST fetch OR the static shared seed fixture (`web/src/mocks/static-fleet.ts`) is valid for the floor; the REST snapshot is LIVE (current positions) where the static fixture is the frozen baseline. Note the snapshot is `no-store` -- a Next `fetch` of it should not `cache: 'force-cache'` (use `no-store`/`revalidate: 0` for the live floor, or fetch the cacheable definitions with a short revalidate).

## Phase 6 close-out (frontend-engineer, 2026-06-06) -- degradation matrix + a11y pass + public/landing + SEO

> The graceful-degradation matrix (all four arms), the accessibility pass, and the public/landing + SEO surface are built and VERIFIED under a prod build. Lighthouse on the landing is 100/100/100/100. What Phase 7 (designer-critic + reviewer) and Phase 8 (tests) build on, plus the gotchas.

### 6.1 The four degradation arms

- **no-WebGL -> fleet table (NEW, the main 6.1 build).** A capability probe `web/src/lib/map/webgl-support.ts` (`isWebglAvailable()` -- throwaway canvas, asks webgl2/webgl/experimental, loses the context immediately, caches; false on the server + when blocked/unsupported; `setWebglAvailableForTest` seam). A `web/src/lib/store/view-mode-store.ts` (Zustand) holds `webglAvailable` (capability) + `preference` (`auto`/`map`/`table`, a FIRST-CLASS user toggle) + the pure `resolveEffectiveView(pref, webgl)` (CAPABILITY ALWAYS WINS -- no WebGL forces the table whatever the preference asks). `web/src/components/dashboard/ops-surface.tsx` is the NEW centre surface: probes on mount, renders EITHER `<MapRegion>`+`<DemoControl>` OR `<FleetTableView>` full-width, with a view toggle (Map/Table chips; Map disabled when WebGL absent) + a non-blocking `<DegradationNotice>`. `ops-dashboard.tsx` now mounts `<OpsSurface>` instead of `<MapRegion>` directly.
  - **THE ONE-SOCKET SUBTLETY (read this).** When the map renders, `use-live-telemetry.ts` (in MapCanvas) opens the socket + feeds the stores + drives the rAF loop. When the map does NOT render (no WebGL / table preference), MapCanvas is UNMOUNTED, so nothing feeds the telemetry store -- so `<FleetTableView>` runs a NEW headless hook `web/src/lib/ws/use-headless-telemetry.ts` (ONE socket, NO controller, NO InterpStore, NO rAF loop -- just the 1 Hz React store-feeding side of the live hook). The two are MUTUALLY EXCLUSIVE (OpsSurface renders one surface XOR the other), so there is NEVER a second concurrent socket -- the hard gate holds. The left-rail `FleetPanel` reads the same telemetry store regardless of which surface is active.
  - **`webglcontextlost` handling (NEW in `map-controller.ts`).** Added `webglcontextlost`/`webglcontextrestored` canvas listeners + an `onContextLost` option: on loss it `preventDefault`s (lets the browser/MapLibre attempt a restore) and starts a 1.5 s grace timer; no restore -> calls `onContextLost`, which MapCanvas wires to `viewMode.setWebglAvailable(false)` -> the dashboard swaps to the table. `destroy()` now also removes these listeners + clears the pulse + context timers (a small leak the prior code had -- pulse timers were never cleared on destroy; fixed).
- **prefers-reduced-motion -> snap/cut (AUDITED, already wired).** Map: controller `setReducedMotion` (fly->jump, pulse easing skipped) + the rAF loop snaps to ticks (Phase 4). Panels: detail + events collapse Motion to opacity-only via `useReducedMotion()` (Phase 5). Demo-control + the NEW surfaces (ops-surface toggle, degradation notice, fleet-table-view) use only CSS `transition-colors` -- no transform/layout animation, reduced-motion-safe by construction. NO new Motion in Phase 6. Audit: complete, no gaps.
- **no-JS SSR floor -> the landing (NEW).** `web/src/app/about/page.tsx` is a SERVER COMPONENT rendered ENTIRELY from a static deterministic source -- `web/src/lib/fleet/static-snapshot.ts` (`buildStaticSnapshot()` derives from the seeded `web/src/mocks/static-fleet.ts` fixture, faker-seeded, identical every render). Renders the pitch, the feature grid, a static fleet table (10 units with status LABELS via the server-safe `StatusBadge`), the route reference (length/stops/loop-mode), the zone reference (kind), the full SEO surface. NO client data dependency -- DELIBERATELY the static fixture, NOT the backend live `GET /api/fleet/snapshot` (that is `no-store`/live = a client-only dependency that hurts Lighthouse + breaks the no-JS contract; the backend note confirms "either is valid"). VERIFIED with `curl` (no-JS client): 70 KB meaningful HTML, pitch + all 10 units + 4 routes + 3 zones + JSON-LD + canonical + OG, no JS executed.
- **socket-down -> reconnect + freeze + reconcile (AUDITED, already wired).** The WS client (Phase 4) does backoff + freeze (InterpStore frozen -> loop holds at the last authoritative point, no drift) + seq-gap snapshot-reconcile; the connection pill flips to "Reconnecting". The NEW headless hook drives the SAME connection store, so the pill + the never-stale-as-live rule hold in the table surface too. Audit: complete.

### 6.2 Accessibility pass (WCAG 2.2 AA) -- findings + fixes

- **Fleet table as the first-class keyboard/SR view.** The rail `FleetPanel` (`role=grid` of `<button>` rows) audited -- sound. The NEW `FleetTableView` is a semantic `<table role="grid">` with `<th scope="col" aria-sort>` sortable headers, `<tr tabIndex=0 aria-selected>` selectable rows (Enter/Space focuses), full per-row `aria-label`, sr-only `<caption>`. The SSR-floor table is a plain semantic `<table>` (non-interactive) with `scope="col"` headers + caption.
- **Status never colour-alone -- AUDITED, single source held.** Every status surface (rail, table view, SSR floor, detail) goes through `StatusBadge` + `status-descriptor.ts` (dot + icon + LABEL). Confirmed `status-badge.tsx` has NO `'use client'` (server-renderable for the SSR floor). No colour-only chip anywhere.
- **FIX: theme-toggle had no visible focus ring.** Added `focus-visible:ring-accent focus-visible:ring-2 focus-visible:outline-none` (it had hover but keyboard focus was invisible -- a real AA gap).
- **FIX: contrast on the landing (Lighthouse-found, real).** `text-fg-subtle` (#6f7e93) on `bg-surface` (#121a26 / #18222f) measured 3.88-4.22 -- below 4.5. Bumped the landing table headers + route/zone meta to `text-fg-muted` (#97a4b6). NOTE for designer-critic/reviewer: `--color-fg-subtle` PASSES 4.5:1 on the darkest `bg-background` (~4.6) but FAILS on the lighter surface tokens -- safe for body on `bg-background`, use `text-fg-muted` for small text on `bg-surface`/`bg-surface-2`. The dashboard's existing `text-fg-subtle`-on-surface uses are mostly aria-hidden duplicate readouts (value is in the row aria-label), but a critic may want them swept for low-vision sighted users -- FLAGGED, not changed (out of 6.2 scope; the data is in the accessible name).
- **aria-live non-flooding -- AUDITED.** Events feed's coalesced 1.5 s polite sr-only region (visible list aria-hidden) -- sound (Phase 5).
- **MapLibre keyboard nav -- AUDITED.** `keyboard: true`; canvas is the focus target with its own aria-label; the wrapper `role="region"` points to the table alternative. Sound.
- **FIX: favicon 404.** Added `web/src/app/icon.svg` (radar-glyph SVG, Next serves it as the declared favicon) -- removed the `/favicon.ico` 404 that dinged best-practices.

### 6.3 Public/landing + SEO

- **Route:** `/about` (the SEO-bearing surface; `/` stays the live dashboard, Lighthouse-SEO-exempt). Per-page metadata + `alternates.canonical: '/about'` + OG.
- **OG image:** `web/src/app/opengraph-image.tsx` (`next/og` `ImageResponse`, runtime nodejs, 1200x630 control-room dark card, NO external fonts/glyphs -- ASCII "ATL" mark, satori-safe; an earlier non-ASCII glyph failed satori's dynamic-font fetch under no-network, replaced). `web/src/app/about/opengraph-image.tsx` re-exports it (with a LOCAL `runtime` export -- Next must read `runtime` statically per-route, it CANNOT be re-exported, which caused a build warning; fixed by declaring `runtime` locally + re-exporting only `default`/`alt`/`size`/`contentType`).
- **JSON-LD:** `web/src/components/seo/json-ld.tsx` (server component, `<script type="application/ld+json">`, `<`-escaped) -- the `/about` page emits a `WebApplication` schema.
- **sitemap.ts:** lists `/about` (priority 1) + `/` (0.8). **robots.ts:** unchanged.
- **LIGHTHOUSE (desktop preset, prod build, headless Chrome via npx): Performance 100 / Accessibility 100 / Best-practices 100 / SEO 100.** All four >= 95, all 100. Re-runnable: `next build && PORT=3093 next start`, then `npx lighthouse http://localhost:3093/about --preset=desktop`.

### Verified vs deferred

- **VERIFIED:** tsc --noEmit clean (web); ROOT strictTypeChecked eslint EXIT 0 over the web glob; `pnpm --filter atlas-web build` clean (`/about` STATIC, 117 kB First Load; `/` dashboard 189 kB exempt); prod CSP intact via `curl -I` on `next start` (`script-src 'self' 'unsafe-inline'`, NO `unsafe-eval`, `connect-src 'self'`, `worker-src 'self' blob:`) on BOTH `/` and `/about`; the no-JS SSR floor renders meaningful content via `curl`; OG images 200 image/png 132 KB on both routes; sitemap + robots + icon.svg serve; Lighthouse 100x4 on `/about`; 59 web unit tests green (added `view-mode-store.test.ts`, `webgl-support.test.ts`, `static-snapshot.test.ts`).
- **DEFERRED (Phase 8 test-engineer):** a Playwright DOM-render assert of the WebGL-stubbed-absent -> table swap; the `webglcontextlost` -> table degradation in a live browser; the headless-table-view ONE-socket browser assertion (mutual-exclusivity is structural; a DevTools check is E2E); Lighthouse CI wired to the >=95 gate (Task 8.3). No Playwright/RTL installed in the workspace yet -- component-render tests need `@testing-library/react`. The local Windows standalone build emits the documented EPERM symlink warnings (non-fatal; Linux/Fly emits clean -- the apex precedent).

### What Phase 7 / 8 needs

- **designer-critic (7.1):** review the NEW surfaces -- the `/about` landing (control-room register on a marketing page; Vercel/Stripe), the `OpsSurface` view-toggle + degradation notice (Linear micro-interaction bar -- currently CSS-only; a critic may want a considered map<->table transition, reduced-motion-safe), the full-width `FleetTableView`, the OG card. The fg-subtle-on-surface contrast note is flagged for a sweep decision.
- **reviewer (7.2):** the WebGL probe + context-lost grace window; the one-socket mutual-exclusivity (OpsSurface renders map XOR table -> use-live-telemetry XOR use-headless-telemetry; confirm no path mounts both); the SSR-floor-is-static-not-live decision (Lighthouse-driven, no client data dependency); the JSON-LD `dangerouslySetInnerHTML` (own static schema, `<`-escaped -- standard JSON-LD pattern, not user input).
- **test-engineer (8):** the deferred E2E items; component-render tests need `@testing-library/react`.

## Phase 7.2 code review verdict (reviewer, 2026-06-06)

> Read-only review of the full atlas diff since the planner (b82c837..HEAD, 8 commits). Verified green state: atlas-shared 14 tests, atlas-server 32 tests, atlas-web 59 tests all PASS; `pnpm --filter atlas-server build` + `atlas-web build` clean; root strictTypeChecked eslint over `web/src` + `server/src` + `src` EXIT 0. No committed `.env`; both `.env.example` present; grep for tokens/keys/secrets in source = clean. CSP in `web/next.config.ts` verified: NO `unsafe-eval`, `worker-src 'self' blob:`, `connect-src 'self'` (prod) with the dev WS origin gated to non-prod only — matches ADR-006. Keyless map gate holds.

**VERDICT: request-changes.** One ship-blocking defect (P0), one should-fix correctness bug (P1), plus smaller items. The reducer determinism/purity, the geofence hysteresis no-flap invariant, the WS backpressure (coalesce-to-latest, fleet-bounded), the one-socket mutual exclusivity (use-live-telemetry XOR use-headless-telemetry), the off-render-path rAF loop, and the keyless/CSP posture are all CORRECT — no change needed there. Next fixer: **backend-engineer** for P0 + P1 (both server-side); the P1 ETA fix touches `server/src/engine/reducer/tick.ts`.

### P0 — must fix before ship

1. **Unbounded `seek` = unauthenticated event-loop DoS on the public WS control channel.**
   `src/lib/schemas/ws/client-frames.ts` validates `seek.tick` as `z.number().int().nonnegative()` with NO upper bound; `gateway/ws-gateway.ts:254` passes it straight to `engineService.seek(frame.tick)` -> `engine.seek()` (`engine/shell/engine.ts:189`) which runs a SYNCHRONOUS `for (i=0; i<target; i++) tick(...)` fold with no cap. A single inbound frame `{t:'sim.control',action:'seek',tick:1000000000}` folds the reducer a billion times on the single-threaded event loop, freezing the engine and EVERY connected client. The per-connection token bucket (10/s) does not help — one frame is enough. This is the one input surface the security posture (AGENT_NOTES "Security posture") explicitly names.
   **Fix:** cap the seek target. Add `.max(MAX_SEEK_TICK)` to the schema (e.g. a few hours of ticks, ~100_000) AND clamp defensively in the gateway/engine (`Math.min(cap, target)`) so the bound is enforced even if the schema drifts (the same defence-in-depth the code already applies to `setSpeed` at gateway-level). Pick the cap from the demo's realistic tick horizon.

### P1 — should fix before merging to main

2. **ETA is wrong (reports 0) for every ping_pong vehicle travelling backward.**
   `engine/reducer/tick.ts:313` computes `remainingDistanceToStop(vehicle.s, nextStop.stop.s)` = `Math.max(0, stopS - currentS)` (geo/eta.ts), which is direction-UNAWARE. For a ping_pong route in direction -1, `findNextStop` (route-progress.ts) correctly returns a stop with `s < currentS`, so `stopS - currentS < 0` and the helper returns 0 -> `etaSeconds` collapses to the dwell remainder (0 while en route) for the backward half of every ping_pong run. 4 of the 6 Porto routes are `ping_pong` (`seed/porto-geometry.ts`), so roughly half the fleet shows ETA 0 half the time. This violates the "ETAs are live and correct" success criterion. Note `findNextStop` ALREADY returns a direction-correct `remainingM = Math.abs(best.s - s)`, and the braking taper uses it correctly — only `telemetryFor`'s separate, direction-blind recompute is wrong.
   **Fix:** in `telemetryFor`, derive the remaining distance from `nextStop.remainingM` (the direction-aware value `findNextStop` already returns) rather than re-calling `remainingDistanceToStop(vehicle.s, nextStop.stop.s)`. Add a backward-direction ETA fixture to the geo/reducer tests.

### P2 — nice-to-have (defer if cheap)

3. **`event` frames ignore the `bbox` scope.** `gateway/ws-gateway.ts:153-156` filters per-tick event fan-out only by `scope.vehicleIds`, not by `scope.bbox`. A connection scoped to a viewport still receives events for vehicles outside its bbox. Harmless at 18 vehicles (telemetry coalescing already applies bbox), and v1 clients use whole-fleet, but it is inconsistent with the telemetry scoping. Filter events through `inScope(conn.scope, ...)` if/when bbox scoping ships in a client.
4. **`bbox` tuple is not validated for west<east / south<north** (`client-frames.ts`). An inverted box silently culls everything rather than being rejected. Cosmetic; the server-side filter degrades safely to "empty". Add a `.refine` only if a client starts sending bboxes.
5. **DB seed against Postgres on 5438 still not exercised end-to-end** (carried from Phase 3/4). The engine + WS + REST run DB-less by design, but the events-feed persisted history, the SSR-from-persisted-snapshot, and reconnect-from-persisted-snapshot want it. Deferred to test/deploy per the existing handoff — re-confirm before the deploy claims "live demo verified".

### Confirmed correct (no action)

- Reducer is pure: no `Date.now()`/`Math.random()`/IO inside `tick`; the mulberry32 PRNG is carried in `WorldState` and threaded, so seek==replay holds (tested). The IO shell owns the clock + fixed-dt accumulator + max-catch-up cap.
- Geofence hysteresis (`geo/geofence.ts`, `CONFIRM_TICKS=2`): a skimmer resets `pendingTicks` on every agreeing tick and never accumulates 2 consecutive disagreements -> no flap; a genuine crossing fires exactly one enter/exit at ~2 ticks latency (within tolerance). The reducer recomputes `currentZoneId` from the CONFIRMED state.
- WS backpressure: per-connection coalesce-to-latest-per-vehicle (`connection.ts` `stageTick`/`takePending`), memory bounded by fleet size not tick count; recovered consumers flushed on the heartbeat drain pass with the NEWEST serverTick (never replayed stale motion).
- Inbound validation: EVERY client frame `clientFrameSchema.safeParse` (jitless Zod) at the boundary; non-JSON and schema-invalid frames dropped silently; handler exceptions caught; per-connection token bucket. (The only gap is the unbounded `seek` VALUE — P0 above — not the validation path itself.)
- Interpolation loop genuinely off the React render path: `InterpStore` is a plain class, one `RafLoop` -> one `setVehiclesGeoJSON` per frame; only low-frequency status + zone-pulse touch React. `tickProgress` clamps at 1 so a late tick FREEZES (no dead-reckoning drift); wrap detection snaps instead of tweening a rewind. lerp-along-`s` + shortest-arc heading are correct.
- Exactly ONE socket: map surface (`use-live-telemetry`) XOR table surface (`use-headless-telemetry`), a strict render-tree XOR in `ops-surface.tsx`; `connect()` is idempotent.
- CSP/keyless: no `unsafe-eval`; MapLibre worker + same-origin `.pmtiles`; dev WS origin gated to non-prod; no paid secret committed; JSON-LD `dangerouslySetInnerHTML` is own static schema, `<`-escaped (standard pattern, not user input).

## Phase 7.1 designer-critic defect list (2026-06-06)

> Reviewed via LIVE headless screenshots (Playwright/Chromium against the prod build on a same-origin proxy: Next prod 3093 + Fastify gateway DB-less 3092, proxied to one origin 3094 so the strict CSP connect-src self covers the WS). The pill read Live in both themes, all four WS frame types flowed, ZERO CSP violations. Surfaces: dashboard (dark+light), vehicle detail open, geofence beat mid-fire, table fallback, mobile 360 (dark+light), tablet 768, wide 2560, /about (dark+light). Gated against Linear (alive/easing micro-interaction bar), Vercel (dense-dark control-room restraint), Stripe (premium finish). Zero pochwal. The HARD GATES (one socket per surface, status-never-colour-alone, reduced-motion guards, no unsafe-eval, keyless map, static SSR floor) MUST survive every fix.

CONTEXT: the keyless .pmtiles Porto extract is a Phase-9 deploy artifact (gitignored, 404 in this review), so the map renders with NO land/water/roads -- only the painted background + the fleet/route/zone app layers. Several P0/P1s below are about how the surface reads in exactly this state. The single most important fix is P0-1.

### P0 -- blockers (the wow surface does not read as a control-room map in the first 10 seconds)

- P0-1 -- The map basemap background is the SAME colour as the app void, so the wow surface reads as a black hole, not a map. web/src/lib/map/basemap-style.ts dark earth (background paint) = #0b1018 = --color-background. With the .pmtiles absent the whole map is one flat #0b1018 field indistinguishable from the chrome; even WITH tiles the land is near-invisible against the same value. Viewer 1 (10-second judgement) lands on what looks like an empty canvas with floating dots -- fails Vercel (looks like money was spent) and Linear (feels alive/premium) outright. FIX: set the dark basemap earth to a distinctly LIFTED tone (e.g. #0d141d/#101824, above --color-surface) so the plate reads as a deliberate dark slate even with zero tiles; add a faint live-grid graticule/vignette as a painted floor; step water/landuse/road tones in clear value tiers above earth. Verify against the deployed .pmtiles too.

- P0-2 -- The light-theme map does not switch: the map plate stays dark in light mode. In the light dashboard capture the chrome flipped to cool-paper but the centre map stayed dark void. Either controller.setTheme(light) -> map.setStyle() is not firing on the system-light/initial path, or the light basemap (earth #eef1f5) is not applied. PLAN.md: the theme toggle switches the MapLibre style, not just chrome; both themes intentional. A half-light app with a dark map is a broken theme. FIX: ensure the style swap runs on initial resolved theme AND on toggle (re-add app sources on style.load); verify the light plate paints #eef1f5-tier land. Re-test under prefers-color-scheme: light (no stored pref) and an explicit toggle.

### P1 -- should-fix

- P1-1 -- Narrow/tablet reflow leaves a MASSIVE black void below the fold. At 360 and 768 px the page is h-dvh + overflow-hidden; the stacked reflow renders ~2-5 fleet rows then the lower 60-70% is dead black (tablet + both mobile shots). The fleet list is height-trapped and the detail/events stacks sit empty. Viewer 2 (dispatcher on a phone) sees a sliver of fleet and a wall of nothing. FIX: let the narrow column SCROLL (drop overflow-hidden/h-dvh lock for under-lg, or a real scroll container + grow the fleet list), or adopt the planned bottom-sheet/tabs. Fails Stripe (every viewport finished).

- P1-2 -- On wide (2560 px) the fleet floats in a small central cluster with vast empty margins; the map does not use the canvas. map-controller.ts inits at a FIXED DEMO_CITY_CENTER/DEMO_CITY_ZOOM regardless of viewport, so on a 1440px+ recruiter monitor (the primary device) and at 2560 the fleet sits in the middle third -- the opposite of Vercel dense. FIX: fitBounds to the fleet/DEMO_CITY_BBOX (with padding) on load and on significant resize; clamp a max zoom so it never over-zooms on ultrawide.

- P1-3 -- Map marker labels (veh-1, veh-14) contradict the fleet panel naming (Unit 1, Unit 14). The core console correspondence -- this dot is that row -- is broken by two identifiers for the same vehicle; reads as debug/unfinished. FIX: render the human label (Unit 1 or a short U1/7) on the marker, matching the panel and the events feed (Unit 14 entered...). Drop the raw veh-N slug from the visible UI.

- P1-4 -- The empty VEHICLE detail rail is a large, under-designed dead zone before selection. On first paint the right rail top half is a big empty panel with two lines of muted helper text in space (~25% of the screen). FIX: collapse the detail panel until a selection exists (give the events feed the reclaimed height -- it is the live-proof surface), or fill the empty state with a designed fleet-summary instrument (counts by status, active geofences, avg speed). Stripe/Vercel never ship a quarter-screen of placeholder text.

- P1-5 -- The map-to-table view transition is an instant hard cut (CSS-only swap). OpsSurface swaps MapRegion XOR FleetTableView with no transition -- the lazy default vs Linear. FIX: a considered, reduced-motion-safe crossfade/height-settle (~180-220 ms cubic-bezier(0.16,1,0.3,1), collapsed under prefers-reduced-motion).

- P1-6 -- /about initial theme under system-light resolves to DARK before flipping. With defaultTheme=dark enableSystem and no stored pref, a prefers-color-scheme: light visitor first sees the dark landing (the dashboard flipped correctly at the same setting; /about is a fast static page caught dark). Wrong-theme first impression / FOUC on the SEO front door. CONFIRMED light renders correctly with an explicit theme -- this is the system-resolution/flash path, not a hard break. FIX: verify the inline theme script applies system before paint on /about; re-test prefers-color-scheme: light cold.

### P2 -- polish

- P2-1 -- The amber radar-sweep + brand accent do little work on the dashboard. Accent mostly appears as at-stop status + the demo button; the top-bar sweep is barely visible. Let the accent carry one more intentional moment (LIVE pill pulse; extend the selected-row rail language).

- P2-2 -- Zone-pulse on a geofence event is too subtle to register as the world reacting. The restricted-zone highlight is a faint fill bump. The PLAN dramatic beat wants a clearer single eased pulse (opacity/line-width bloom over ~1.2-1.4 s, reduced-motion-safe). Tune amplitude/easing.

- P2-3 -- Fleet-row progress bars + percent/ETA readouts are visually quiet. Thin low-contrast amber on a dark track; cannot read progress distribution at a glance. Taller/cleaner bar + sweep small readouts to fg-muted (fg-subtle fails 4.5:1 on bg-surface, per Phase 6 note) for sighted low-vision users, not just the aria-label.

- P2-4 -- Demo control + speed chips lack affordance polish. 1x/2x/4x/8x + pause read as plain text toggles; inactive chips are very low contrast. A clearer rest/hover/active tier (Stripe control finish) lifts the floating bar from dev-control to product.

- P2-5 -- /about is strong but very mono-tonal (one dark panel after another). Type hierarchy is genuinely good (keep the wow sentence as H1). For Stripe long-form: one moment of art direction (a faint map silhouette behind the snapshot, or the amber sweep as a section rule). Low priority -- already Lighthouse 100x4.

### Verified-good (do not regress -- not praise)

- One WebSocket per surface; pill Live; all four frame types; ZERO CSP violations under the strict no-unsafe-eval prod CSP. Detail-panel pin (route/ETA/stops/NEXT badge/recent events), the geofence beat (focus + 8x + event lands in feed AND detail), the table fallback density, and status-never-colour-alone (dot+icon+label) all function as specified.

### Capture recipe (re-review after fixes)

pnpm --filter atlas-web build (Windows: standalone EPERM symlink warning is non-fatal, BUILD_ID still emits) -> next start -p 3093 + server on 3092 (env inline; tsx watch/dev does NOT load .env) -> a tiny same-origin proxy forwarding /ws to 3092 and the rest to 3093 (so connect-src self covers the WS) -> Playwright/Chromium with --use-gl=swiftshader against the proxy origin, colorScheme per theme, ~5-6 s settle.

## Phase 7.2 fixes applied (backend) — backend-engineer, 2026-06-06

> The two ship-blocking server-side defects from the Phase 7.2 review (P0 + P1) are fixed, plus both P2 items. All server-side; the web package was NOT touched (the frontend-engineer is applying the designer-critic 7.1 fixes there in parallel). Reviewer: re-verify against the verdict above.

- **P0 — unbounded WS `seek` DoS — FIXED with defence-in-depth (mirrors the `setSpeed` clamp precedent, ADR-002).**
  - New shared constant **`MAX_SEEK_TICK = 3_600`** in `src/lib/schemas/ws/protocol.ts` (next to `HEARTBEAT_INTERVAL_MS` — the discoverable, FE/BE-shared home; exported via the ws barrel so both `atlas-shared/schemas/ws` consumers see it).
  - **Schema bound:** `seek.tick` is now `z.number().int().nonnegative().max(MAX_SEEK_TICK)` (`client-frames.ts`). An over-cap frame fails `clientFrameSchema.safeParse` at the gateway boundary and is dropped (the existing validation path).
  - **Engine clamp (defence in depth):** `SimulationEngine.seek()` (`server/src/engine/shell/engine.ts`) now clamps `target = Math.min(MAX_SEEK_TICK, Math.max(0, Math.floor(targetTick)))`, so the synchronous fold is bounded even if the schema is bypassed (a direct `seek()` call, or future schema drift). The gateway dispatch at `ws-gateway.ts` was left calling `engineService.seek(frame.tick)` — the clamp lives in the engine, the single choke point, so every caller is covered.
  - **WHY THE CAP IS TIGHT (3_600, not 86_400):** I first tried 24 h (86*400). The clamp worked, BUT folding even the \_capped* max took ~10.5 s of synchronous event-loop time — a milder DoS in its own right (one max-seek frame freezes every client for 10 s). The review explicitly said "keep the cap tight so even the max is fast." Measured: 3_600 ticks folds in ~0.25 s, 7_200 in ~0.5 s. The longest Porto route is traversed in a few hundred ticks, so 1 h (3_600) is already far beyond any demo replay horizon while keeping the worst case cheap. The cap value is now load-bearing for liveness, not just correctness — document this if it is ever raised.

- **P1 — ETA reports 0 for backward ping_pong vehicles — FIXED.** In `telemetryFor` (`server/src/engine/reducer/tick.ts`) the ETA remaining distance is now `nextStop.remainingM` (the DIRECTION-AWARE `Math.abs(stop.s - s)` that `findNextStop` already returns) instead of the direction-blind `remainingDistanceToStop(vehicle.s, nextStop.stop.s)` (`Math.max(0, stopS - currentS)`, which returned 0 for direction -1). The `remainingDistanceToStop` import was removed from `tick.ts` (now unused there; it still lives in `atlas-shared/geo` for any other consumer). 4 of 6 Porto routes are ping_pong, so this restored a live ETA for ~half the fleet on the backward half of every run.

- **P2-1 (DONE) — event frames now respect `bbox` scope.** In the `fanOutTick` event loop (`ws-gateway.ts`) the per-tick events are now filtered by the in-scope vehicle-id set derived from the _already-scoped_ telemetry (`new Set(scoped.map(t => t.vehicleId))`), which is the authoritative in-scope set for that tick (vehicleIds AND bbox). Previously it filtered only by `scope.vehicleIds`. Cheap (one Set per connection per tick, only when events fire) and exactly consistent with the telemetry cull. v1 clients still use whole-fleet, so behaviour is unchanged for them.

- **P2-2 (DONE) — `bbox` tuple ordering validated.** `bboxSchema` (`client-frames.ts`) gained `.refine(([w,s,e,n]) => w < e && s < n)`. An inverted/degenerate box is now rejected at the boundary (frame dropped). The server-side `inScope` filter already degraded safely to "empty" for a bad box, but rejecting at the boundary is the honest "validate at the boundary" posture. NOTE: `bboxSchema` is now a `ZodEffects` (refined), not a bare `z.tuple` — `z.infer<typeof bboxSchema>` (`Bbox`) is unchanged (`[number,number,number,number]`), so no downstream type change; the web imports the type only.

- **P2-3 (DB seed e2e) — NOT done (out of scope for this fix pass, correctly deferred).** The review's P2 #5 (run the DB seed against Postgres on 5438 end-to-end) is a test/deploy task, not a code defect, and the existing handoff already carries it. Left for test-engineer / deploy.

- **`.env.example` — UNCHANGED.** No new env var; no secret introduced. `MAX_SEEK_TICK` is a compile-time shared constant, not config.

- **VERIFIED:** `pnpm --filter atlas-server build` (tsc) clean; root strictTypeChecked eslint over `server/src/**` + `src/**` EXIT 0; **34 server tests green** (was 32 — added the seek-clamp test in `engine.test.ts` and the backward-direction ETA test in `tick.test.ts`) + 14 shared tests green. The seek-clamp test asserts a `seek(1e9)` lands on exactly `MAX_SEEK_TICK` and returns in < 5 s (proving the fold is bounded, not unbounded); the ETA test builds a direction-`-1` ping_pong `VehicleState` past a stop and asserts a finite non-zero `etaSeconds`.

## Phase 7.1 fixes applied (frontend) — designer-critic defect list (frontend-engineer, 2026-06-06)

> Applied the Phase 7.1 designer-critic defect list. Both P0, all six P1, four of five P2 resolved; re-captured live (prod build + same-origin WS proxy 3094 -> next 3093 / gateway DB-less 3092, Chromium swiftshader against the proxy origin per the critic recipe). All hard gates held: ONE WebSocket per surface, status-never-colour-alone, reduced-motion guards, NO unsafe-eval, keyless map, static SSR floor. tsc clean, root eslint exit 0, prod build clean, 59 web tests green. Changes are ALL in `web/` (the server defects are a parallel backend-engineer's).

**P0-1 (map plate == app void) — RESOLVED.** `web/src/lib/map/basemap-style.ts`: lifted the dark `earth` #0b1018 -> #10171f (distinctly above `--color-surface`), stepped water/landuse/park/buildings/roads/boundary into clear value tiers above earth. Added a PAINTED control-room graticule as the keyless floor: `buildGraticule()` emits two GeoJSON line FeatureCollections (minor + every-4th major) over a 1.5x-padded `DEMO_CITY_BBOX` at a 0.005-deg step, snapped to the grid so lines are stable under pan; two geojson sources + line layers drawn just above `background`. RESULT: with the `.pmtiles` absent (Phase-9 artifact, 404 here) the map reads as a deliberate dark slate with a faint lat/lng grid, not a black hole; the tiers also lift the with-tiles look. Re-capture confirms the lifted plate in both themes.

**P0-2 (light map plate does not switch) — RESOLVED.** Root cause: next-themes resolves the SYSTEM theme AFTER the map mounts, so a `setTheme('light')` arrived while the dark style was still loading and was lost; and the map-canvas theme effect only depended on `resolvedTheme` (undefined at mount). Fix in `map-controller.ts`: a `styleReady` flag set on first `load`, a `pendingTheme` that a pre-ready setTheme defers and `load` applies, `applyTheme` split from the readiness gate. In `map-canvas.tsx` the theme effect now also depends on `ready`. Re-capture: light-system dashboard bodyBg #eef1f5 with the map plate cool-paper #e7ecf2 (light graticule + light zone tints), dark-system stays #0b1018 — the system/toggle genuinely swaps the basemap, not just chrome.

**P1-1 (narrow/tablet black void) — RESOLVED.** `ops-dashboard.tsx`: dropped the unconditional h-dvh+overflow-hidden trap. Now min-h-dvh (scrolling document) with lg:h-dvh lg:overflow-hidden (fixed control-room only on lg+); the ops-surface block is h-[60vh] min-h-[22rem] lg:h-auto lg:min-h-0 and the narrow stacked panels flow at natural height in the scroll column. Re-capture at 360 + 768: map block then Fleet/Detail/Events flow beneath, page scrolls, no void.

**P1-2 (fixed centre/zoom, tiny cluster on wide) — RESOLVED.** `map-controller.ts`: new `fitToFleet(animate)` computes bounds from the live vehicle positions (fallback DEMO_CITY_BBOX), fitBounds with 56px padding + maxZoom 15.2 clamp. Called on `load`, on the FIRST live WS snapshot (the map mounts on the static fixture; the real positions arrive over WS), and debounced on `resize` — unless the user manually moved the camera (`userInteracted` set from drag/zoom/rotate start with originalEvent). Re-capture at 2560 + 1440: the fleet fills the centre column.

**P1-3 (veh-N vs Unit N) — RESOLVED.** The marker text-field label needed glyphs (the keyless basemap has none -> silently no-op) AND the rAF loop emitted label: state.next.vehicleId (= veh-N). Fix: `shortVehicleLabel("Unit 7") -> "U7"`, a keyless canvas label chip per short-label (`createLabelImage`, registered by the controller `registerLabelImages` on style.load [theme recolour] + on snapshot [new vehicles]), the label layer switched from text-field to icon-image ['concat','atlas-label-',['get','marker']] with icon-optional. The rAF loop emits marker: store.markerLabel(id) (InterpStore gained setMarkerLabels/markerLabel, seeded in use-live-telemetry from the snapshot vehicles). Re-capture: markers read U1..U18, matching the panels/feed. No glyph server, no third-party — keyless intact.

**P1-4 (empty detail rail) — RESOLVED.** New `web/src/components/panels/fleet-summary.tsx` fills the detail-panel empty state: status breakdown (per-status counts, each dot+icon+label — never colour-alone), vehicles-in-a-geofence count, fleet avg speed — from the SAME 1 Hz telemetry store (no extra socket, no rAF coupling). detail-panel.tsx renders it instead of the placeholder; the title flips Vehicle/Overview.

**P1-5 (map<->table hard cut) — RESOLVED.** `ops-surface.tsx`: the swap is wrapped in Motion AnimatePresence mode=wait keyed on the effective view, a ~200ms opacity crossfade with Linear easing [0.16,1,0.3,1]; under useReducedMotion() the duration collapses to 0. Map and table stay mutually exclusive (one socket).

**P1-6 (/about dark under prefers-color-scheme: light) — RESOLVED.** `providers.tsx`: defaultTheme dark -> system (with enableSystem). next-themes resolves prefers-color-scheme before first paint (confirmed: the served /about HTML carries the matchMedia + system branch). DARK stays the fallback when there is no system pref. Re-capture: /about light renders light, dark renders dark — no FOUC.

**P2 quick wins DONE: P2-1/P2-2/P2-3.** P2-1 the pill adopts the amber language when live (bg-accent-soft text-accent-ink ring-accent/40). P2-2 a clearer geofence pulse — pulseZone raises fill-opacity 0.08->0.42, line-opacity 0.55->1, blooms line-width 1.25->3.5 with a transition eased back over 1.4s (reduced-motion skips the transition). P2-3 taller fleet-row progress bars (h-1->h-1.5, inset ring, full-amber fill) + readouts swept fg-subtle->fg-muted (Phase-6 note: fg-subtle fails 4.5:1 on bg-surface).

**P2 DEFERRED.** P2-4 (demo-control + speed-chip affordance tiering) and P2-5 (/about art-direction moment) deferred: pure polish, P2-5 is already Lighthouse 100x4, both larger than quick wins. A future polish pass or the doc-writer screenshot pass can take them.

**GATES RE-CONFIRMED.** ONE WebSocket per surface (untouched — use-live-telemetry XOR use-headless-telemetry; pill Live, no second socket); status-never-colour-alone (FleetSummary reuses statusDescriptor dot+icon+label); reduced-motion (the new crossfade + pulse + summary guard or are CSS-token-only); CSP NO unsafe-eval verified via curl -I on next start (script-src 'self' 'unsafe-inline'; connect-src 'self'; worker-src 'self' blob:); keyless (label chips + graticule are canvas/GeoJSON, no glyph server, no third-party); static SSR floor (/about untouched except the theme-resolution fix). The actual Porto `.pmtiles` extract stays a Phase-9 deploy artifact (gitignored) — P0-1's painted graticule is precisely what makes the keyless no-tiles state read intentionally; verify the lifted tiers also hold once the extract deploys.

## Phase 8 close-out (test-engineer, 2026-06-06) — Tasks 8.1 + 8.2 + 8.3

> The test suite is rounded out. The deferred E2E + Lighthouse CI items are built and VERIFIED. Everything below is the recipe the doc-writer + deploy phase reuse, plus the one product finding the E2E surfaced.

### 8.1 — the one genuine unit gap: WS frame Zod schemas

`src/lib/schemas/ws/ws-frames.test.ts` (atlas-shared, co-located with the schemas it owns). It was the ONLY real 8.1 gap — the gateway has live integration tests (`gateway.test.ts`) but nothing exercised the frame SCHEMAS at the contract level. Covers: client + server frame ROUND-TRIP over the wire (`JSON.stringify`→`JSON.parse`→`safeParse`, the way the gateway parses an inbound message); and the malformed-REJECTION cases the Phase 7 review hardened — the over-`MAX_SEEK_TICK` seek (asserts cap+1 fails AND cap passes, so the exact boundary is pinned), the inverted/degenerate bbox (the P2 `.refine`, both west≥east and south≥north + a zero-area box), out-of-WGS84-range coords, unknown discriminators, an unknown `sim.control` action, missing `multiplier`/`tick` payloads, non-object inputs (what a non-JSON message parses to), empty-string vehicleIds, the wrong `protocolVersion`, out-of-range telemetry. **+30 tests → atlas-shared 44 (was 14).** The OTHER 8.1 paths the PLAN names ALREADY exist + pass (verified, not duplicated): backward-direction ping_pong ETA = `server/src/engine/reducer/tick.test.ts` "ETA (direction-aware)"; seek-clamp = `server/src/engine/shell/engine.test.ts`; geofence no-flap + projection + ETA-vs-known-geometry = `src/lib/geo/geo.test.ts`; reducer determinism/purity/seek==replay = `tick.test.ts`. No coverage padding added.

### 8.2 — the Playwright E2E package (`projects/atlas/e2e/`) — LAYOUT + THE LOAD-BEARING RECIPE

A dedicated workspace package (pulse-style, NOT apex's web/e2e — atlas needs to orchestrate THREE processes, which belongs in its own package), in the `pnpm-workspace.yaml` `projects/*/e2e` glob. Files:

- **`playwright.config.ts`** — Chromium only (the viewer-1 desktop audience), serial + 1 worker (the suite shares ONE in-process engine = one source of truth, ADR-007; the demo speed/seek controls affect the WORLD every connection sees, so parallel specs would race the shared sim). `colorScheme: 'dark'` (canonical). `launchOptions.args: ['--use-gl=swiftshader','--enable-unsafe-swiftshader']` so MapLibre gets a software WebGL context headless. `expect.timeout: 20s` (the engine ticks ~1 Hz; web-first assertions retry until real state transitions land — no fixed sleeps anywhere). `webServer.command` builds the web then boots the stack; `reuseExistingServer: !CI` lets a dev point at an already-running :3096.
- **`atlas-stack.mjs`** — the single `webServer` command. Spawns + supervises THREE processes and keeps them alive: (1) the Fastify gateway **DB-LESS** on :3092 (`pnpm -F atlas-server start`, with `DATABASE_URL` pointed at the unreachable 5438 — REQUIRED by the env schema, but the engine boots from the frozen Porto baseline + the sink swallows DB failures off the hot path, so the fleet MOVES with no Postgres); (2) Next prod `next start` on **:3097** (an off-port so an E2E run never collides with the :3093 dev server; `NEXT_PUBLIC_WS_URL` UNSET so the client derives the WS same-origin = the proxy origin); (3) the proxy on :3096.
- **`atlas-proxy.mjs`** — THE recipe (the prod-CSP same-origin-WS lesson, the same as the Phase 4.2/7.1 throwaway proxies, now committed for the suite). A dependency-free Node `http` reverse proxy on **:3096** (the single origin the suite + LHCI target): forwards `/ws` (WebSocket upgrade via raw socket piping) AND `/api/*` + `/health` to the gateway :3092, everything else to Next :3097. So the browser sees ONE origin and the strict prod `connect-src 'self'` covers the WS — exactly the deploy posture (ADR-007), verified locally. WHY prod, not `next dev`: the no-`unsafe-eval` CSP blocks React Refresh's eval which aborts the ssr:false map chunk (the documented dev-only artifact); the zero-CSP-violation + one-socket assertions are only meaningful against the prod build.
  - **TWO GOTCHAS that bit me building the .mjs (both fixed):** (a) a block comment containing the literal `**/e2e/*.mjs` glob TERMINATES the comment early at the `*/` → `ReferenceError` at load; reworded both comments to avoid `*/`. (b) spawning the proxy via `process.execPath` (the node binary) breaks under `shell:true` on Windows because the path contains spaces (`C:\Program Files\nodejs\node.exe`); spawn via `'node'` resolved on PATH instead.
- **`helpers/ws-tracker.ts`** — the senior-signal helper (mirrors pulse's sse-tracker). Tracks every `/ws` WebSocket + `/api/fleet/snapshot` polls; exposes `openCount()` = sockets NOT yet closed. KEY INSIGHT: across a surface swap (map→table) the old socket CLOSES and a new one OPENS, so the CUMULATIVE count is 2 but the CONCURRENT (`openCount()`) count must never exceed 1 — that is the real "exactly one socket" invariant. Assert `openCount()` for the concurrency gate, not cumulative `.length`.
- **`tests/`** — 7 specs, ALL PASS (~1.4 min full run): `live-map.spec.ts` (canvas mounts under prod CSP, pill Live, ONE WebSocket carrying snapshot/tick frames, the server tick CLIMBS = fleet moving, NO `/api/fleet/snapshot` polling = pushed-not-polled); `eta-and-geofence.spec.ts` (focused-vehicle ETA decrements read off the detail `dd.text-accent-ink` metric; a geofence enter/exit row "Unit N entered/left ..." lands in the feed — both DRIVEN by the deterministic "Play geofence beat" affordance + web-first polling, NEVER a sleep); `no-webgl.spec.ts` (the Table view shows the live fleet grid, map canvas gone, `openCount()` settles to 1 across the swap); `degradation.spec.ts` (reduced-motion keeps the map live + the tick climbing, no crash); `keyboard-and-reconnect.spec.ts` (Tab→fleet row→Enter sets aria-pressed + pins the detail h3→Tab flows on; offline→"Reconnecting" pill→online→"Live").

**DETERMINISM DISCIPLINE HELD.** The geofence beat is reproduced by clicking "Play geofence beat" (focuses an approaching en-route vehicle + setSpeed 4x via `sim.control`) and waiting on REAL STATE (an event row appears / the ETA value drops) — the engine is driven, never slept on. The `setSpeed` advances the world faster but the math is identical; the assertion is the state transition.

### 8.3 — Lighthouse CI

`e2e/lighthouserc.json` (mirrors pulse/apex): audits ONLY `/about` (the SEO surface, server-rendered from the static fixture — no client live-data dependency), `/` dashboard documented EXEMPT (client-only live first paint, no crawler value). Gates `>= 0.95` on all four categories (median of 5, desktop preset, swiftshader chromeFlags) + CWV (LCP < 2.5 s, CLS < 0.1; TBT warn). **RESULT: 100/100/100/100 across ALL 5 runs** (matches the frontend-engineer's Phase 6 measurement; now ENCODED as a re-runnable gate). Run: stack serving :3096, then `pnpm -F atlas-e2e test:lh` (or `lhci autorun --config=lighthouserc.json`). LHCI does NOT start the stack — bring it up first (the webServer, or `pnpm -F atlas-e2e proxy` with web+gateway up).

### LINT / GATE posture (matches pulse/apex, pre-commit will pass)

The e2e TS (config + tests + helpers + env.d.ts) is linted by the ROOT strictTypeChecked config and PASSES exit 0 (the `*.spec.ts` test-relaxed rules apply). The `.mjs` orchestrator + proxy are root-eslint-IGNORED by the existing `**/e2e/*.mjs` ignore in `eslint.config.mjs` (operational tooling, the apex precedent) — they carry `// @ts-nocheck` and are not in any tsconfig. `e2e/tsconfig.json` extends `../tsconfig.base.json`, includes only the config/tests/helpers/env.d.ts, `@helpers/*` path alias. Deps in the `atlas-e2e` package ONLY (`@playwright/test`, `@lhci/cli`, `dotenv`, `typescript`, `@types/node`) — NOT added to the production server/web packages (the constraint). Chromium reused from the workspace install (`pnpm -F atlas-e2e install:browsers` if absent).

### THE ONE PRODUCT FINDING (flagged, NOT fixed — out of test scope, for the frontend-engineer)

A genuine no-WebGL browser (WebGL HARD-disabled at the Chromium level, `--disable-webgl`) CRASHES the dashboard with an uncaught "Application error: a client-side exception has occurred". Root cause: `view-mode-store.webglAvailable` DEFAULTS to `true`, so the first render mounts `<MapRegion>` and the lazy MapCanvas chunk; `AtlasMapController`/`new maplibregl.Map()` is constructed UNCONDITIONALLY in `map-canvas.tsx` (NOT wrapped in try/catch, and NOT gated on `isWebglAvailable()` first), so when no WebGL context exists MapLibre throws synchronously during init. The OpsSurface capability probe (`useEffect` → `setWebglAvailable(false)` → swap to table) usually WINS the race because the MapCanvas chunk is async — which is why the degradation works in practice and in a swiftshader headless run — but a hard-disabled-WebGL client loses the race and hits the uncaught throw; `onContextLost` only covers `webglcontextlost` AFTER a successful init, not a failed initial mount. **The fix is product code (frontend-engineer):** wrap the initial mount in try/catch (or gate it on `isWebglAvailable()`) → `viewMode.setWebglAvailable(false)` on failure, so the table renders instead of crashing. **Test-side handling:** I did NOT weaken product code; the no-WebGL E2E drives the table via the app's first-class Table TOGGLE (the deterministic, faithful fallback path that exercises the identical table surface + the one-socket mutual-exclusivity) and this finding is documented here for the fix.

### VERIFIED (honest)

- atlas-shared 44 / atlas-server 34 / atlas-web 59 unit tests GREEN (re-run after the 8.1 addition — no regression).
- e2e `tsc --noEmit` clean; root strictTypeChecked eslint exit 0 over all e2e TS + the new `ws-frames.test.ts`.
- **7/7 Playwright specs PASS** against the built prod stack behind the same-origin proxy (DB-less, no Postgres), Chromium swiftshader headless; ZERO CSP violations implicit (the map mounts + the WS opens under `connect-src 'self'` — would fail otherwise).
- **Lighthouse `/about` = 100/100/100/100** (median of 5), the ≥ 95 gate encoded.
- The DB seed against Postgres on 5438 end-to-end is STILL not exercised (carried from Phase 3/4/7 — the live channel + REST run DB-less by design; the persisted events history + reconnect-from-persisted-snapshot want it; deploy phase).
