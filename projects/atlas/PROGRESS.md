# atlas — Progress

> Read on start. Write on end.

## State

- **Phase:** **DEPLOYED LIVE (2026-06-06)** — demo at **https://atlas-ops.fly.dev**. Fly apps: `atlas-fleet-eu` (server+engine+WS, ONE warm machine scaled to count 1 — the single source of truth), `atlas-ops` (Next web, same-origin `/ws`+`/health` proxy), `atlas-db-eu` (Fly Postgres, region fra, attached). Verified live: `/health` ok; the fleet moves (1 Hz ticks); the production WS THROUGH the web origin (`wss://atlas-ops.fly.dev/ws`) streams snapshot(18 vehicles)+tick+event frames; prod CSP `connect-src 'self'` with NO `unsafe-eval`; `/about` 200; keyless vector basemap (a 3.8 MB Porto `.pmtiles` extract — go-pmtiles v1.30.3, build 20260606, bbox `-8.645,41.135,-8.585,41.165` — now SHIPS in the atlas-ops image via a `.dockerignore` `!porto.pmtiles` exception; real streets/water/buildings render under the fleet, verified live in BOTH dark + light; gitignored so a fresh-checkout deploy must re-run the extract per DEPLOY.md, else the graticule fallback shows); DB seed ran (`Seed complete`) and the event sink persists (FK errors stopped after the seed fix). THREE deploy bugs found+fixed live (all real, surfaced only on Fly): (1) **seed crashed** — `const { sql } = createDbHandle()` shadowed the imported drizzle `sql` operator, so the upsert `set` clauses serialised postgres-js tagged-templates ([object Promise]) instead of `excluded.*` SQL → renamed the client to `pgClient`; (2) **the same-origin WS/health proxy 500/502'd** — Fastify bound `0.0.0.0` (IPv4) but Fly's app-to-app 6PN is IPv6-only (ECONNREFUSED on the `.internal` IPv6 addr) → changed the bind to `::` (dual-stack); (3) **`atlas-fleet` name was taken globally** → renamed to `atlas-fleet-eu` in `fly.toml` + the atlas-ops `--build-arg API_PROXY_TARGET`. Optional Porto `.pmtiles` extract NOT generated (the graticule keyless default ships — verified). `ATLAS_COMMIT_SHA` not set (health shows commit "dev" — cosmetic). _Below: the original Phase 9.2 deploy-config note (backend-engineer, 2026-06-06)._ Produced: `Dockerfile` (atlas-fleet — two-stage `pnpm deploy` server-builder → slim runtime, runs TS via tsx, carries drizzle-kit + the seed script, materialises the `atlas-shared` workspace package incl. its `.ts` source so tsx resolves the Zod-runtime + geo impl at runtime — the apex "inline what the context resolves" lesson applied to the THREE-package shape), `Dockerfile.web` (atlas-ops — Next 15 standalone, one-hop trace root, includes `src/` for the atlas-shared types+geo, bakes `NEXT_PUBLIC_SITE_URL` + `API_PROXY_TARGET`, keyless CSP held), `entrypoint.sh` (straight-line migrate → idempotent seed [non-fatal] → exec server; POSIX-sh-validated, LF), `fly.toml` (atlas-fleet: ONE warm machine `min_machines_running=1` + `auto_stop_machines='off'`, internal port 3092, `/health` check — the single source of truth, no broker), `fly.web.toml` (atlas-ops: Next standalone port 3000, warm floor, `/` check), `.dockerignore` (keeps `src/`+root manifest+tsconfig.base.json; excludes `.env`/node*modules/`.pmtiles`/tests/docs), `lighthouserc.json` (`/about` ≥ 95 ×4; `/` exempt), and `DEPLOY.md` (the full copy-pasteable runbook). **CSP prod-WS decision:** SAME-ORIGIN — atlas-ops proxies `/ws` (+ `/health`) to `atlas-fleet.internal:3092` via Next `rewrites()` (added to `next.config.ts`, the ONE web file touched), so the browser opens `wss://<web-host>/ws` and the prod CSP stays `connect-src 'self'` with NO `unsafe-eval` (verified). **App names:** `atlas-fleet` (server) + `atlas-ops` (web). **Verified locally:** `pnpm -F atlas-web build` clean (the Windows EPERM standalone-trace warning is the documented non-fatal artifact, resolves on Linux/Fly), prod CSP no-`unsafe-eval` + `connect-src 'self'` asserted via the config, rewrites engage only with `API_PROXY_TARGET` set, `pnpm -F atlas-web typecheck` + root eslint on `next.config.ts` clean, `pnpm -F atlas-server build` clean, `drizzle-kit/bin.cjs` present at the entrypoint's path, entrypoint.sh passes `sh -n`. **OWNER-GATED / unverified until live:** `fly auth login` + the two `apps create` + Fly Postgres `create`/`attach` + the two `flyctl deploy` + the DB seed against Fly Postgres end-to-end + the optional Porto `.pmtiles` extract + setting the demo URL. Docker Desktop was DOWN in this env, so the Dockerfiles were validated by careful mirror-comparison against pulse/apex (the proven sibling pattern) + the local pnpm/tsc/eslint checks, NOT a local `docker build`. See AGENT_NOTES "Phase 9.2 deploy-config close-out". **Phase 9.1 DONE** (doc-writer, 2026-06-06) — `README.md` (pitch + wow + the honest server-authoritative-sim / client-interpolation credibility line + the seed-vs-live boundary, the full stack pulled from package.json + DECISIONS, DB-optional run instructions, the keyless-map note, an architecture Mermaid diagram, architecture notes, ADR-001..007 summary, the testing/quality story), `CHANGELOG.md` (Keep a Changelog, Unreleased = the full v1 build), `docs/capture-screenshots.mjs` + 6 PNGs under `docs/screenshots/` (dashboard dark/light/mobile, the geofence beat = vehicle detail + zone pulse + events feed, the no-WebGL table), and the root README table row for atlas (slot 6, Fastify, demo = \_deploying*). Mirrors the pulse/apex README structure. **Phase 9.2 (deploy) is next — it fills the demo URL.** Earlier — **Phase 8 DONE** (test-engineer, 2026-06-06) — the test suite is rounded out: the WS-frame Zod schema round-trip + malformed-rejection unit gap (atlas-shared 14→44), the full Playwright E2E suite (`projects/atlas/e2e/`, 7 specs ALL PASS against the CSP-faithful prod stack behind a same-origin proxy: one WebSocket + fleet moving, ETA decrements, geofence beat lands, no-WebGL table fallback, reduced-motion live, keyboard flow, socket-drop reconnect), and Lighthouse CI (`/about` = 100/100/100/100 ≥ 95 gate). Unit suites green: shared 44 / server 34 / web 59. **Phase 9 (docs + deploy) is next.** Earlier — **Phase 6 DONE** (frontend-engineer) — the graceful-degradation matrix (all four arms), the accessibility pass, and the public/landing + SEO surface. Phases 0–5 + Phase 7 review fixes committed. Built: **no-WebGL → fleet table** (a WebGL capability probe `webgl-support.ts` + a `view-mode-store` [capability + a first-class user Map/Table toggle, capability always wins] + a new `OpsSurface` rendering the map XOR the full-width `FleetTableView` + a non-blocking `DegradationNotice` + `webglcontextlost` handling in the controller; the table runs a NEW headless one-socket hook `use-headless-telemetry.ts` so there is never a second concurrent socket — the surfaces are mutually exclusive); **prefers-reduced-motion** (audited end-to-end — map snap/cut + panel Motion collapse already wired; the new surfaces are CSS-only); **no-JS SSR floor** (`/about` landing — a Server Component rendered entirely from a static deterministic snapshot `static-snapshot.ts` off the seeded fixture: pitch + static fleet table + route/zone reference + full SEO, NO client data dependency); **socket-down** (reconnect indicator + freeze + reconcile audited, holds in the table surface too). **A11y pass:** the new full-width table is a semantic `<table role=grid>` with sortable `scope=col aria-sort` headers + selectable rows; status-never-colour-alone via the single `StatusBadge`; FIX theme-toggle focus ring; FIX landing contrast (fg-subtle→fg-muted on surfaces); FIX favicon 404 (`icon.svg`). **SEO:** `/about` metadata + canonical, `next/og` OG image (root + about), JSON-LD `WebApplication`, `sitemap.ts` (about priority 1), `robots.ts`. **VERIFIED under a prod build: Lighthouse on `/about` = 100/100/100/100 (Performance/Accessibility/Best-practices/SEO); the no-JS floor renders meaningful content via curl; CSP intact (NO `unsafe-eval`) on `/` and `/about`; 59 web unit tests green.** **Phase 7 (designer-critic + reviewer) is next.**
- **Category:** api-heavy. Portfolio slot 6.
- **Backend:** Fastify (Node 22 LTS) + `@fastify/websocket` + Drizzle + PostgreSQL (plain, `jsonb` GeoJSON; PostGIS deferred to v2) + Zod (`fastify-type-provider-zod`). **Fastify is the one § 11 backend not previously used — Atlas completes the four-backend variance story.**
- **Real-time transport:** WebSocket (`@fastify/websocket`). SSE rejected (bidirectional control + high-frequency telemetry — see ADR-001).
- **Map engine:** MapLibre GL JS, **keyless-by-default tile source** (optional richer-style key via env — HARD constraint: no paid secret committed).
- **Geo math:** turf.js, shared FE+BE via `src/lib/geo/`.
- **Simulation:** server-side, deterministic pure tick reducer + IO shell, seeded, replayable.
- **Animation:** map motion = hand-rolled rAF interpolation (NOT a library) + MapLibre native camera; Motion (single declarative library, § 15) for UI/panel transitions only.
- **Tokens:** sovereign operations/control-room, NO reuse from tape / meld / razors-edge / pulse / apex (§ 14).
- **Wow moment:** a live map where the fleet glides smoothly in real time (1 Hz authoritative ticks, 60 fps client interpolation) along routes, with route trails, live ETAs ticking down, and geofence enter/exit events firing live as vehicles cross zones — reproducible on demand via the deterministic engine's focus/replay affordance.
- **Created:** 2026-06-06
- **Demo URL:** not yet deployed.
- **Ports (CONFIRMED by architect — supersedes the planner's proposal):** Fastify server **3092**, Next web **3093**, Postgres **5438**. **No Redis** (no broker/queue — in-process single-instance engine, ADR-002/ADR-007). NOTE: the planner's proposed **3090/3091 COLLIDE with apex (slot 5)** — apex-web binds 3090 (dev/start) and 3091 (Playwright E2E). Reassigned to 3092/3093 (both confirmed free, nothing listening). 5438 confirmed free (taken: Mila 5434 / tape 5435 / meld 5436 / pulse 5437). See DECISIONS.md "Port allocation".
- **Architect pass:** DONE (2026-06-06). ADR-002 (sim engine: pure reducer, 1 Hz fixed-dt, in-process single-instance, no broker), ADR-003 (WS contract: JSON tagged-union envelope, snapshot+coalesced-tick, server-scoped subs, 20 s heartbeat), ADR-004 (geo: scoped turf in `src/lib/geo/`, hysteresis geofence debounce, rolling-avg ETA), ADR-005 (plain Postgres + jsonb GeoJSON, PostGIS rejected for v1, hand-authored real-city routes, bounded telemetry), ADR-006 (MapLibre GeoJSON symbol layer, self-hosted Protomaps `.pmtiles` keyless default, strict no-`unsafe-eval` CSP), ADR-007 (Fly single warm Fastify machine + Postgres + Next web).

## Done

- [x] Planner pass (2026-06-06): PLAN.md (problem, audience, wow + mandatory fallback, api-heavy justification, Fastify rationale, keyless-MapLibre constraint, WS-vs-SSE, simulation-engine design, turf/Postgres posture, animation split, success criteria, Phase 0–9 tasks, simulation/mock data shape, out-of-scope, deferred-to-architect), DECISIONS.md ADR-001, this PROGRESS.md, AGENT_NOTES.md, TASKS.md.
- [x] **Architect Phase 0 pass (2026-06-06):** ADR-002..007 authored in DECISIONS.md (PLAN.md Tasks 0.1–0.6), each challenging the AGENT_NOTES "Decisions to revisit" assumptions rather than inheriting them. Ports confirmed and corrected (3092/3093/5438 — the planner's 3090/3091 collided with apex). Engineering kickoff unblocked (ADR-002 + ADR-005 accepted).
- [x] **Phase 1 backend scaffold (backend-engineer, 2026-06-06):**
  - **Task 1.1** — Fastify scaffold under `server/` (package `atlas-server`, Node 22 `.nvmrc`, TS strict). `fastify-type-provider-zod` v6 wired (validator + serializer compilers). Zod-validated env (`src/config/env.schema.ts`, fail-fast at boot). `GET /health` -> `{ status, commit, ts }`, verified live (200, Zod-serialized). Binds `0.0.0.0:3092`. Jitless Zod global side-effect imported first in `main.ts` (and transitively via the schema barrel). `@fastify/cors` + `@fastify/rate-limit` baseline wired. Graceful shutdown drains Fastify + the postgres-js pool.
  - **Task 1.2** — `docker-compose.yml` Postgres on **5438** (atlas user/db). Drizzle schema barrel `server/src/db/schema/` per ADR-005 (routes, route*stops, zones, vehicles, telemetry_snapshots, events; GeoJSON in `jsonb`; the ADR-005 indexes + the enum-from-shared-tuples seam). Migration generated (`server/drizzle/0000*\*.sql`, verified: 6 tables, 4 FKs, 4 indexes). `postgres-js` + Drizzle client provider (`db/drizzle.ts`).
  - **Task 1.3** — Shared Zod contract package **`atlas-shared`** at the atlas root (`src/lib/schemas/`): vehicle, route, stop, zone, telemetry tick, event, GeoJSON primitives, shared enums, and the WS frame envelope + control frames in `src/lib/schemas/ws/` (ADR-003 tagged-union `{ t, seq }`). Types-only cross-package seam via `exports` (`atlas-shared`, `/schemas`, `/schemas/ws`, `/geo`).
  - **Task 1.4** — Shared geo module `src/lib/geo/` (ADR-004): route cumulative-table projection (`s` -> lat/lng/heading), route length, rolling-average ETA + dwell folding, point-in-polygon + the geofence hysteresis state machine. Scoped `@turf/*` imports, units pinned to metres, pure + testable. 14 smoke tests pass (projection vs turf `along` oracle, the no-flap invariant).

- [x] **Phase 2 frontend scaffold (frontend-engineer, 2026-06-06):**
  - **Task 2.1** — Next.js 15 + React 19 + Tailwind v4 + shadcn-ready scaffold under `web/` (package `atlas-web`, **dev port 3093**, `start`/`dev` both 3093). `next-themes` wired DARK-canonical (the control-room register is the lead surface; LIGHT is the intentional clean-dispatcher counterpart). **Sovereign operations/control-room token set** authored in `globals.css` — petrol-navy dark stage + cool-paper light, a single SIGNAL-AMBER accent, a dedicated fleet-STATUS palette (en_route / at_stop / idle / returning) + zone tints (depot / delivery / restricted), the "live grid" graticule + amber "radar sweep" motif. NO token reuse from tape/meld/razors-edge/pulse/apex (§ 14). Fonts: IBM Plex Sans + IBM Plex Mono (console register, tabular numerals for telemetry) self-hosted via next/font. TanStack Query + Zustand + react-hook-form + Zod + Motion installed. App shell: control-room top bar (wordmark, city context, connection pill, theme toggle, amber sweep underline) + the map-region/side-panels layout (fleet left, detail+events right, responsive reflow). zod jitless side-effect (`@/lib/zod-config`) imported first in layout + providers. `prefers-reduced-motion` + `prefers-color-scheme` respected; no FOUC (dark `:root` matches the server render).
  - **Task 2.2** — MapLibre integration boundary. `AtlasMapController` (a plain class) owns the MapLibre GL JS instance OFF the React render path (imperative, in a ref — the hard "not React state per frame" gate); the `'use client'` `MapCanvas` wrapper mounts it once, dynamic-imported `ssr:false` so MapLibre never enters the server bundle (`/` First Load JS stays 116 kB). Keyless self-hosted Protomaps `.pmtiles` basemap via the `pmtiles://` protocol (same-origin, NO secret, optional `NEXT_PUBLIC_MAP_TILE_KEY` documented in `.env.example` only). DARK + LIGHT MapLibre style JSONs wired to the theme toggle (`setStyle` on theme switch — the basemap changes, not just chrome). MapLibre keyboard nav enabled. Static seeded fleet (~10 vehicles) + routes + zones rendered as GeoJSON symbol/line/fill layers off the render path. **CSP-clean under a PROD build, verified with Playwright: 1 WebGL canvas mounts, ZERO CSP violations, no third-party requests, keyless render works with no `.env`.**
  - **Verification:** `pnpm --filter atlas-web build` OK; typecheck/lint/test (5/5) green; prod-build CSP confirmed exactly per ADR-006 (`worker-src 'self' blob:`, `img-src 'self' data: blob:`, `connect-src 'self'`, `script-src 'self' 'unsafe-inline'` — NO `unsafe-eval`). See AGENT_NOTES "Phase 2 frontend close-out".

- [x] **Phase 3 simulation engine (backend-engineer, 2026-06-06):**
  - **Task 3.1** — the pure tick reducer under `server/src/engine/reducer/` (`tick.ts`, `prng.ts` mulberry32-in-state, `route-progress.ts`, `speed-model.ts`, `events.ts`, `world-state.ts`). `tick(state, dt) -> { state, events }`, no wall-clock / no IO inside; advances `s`, projects via the shared `atlas-shared/geo` cumulative table, speed-modulation + dwell-at-stop, route-end loop/ping-pong, per-vehicle ETA, geofence transitions via the shared hysteresis `GeofenceTracker`. Unit-tested: determinism (same baseline + N ticks → identical world; seek == replay), purity (no input mutation), motion (valid Porto-bbox telemetry, heading/progress ranges), geofence (enter/exit fires deterministically; no duplicate consecutive enters — the no-flap invariant).
  - **Task 3.2** — the IO shell `server/src/engine/shell/engine.ts` (`SimulationEngine`): fixed-dt accumulator loop at 1 Hz, owns wall-clock, applies ticks, `onTick` listener fan-out, `snapshot()`, and the demo control surface (`pause`/`resume`/`setSpeed`/`seek`). In-process, single-instance, no broker (ADR-007). Loop/reducer cleanly separated.
  - **Task 3.3** — persistence + event sink + seed. `server/src/engine/persistence/event-sink.ts` (queued async writes, off the tick hot path — the loop never blocks on the DB). The composition root `server/src/engine/engine-service.ts` (`EngineService`) builds the FROZEN baseline from the Porto fixture, owns the engine + sink, and exposes `onTick` / `snapshot` / `definitions` + the control surface for the Phase 4 WS gateway. The deterministic seed: `server/src/seed/porto-fixture.ts` + `porto-geometry.ts` (hand-authored Porto routes/stops/zones inside `DEMO_CITY_BBOX`), `server/scripts/seed.ts` (`faker.seed(n)` frozen baseline, idempotent). The in-memory engine boots from the fixture directly (DB not its source of truth — ADR-005), so the fleet moves even before the seed runs.
  - **Verification:** `pnpm --filter atlas-server build` (tsc) clean; root strictTypeChecked eslint clean over `server/src/**` + `src/**`; 14 engine tests green. NOT YET RUN: the DB seed end-to-end against Postgres on 5438 (Docker not brought up this session) — `docker compose up -d` then `pnpm --filter atlas-server db:migrate && pnpm --filter atlas-server seed` is the deferred step for the test/deploy phase.

- [x] **Phase 4.1 WS gateway (backend-engineer, 2026-06-06):**
  - **Task 4.1** — the `@fastify/websocket` gateway at **`/ws`** per ADR-003, under `server/src/gateway/`: `ws-gateway.ts` (the plugin — registers `@fastify/websocket`, the `/ws` route, the per-tick fan-out, the heartbeat, inbound frame routing), `connection.ts` (per-connection state: monotonic `seq`, subscription scope, the coalesce-to-latest pending-tick buffer = the backpressure rule), `frames.ts` (pure outbound frame builders, schema-shaped + validated-in-dev against `serverFrameSchema`), `scope.ts` (server-side vehicleIds + bbox filtering), `rate-limiter.ts` (per-connection token bucket for inbound frames). Wired into `app.ts` (the `EngineService` composition root is instantiated, `start()`/`stop()` owned by `main.ts`) and `main.ts` (engine starts after `listen`, `await engineService.stop()` is the FIRST shutdown step so the sink flushes before the pool closes). Frame flow: snapshot-on-connect (definitions + telemetry, so a cold client renders with no REST round trip) → `tick` deltas (coalesced-to-latest per vehicle under backpressure) → `event` frames (the tick's `events[]`) → 20 s `heartbeat` + ws ping/pong dead-socket reaping. Client→server: `subscribe`/`unsubscribe` (re-snapshot on scope change), `snapshot.request` (debounced 1 s), `sim.control` → `pause`/`resume`/`setSpeed`(capped 16)/`seek`(returns a fresh snapshot for deterministic replay). Every inbound frame Zod-validated (jitless) at the boundary; malformed/over-rate frames dropped, never trusted. Added `ws` + `@types/ws` to `server/package.json` (the `WebSocket` type source — `@fastify/websocket` carries `ws` transitively but does not surface its types for direct import).
  - **Verification:** `pnpm --filter atlas-server build` (tsc) clean; root strictTypeChecked eslint clean over `server/src/**` + `src/**`; per-package `eslint .` clean; **27 tests green** (14 engine + 13 new `gateway.test.ts` — frame builders pass the shared contract, token bucket throttles/refills, scope filters, coalesce-to-latest keeps fleet-bounded latest-per-vehicle, client-frame accept/reject). **LIVE smoke PASSED** (`server/scripts/ws-smoke.ts`, runnable DB-less): snapshot-on-connect (18 vehicles + telemetry), tick frames stream + vehicle positions change, sim.control pause halts the tick index + resume advances it, heartbeat arrives, malformed frames dropped (socket stays open). Confirmed the persistence sink fails NON-FATALLY without Postgres (logs `event sink drain failed (engine state is unaffected)`, the live channel is unaffected — so the gateway runs without a DB for the smoke/demo).

- [x] **Phase 4.2 + 4.3 WS client + rAF interpolation + trails/remaining/zone-pulse (frontend-engineer, 2026-06-06):**
  - **Task 4.2** — the live channel + the wow. `web/src/lib/ws/`: `ws-client.ts` (`TelemetryWsClient` — ONE connection, capped exponential backoff + jitter reconnect, `seq`-gap → debounced `snapshot.request`, ~2-missed-heartbeat liveness reaper → reconnect, framework-free + mock-socket-testable), `frames.ts` (types-only WS frame imports + Zod-free structural narrowing — the browser bundle stays Zod-free), `ws-url.ts` (env/origin derivation), `snapshot-adapter.ts` (WS `SnapshotFrame` → the controller's `FleetSnapshot` view model). `web/src/lib/interp/`: `interpolation.ts` (PURE lerp-along-route-`s` + shortest-arc heading + tick-progress-clamped-freeze), `interp-store.ts` (the OFF-render-path ref store: last+next authoritative ticks per vehicle, cached route projectors, wrap/snap detection), `raf-loop.ts` (one `requestAnimationFrame` loop → `controller.setVehiclesGeoJSON` once/frame; trails + remaining throttled to 200 ms), `stop-index.ts` (stop→`s` via shared `projectPointToS` for the remaining-route slice), `use-live-telemetry.ts` (the glue hook — wires the client + loop to the controller; only connection-status + zone-pulse touch React). `connection-store.ts` (Zustand, low-frequency status). The connection pill now reflects the real WS lifecycle. Reduced-motion → the loop snaps to ticks.
  - **Task 4.3** — trails + remaining + zone pulse. `fleet-layers.ts` gained `SOURCE_TRAILS` (lineMetrics, a `line-gradient` fade tail) + `SOURCE_REMAINING` (signal-amber planned path) + their layers; the controller gained `setTrailsGeoJSON` / `setRemainingGeoJSON` (per-frame `setData`, off-render) + `pulseZone(zoneId)` (a reduced-motion-safe `setPaintProperty` highlight on the geofence zone fill/line, eased back over ~1.4 s). The rAF loop slices `start..current` (bounded tail) for the trail and `current..nextStop` for the remaining route via the shared `sliceRouteGeometry` (added to `atlas-shared/geo/route.ts`). All off the React render path, none through Motion.
  - **CSP change:** `web/next.config.ts` `connect-src` gains the dev WS origin (`ws://localhost:3092`, or the `NEXT_PUBLIC_WS_URL` origin) **in non-production only** — the prod build stays `connect-src 'self'` (same-origin WS), NO `unsafe-eval` in either case. Verified live via `curl -I` on both `next start` (prod: `connect-src 'self'`) and `next dev` (dev: `connect-src 'self' ws://localhost:3092`).
  - **Verification:** `pnpm --filter atlas-web build` OK (First Load JS `/` = 116 kB, the interp+WS code is in the `ssr:false` map chunk); `tsc --noEmit` clean (web + shared); root strictTypeChecked eslint clean over the web glob (exit 0) + `next lint` clean (no react-hooks/exhaustive-deps escape hatch — refs, the Phase-2 lesson); **25 web tests green** (interpolation lerp/slerp/freeze, RafLoop moves-between-ticks + snap-under-reduced-motion + trail/remaining emit, WS client one-socket/dispatch/seq-gap/heartbeat-reconnect/freeze-on-drop) + 14 shared geo tests. **LIVE under a prod build (same-origin proxy + Playwright headless):** exactly ONE WebSocket, all four frame types in the browser, MapLibre canvas mounts, ZERO CSP violations, pill reads "Live". A live server probe (`ws://localhost:3092/ws`, DB-less) confirmed authoritative `distanceAlongRouteM` ADVANCES across ticks (the interpolation input is genuinely live) and geofence `enter`+`exit` events fire with valid `zoneId`s (the zone-pulse trigger). See AGENT_NOTES "Phase 4.2/4.3 close-out".

- [x] **Phase 5 fleet panel / detail / events feed / demo affordance (frontend-engineer, 2026-06-06):**
  - **Data layer (FIRST, per the handoff):** `web/src/lib/store/telemetry-store.ts` (Zustand, 1 Hz — `applySnapshot` seeds static defs [vehicle identity, routes+nested-stops, zones] + first telemetry; `applyTick` replaces only changed vehicles, unchanged keep their reference so rows do not spuriously re-render) + `web/src/lib/store/events-store.ts` (bounded 60-event newest-first ring, dedup by id, `lastAnnounced`+`announceSeq` for the coalesced aria-live). Both fed from the EXISTING `use-live-telemetry.ts` `onSnapshot`/`onTick`/`onEvent` callbacks — the SAME single socket, no second connection. Plus `sim-control-store.ts` (bridges the demo UI to the one WS client's `send`, null until live) + `controller-store.ts` (the MapCanvas publishes the controller so sibling panels can drive the camera).
  - **Task 5.1** — `fleet-panel.tsx`: the first-class non-map alternative. A `role=grid` of `<button>` rows (Tab between, Enter/Space focuses), sortable by unit/status/speed/progress/ETA (`aria-sort` headers + sort chips), every vehicle with status (the `StatusBadge` — dot + icon + label, NEVER colour-alone), current zone, speed, % complete (bar + text percent), live ETA. A row click/Enter → `useFocusVehicle` (selects in ops-store + flies the camera to the LIVE position via `controller.focusVehicleAt`). Each row has a full descriptive `aria-label`.
  - **Task 5.2** — `detail-panel.tsx`: Motion `AnimatePresence` slide-in (reduced-motion → fade only, no transform), pinned on selection — label + status, route name, live metrics (ETA/speed/% complete, tabular numerals), ordered stops (the next stop highlighted), and the vehicle's recent events. All from the 1 Hz stores.
  - **Task 5.3** — `events-panel.tsx`: Motion row enter/exit (reduced-motion → opacity-only, no slide/height), newest-first; a SEPARATE `aria-live="polite"` sr-only region announces only the latest event, throttled/coalesced (1.5 s window) so a geofence burst does not flood the SR (the visible list is `aria-hidden` to avoid double-announce). The geofence beat is wired three ways: zone pulse (the controller, already 4.3) + the event row (here) + the status flip (the next tick's telemetry → fleet/detail re-render).
  - **Task 5.4** — `demo-control.tsx` floating over the map: "Play geofence beat" picks an en-route vehicle NOT yet in a zone (heading toward one), focuses it, and bumps demo speed to 4x via `sim.control` so the cross-the-boundary beat fires within seconds; plus pause/resume + 1/2/4/8x speed chips. Tight (no full scrub UI — PLAN.md is explicit); the deterministic `seek` stays a clean lever. Disabled until the socket is live.
  - **Verification:** `tsc --noEmit` clean; root strictTypeChecked eslint clean over the web glob (exit 0); `pnpm --filter atlas-web build` compiles (`/` First Load JS 164 kB — the panels + Motion; the dashboard route is documented exempt from the Lighthouse-SEO budget, the landing surface is Phase 6); **46 web tests green** (8 files — added format helpers, the events-store reduction [prepend/dedup/bound + vehicle filter], the telemetry-store tick reduction [snapshot seeds + ordered stops + replace-only-changed + empty-tick-noop], and event-copy [the geofence/status/arrived/departed lines]). **LIVE under a prod build (same-origin proxy + Playwright):** fleet panel lists the live fleet + rows update between ticks, exactly ONE WebSocket, snapshot/tick/event frames flow in-browser, clicking a row pins the detail panel + the map canvas visibly changes on focus (two different focuses → different canvas screenshots = camera moves), keyboard Enter focuses a vehicle, the demo beat drives the events feed (44→112 rows), zero CSP violations. See AGENT_NOTES "Phase 5 close-out".

- [x] **Phase 6 public read REST endpoints (backend-engineer, 2026-06-06) — Task 6.3 backend portion:**
  - Four rate-limited public GET endpoints under `server/src/routes/public-read.ts`, registered in `app.ts` after the engine composition root, serving **DB-LESS from the in-memory `EngineService`** (the engine is the live source of truth, ADR-005 — available without Postgres exactly like the WS channel): **`GET /api/fleet/snapshot`** (the current authoritative per-vehicle telemetry from `engineService.snapshot()` + the static vehicle metadata — the SSR floor / no-WebGL initial paint read surface), **`GET /api/routes`** (route GeoJSON LineStrings + ordered stops from `definitions`), **`GET /api/zones`** (geofence polygons), **`GET /api/vehicles`** (the static fleet roster).
  - **Shared response contract** added to `atlas-shared`: `src/lib/schemas/rest.ts` (`fleetSnapshotResponseSchema`, `routesResponseSchema`, `zonesResponseSchema`, `vehiclesResponseSchema` + inferred types), re-exported from the schemas barrel. The server uses them as `fastify-type-provider-zod` serializer compilers; the web SSR floor imports the **types** (types-only — Zod erased). No duplicated DTO — single source for the read surface (conventions section 5). The snapshot response reuses the entity schemas (vehicle + telemetry) but drops the WS envelope (`t`/`seq`/`protocolVersion`) so the transport does not leak into the read contract.
  - **Rate limiting** (the security criterion's public-REST limits): per-route `config.rateLimit` overriding the global floor — snapshot 60/min/IP (a no-WebGL client may poll), the static definitions 30/min/IP. **Cache-control**: static defs `public, max-age=60`; the live snapshot `no-store` (never serve a cached snapshot as current — the stale-as-live rule). CORS unchanged (the existing `@fastify/cors` allows the web origin; verified reachable with an `Origin: http://localhost:3093` GET).
  - **VERIFIED.** `pnpm --filter atlas-shared typecheck` + `pnpm --filter atlas-server build` (tsc) clean; root strictTypeChecked eslint over `server/src/**` + `src/**` exits 0; **32 server tests green** (27 prior + 5 new `public-read.test.ts` via `app.inject()` against a real built app — response re-parses the shared schema, rate-limit headers present + per-route budgets, cache-control posture). **LIVE smoke DB-less** (`node --import tsx src/main.ts`, no Postgres): `/api/fleet/snapshot` → 18 vehicles + 18 telemetry with live lat/lng/heading, `serverTick` advancing and `distanceAlongRouteM` increasing across calls (genuinely live), `cache-control: no-store`, `x-ratelimit-limit: 60`; `/api/routes` → 6 routes (LineString geometry) + 13 stops, `/api/zones` → 5 polygons, `/api/vehicles` → 18, all `public, max-age=60` + `x-ratelimit-limit: 30`; CORS `access-control-allow-origin` returned for the web origin. `/health` and `/ws` unaffected.

- [x] **Phase 6 degradation matrix + a11y pass + public/landing + SEO (frontend-engineer, 2026-06-06):**
  - **Task 6.1 — the four degradation arms.** **no-WebGL → fleet table:** `web/src/lib/map/webgl-support.ts` (capability probe, cached, test seam) + `web/src/lib/store/view-mode-store.ts` (capability + a first-class user Map/Table `preference`; the pure `resolveEffectiveView` — capability always wins) + `web/src/components/dashboard/ops-surface.tsx` (the new centre surface: map XOR `FleetTableView` full-width, a view toggle, a non-blocking `degradation-notice.tsx`) + `webglcontextlost`/`webglcontextrestored` handling in `map-controller.ts` (a 1.5 s grace window → `onContextLost` → swap to the table). The table runs a NEW headless one-socket hook `web/src/lib/ws/use-headless-telemetry.ts` (the surfaces are mutually exclusive → never a second concurrent socket). **prefers-reduced-motion:** audited end-to-end (map snap/cut + panel Motion collapse already wired; the new surfaces are CSS-only, no new Motion). **no-JS SSR floor:** `web/src/app/about/page.tsx` (a Server Component) rendered entirely from the static deterministic `web/src/lib/fleet/static-snapshot.ts` (off the seeded fixture — NO client data dependency, NOT the live REST snapshot, to keep Lighthouse green + the no-JS contract honest). **socket-down:** reconnect + freeze + reconcile audited (holds in the table surface too via the shared connection store).
  - **Task 6.2 — accessibility pass (WCAG 2.2 AA).** The new full-width `web/src/components/dashboard/fleet-table-view.tsx` is a semantic `<table role=grid>` with sortable `scope=col aria-sort` headers + selectable `tabIndex=0 aria-selected` rows (Enter/Space) + per-row `aria-label` + sr-only caption. Status-never-colour-alone confirmed single-source (`StatusBadge`, server-renderable). FIX: theme-toggle missing focus ring (added). FIX: landing contrast (`text-fg-subtle`→`text-fg-muted` on surfaces — Lighthouse-found). FIX: `/favicon.ico` 404 (added `web/src/app/icon.svg`). aria-live non-flooding + MapLibre keyboard nav audited — sound.
  - **Task 6.3 — public/landing + SEO.** `/about` landing with per-page metadata + `alternates.canonical`, `next/og` OG image (`web/src/app/opengraph-image.tsx` + `about/opengraph-image.tsx` re-export), JSON-LD `WebApplication` (`web/src/components/seo/json-ld.tsx`), `sitemap.ts` (about priority 1, `/` 0.8), `robots.ts`. SSR/static first paint, no client-only data dependency. The backend's public read REST endpoints (below) are available but the SSR floor deliberately uses the static fixture for the Lighthouse budget.
  - **Verification:** `tsc --noEmit` clean; root strictTypeChecked eslint exit 0 over the web glob; `pnpm --filter atlas-web build` clean (`/about` STATIC, 117 kB First Load; `/` dashboard 189 kB exempt); prod CSP intact (NO `unsafe-eval`, `connect-src 'self'`, `worker-src 'self' blob:`) on `/` + `/about` via `curl -I` on `next start`; the no-JS floor renders meaningful content via `curl`; OG images 200 image/png; **Lighthouse on `/about` = 100/100/100/100** (Performance/Accessibility/Best-practices/SEO, desktop preset); **59 web unit tests green** (added `view-mode-store.test.ts`, `webgl-support.test.ts`, `static-snapshot.test.ts`). See AGENT_NOTES "Phase 6 close-out". DEFERRED to Phase 8: the WebGL-stubbed→table swap + the headless-one-socket browser E2E (no Playwright/RTL in the workspace yet); Lighthouse CI (Task 8.3).

- [x] **Phase 7.2 code review (reviewer, 2026-06-06):** read-only review of the full diff since the planner (b82c837..HEAD). **Verdict: request-changes.** Re-verified green: shared 14 / server 32 / web 59 tests pass, both builds clean, root strictTypeChecked eslint exit 0, no committed secret, CSP NO `unsafe-eval` + keyless gate hold. Defect list in AGENT_NOTES "Phase 7.2 code review verdict": **P0** unbounded WS `seek` = event-loop DoS (cap the tick target); **P1** ETA reports 0 for backward ping_pong vehicles (use direction-aware `findNextStop.remainingM` in `telemetryFor`); P2 event-frame bbox scoping, bbox order validation, DB-seed e2e. Reducer determinism/purity, geofence no-flap, WS backpressure, one-socket XOR, off-render rAF loop, CSP/keyless all confirmed correct. Next fixer: **backend-engineer** (both P0+P1 are server-side).

- [x] **Phase 7.2 review fixes — backend (backend-engineer, 2026-06-06):** applied the ship-blocking server-side defects from the Phase 7.2 review. **P0 (event-loop DoS):** bounded the WS `seek` target — new shared `MAX_SEEK_TICK = 3_600` in `src/lib/schemas/ws/protocol.ts`, applied as `.max(MAX_SEEK_TICK)` on `seek.tick` in `client-frames.ts` AND clamped defensively in `engine.seek()` (`Math.min(MAX_SEEK_TICK, …)`, mirroring the `setSpeed` clamp) so the synchronous fold is bounded even if the schema is bypassed. Cap kept TIGHT (1 h of 1 Hz sim, ~0.25 s max fold) so even the maximum seek is fast — a generous cap would let one max-seek frame block the loop for ~10 s. **P1 (ETA wrong for backward ping_pong):** `telemetryFor` now uses the direction-aware `nextStop.remainingM` (the value `findNextStop` already returns) instead of the direction-blind `remainingDistanceToStop(s, stop.s)`; the unused import was dropped. ~4 of 6 Porto routes are ping_pong, so this fixed ETA-0 for ~half the fleet on the backward half of every run. **P2 (both done):** event frames are now bbox-scoped too (reuse the already-scoped telemetry's in-scope vehicle-id set in the fan-out), and `bboxSchema` gained a `.refine(west<east && south<north)` so an inverted box is dropped at the boundary. **Tests added:** a seek-clamp test (giant target clamps to `MAX_SEEK_TICK`, returns fast — proves no unbounded fold) and a backward-direction (direction -1) ETA test asserting a correct non-zero finite ETA. **VERIFIED:** `pnpm --filter atlas-server build` clean; root strictTypeChecked eslint over `server/src/**` + `src/**` exit 0; **34 server tests green** (was 32 — +2 new) + 14 shared tests green.

- [x] **Phase 8 tests (test-engineer, 2026-06-06):**
  - **8.1 — unit gaps filled.** Added `src/lib/schemas/ws/ws-frames.test.ts` to `atlas-shared` (the one genuine 8.1 gap — no dedicated WS-frame schema test existed): client/server frame round-trip over the wire (`JSON.stringify`→`parse`→`safeParse`) + malformed rejection — the over-`MAX_SEEK_TICK` seek (cap+1 fails, cap passes), the inverted/degenerate bbox (the P2 `.refine`), out-of-range WGS84 coords, unknown discriminators, missing payloads, non-object inputs, the wrong `protocolVersion`, out-of-range telemetry. **30 new tests → atlas-shared 44 (was 14).** Verified the other 8.1 paths already exist + pass (backward-direction ping_pong ETA in `server/.../reducer/tick.test.ts`, seek-clamp in `server/.../shell/engine.test.ts`, geofence no-flap/projection/ETA in `src/lib/geo/geo.test.ts`, reducer determinism/purity). No padding beyond the real gap.
  - **8.2 — Playwright E2E, the main deliverable.** New `projects/atlas/e2e/` workspace package (mirrors pulse/apex; in the `projects/*/e2e` glob). `playwright.config.ts` `webServer` = `pnpm --filter atlas-web build && node atlas-stack.mjs` — the orchestrator spawns **Fastify gateway DB-less :3092 + Next prod :3097 + a same-origin proxy :3096** (`atlas-proxy.mjs` forwards `/ws` + `/api/*` to the gateway, the rest to Next) so the strict `connect-src 'self'` CSP covers the WS. **7 specs, all PASS** (Chromium swiftshader): live-map (ONE WebSocket + telemetry frames + tick CLIMBS + no `/api/fleet/snapshot` polling), eta-and-geofence (ETA decrements; geofence enter/exit row lands — driven by the deterministic "Play geofence beat" affordance, no sleep), no-webgl (Table view = live fleet, map gone, ONE socket open across the swap via `openCount()`), degradation (reduced-motion keeps the map live + tick climbing), keyboard (Tab→row→Enter focuses→detail pins→Tab flows), reconnect (offline → "Reconnecting" → online → "Live"). All web-first / state-transition assertions.
  - **8.3 — Lighthouse CI.** `e2e/lighthouserc.json` gates **`/about` ≥ 95 ×4** (median of 5, desktop) + CWV; `/` exempt. **RESULT: 100/100/100/100 across all 5 runs.** Re-run: stack up, then `pnpm -F atlas-e2e test:lh`.
  - **Verification:** shared 44 / server 34 / web 59 unit green (no regression); e2e `tsc --noEmit` clean; root eslint exit 0 over all e2e TS + the new shared test (`.mjs` orchestrator/proxy root-eslint-ignored by `**/e2e/*.mjs`); 7/7 Playwright pass; Lighthouse 100×4. **Product finding (flagged, NOT fixed — out of test scope):** with WebGL HARD-disabled at the browser level, MapLibre's optimistic initial `new Map()` throws uncaught → page crash (the OpsSurface probe races the lazy MapCanvas chunk; the initial mount is unwrapped, `onContextLost` only covers post-init loss). The no-WebGL E2E drives the table via the first-class Table TOGGLE (the deterministic faithful path). See AGENT_NOTES "Phase 8 close-out".

## In progress

- (nothing — Phases 0–9.2-config complete; awaiting commit by the main thread, then the OWNER-GATED `fly deploy` to go live)

## Next

1. **OWNER-GATED go-live (owner)** — Phase 9.2 deploy-config is DONE + committed; the actual deploy needs the owner's Fly auth. The owner runs (from `projects/atlas/`, per DEPLOY.md): `flyctl auth login` → `flyctl apps create atlas-fleet --org personal` + `flyctl apps create atlas-ops --org personal` → `flyctl postgres create --name atlas-db --region fra ...` + `flyctl postgres attach atlas-db --app atlas-fleet` → (optional) generate the Porto `.pmtiles` extract into `web/public/map/porto.pmtiles` → `flyctl deploy --remote-only --app atlas-fleet --config fly.toml --dockerfile Dockerfile .` → `flyctl deploy --remote-only --app atlas-ops --config fly.web.toml --dockerfile Dockerfile.web .` → confirm migrate+seed ran (the atlas-fleet entrypoint does this on boot; or re-run via `flyctl ssh console`) → verify (warm single machine each, ONE DevTools WebSocket, a geofence beat, `/about` Lighthouse ≥ 95, keyless map with no committed secret). The DB seed against Fly Postgres end-to-end happens automatically in the entrypoint (still never exercised against a real Postgres — the live WS + REST run DB-less, so this is the one path the deploy first proves).
2. **set the demo URL (after go-live)** — replace the `_deploying_` placeholder in `projects/atlas/README.md` (the Demo section) AND the root README table row, and remove the "deploying" note in the README Demo paragraph. Demo URL = `https://atlas-ops.fly.dev` (unless the apps were renamed).

## Handoff

**Next subagent: deploy (main thread / backend-engineer) — Phase 9.2.** Phase 9.1 docs are done (README + CHANGELOG + screenshots + the root table row). The ONE thing deploy must fill back into the docs: the demo URL (the README Demo section currently says `_deploying_`, and the root README table row reads `_deploying_`).

**Earlier handoff (still accurate for deploy):** Phases 0–8 are done: engine + WS gateway + live map + panels/feed + degradation matrix + a11y + SEO landing + Phase 7 review fixes + the full test suite (unit + E2E + Lighthouse CI) are complete and green. **Test entry points:** `pnpm -F atlas-shared test` (44), `pnpm -F atlas-server test` (34), `pnpm -F atlas-web test` (59); `pnpm -F atlas-e2e test` (7 Playwright specs — the `webServer` builds + boots the whole stack DB-less, no Postgres needed; Chromium must be installed: `pnpm -F atlas-e2e install:browsers`); `pnpm -F atlas-e2e test:lh` (Lighthouse `/about` ≥ 95, needs the stack serving :3096). The load-bearing prod-CSP same-origin-WS recipe is in `e2e/atlas-stack.mjs` + `e2e/atlas-proxy.mjs`, documented in AGENT_NOTES "Phase 8 close-out". **Remaining deploy TODO (unchanged):** the DB seed against Postgres on 5438 end-to-end; the Porto `.pmtiles` extract (gitignored Phase-9 artifact); the live-demo + keyless + Lighthouse verification on Fly. **One product robustness finding for the frontend-engineer (flagged in Phase 8, not a blocker):** wrap the initial MapLibre `new Map()` mount in try/catch → swap to the table on failure, so a genuine no-WebGL browser degrades gracefully instead of crashing (today the OpsSurface probe usually wins the race, but a hard-disabled-WebGL client hits the uncaught throw). Hard gates that MUST stay intact: ONE WebSocket per surface; status never colour-alone; `prefers-reduced-motion` guards; prod CSP NO `unsafe-eval`; keyless map; static SSR floor. The server dev script does NOT auto-load `.env` — re-create `server/.env` from `.env.example` (gitignored) for any local run.

## Portfolio composition note

Atlas = slot 6, api-heavy, **Fastify** — the fourth distinct backend across four api-heavy projects (Elysia/Bun · Hono/Node · NestJS · Fastify). The § 12 backend-variance target was already satisfied with margin by pulse; Atlas **completes** it (every § 11 framework now represented). The root `PROGRESS.md` composition tracker row is added by the main thread.

- **Phase 7.1 designer-critic critique DONE (2026-06-06):** reviewed all UI surfaces via LIVE headless screenshots (prod build + same-origin WS proxy; pill Live, all 4 frame types, zero CSP violations) against Linear/Vercel/Stripe. **2 P0 blockers, 6 P1, 5 P2 = 13 defects.** P0-1 (the map basemap earth equals the app background, so the wow surface reads as a black void in both themes -- single most important fix) and P0-2 (the light-theme map plate does not switch to light). Full prioritised list in AGENT_NOTES.md under "Phase 7.1 designer-critic defect list" -- the frontend-engineer applies, hard gates (one socket/surface, status-never-colour-alone, reduced-motion, no unsafe-eval, keyless, static SSR floor) must survive every fix.
- **Phase 7.1 designer-critic fixes APPLIED (frontend-engineer, 2026-06-06):** both P0 + all six P1 + four of five P2 resolved and RE-CAPTURED live. **P0-1** map plate lifted above the void (dark earth #0b1018->#10171f, value-tiered water/landuse/roads) + a painted keyless control-room graticule floor over the bbox -> reads as a deliberate map with zero tiles. **P0-2** theme swap fixed (controller defers a pre-`load` setTheme + the map-canvas effect re-runs on `ready` -> a prefers-color-scheme: light visitor gets the light plate). **P1-1** narrow/tablet reflow scrolls (min-h-dvh + lg:h-dvh, map a 60vh block, panels flow). **P1-2** fitBounds to the fleet on load/first-live-snapshot/resize + maxZoom clamp -> fleet fills 1440px+/2560. **P1-3** markers show the human "U7" (keyless canvas label chip, no glyph server) unified with the panels -- no veh-N. **P1-4** empty detail rail now a live FleetSummary instrument (status counts, in-geofence, avg speed). **P1-5** map<->table crossfade (Motion, reduced-motion-safe). **P1-6** defaultTheme="system" -> /about respects prefers-color-scheme, no dark FOUC. **P2-1** amber LIVE pill, **P2-2** stronger zone-pulse (fill+line+width bloom), **P2-3** taller progress bars + readouts swept fg-subtle->fg-muted. P2-4/P2-5 deferred (see AGENT_NOTES "Phase 7.1 fixes applied (frontend)"). **VERIFIED:** tsc clean, root eslint exit 0, prod build clean (CSP NO unsafe-eval, connect-src 'self', worker-src blob:), 59 web tests green; re-captured dark+light dashboard, 360 mobile, tablet 768, wide 2560, /about light+dark via the same-origin-proxy + Chromium recipe -- pill Live, zero CSP violations, light bodyBg #eef1f5 (P0-2/P1-6 confirmed), map plate lifted + fleet filling (P0-1/P1-2 confirmed), narrow scrolls (P1-1 confirmed). Phase 7.2 reviewer P0/P1 were server-side (backend-engineer, done in parallel).
