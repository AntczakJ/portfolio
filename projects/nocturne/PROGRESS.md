# nocturne — Progress

> Read this on start, write it on end. The active project's PROGRESS.md takes precedence over the root one.

## State

**Root-ESLint gate hardened (frontend-engineer, 2026-06-14)** — nocturne now passes the **repo-root** `npx eslint .` (typescript-eslint strictTypeChecked + stylisticTypeChecked — the pre-commit + CI gate every other web project clears), not just the looser project `eslint-config-next`. Fixed **63 root-lint errors + 2 unused-disable warnings → 0** across 14 files, all **behavior-preserving**. `pnpm -F nocturne-web typecheck` clean; **123 Vitest still green** (unchanged). **No per-frame perf regression**: the hot-path `non-nullable-type-assertion-style` fixes in `palette-data.ts` / `preset-uniforms.ts` replaced `x as Rgb` with `x ?? RGB_FALLBACK` against a single module-level hoisted constant — a branch-predicted, allocation-free guard (NOT an `x!`, which `no-non-null-assertion` would re-flag, and NOT a new branch in the per-particle path; the zero-per-particle-CPU 60 fps + N1 hoist invariants hold). Key non-mechanical fixes: `routing.ts` dropped the always-true `motionMode === 'full'` conjunct (provably narrowed); `presets.ts` hoisted `FIRST_PRESET` as a typed `Preset` so the `getPresetOrDefault` fallback is type-provable (no `PRESETS[0]!`); `audio-engine.ts` / `detect-gpu-tier.ts` modelled `AudioContext`/`webkitAudioContext`/`mediaDevices`/`hardwareConcurrency` as genuinely optional (lib.dom types them required) so the real runtime guards stop reading as dead code; `nocturne-field.tsx` removed the `react-hooks/exhaustive-deps` inline disable (a rule the root config has no plugin for) by reading the current preset+DPR from refs in the one-time allocation effect, making `[config.simResolution, gl, scene]` genuinely exhaustive — re-allocation semantics unchanged. Test-file `x as number` → `x!` (the test override disables `no-non-null-assertion`). See AGENT_NOTES for the reviewer re-examination list. (frontend-engineer, 2026-06-14)

**Docs shipped (Phase 8 — doc-writer, 2026-06-14)** — the end-user-facing docs are written. `projects/nocturne/README.md` (pitch, an honest **deploy-pending** Demo section with the real-GPU-vs-software-GL caveat mirroring apex's WebGL note, curated screenshots, the Next 16 + R3F + drei + postprocessing + custom GLSL + Web Audio stack, run instructions incl. the three test entrypoints + lighthouse, architecture notes on the GPGPU FBO ping-pong / audio pipeline / four-tier degradation / perf tiers, the ADR links, Credits, and the v2 path), `projects/nocturne/CHANGELOG.md` (Keep a Changelog — [0.1.0] 2026-06-14 v1 summary + [Unreleased] for deploy + deferred mediums + v2), and the root `README.md` row + Philosophy paragraph (now EIGHT projects; four api-heavy + FOUR web-only creative). Screenshots curated from `web/docs/pass3-shots/` + `critique-fix-shots/` into `docs/screenshots/` (9 files, listed in AGENT_NOTES for the reviewer). **No GIF shipped** (a software-GL field GIF would misrepresent the piece — the apex precedent; stated in the README). Demo status is honestly **deploy pending** — no fake live Fly URL asserted; the main thread flips it to the live URL after deploy. Next: deploy (main thread + architect ADR-005). (doc-writer, 2026-06-14)

**E2E + Lighthouse CI shipped (Phase 7 — test-engineer, 2026-06-14)** — a `nocturne-e2e` Playwright workspace package (mirroring atrium's separate `e2e/` member) + the Lighthouse CI config + both GitHub workflows are in place, all green. **29 Playwright E2E pass** against the built + served prod app on `:3100` (`next build --webpack` → `next start -p 3100`, never `next dev` — the strict CSP forbids `unsafe-eval`). **123 Vitest still green** (unchanged). nocturne-web `tsc --noEmit` + `eslint .` clean; the e2e package `tsc --noEmit` clean. **Lighthouse on `/about` (the non-canvas surface, the apex precedent): perf 100 / a11y 100 / best-practices 96 / SEO 100, median of 5 runs — all ≥ 95** + CWV/first-paint green (LCP 590 ms, CLS 0.007, FCP 351 ms, TBT 28 ms). The canvas route `/` is deliberately NOT a Lighthouse subject (continuous audio-reactive WebGL on software-GL headless is confounded; its 60 fps + tiers are measured separately — documented in `lighthouserc.json` + the workflow). Next: `doc-writer` → deploy. (test-engineer, 2026-06-14)

**critique + review must-fix applied (Pass 4 — frontend-engineer, 2026-06-14)** — H1 (reviewer HIGH) + D-01..D-04 (critique HIGH) + the cheap nits (N1..N4) + cheap critique mediums (D-06, D-08) are DONE and re-verified on the prod standalone. **123 Vitest green** (was 116; +7 from the new pure `easeOutExpo`/`easeOutQuart` in `@/lib/engine/easing.ts`). typecheck + lint clean; `next build --webpack` clean + standalone `server.js` emitted; 0 CSP / console / page errors; engine verify still green (live GPGPU + audio reactivity + leak-free teardown — no regression to the zero-per-particle-CPU invariant). Headless re-verification + re-captured shots in `web/docs/critique-fix-shots/` via `web/scripts/verify-critique-fixes.mjs`. **Latent bug fixed in passing:** once D-01 made light chrome reachable, the over-stage HUD + intro ink (which referenced the theme-variant `--color-foreground`) flipped DARK-on-the-always-dark-stage = illegible; fixed by adding theme-INVARIANT `--stage-ink`/`--stage-ink-muted` and repointing `--hud-ink`/`--hud-ink-muted` + the over-stage components (hud-button, preset-picker, the about link) at them (proven: in `html.light` the intro/HUD ink computes to the near-white `oklch(0.93 ...)`, while `/about` chrome correctly stays `oklch(0.22 ...)`). Next: `reviewer` (re-confirm H1 + no contract regression) → `test-engineer` → `doc-writer` → deploy.

**designer-critic critique complete (2026-06-14)** — 0 blockers, 4 high, 12 total. File `web/docs/critique.md`. The S14 no-sibling-re-skin gate vs apex: **PASS** (motif/GLSL/palette/interaction/post all diverge; only the chrome SHAPE is inherited, which is allowed). Highest-leverage: **D-01** /about + / ignore prefers-color-scheme and force dark on first load (providers.tsx defaultTheme=dark beats enableSystem — CLAUDE.md S4 violation, confirmed by probe); **D-02** intro wordmark clips both edges at 390px (--text-6xl unconstrained) + positioning tagline illegible over the bloom; **D-03** preset cross-fade is linear (nocturne-field.tsx:425, no easing despite --ease-out-expo existing); **D-04** HUD scrim fails AA (~2.2:1 reasoned) at the field additive bloom peak — the engineer flag is REAL, strengthen --stage-scrim / apply --stage-scrim-strong to the bottom bar. Preset palettes as a set = rich + distinct (PASS, not muddy). Engine wow = credible by construction (headless caveat — final colour/bloom/60fps unverifiable). Captures in `web/docs/critique-shots/`. Next: `frontend-engineer` applies the must-fix tier (D-01..D-04), then `reviewer`.

**review-ready (Pass 3 of 3 — Phase 5 complete)** — the `frontend-engineer` has built the cinematic auto-dimming HUD (replacing the dev panel), the cinematic intro/gesture gate, the four-tier degradation wired end-to-end, the designed poster + per-preset thumbnails, the full `/about` (technique/directory/credits/accessibility + JSON-LD), the `aria-live` text alternative, and the SEO surface (per-page metadata, next/og OG image, robots, sitemap, Person+WebSite+CreativeWork JSON-LD). VERIFIED: clean build + standalone server.js, headless chromium 0 CSP/console/page errors on `/` + `/about` + reduced-motion + Tier-4, full keyboard reachability of every HUD control, the gated mic affordance, the reduced-motion no-autoplay/calm/Still path, the no-WebGL/no-JS poster+directory real DOM, 320px no overflow, the SEO routes resolving, LCP=poster. **116 Vitest green** (was 96; +auto-dim +describe-state +poster-gradient). Next: `designer-critic` → `reviewer` → `test-engineer` → `doc-writer` → deploy (2026-06-14).

**foundation built (Pass 1 of 3 — Phases 1, 2, 3 complete)** — the `frontend-engineer` has scaffolded the project, the sovereign tokens + type, and all the Zod schemas + curated presets + pure logic, and VERIFIED the build + standalone artifact + a CSP-clean headless-chromium smoke render + the full typecheck/lint/test gate (2026-06-14). The GPGPU engine, shaders, audio runtime, post, and HUD are **Pass 2/3** (not started). Slot 8. Dev port **3100**. Web-only, R3F single-family (R3F + drei + @react-three/postprocessing), custom GLSL GPGPU + Web Audio.

**Pass-1 verification (all green):**

- `pnpm -F nocturne-web build` (`next build --webpack`) — clean; standalone `web/.next/standalone/web/server.js` emitted (the deploy artifact).
- Headless-chromium smoke against the standalone server on :3100 — the live WebGL2 smoke Canvas renders (1280×720, GL context), **0 CSP violations, 0 console errors, 0 page errors** under the exact eval-free ADR-002 §6 prod CSP. (Verifier: `web/scripts/verify-csp-smoke.mjs`.)
- `tsc --noEmit` clean · `eslint .` clean (native flat config) · **79 Vitest tests pass across 6 files.**
- three.js confirmed code-split out of the `/` initial bundle (own lazy chunks).

**Phase-0 ADR headlines (the load-bearing decisions the frontend-engineer must not re-litigate):**

- **ADR-002 (accepted)** — GPGPU engine = three.js `GPUComputationRenderer` (two variables: position+velocity), **`HALF_FLOAT`/`RGBA16F`** render targets (full-float fallback) gated on WebGL2 + **`EXT_color_buffer_float`** (the hard capability gate → Tier-4 poster). Curl-noise advection in custom GLSL, index→sim-UV via a once-built `reference` attribute (**zero per-particle CPU work per frame** — the 60 fps invariant), respawn/lifetime on particle age. Render = `THREE.Points` additive soft sprites, palette-ramp color. **Default desktop tier = `high` 512²≈262k** (NOT 1M — `ultra` is adapt-up-only on proven headroom). Single client island (`next/dynamic ssr:false`), **`frameloop="always"`** (continuous, paused when hidden/unarmed/reduced-motion-still), one `useFrame` owns sim→render→post. **CSP is eval-free and tighter than apex** (no WASM at all; `connect-src 'self'`), with one Web-Audio delta: `media-src 'self' blob:`. AnalyserNode chosen over AudioWorklet to keep `script-src` free of `blob:`.
- **ADR-003 (accepted)** — three sources (bundled CC0 `<audio>` loop / mic `MediaStream` / upload object-URL `<audio>`) into **one shared `AnalyserNode`** behind a master `GainNode`. **Autoplay gesture-gate = the cinematic intro** (gesture → create context → `resume()` → `play()` → arm field); **no autoplay ever**, including under reduced-motion. **Pure** `reduceBands` (`fftSize=2048`, bins→subBass/bass/mid/high+rms) + `applyEnvelope` (attack/release so it breathes, not strobes) — Vitest-testable with zero Web Audio. Mapping: bass→turbulence/flow-energy, mid→spread, high→colorShift/sparkle, rms→bloom/vignette. **Idle drift** (field never dead) doubles as the reduced-motion calm-drift source. **Teardown contract**: one AudioContext closed on unmount, mic tracks stopped on switch-away+unmount, object URLs revoked (reviewer item). **A real CC0 track must be sourced + credited — do NOT invent one.**
- **ADR-004 (accepted)** — capability decision tree (JS→WebGL2→float-ext→reduced-motion→reduced-data/mobile→cores). **Four tiers:** (1) full cinema, (2) reduced-capability (smaller N/DPR/post), (3) **reduced-motion = calm autonomous drift** (audio muted, bloom-pulse off, presets still re-skin) + an accessible **"Still" toggle**, (4) no-WebGL/no-JS = **static AVIF poster + preset directory as real DOM**. **Poster→live reveal-when-ready** (apex S1): poster is the LCP, live canvas mounts behind it in a reserved box, cross-fades in on first-frame-ready — no empty-canvas flash, no CLS. **Chrome themes light/dark but the canvas STAGE stays fixed-dark in both** (theme-invariant stage tokens vs theme-variant chrome tokens). Canvas `aria-hidden` decorative + `aria-live` text alternative (preset + source) + all-real-DOM keyboard-operable HUD (auto-dim never drops keyboard reachability).
- **ADR-005 (pending)** — deploy posture, finalized at Phase 8 (atrium/apex Fly single-Machine standalone pattern).

## Done

- [x] `DECISIONS.md` — **ADR-002 / ADR-003 / ADR-004 authored + accepted** by the `architect` (Tasks 0.1/0.2/0.3); ADR-005 (deploy) refined as explicitly pending for Phase 8. The GPGPU engine + R3F/Next integration + perf tiers + the exact eval-free CSP, the Web Audio pipeline + gesture gate + pure band-reduction + teardown, and the reduced-motion + four-tier degradation + poster→live hand-off + chrome-theme/stage-dark contract are all pinned. Phase-0 architecture lock-in complete; implementation unblocked. (`architect`, 2026-06-14)
- [x] `PLAN.md` — problem, audience, the detailed wow-moment spec (GPGPU FBO ping-pong + curl-noise + Web Audio FFT + pointer + presets + cinematic post), web-only justification, the R3F single-family pick, sovereign dark-cinematic design direction + the minimal-HUD shape, the audio-source model, the two-route IA, success criteria (perf tiers, a11y, reduced-motion, no-WebGL poster fallback, CSP, SEO, mobile), the phased task list, and out-of-scope (v2: more presets, recording/export, MIDI). (`planner`, 2026-06-14)
- [x] `DECISIONS.md` — ADR-001 (web-only + the § 12 composition rationale + the R3F single-family posture + sovereign dark-canonical tokens + the **§ 14 R3F-re-use justification vs apex**); ADR-002/003/004/005 listed as pending for the architect. (`planner`, 2026-06-14)
- [x] `PROGRESS.md` + `AGENT_NOTES.md` seeded. (`planner`, 2026-06-14)
- [x] Root `PROGRESS.md` composition tracker updated — slot 8, nocturne, web-only (creative), backend "—". (`planner`, 2026-06-14)
- [x] Task list created (TaskCreate) from the PLAN phases. (`planner`, 2026-06-14)

## Done (Pass 1 — frontend-engineer, 2026-06-14)

- [x] **Task 1.1 (scaffold)** — umbrella `nocturne` + workspace member `nocturne-web`, mirroring apex/atrium: `package.json` (build `next build --webpack`, dev `next dev -p 3100 --webpack`, lint `eslint .`), project `tsconfig` extending a project-level `tsconfig.base.json`, native flat `eslint.config.mjs` (`eslint-config-next/core-web-vitals` + `/typescript`, NOT FlatCompat), `postcss.config.mjs`, `next-env.d.ts`, `vitest.config.ts`, `web/.gitignore`. `next-themes` **dark-canonical default**. `output: 'standalone'` + one-hop `outputFileTracingRoot`. **The exact ADR-002 §6 CSP** in `next.config.ts` via the atrium dev/prod `headers()` split — VERIFIED served (eval-free prod; `media-src 'self' blob:`; `connect-src 'self'`; no WASM).
- [x] **Task 1.2 (providers + store)** — `Providers` (next-themes dark-default, `attribute="class"`, NO TanStack Query) in a low `'use client'` boundary; the **Zustand `experience-store`** skeleton (preset + audio-source + HUD state machine: `selectPreset`/`completePresetTransition`, `setAudioSource`, `toggleMute`, `arm`, `setMotionMode`, `togglePointerInteraction`, `setHudDimmed`).
- [x] **Task 1.3 (smoke Canvas + capability probe)** — `next/dynamic ssr:false` Canvas (`frameloop="always"`, capped dpr, EffectComposer/Bloom/Vignette) behind the `Stage` shell; **`detectGpuTier()`** implemented EXACTLY per the ADR-002/ADR-004 decision tree (webgl2 → float-ext hard gate → reduced-motion → reduced-data → coarse-pointer+<768 → cores/memory), pure-ish + injectable, NOT touching the GPU at import. Verified: code-split + 0 CSP violations + 0 console errors on the standalone artifact.
- [x] **Task 2.1 (tokens)** — sovereign OKLCH `globals.css`: theme-INVARIANT `--stage-*` (fixed dark canvas ground + HUD scrim) vs theme-VARIANT `--color-*`/`--hud-*` chrome (dark `:root` canonical + `.light` reading mode). NO sibling reuse (§ 14). Chrome-text contrast ≥ AA both themes (recorded in `globals.css` + `AGENT_NOTES.md`).
- [x] **Task 2.2 (type)** — Sora (display) + Outfit (sans), both SIL OFL 1.1 variable, via `next/font` self-hosted; provenance in `AGENT_NOTES.md`. Distinct from every sibling.
- [x] **Task 3.1 (schemas)** — `src/lib/schemas/`: `preset` (palette/flow/particle/post/audio-gains), `audio-source`, `audio-bands`, `tier`, `hud-state`, barrel (+ zod-config jitless guard).
- [x] **Task 3.2 (presets)** — `src/data/presets.ts`: **6 curated, dramatically-distinct looks** (glacial-drift, molten-swirl, aurora, nocturne-noir, solar-wind, ink-bloom), each a complete typed `Preset` validated by the schema at module load, + the directory copy (name + vibe).
- [x] **Task 3.3 (pure audio)** — `src/lib/audio/reduce-bands.ts`: pure `reduceBands(freqData, sampleRate, fftSize)` → {subBass,bass,mid,high,rms} + `applyEnvelope(prev, raw, attack, release, dt)`. No React, no Web Audio. Vitest-covered (silence/clipping/spike/fold-in/empty + attack-fast/release-slow/convergence/dt-independence).
- [x] **Task 3.4 (pure logic)** — `src/lib/engine/preset-uniforms.ts` (the full uniform-set + ramp cross-fade interpolator, exact at t=0/1) + `src/lib/engine/routing.ts` (reduced-motion branch selector + capability routing + loop gating) as pure functions. Vitest-covered.

## Done (Pass 2 — the engine, frontend-engineer, 2026-06-14)

- [x] **Task 4.1 (GPGPU sim)** — `@/lib/engine/gpgpu-sim.ts`: a `GpgpuSim` class wrapping `GPUComputationRenderer` with two ping-ponged variables (`texturePosition` xyz+age, `textureVelocity` xyz+seed), `HalfFloatType` targets (WebGL2; `FloatType` fallback), `setVariableDependencies` both-on-both. Custom GLSL in `@/lib/glsl/`: `noise.glsl` (Ashima 3D simplex + finite-difference **curl** → divergence-free flow + hash helpers), `sim-velocity.glsl` (curl advection + pointer attractor + mid-spread + domain containment), `sim-position.glsl` (integrate + age + in-shader respawn by spawn shape). The `reference` attribute + seed textures built ONCE on the CPU (`@/lib/engine/particle-seed.ts`, pure + Vitest-covered). **Zero per-particle CPU work per frame.** Default tier `high` = 512²≈**262144** particles (verified rendered).
- [x] **Task 4.2 (render)** — `THREE.Points` reading the position texture in the vertex shader (`@/lib/glsl/render.glsl`), additive soft radial sprites, palette-ramp colour by speed/energy + high-band shift, size by tier + high sparkle, life-fade in/out. Palette ramp → 1D gradient `DataTexture` via the pure `@/lib/engine/palette-data.ts` (Vitest-covered), updated each frame from the cross-fade ramp. Pointer/touch wake wired (local attractor uniform in the velocity field).
- [x] **Task 4.3 (post + reveal)** — `EffectComposer` (Bloom audio-pulsed by rms, Vignette breathing, ChromaticAberration) tiered by `postQuality`. Post intensities driven via a shared ref read off the **composer** (NOT per-effect refs — see AGENT_NOTES gotcha). Poster→live reveal: poster placeholder cross-fades out / field fades in on first-frame `onReady` (no empty-canvas flash, reserved box = no CLS). Float-FBO-init-failure routes to the poster (no crash).
- [x] **Task 4.4 (audio pipeline)** — `@/lib/audio/audio-engine.ts`: one `AudioContext` + master `GainNode` + shared `AnalyserNode`, created lazily on the gesture. **Procedural default source** (oscillator pad + LFO-swept lowpass + shimmer + rhythmic kick pulse — no shipped file, no licensing). Mic (`getUserMedia`, never→destination) + file upload (object URL, revoked) sources swap into the same gain→analyser. Gesture gate (`@/components/stage/stage-gate.tsx`): create→`resume()`→procedural source→`arm()`. Per-frame `getByteFrequencyData`→`reduceBands`→`applyEnvelope` (prev held in a ref)→band→uniform mapping (bass→turbulence/flow, mid→spread, high→colorShift/sparkle, rms→bloom/vignette), with an idle floor so it drifts, never dead. **Leak-free teardown** (context closed, mic tracks stopped, URLs revoked) on unmount + source-switch.
- [x] **Engine R3F island** — `@/components/stage/nocturne-field.tsx` replaces `SmokeScene` (deleted). One `useFrame` owns audio→uniforms→sim-step→render-textures→post each frame. `frameloop="always"`, paused via `shouldRunLoop(route, armed, tabHidden)` (tab-visibility hook). `@/components/stage/field-canvas.tsx` is the `next/dynamic ssr:false` boundary (three.js stays code-split). Stage orchestrates the probe + gate + reveal + minimal dev HUD.
- [x] **Tests** — `particle-seed.test.ts` + `palette-data.test.ts` added (pure logic only; no GLSL/GPU). **96 Vitest pass** (was 79). typecheck + lint clean. `pnpm -F nocturne-web build` clean, standalone `server.js` emitted, three.js code-split confirmed (Pass-1 mechanism unchanged).

## Done (Pass 3 — HUD + degradation + about + SEO, frontend-engineer, 2026-06-14)

- [x] **Task 5.1 (cinematic HUD)** — replaced the dev panel (`stage-gate.tsx`, deleted) with: `@/components/stage/intro-overlay.tsx` (the cinematic gesture gate — wordmark + positioning + a real `<button>` "Press to begin · sound on", drops "sound on" under reduced-motion; CSS `nocturne-rise` reveal gated behind `motion-safe`), `@/components/stage/hud.tsx` (the auto-dimming HUD: top bar = wordmark + fullscreen + theme + about; bottom bar = preset picker + audio source + mute + motion mode + pointer toggle), `@/components/stage/preset-picker.tsx` (WAI-ARIA `radiogroup` roving-tabindex, arrow/Home/End keys → `selectPreset` cross-fade; reflects active + transitioning-toward), `@/components/stage/theme-toggle.tsx` (next-themes, mounted-guard, stage stays dark), `@/components/stage/hud-button.tsx` (shared AA-contrast focus-visible primitive). Auto-dim = the pure `@/lib/hud/auto-dim.ts` (`shouldDimHud` — focus-within / unarmed / reduced-motion never dim; opacity-only, never drops tab order or a11y-tree). Mic gated on `micAvailable` (disabled affordance with a clear title in insecure contexts). Fullscreen via the Fullscreen API. Mobile 320px thumb-reachable reflow.
- [x] **Task 5.2 (cross-fade wiring)** — confirmed end-to-end: the picker drives `selectPreset(id)` → engine cross-fades (`crossfadePresets`) → `completePresetTransition` on settle; the HUD reflects active + `transitioningToId` (the picker leads the field). Verified the field morphs (aurora→molten capture) with the band uniforms moving.
- [x] **Task 5.3 (/about)** — `@/app/about/page.tsx` expanded: the technique in accessible prose (GPGPU ping-pong + curl-noise + Web Audio FFT), the PRESET DIRECTORY (the shared `@/components/poster/preset-directory.tsx` with designed thumbnails), CREDITS (Sora/Outfit OFL; the **procedurally-synthesized** audio note — no third-party track; MIT libs), and the ACCESSIBILITY STATEMENT (reduced-motion, the canvas text alternative, keyboard operation) + CreativeWork JSON-LD.
- [x] **Task 5.4 (four-tier degradation)** — wired end-to-end via `detectGpuTier` + `resolveRenderRoute`: Tier 1 full / Tier 2 reduced-capability / Tier 3-RM calm drift + "Still" toggle / Tier 4 poster + directory. **Designed poster** (`@/components/poster/poster.tsx` + the pure `@/lib/poster/poster-gradient.ts`) — an authored luminous-field gradient evoking the preset palette + SVG grain + the wordmark, the LCP in a reserved CLS-safe box; the live canvas cross-fades in on first-frame-ready. Per-preset thumbnails reuse the same designed approach. Tier-4 / no-JS = the `#main [data-nojs-fallback]` real-DOM wordmark + positioning + full preset directory (hidden via `[data-armed]` only when the live/calm experience mounts; always visible with no JS or on the poster route). Canvas `aria-hidden`; `aria-live="polite"` text alternative via the pure `@/lib/hud/describe-state.ts`.
- [x] **SEO surface** — per-page metadata (root + /about), a designed **next/og** OG image (`@/app/opengraph-image.tsx` — the wordmark over the luminous-field motif, 1200×630), `robots.ts`, `sitemap.ts`, and Person + WebSite + CreativeWork JSON-LD on `/`. `metadataBase` via `NEXT_PUBLIC_SITE_URL` (placeholder `https://nocturne-demo.fly.dev`).
- [x] **Tests** — `@/lib/hud/auto-dim.test.ts` + `@/lib/hud/describe-state.test.ts` + `@/lib/poster/poster-gradient.test.ts` added (pure logic, no DOM/GPU). **116 Vitest pass** (was 96). typecheck + lint clean.

**Pass-3 verification (all green) — headless chromium on the prod standalone (`web/scripts/verify-pass3.mjs` + `verify-engine.mjs`, :3100):**

- `pnpm -F nocturne-web build` (`next build --webpack`) clean; standalone `web/.next/standalone/web/server.js` emitted; all routes static (incl. `/opengraph-image`, `/robots.txt`, `/sitemap.xml`).
- **0 CSP violations, 0 console errors, 0 page errors** on `/` + `/about` + reduced-motion + Tier-4, under the exact eval-free ADR-002 §6 prod CSP (unchanged).
- **HUD fully keyboard-reachable** — tab order: fullscreen → theme → about → preset(Aurora) → Synth/Mic/File/Upload → Mute → Full/Calm/Still motion → pointer. Preset radio-group arrow keys select. Mic affordance present (enabled on the secure localhost ctx; disabled w/ a clear title otherwise).
- **Reduced-motion path** — gate drops "sound on" (no autoplay), aria-live reports calm drift with reactivity muted, the "Still" toggle is operable; nothing strobes.
- **Tier-4 (stubbed no-WebGL)** — no canvas mounts, no gate; the preset directory renders as 6 real-DOM cards + the wordmark/positioning are visible (the readable floor).
- **320px** — no horizontal overflow on `/` or `/about`; the HUD reflows thumb-reachable.
- **LCP = the designed poster** (the SSR `[data-nojs-fallback]` wordmark/poster is in the HTML before any canvas; the live canvas is lazy + reveals over it).
- Screenshots → `web/docs/pass3-shots/`: `hud-dark-desktop.png`, `hud-light-desktop.png` (stage stays dark), `hud-dark-mobile.png`, `tier4-poster-directory.png`, `about-dark.png`, `about-light.png`, `opengraph-image.png`.
- `tsc --noEmit` clean · `eslint .` clean · **116 Vitest pass** · engine verify still green (live GPGPU + audio reactivity, leak-free teardown).

## Done (Pass 4 — critique + review must-fix, frontend-engineer, 2026-06-14)

- [x] **H1 (reviewer HIGH — mute now mutes)** — `@/lib/audio/audio-engine.ts` re-routed so the master gain is the SINGLE node between the sources and BOTH outputs: `gain.connect(analyser)` AND `gain.connect(ctx.destination)`. Audible sources (`connectSynth` `out`, `connectUpload` `src`) now connect ONLY to the gain (their direct `ctx.destination` edges removed). So `setMuted(true)` (gain → 0) silences the audible output AND drops the analyser feed → the field falls to idle drift (ADR-003 §1). The MIC is the lone exception — it taps the `analyser` DIRECTLY (bypassing the gain that feeds destination) so the room is never echoed; it has no audible output to mute, so the contract is intact. Teardown still leak-free (`gain.disconnect()` drops both edges; mic disconnected via `disconnectCurrentSource`). Verified by graph reasoning + the engine verifier (reactivity unbroken).
- [x] **D-01 (chrome respects prefers-color-scheme)** — `@/app/providers.tsx` `defaultTheme="system"` (was `"dark"`, which beat `enableSystem`). A light-OS visitor now gets LIGHT chrome on first load; dark-OS + unset get dark. The STAGE stays dark in both (theme-invariant `--stage-*`). No FOUC (next-themes' blocking script applies the resolved class pre-paint). VERIFIED: Playwright `colorScheme:'light'` → `html.light` + stage bg `oklch(0.13 ...)`; `colorScheme:'dark'` → `html.dark`.
- [x] **D-01 follow-on (over-stage ink theme-invariant)** — added `--stage-ink`/`--stage-ink-muted` (theme-INVARIANT near-white) in the STAGE token block and repointed `--hud-ink`/`--hud-ink-muted` + `hud-button.tsx`/`preset-picker.tsx`/the about-link resting ink at them, so the HUD + intro over the always-dark stage stay legible in light chrome (the latent dark-on-dark bug D-01 surfaced). `/about` chrome unchanged (still theme-variant `--color-foreground`).
- [x] **D-02 (intro wordmark + tagline)** — `@/components/stage/intro-overlay.tsx`: the wordmark uses its OWN `clamp(2.4rem, 13vw, 10.5rem)` (not the shared `--text-6xl`) + `--tracking-tight` (was `--tracking-wider`, which fragmented the lockup — also closes D-06). VERIFIED no clip at 320px (box 37→283 of 320) and 390px (45→345 of 390), no document overflow. The tagline now sits on a dedicated `.hud-scrim-strong` pill + soft text-shadow + medium weight → legible over peak bloom (independent of the field).
- [x] **D-03 (eased cross-fade)** — new pure `@/lib/engine/easing.ts` (`easeOutExpo` = the JS twin of CSS `--ease-out-expo`, `easeOutQuart`; clamped, exact endpoints). `resolveLook` in `nocturne-field.tsx` now samples `crossfadePresets` at `easeOutExpo(tr.t)` (the raw `tr.t` stays the linear time progress; the field is sampled on the curve). Duration trimmed 1.4s → 1.1s (decisive, per the critique). Throw-free (pure arithmetic in `useFrame`). Vitest-covered (7 new tests: endpoints/clamp/front-loaded/monotonic).
- [x] **D-04 (strong scrim on the bottom HUD bar)** — new `.hud-scrim-strong` utility in `globals.css` (the 82% `--stage-scrim-strong` + a soft downward shadow). Wired to the bottom control bar (`hud.tsx`) and the intro tagline. VERIFIED the bar's computed bg = `oklch(0.08 0.025 274 / 0.82)`. Also capped the bar at `max-w-[min(96vw,64rem)]` (closes D-08 — compact instrument on ultra-wide). Keyboard reachability of every control re-confirmed.
- [x] **Nits** — N1: the per-frame `raw` band object + the two pointer tuples in the field `useFrame` are hoisted to refs and mutated in place (zero per-frame allocation; the 60fps invariant unchanged). N2: `connectSynth` detune now uses a seeded `mulberry32(SYNTH_SEED)` (no `Math.random` in the runtime). N3: the pointer wake stores raw NDC and scales by `base.domainScale * POINTER_DOMAIN_REACH` per frame (preset-relative, replacing the `*3.2` magic). N4: `freqData` initial allocation reduced to a 0-length placeholder (the real `frequencyBinCount` array is built in `arm()`).
- [x] **Deferred (noted for downstream, not regressions):** D-05 (hairline over a bright field — cosmetic, decorative-only, not an AA item), D-07 (animate the variable-font weight axis — subjective art-direction enhancement), D-09 (arm-into default preset — art-direction call), D-10 (OG tagline over the glow — needs an OG re-render pass), D-11 (/about heading rhythm — subjective long-form polish), D-12 (idle button shimmer — subjective). All require either a real-GPU look judgement or subjective art-direction; left for a designer-critic re-pass / doc-writer.

**Pass-4 verification (all green) — `web/scripts/verify-critique-fixes.mjs` + `verify-engine.mjs` on the prod standalone (:3100):**

- `pnpm -F nocturne-web build` (`next build --webpack`) clean; standalone `web/.next/standalone/web/server.js` emitted.
- **0 CSP violations, 0 console errors, 0 page errors** on `/` (light + dark first-load), the 320/390 intro, and the molten-armed HUD; CSP string UNCHANGED (the exact eval-free ADR-002 §6).
- **D-01** light/dark first-load chrome correct + stage stays dark; **D-02** wordmark no clip at 320/390 + tagline strong-scrim present; **D-04** bottom bar uses the 82% strong scrim + keyboard reach intact; reduced-motion gate still drops "sound on" (no autoplay).
- Engine verify still green: live GPGPU path ran, 65k field, **audio reactivity moves frame-to-frame** (bands hot, turbulence/bloom changing) — H1 + N1 did not regress reactivity; leak-free teardown intact.
- `tsc --noEmit` clean · `eslint .` clean · **123 Vitest pass** (was 116; +7 easing).
- Re-captured shots → `web/docs/critique-fix-shots/`: `d01-light-chrome-stage-dark-1280.png`, `d02-intro-320.png`, `d02-intro-390.png`, `d04-bottom-bar-strong-scrim-molten.png`.

## Done (Phase 7 — E2E + Lighthouse CI, test-engineer, 2026-06-14)

- [x] **`nocturne-e2e` Playwright workspace package** — a separate `projects/nocturne/e2e` member mirroring atrium's harness shape: `package.json` (`nocturne-e2e`, the standard test/list/report scripts), `playwright.config.ts` (Zod-parsed env loader; `BASE_URL` / `NOCTURNE_E2E_TARGET_URL` resolution; `workers:1`; `webServer` builds + serves on `:3100` when owning the surface, skipped against an external target), `tsconfig.json` (`@helpers/*` path), `env.d.ts`, `.gitignore`, and `helpers/` (`env.ts`, `diagnostics.ts` = the CSP/console/page-error collector, `webgl.ts` = `denyWebgl`/`denyFloatBuffer` init scripts, `index.ts` barrel with `gotoStage`/`armFromGate` + the 6-preset facts).
- [x] **29 E2E specs across 8 files, all green** (built + served `:3100`, 1.9 min):
  - `intro-gate.spec.ts` (3) — loads to the gate (real begin `<button>`, no autoplay, no HUD yet); Enter arms (gate→HUD swap); Space arms.
  - `hud-keyboard.spec.ts` (3) — every control reachable + semantically named (fullscreen, theme, about, preset radiogroup, Synth/Mic/File, mute, Full/Calm/Still, pointer); keyboard-operable state toggles; the WAI-ARIA preset **radiogroup** roving-tabindex (Arrow/Home/End, single tab stop, selection-follows-focus).
  - `theme.spec.ts` (4) — light-OS → light chrome + **stage stays dark**; dark-OS → dark + stage dark; toggle persists across reload (stage dark throughout); no-FOUC (persisted light applied pre-paint on a dark OS).
  - `reduced-motion.spec.ts` (3) — no autoplay, label drops "sound on", nothing plays; aria-live reports the calm/muted state once armed; the **Still** toggle returns to the poster surface.
  - `degradation.spec.ts` (3) — Tier-4 no-WebGL → poster + 6-card directory real DOM (no canvas, no gate); WebGL2-without-`EXT_color_buffer_float` → same poster floor (the GPGPU float gate); **no-JS** (`javaScriptEnabled:false`) → SSR wordmark + positioning + directory + about link (the SEO/SR floor).
  - `mute.spec.ts` (1) — arm with the synth, mute flips the store `muted` (Mute↔Unmute + `aria-pressed`), field stays on the live idle path (not torn down).
  - `a11y.spec.ts` (3) — the canvas is `aria-hidden`; the `aria-live` alternative is present + **updates** when the preset changes; `/about` skip link + headings + links keyboard-navigable.
  - `seo.spec.ts` (6) — `/robots.txt` (text/plain + sitemap ref), `/sitemap.xml` (xml, lists `/` + `/about`), `/opengraph-image` (image/png), Person+WebSite+CreativeWork JSON-LD on `/`, canonical + description + og:image on `/`, `/about` canonical + CreativeWork JSON-LD.
  - `responsive.spec.ts` (3) — no horizontal overflow at 320px on `/` (intro gate), `/` (armed HUD reflow), and `/about`.
- [x] **Lighthouse CI** — `projects/nocturne/lighthouserc.json` (desktop preset, `startServerCommand` = `next start -p 3100`, 5 runs) asserting **≥ 95 in all four categories on `/about`** + LCP < 2.5s / CLS < 0.1 / FCP < 1.8s / TBT warn-200. Documented WHY `/about` not `/`: the canvas route is a continuous audio-reactive WebGL field on software-GL headless (confounded TBT/CPU; the apex/tape precedent) — its 60 fps + tiers are measured separately. **Local run: perf 100 / a11y 100 / best-practices 96 / SEO 100; LCP 590 ms, CLS 0.007, FCP 351 ms, TBT 28 ms** (median of 5).
- [x] **Workflows** — `.github/workflows/nocturne-e2e.yml` (build + `next start :3100` + Playwright, `workflow_dispatch` target_url + push-to-main path filter, report artifact) and `.github/workflows/nocturne-lighthouse.yml` (LHCI autorun against the config), both mirroring the atrium shapes.

## In progress

- Nothing in active build. Phases 7 (E2E + Lighthouse) and 8 docs (README + CHANGELOG + root README) are complete. nocturne is ready for **deploy** (main thread executes; architect finalizes ADR-005). After deploy: flip the root README `nocturne` Demo cell + the README Demo section from "deploy pending" to the live `https://nocturne-demo.fly.dev` URL, and promote CHANGELOG `[Unreleased]` deploy note to a dated entry.

**Pass-2 verification (all green) — headless chromium on the prod standalone (`web/scripts/verify-engine.mjs`, :3100):**

- `pnpm -F nocturne-web build` (`next build --webpack`) clean; standalone `web/.next/standalone/web/server.js` emitted.
- **WebGL2 = true, EXT_color_buffer_float = true**; the GPGPU sim initialized; default capability tier = **`high` = 512² = 262144 particles** (the `?tier=low` 65536 path is used only so software-GL SwiftShader can complete frames for screenshot capture).
- **0 CSP violations, 0 console errors, 0 page errors** under the exact eval-free ADR-002 §6 prod CSP (`media-src 'self' blob:` covers the upload object URL; the synthesized audio + mic need no new directive). `ENGINE VERIFY PASSED`.
- **Audio gesture-gate → field-arms → reactivity VERIFIED:** clicking the gate creates+resumes the AudioContext, starts the procedural source; `reduceBands` over the live AnalyserNode returns hot bands and the band→uniform mapping MOVES frame-to-frame (turbulence 1.648→1.66, bass 0.861→0.873) — the synthesized pad+LFO+kick drives the field.
- Teardown clean (page close → React unmount → `AudioEngine.dispose()` closes the context, stops mic tracks, revokes URLs).
- Screenshots of the live field in 2 distinct presets captured to `web/docs/engine-shots/` (`field-aurora.png` disc/cool, `field-molten-swirl.png` sphere/warm).
- `tsc --noEmit` clean · `eslint .` clean · **96 Vitest pass** (was 79; +particle-seed +palette-data pure-logic tests).
- **Perf:** headless is software-GL (SwiftShader — multi-second frames, an artifact of no GPU). The 60 fps target rests on the **zero-per-particle-CPU-work** design (ADR-002 §1): per frame the CPU only reads the analyser once, runs `reduceBands`/`applyEnvelope`, and writes ~20 scalar/vec uniforms; ALL motion is in the sim shaders, ALL placement in the render vertex shader. On a real GPU the 262k field is GPU-bound (two N² ping-pong passes + one POINTS draw + bloom), which mainstream desktop GPUs hold at 60 fps. `PerformanceMonitor`/`AdaptiveDpr` tuning + the adapt-up-to-ultra rule are Pass-3 (against the deployed profile).

## Next — Pass 2 (the engine, `frontend-engineer`)

1. **Phase 4.1** — the GPGPU sim: `GPUComputationRenderer` two-variable FBO ping-pong (position+velocity, `RGBA16F`), the curl-noise advection sim shader (custom GLSL), the index→sim-UV `reference` attribute (built ONCE), respawn/lifetime. Prove 262k at locked 60 fps before audio/post.
2. **Phase 4.2/4.3** — the `THREE.Points` additive render + palette-ramp color + pointer/touch wake; the `EffectComposer` (bloom/vignette/aberration) in the single render pass; the poster→live reveal seam.
3. **Phase 4.4** — the Web Audio runtime (one AudioContext, one GainNode, one shared AnalyserNode, two `<audio>` elements + mic), the gesture gate, source switching, teardown — wired into the engine uniforms via the Pass-1 pure functions.

## Handoff

**Next subagent: `designer-critic` → `reviewer` → `test-engineer` → `doc-writer` → deploy (Pass 3 complete).** nocturne is review-ready: the engine (Pass 2) + the cinematic HUD, four-tier degradation, designed poster, `/about`, and SEO surface (Pass 3) are all built + verified. Pass-3 surfaces the downstream agents touch (DO NOT re-author):

- **`@/components/stage/hud.tsx`** — the auto-dimming cinematic HUD; **`intro-overlay.tsx`** — the gesture gate; **`preset-picker.tsx`** — the ARIA radiogroup; **`theme-toggle.tsx`**, **`hud-button.tsx`**.
- **`@/components/poster/`** — `Poster` (the designed LCP still), `PresetThumbnail`, `PresetDirectory` (the Tier-4 + /about directory DOM).
- **`@/lib/hud/`** — `auto-dim.ts` (`shouldDimHud`, pure), `describe-state.ts` (`describeFieldState`/`sourceLabel`, pure — the aria-live text); **`@/lib/poster/poster-gradient.ts`** (`posterBackground`/`posterGradient`, pure — the designed-poster colour derivation).
- **`@/app/opengraph-image.tsx`** — the next/og OG image; **`@/app/about/page.tsx`**, **`robots.ts`**, **`sitemap.ts`** — the SEO surface.
- **`web/scripts/verify-pass3.mjs`** — the Pass-3 headless verifier (HUD keyboard / reduced-motion / Tier-4 / SEO / 320px / screenshots). `verify-engine.mjs` still covers the live engine + audio + CSP.

**For `designer-critic`:** the § 14 no-sibling-re-skin gate vs apex (ADR-001) is still the hard review item. Judge the field against Bruno Simon / Codrops, the HUD restraint against Linear/Vercel, the type/wordmark against Klim/Linear, the cross-fade timing (1.4s `crossfadePresets`) against Linear's micro-interaction bar. Re-verify the HUD-scrim contrast over a BRIGHT field specifically (the light-chrome shot shows the field washing the scrim faint — confirm AA holds at the field's brightest).

**For `reviewer`:** keep the two Pass-2 gotchas (no ref on post effects → ref the composer; no throw in any useFrame). Pass-3 added 3 scoped eslint overrides total (now incl. `theme-toggle.tsx` for the next-themes mounted-guard). Confirm the `dangerouslySetInnerHTML` JSON-LD is static app-authored only (no user input). Outstanding for Phase 8: the CC0 track decision is RESOLVED (procedural synth, no file) but doc-writer credits it; `PerformanceMonitor`/`AdaptiveDpr` runtime adapt-to-ultra tuning against the deployed profile is still untuned (boot default stays `high`).

---

**(Pass-1 handoff, retained.) Pass 2 consumed these Pass-1 module surfaces (DO NOT re-author):**

- **`@/lib/schemas`** — `Preset`, `AudioBands` (`SILENT_BANDS`), `AudioSourceKind`, `TierConfig`/`RenderRoute`/`PostQuality`/`TierName`, `HudState`/`MotionMode`/`ArmState`. The shared contracts.
- **`@/data/presets`** — `PRESETS`, `DEFAULT_PRESET_ID`, `getPreset(id)`, `getPresetOrDefault(id)`, `PRESET_DIRECTORY`.
- **`@/lib/audio/reduce-bands`** — `reduceBands(freqData, sampleRate, fftSize): AudioBands`, `applyEnvelope(prev, raw, attack, release, dt): AudioBands`, `DEFAULT_ATTACK_S`, `DEFAULT_RELEASE_S`. The render loop reads the AnalyserNode once/frame → `reduceBands` → `applyEnvelope` (holding `prev` in a ref) → the band→uniform mapping.
- **`@/lib/engine/preset-uniforms`** — `presetToUniforms(preset)`, `crossfadePresets(from, to, t): { uniforms, ramp }`, `lerpPresetUniforms`, `resampleRamp`/`lerpRamp`, `hexToRgb`, `RAMP_STOPS`. The cross-fade base the audio modulates around (`uniform = base + gain * band`).
- **`@/lib/engine/detect-gpu-tier`** — `detectGpuTier(deps?, options?): TierConfig`. Already called by `Stage` after mount.
- **`@/lib/engine/routing`** — `resolveRenderRoute`, `defaultMotionMode`, `isAudioReactive`, `isPointerWakeActive`, `shouldRunLoop(route, armed, tabHidden)`. Drive the loop/reactivity gating from the store + the TierConfig.
- **`@/lib/store/experience-store`** — `useExperienceStore`. The engine reads `presetId`/`transitioningToId` (cross-fade), `audioSource`/`muted` (graph), `motionMode`/`pointerInteraction`/`armState` (routing).
- **`@/components/stage/`** — `Stage` (the layer-model shell, capability-gated), `StageCanvas` (the `ssr:false` boundary), `SmokeScene` (REPLACE this with the real engine — the only three.js import site). `globals.css` `.stage-ground` / `.hud-scrim` utilities are ready.

The Phase-0 ADRs (ADR-002/003/004) remain the pinned contract; nothing in Pass 1 re-litigated them. The CC0 audio track + the precise envelope/PerformanceMonitor tuning are still outstanding (Pass 2/3 + Phase 8). ADR-005 (deploy) is authored by the `architect` at Phase 8.

## Reviewer gate (reviewer, 2026-06-14)

- [x] **Final code review (Phase 6.3)** - verdict **APPROVE-WITH-NITS**, **0 blockers**, 1 HIGH (H1: Mute does not mute audible output - the audible path bypasses the master gain, contradicting ADR-003 sec 1; fix before deploy, not a commit blocker) + 4 nits (per-frame allocation hoist, Math.random detune, pointer magic-scale, redundant freqData init). VERIFIED green: the GPGPU FBO ping-pong + divergence-free curl + once-built reference attr + zero-per-particle-CPU 60 fps invariant; the leak-free Web Audio teardown + no-SSR-context + gesture gate; the exact eval-free ADR-002 sec 6 CSP + zod jitless guard; the R3F ssr:false code-split + both post/useFrame gotchas + full dispose; strict TS, four-tier degradation wired, canvas aria-hidden + aria-live + keyboard HUD, no secrets/emojis. tsc + eslint clean, 116/116 Vitest. next build compile clean (standalone-trace EBUSY = the documented Windows host gotcha, resolved in Docker at deploy). Full verdict: web/docs/review.md. **Next: frontend-engineer fixes H1, then test-engineer.** (reviewer, 2026-06-14)
