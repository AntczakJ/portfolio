{
  "$comment": [
    "Lighthouse CI config for pulse (Phase 8.3, Task 8.3).",
    "Audits the BUILT + SERVED production surfaces on :3081 (next start, NOT",
    "next dev — the strict CSP forbids unsafe-eval and next dev cannot run under",
    "it; the AGENT_NOTES CSP lesson carried from meld/razors-edge). The two",
    "PUBLIC SEO surfaces are audited: the landing (/) and the public status page",
    "(/status/demo). The authenticated dashboard is EXEMPT — it is an app-behind-",
    "auth surface, not an SEO surface (it has a client-only live-data dependency",
    "for first paint and no crawler value), so it is deliberately NOT a Lighthouse",
    "target (PLAN.md success criterion scopes Lighthouse to the public status",
    "page; the landing is the second public surface).",
    "",
    "Asserts the CLAUDE.md s4 / PLAN.md success criteria: >= 95 in all four",
    "categories on both URLs, median of 5 runs, plus the public-page Core Web",
    "Vitals (LCP < 2.5s, CLS < 0.1). Desktop preset matches the recruiter audience.",
    "",
    "NOTE: LHCI does NOT start the NestJS API + worker or the docker services —",
    "those must already be up so the status page SSR fetch (GET /public/:slug) and",
    "the seeded demo data resolve. The workflow + the local runbook bring the full",
    "stack up first, then point LHCI at the already-served :3081."
  ],
  "ci": {
    "collect": {
      "url": ["http://localhost:3081/", "http://localhost:3081/status/demo"],
      "numberOfRuns": 5,
      "settings": {
        "preset": "desktop"
      }
    },
    "assert": {
      "assertions": {
        "categories:performance": ["error", { "minScore": 0.95 }],
        "categories:accessibility": ["error", { "minScore": 0.95 }],
        "categories:best-practices": ["error", { "minScore": 0.95 }],
        "categories:seo": ["error", { "minScore": 0.95 }],
        "largest-contentful-paint": ["error", { "maxNumericValue": 2500 }],
        "cumulative-layout-shift": ["error", { "maxNumericValue": 0.1 }],
        "total-blocking-time": ["warn", { "maxNumericValue": 200 }]
      }
    },
    "upload": {
      "target": "temporary-public-storage"
    }
  }
}
