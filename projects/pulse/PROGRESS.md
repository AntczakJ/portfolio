# pulse — Progress

> Read on start. Write on end.

## State

- **Phase:** **Frontend Phase 6 (auth UI + session header + demo gating + public status page + SEO + landing, Tasks 6.4-6.7) DONE (2026-06-04, frontend-engineer). THE PRODUCT IS FEATURE-COMPLETE FOR THE PHASE 7 REVIEW.** The auth UI is real (sign-in/up dialog + session-aware header + sign-out, all `credentials:'include'` against the better-auth endpoints), the demo stays OPEN (an unauthenticated visitor reads the board/detail/incidents + runs the demo incident; every WRITE affordance prompts a tasteful "Sign in to create your own monitors" instead of a raw 401), the public `/status/[slug]` page SSRs the REDACTED payload + live-updates off the public SSE stream + carries the full SEO surface (per-page metadata, an on-brand `next/og` OG image, JSON-LD, sitemap, robots), and the landing routes into the open demo + the example status page. **114 web tests green (+28: 11 session-state, 10 public-status-view, 7 auth-form). typecheck/lint/build pass. FULL-STACK LIVE-PROVEN with ZERO CSP violations across every Phase 6 route** (unauth board + demo button + "New monitor"→sign-in prompt; sign up→own private workspace→create monitor; `/status/demo` SSR + redaction + OG/sitemap/robots/JSON-LD; sign-in dialog). Screenshots in `web/.review/p6-*.png`. The remaining work is the Phase 7 `designer-critic` + `reviewer` passes, Phase 8 `test-engineer` (E2E + the auth-boundary assertion + Lighthouse>=95 on the public page), and Phase 9 (docs + Fly deploy). **Prior:** Backend Phase 6 (better-auth + the public read surface + the demo-open posture, ADR-007) DONE (2026-06-04, backend-engineer). AUTH IS REAL AND THE DEMO STAYS OPEN — PROVEN LIVE END-TO-END.** ADR-007 ratifies the demo-open posture: better-auth is fully wired (email+password sign up / in / out / get-session under `/api/auth/*` via a catch-all controller bridging Express<->Web-Fetch to better-auth's handler), but the deployed demo NEVER hits a login wall — an UNAUTHENTICATED visitor reads the SHARED seeded DEMO workspace (board, monitor detail, incidents, the demo-trigger button all work), while MUTATIONS (create/edit/delete monitors + alert channels) require a real session (a demo visitor gets a clear `401 authentication_required` "sign in to manage"). The boundary is ONE crisp seam — `CurrentOwnerService` (in the `@Global` AuthModule): `resolveOwnerUserId(req)` (READS: session user, else the demo owner) and `requireUserId(req)` (WRITES: session user, else 401 on the demo fallback). It REPLACES the Phase-1 `resolveOwnerUserId(db)` call sites + the Phase-3 `getCurrentUserId` stream seam (deleted). **The Phase-1 uuid-vs-text-id FK flag is RESOLVED additively:** `users.id` stays `uuid`, better-auth is configured to emit UUIDs (`advanced.database.generateId=randomUUID`) onto the existing `users` table (`schema:{user:users}`), so migration `0002_mixed_scalphunter.sql` is PURELY ADDITIVE (3 cols on `users` + `session`/`account`/`verification` tables, their `user_id` FK->`users.id` uuid cascade) — NO FK is repointed, all Phase-1..5 data/FKs untouched. **Cookie posture (dev split-origin 3081->3080 vs prod single-origin proxy):** `SameSite=Lax`+`HttpOnly`, `Secure` in prod only; DEV cookie travels cross-origin via CORS `credentials:true` + better-auth `trustedOrigins`(=`CORS_ORIGINS`); PROD the pulse-web proxy makes it first-party — works for `EventSource(withCredentials)` with no second credential (the SSE-over-cookie argument). **Rate limiting (§4):** better-auth's built-in brute-force limiter ENABLED (`/sign-in/email`+`/sign-up/email` 5/min — keys on IP, so `trust proxy` is set + the controller injects `x-forwarded-for` from the socket in dev, else better-auth SKIPS the limiter); the public read endpoint gets a small in-memory `RateLimitGuard` (60/min/IP). **Public read surface (Task 6.3):** `GET /public/:slug` -> the REDACTED public payload (page title/description + public monitors with current status + 30d uptime % + recent public incidents) — NEVER raw response times / alert data / private monitors. Exposes ONLY monitors in the `public_status_page_monitors` join; the public-monitor shape literally has no field for a response time, so a private field cannot leak. The 30d uptime reuses `MonitorReadService.uptime` (rollup-backed, ADR-004) but only the % escapes. **168 server tests green (+12: 5 owner-seam — authed vs demo + requireUser-rejects-demo; 7 public — redaction + banner derivation).** lint/typecheck/build pass; `pulse-web typecheck` passes against the new `PublicStatusPage`/`PublicMonitor`/`PublicIncident` contract. **FULL-STACK LIVE PROOF (docker postgres:5437 + redis:6381, web:3080 + worker):** signed up a user (got a UUID id — reconciliation proven) -> session cookie -> created a monitor AS that user (`GET /monitors` shows ONLY their monitor; the demo workspace's `GET /monitors` shows the 3 demo monitors, NOT the user's; the user's dashboard SSE stream carries ONLY their monitorId, the demo stream carries the demo monitorIds — per-user scope proven). UNAUTHENTICATED: `GET /monitors` + `/incidents` + `/monitors/:id/uptime` -> 200 (demo workspace); `POST /monitors` + `DELETE /monitors/:id` + `POST /alert-channels` -> 401 `authentication_required`; `POST /demo/trigger` -> 201 ALLOWED (the wow). An authed user PATCHing a DEMO monitor -> 404 (ownership, never trusts a client id). Sign-in happy path + sign-out (cookie falls back to demo). Auth brute-force: 6th bad sign-in -> 429. `GET /public/demo` -> ONLY id+name+status+uptime% per monitor + window+severity+cause per incident; grep-proved targetUrl/responseTimeMs/userId/secret/error/statusCode ALL absent + the 2 PRIVATE demo monitors + the test user's monitor absent; the public rate limit fired (8x 429 after 60). Test user cascade-deleted, demo workspace intact. **Prior:** Frontend Phase 5 (incidents view + board incident indicators + alerts config + the demo-incident button — the wow beat) DONE (2026-06-04, frontend-engineer). THE DRAMATIC ARC IS LIVE-PROVEN HEADLESSLY WITH ZERO CSP VIOLATIONS.** The "Incidents" + "Alerts" nav items are now real routes (`/dashboard/incidents`, `/dashboard/alerts`); the nav is PATH-AWARE (`usePathname`) and carries a live open-incident count badge; the unbuilt Status page / Settings items render disabled ("SOON"), never dead links. **Task 5.4 (incidents view):** a clean timeline from `GET /incidents` with all/open/resolved filter tabs, live-updating from the SSE store (a new open incident prepends + pulses, a close stamps the row, open durations tick up each second off a real `startedAt` baseline), AnimatePresence enter/exit (reduced-motion safe), empty/loading/error states. The REST snapshot is authoritative; the live overlay is merged ON TOP and reconciles via a refetch on each `incidentToken` bump; a live-ONLY incident (one REST has not returned) only surfaces if it opened within 2 min (`LIVE_ONLY_MAX_AGE_MS`) so a stale SSE-ring-buffer replay of an old/deleted-monitor incident does NOT materialize. **Task 5.5 (board incident indicators):** the monitor card already had the down-ring on `hasOpenIncident`; ADDED a top-of-board LIVE INCIDENT STRIP (`LiveIncidentStrip`) that materialises open incidents the instant `incident.open` lands (the dramatic beat) and settles out on close — filtered to monitors currently on the board (so a stale replay does not show). The summary bar now reflects an open incident ("Outage detected" + "N open incidents" + a red dot) driven by an `openIncidents` count (board-scoped). The DETAIL header shows an `ActiveIncidentBanner` (severity + cause + ticking duration) and a new "Incidents" section renders the monitor's incident history from `GET /monitors/:id/incidents` (same live-merge). **Task 5.6 (alerts config):** lists the channels (`GET /alert-channels`), a create dialog (RHF + Zod, a webhook|email segmented toggle, by-type target validation mirroring the server `superRefine`, the write-only webhook signing secret), delete with a confirm, the `WebhookSigningHint` documenting the EXACT `X-Pulse-Signature`/`X-Pulse-Timestamp` HMAC scheme so a receiver can verify, and the honest amber "Mock — no real email sent" badge on the email channel. Coverage is stated plainly ("Covers all monitors" — v1 fires every enabled channel on every transition, no per-monitor routing). **Task 5.7 (THE demo button):** `DemoIncidentButton` on the board next to the live indicator + create CTA. Click -> `POST /demo/trigger` -> a guided RUNNING state ("Arming" -> "Incident in progress · Ns" with the fail-window countdown polled from `GET /demo/status` -> "Recovering" -> "Demo complete") with a pulsing dot + a timed progress sweep + a narration line, so the ~30-45s arc reads as DELIBERATE THEATRE not lag. The incident opens/closes ORGANICALLY through the real probe cycle; the board reacts via the SSE events it already consumes. **The `alert.fired` toast is wired:** the board's already-threaded `onAlertFired` sink now pushes a toast — "Alert sent: <monitor> is down" on open, "Recovered: <monitor> is back up" on close — through a HAND-ROLLED Motion-based toaster (NO sonner: the project's single animation library is Motion per § 15; a toast is exactly the AnimatePresence enter/exit Motion is the default for). NO new web dep. **CSP FIX (reviewer note):** wiring Zod-using forms surfaced a latent regression — `z.config({ jitless: true })` was only imported at the SERVER-side root layout, so client chunks JIT-probed `new Function` under the strict no-`unsafe-eval` CSP. Fixed by importing the `@/lib/zod-config` side-effect in the CLIENT `providers.tsx` + the SSE/form schema modules, so jitless is set in the client graph before any schema runs; ALL routes (incl. `/` and the new alerts/incidents) now report ZERO `securitypolicyviolation`. **86 web tests green (+21: 10 duration, 8 alert-channel-form mapping, 3 incident-store reducer).** typecheck/lint/build pass. **FULL-STACK LIVE PROOF (docker postgres:5437 + redis:6381, NestJS web:3080 + worker, prod web `next start -p 3081` with NEXT_PUBLIC_API_URL/SSE_URL=:3080, the demo monitor seeded targeting a PUBLIC ngrok tunnel to `/demo/flaky` so the SSRF guard lets the arc CLOSE locally):** clicked "Trigger demo incident" -> after ~28s the demo card flipped DOWN + got the incident ring + HTTP 500 + a red sparkline, the summary read "Outage detected / 1 open incident", the live incident strip materialised ("Demo flaky — down after 2 consecutive down checks", ticking duration), the nav Incidents badge lit, the **"Alert sent: A monitor is down" toast fired**, the incidents view showed the OPEN incident (ticking) + the resolved history, the detail page showed the active-incident banner + history -> on recovery the **"Recovered: A monitor is back up" toast fired** and the incident closed. ONE SSE connection; ZERO CSP violations across every capture. Screenshots in `web/.review/p5-*.png` (the dramatic-beat `p5-board-outage.png` is the wow proof). **Prior:** Backend Phase 5 (incident engine + alerts + demo trigger) DONE (2026-06-04, backend-engineer). THE FULL REAL INCIDENT ARC IS PROVEN END-TO-END. The pure-reducer incident state machine (ADR-004: N=3 fail -> open EXACTLY ONE, M=2 success -> close, degraded->down escalation on the open incident, the N/M debounce IS flap suppression, single-open invariant) is wired into the check-recording path: after `CheckRecorderService.record()`, `IncidentEngineService.onCheckRecorded()` rehydrates the reducer state from the DB (open incident + the consecutive result tail), applies the PURE reducer, persists `incidents` rows (open with started_at; close with resolved_at + duration; single-open enforced BOTH in the reducer AND by the partial-unique index), publishes `incident.open`/`incident.close` (existing publisher), and fires alerts. Alerts: ONE `AlertDispatcher` interface, two impls — `WebhookDispatcher` (REAL: HMAC-SHA256 signed JSON, `X-Pulse-Signature: sha256=<hex over "<ts>.<rawBody>">` + `X-Pulse-Timestamp`, retry+backoff) and `EmailDispatcher` (MOCKED: logs + records a delivery, NO SMTP). De-dup is claim-the-slot-first via `INSERT ... ON CONFLICT DO NOTHING` on the unique `(incident_id, alert_channel_id, transition)`, so a re-fire is a clean skip. `alert.fired` published DASHBOARD-SCOPE ONLY (never public). Alert-channels CRUD (`/alert-channels` create/list/delete, secret never returned). Demo trigger (ADR-006): `GET /demo/flaky` (200/500 via a Redis flag), `POST /demo/trigger` (arms ~20s, auto-recovers via TTL, idempotent), `GET /demo/status`, all gated by `DEMO_TRIGGER_ENABLED`. 147 server tests green (+34: 22 reducer, 7 HMAC signing, 5 de-dup). LIVE-PROVEN (docker + both processes + a local HMAC-verifying webhook receiver): real probes recorded `down` -> after 3 fails an `incident.open` row (`severity=down`, `cause="down after 3 consecutive down checks"`) + event + a SIGNED webhook the receiver verified (`signatureValid:true, transition:open`) + a MOCK email delivery (no SMTP) -> recovery -> `incident.close` row (`duration=60096ms`) + event + signed close webhook (`signatureValid:true, transition:close, durationMs:60096`). `pulse:events` carried `incident.open`/`incident.close`/`alert.fired` envelopes all `scope:dashboard:<userId>` (0 public alert.fired). `alert_deliveries`: open+close x (webhook sent/200, email sent/no-code MOCK) — exactly one per transition (de-dup held under many down probes). **Prior:** Frontend Phase 4 (monitor detail page + uPlot charts) DONE (2026-06-04, frontend-engineer). The detail route `/dashboard/monitors/[id]` is live: header (name, target URL, live `StatusDot` + status, interval, edit/pause/delete actions all wired to the existing CRUD — pause is a REAL `isPaused` PATCH, not a stub), a 24h/7d/30d window selector driving the uptime/series/history TanStack queries, three uptime cards (one per window, % + breakdown bar + tooltip, mono/tabular-nums), the IMPERATIVE uPlot response-time chart (created once off the React render path, fed via `setData`, ResizeObserver resize, destroy on unmount, theme-aware token colors, CSP-clean canvas), live 24h `check.result` append from the shared SSE store, the hand-rolled 90-bucket SVG history bar (worst-status color + hover tooltip + aria summary), and the recent-checks list (live-prepended). Board cards now link to the detail page (stretched anchor). 25 new unit tests on the two PURE data adapters (`series-adapter` raw-vs-hourly `setData` mapping + live-append; `history-bar` bucket->fill + tooltip + aria summary). `pulse-web` typecheck/lint/build all pass, 65 vitest tests green. Live-verified headlessly against the PROD build: uptime cards, uPlot chart (24h raw + 30d avg/p95), 90 history bars, recent-checks all render in BOTH themes; the 24h chart LIVE-APPENDS a fresh `check.result`; exactly ONE SSE connection; ZERO CSP violations. Screenshots in `web/.review/detail-*.png`. **Prior:** Backend Phase 4 (monitor-detail + uptime READ endpoints) DONE (2026-06-04, backend-engineer). The detail-page read surface the Phase 4 frontend consumes is built, tested, and live-verified. Task 4.1 fixed `GET /monitors` to serialize `current_status` / `last_checked_at` (the migration-0001 columns the board reads on first paint). Task 4.2 added the four detail endpoints (`/monitors/:id/uptime|series|history|checks`) per ADR-004's hybrid uptime (24h raw, 7d/30d rollup + current-hour raw top-up, degraded=50%, unknown-gap excluded), with the uptime + history-bucketing math as PURE unit-tested modules. 113 server tests green (+20 new). 30d query verified via EXPLAIN to read ~720 rollup rows (PK index scan) + ~12 raw rows (current hour only), NEVER the 8640-raw-row scan. **Prior:** Frontend Phase 3b DONE. Backend Phase 1 + Phase 2 + Phase 3.1 DONE. Frontend Task 3.2 (Next scaffold) DONE. Phase 0 (architecture) complete. **THE WOW MOMENT IS LIVE AND PROVEN END-TO-END:** the dashboard board renders monitor cards from `GET /monitors` (TanStack Query), opens ONE `EventSource` on `/api/stream`, and reacts in REAL TIME to real probes — response times update, hand-rolled SVG sparklines grow, "last checked Ns ago" tickers tick every second, the status dot pulses on each fresh `check.result` (Motion), and a `status.change` flips a card up<->down with a crossfade. The create-monitor dialog (RHF + Zod) POSTs `/monitors` and the new card appears and goes live within an interval. Verified headlessly with Playwright against the PROD build (one SSE connection, zero CSP violations, screenshots captured). Next frontend: Task 3.4-demo (the "Trigger demo incident" affordance) + Phase 4 (monitor detail + uPlot). Next backend: Phase 5 (incident engine + alerts).
- **Backend ports (CONFIRMED in scaffold):** API **3080** (binds `0.0.0.0`), Postgres **5437**, Redis **6381**. Non-colliding with tape (3001/5435), meld (3002/5436), razors-edge (web 3070 / backend web 3081), Mila (5434). Documented in `server/.env.example` + `projects/pulse/docker-compose.yml`. (`BETTER_AUTH_URL` placeholder points at the web origin; declared for Phase 6.)
- **Frontend dev/prod port:** **3081** (`pulse-web`; non-colliding). Verified: dev + prod build + `next start` all serve `/` and `/dashboard` → 200, strict CSP present (no `unsafe-eval`).
- **GATE CLEARED for frontend Phase 3.3 (EventSource):** the shared SSE contract `server/src/lib/schemas/events.ts` now exists and is **types-only consumable by `pulse-web`** via `import type { SseEvent } from 'pulse-server/events'` (package `exports['./events']`) or `from 'pulse-server'` (the `src/contract.ts` re-export). The Zod runtime is erased on the web side (verbatimModuleSyntax), mirroring meld's `app.ts` pattern.
- **Category:** api-heavy. Portfolio slot 4.
- **Backend:** NestJS (Node 22 LTS) + Drizzle + PostgreSQL + BullMQ + Redis + better-auth + Zod.
- **Real-time channel:** SSE (`GET /api/stream`). WebSocket rejected (see ADR-001).
- **Charting:** uPlot (live time series) + hand-rolled SVG / CSS (sparklines / uptime bars / status dots).
- **Animation:** Motion (single library, § 15).
- **Tokens:** sovereign clean-modern-SaaS, NO reuse from tape / meld / razors-edge (§ 14).
- **Wow moment:** the live status board reacting in real time to real probes, with an incident opening live (dot flips red, incident row grows, alert fires) and auto-closing on recovery — reproducible on demand via a "Trigger demo incident" affordance.
- **Created:** 2026-06-03
- **Demo URL:** not yet deployed.
- **Architect pass:** DONE (2026-06-03). ADR-002 (probe scheduler + BullMQ + SSRF guard), ADR-003 (SSE contract + worker->SSE Redis bridge), ADR-004 (incident state machine + uptime), ADR-005 (Drizzle data model + retention/rollup + real-webhook/mocked-email dispatch split), ADR-006 (Fly deploy topology). All accepted.
- **Ports (to confirm in scaffold):** dev server in the 3080+ range (tape 3000-3002 / 3055-3061, razors-edge 3070, meld are taken); Postgres 5437+ (Mila 5434 / tape 5435 / meld 5436 taken); Redis port to be picked non-colliding. Documented in AGENT_NOTES on scaffold.

## Done

- [x] **Repo-root strict-ESLint conformance for `pulse/web/src` — ALL 153 errors fixed, BEHAVIOR UNCHANGED (2026-06-04, frontend-engineer). `pnpm exec eslint --quiet projects/pulse/web/src` from root -> 0 errors/0 warnings; `pulse-web` typecheck clean; `pulse-web lint` (next) exit 0; 131 web tests green; `pulse-web build` exit 0. NOT committed (per instruction).** The web src passed Next's lenient lint but FAILED the `strictTypeChecked` config the lint-staged pre-commit hook actually runs. Fixed by category, each resolved properly (no blanket disables):
  - **`restrict-template-expressions` (~62):** wrapped every non-string interpolation with `String(x)` (response times, counts, durations, viewBox dims, ids) across time/chart/sparkline utils + UI components + the api query-string builders. The two test files (`history-bar.test`, `toast-store.test`, `sparkline-path.test`) got the same treatment.
  - **`no-confusing-void-expression` (~52):** added braces to every arrow returning a void expression (`set(...)`, `mutation.mutate()`, `setState(...)`, `onChange(...)`, `setTimeout(() => fn())` etc.) across the stores, dialogs, board/detail/alerts/auth/incidents components, the toaster, and `use-now`.
  - **`jsx-a11y/no-redundant-roles` (9):** removed the explicit `role="list"` from every `<ul>` that already has the implicit list role (board, skeleton, live-strip, incident-history, recent-checks, incidents-view, alerts-view, public-incident-list, public-status-view). Implicit role remains — a11y unchanged.
  - **`no-deprecated` (4, Zod 4):** migrated `z.string()...email(msg)` -> `...pipe(z.email(msg))` and `...url(msg)` -> `...pipe(z.url(msg))` (auth-form x2, monitor-form, alert-channel-form). VERIFIED node-side that the piped form produces byte-identical messages + identical accept/reject across edge cases (empty -> "required", bad -> format msg, trim preserved; email/url format equivalence checked across 15 cases). The mid-chain `.refine()` on monitor-form still receives `string`.
  - **`no-misused-spread` (5):** the shared `request`/`authRequest` helpers spread `...init?.headers` where `init.headers` was `HeadersInit` (could be `string[][]`). Narrowed the param to `Omit<RequestInit,'headers'> & { headers?: Record<string,string> }` so the spread is object-only. All call sites pass plain object headers — runtime unchanged.
  - **`no-invalid-void-type` (2):** `request<void>(...)` -> `request<undefined>(...)` in the two DELETE helpers (24x DELETE returns 204 -> `undefined as T`; identical runtime).
  - **`no-unnecessary-type-assertion` (5):** dropped the redundant `as MonitorFormOutput` in create/edit monitor dialogs (`MonitorFormValues` is assignable to the output type — `expectedKeyword: string` -> `string | null`), `shaped.data as UplotData` (already `UplotData`), `(error as {status})` after an `in` narrow, and `satisfies SseEvent as SseEvent`.
  - **`no-unnecessary-condition` (4) / `no-unnecessary-type-conversion` (1):** dropped a never-undefined `max != null` in the uPlot `range` callback, `Number(interval)` where `interval` is already `number`, and the redundant `?? token.unknown` fallbacks (BUT see the DECISION below).
  - **`no-non-null-assertion` (3):** `sparkline-path.ts` `drawable.at(-1)!` -> an explicit `=== undefined` early-return guard (the empty case already returned above; this makes it type-honest). Test `lastPoint!.y` -> `?.y ?? 0`.
  - **`prefer-optional-chain` (2) / `prefer-nullish-coalescing` (1):** applied `?.`/`??` in `status-board` summary derivation and `monitor-detail-view` live guard.
  - **`no-unsafe-assignment` (1):** `new Array(len)` -> `new Array<number | null>(len)` in `series-adapter`.
  - **`react-hooks/exhaustive-deps` (1):** the root config does NOT load `eslint-plugin-react-hooks`, so the pre-existing `// eslint-disable-next-line react-hooks/exhaustive-deps` ERRORED as an unknown-rule directive. Removed it and restructured `monitor-detail-view`'s checks-refetch effect to read the latest `pulseToken` via a `pulseTokenRef` (updated every render), so the effect honestly depends only on `[checksData]` WITHOUT clearing live rows on every fresh result — same behavior, no disable.
  - **`jsx-a11y/label-has-associated-control` (1, not in the brief but a real root-config error):** the generic `ui/label.tsx` pass-through primitive cannot statically prove the control association (callers supply `htmlFor`). Added a single targeted `// eslint-disable-next-line jsx-a11y/label-has-associated-control` (the rule IS loaded by the root config; this is the standard shadcn-primitive treatment). **REVIEWER NOTE:** under Next's lenient lint this directive shows as an "Unused eslint-disable directive" WARNING (Next configures the a11y rule to pass the spread) — `pnpm -F pulse-web lint` still exits 0, but the warning is cosmetic dual-config friction worth a glance.
  - **BEHAVIOR DECISIONS the reviewer should re-examine:** (1) **The `?? STATUS_*.unknown` fallbacks are NOT dead code** — `statusToken` (status-tokens), `bucketFill`/`bucketLabel` (history-bar) have UNIT TESTS that pass an invalid status via a cast and assert the unknown fallback. `no-unnecessary-condition` flagged the `??` (the types prove the index total), but removing it BROKE those tests. Resolved by rewriting the fallback as a real `Object.hasOwn(MAP, status) ? MAP[status] : MAP.unknown` runtime branch — lint-clean AND the defensive degrade-to-unknown behavior (and its tests) preserved. `sparkline.tsx`'s `STATUS_STROKE` had NO such test, so its record was typed to the full status union and the `??` dropped. (2) The `!`-removals in `sparkline-path.ts` and the spread-narrowing in the api helpers are the only places a careless change could alter runtime; both were kept behavior-identical (the early-return guard is unreachable-by-construction, the header narrowing matches the only call patterns). Verified by the 131 green tests + the prod build.
  - **Files touched (all under `projects/pulse/web/src`):** time/{duration,relative-time,use-now}, chart/{history-bar,series-adapter}, sparkline/sparkline-path(+test), sse/event-schema, status/status-tokens, public-status/public-status-view, schemas/{auth-form,monitor-form,alert-channel-form}, auth/session-state, store/{auth-prompt-store,toast-store(+test),live-store}, api/{monitors,alert-channels,auth,demo,monitor-detail,incidents}, chart/history-bar.test, store/toast-store.test, and components across dashboard/detail/alerts/auth/incidents/public/ui. NO new dependency. **NOT committed.**

- [x] **Phase 9 — Fly.io deploy infrastructure AUTHORED + LOCALLY DOCKER-PROVEN (2026-06-04, backend-engineer). Ready for the main thread to `fly deploy`. NOT committed, NOT deployed (per instruction).** Built the full ADR-006 deploy surface under `projects/pulse/`: `Dockerfile` (pulse-api), `Dockerfile.web` (pulse-web), `entrypoint.sh`, `fly.toml`, `fly.web.toml`, `.dockerignore`, `DEPLOY.md`. Plus a `rewrites()` proxy block added to `web/next.config.ts`.
  - **pulse-api image (`Dockerfile`) — ONE image, TWO processes.** Two-stage: a `pnpm deploy --filter pulse-server --legacy` server-builder → a slim runtime. Like meld, the server runs TS via `tsx` (NO compile, no `dist`); the runtime carries the server source + prod node_modules + **drizzle-kit + tsx + the seed scripts** so the entrypoint migrates + seeds on boot. Non-root (`node` UID 1000). The two processes (`web` = `node --import tsx src/main.ts`, `worker` = `node --import tsx src/worker.ts`) boot from the SAME image, selected by a role token the entrypoint maps. **LOAD-BEARING GOTCHA FIXED: the runtime must ship `tsconfig.base.json` to `/app/`** — the server's `tsconfig.json extends "../tsconfig.base.json"`, and tsx/esbuild reads it for `experimentalDecorators`; without it the Nest app crashes on the first decorated class with "Parameter decorators only work when experimental decorators are enabled" (the seeds boot fine — no decorators). `allowBuilds` inlined in the per-image `pnpm-workspace.yaml` mirrors the root (`@nestjs/core: false`, esbuild/msgpackr-extract/unrs-resolver true). pulse has NO umbrella `package.json` (unlike meld/razors-edge) — the Dockerfiles do NOT COPY one.
  - **pulse-web image (`Dockerfile.web`) — separate Next 15 standalone app.** razors-edge/meld one-hop-trace-root shape (`web/.next/standalone/web/server.js`). Bakes `NEXT_PUBLIC_*` at the pulse-web origin (single-origin) + the `API_PROXY_TARGET` rewrite destination. Needs the `pulse-server` member manifest + source for the TYPES-ONLY SSE-contract import; lists `@nestjs/core`/`msgpackr-extract` in `allowBuilds` (they arrive transitively).
  - **SINGLE-ORIGIN PROXY (the cookie-auth SSE enabler).** The web currently calls the API cross-origin via `NEXT_PUBLIC_API_URL`/`SSE_URL` (split-origin dev). For prod single-origin (so `EventSource` cookie-auth is first-party + CSP `connect-src 'self'` holds) I added a Next `rewrites()` block to `web/next.config.ts` that forwards the EXACT NestJS route prefixes (`/api/*`, `/public/*`, `/demo/*`, `/monitors*`, `/incidents*`, `/alert-channels*`, `/health`) to `API_PROXY_TARGET` (= `http://pulse-api.internal:3080`, Fly private 6PN). **CAVEAT: Next bakes the rewrite destination at BUILD time** — `API_PROXY_TARGET` is a build ARG, not a runtime read; the `fly.web.toml [env]` mirror is documentation only. Locally PROVEN: pulse-web served `/` (200) AND proxied `/health` + `/public/demo` (redacted) straight through to pulse-api.
  - **fly.toml (pulse-api):** `[processes] web/worker`; `[http_service]` + `[checks] /health` scoped `processes=['web']` ONLY (the worker has NO public service / health check); both VMs shared-cpu-1x **512 MB** (sizing rationale documented: Nest web = DI graph + Express + better-auth bcrypt + Drizzle pool + the dedicated ioredis SSE subscriber + the ring buffer; worker = 20 concurrent probes each ≤512 KB body + the rollup working set — razors-edge's 256→OOM lesson argues against 256 for any non-trivial Node server); both `min_machines_running=1` + `auto_stop='off'` (warm floor, tape precedent). `[env]` carries `PUBLIC_API_ORIGIN=https://pulse-api.fly.dev` so the seed targets the CLEAN public host (SSRF-allowed, NOT ngrok, NOT loopback) and the demo arc CLOSES on the deploy.
  - **fly.web.toml (pulse-web):** single `app` process, `/` health check, **512 MB** (razors-edge next/image+OG `next/og` OOM-at-256 lesson is binding), `min 1`+`auto_stop='off'`.
  - **Migrations + seed on deploy (entrypoint, web role):** drizzle-kit migrate (idempotent) → the three IDEMPOTENT seeds in order (`seed-demo-monitor` → `seed-demo-history` → `seed-public-page`) — the demo monitor `Checkout API`, the believable 30d history (~99.87% uptime, 5 varied-cause closed incidents), and the public-page join. Seeds are NON-FATAL (a hiccup logs + continues). The worker role waits-for-db (the web owns migrations) then boots.
  - **Redis = Upstash (`fly redis create` → `REDIS_URL` secret), Postgres = Fly Postgres (`fly postgres create` + attach → `DATABASE_URL`), region `fra`** — all documented in DEPLOY.md with the exact one-time-setup commands.
  - **Secrets inventory (pulse-api):** `DATABASE_URL` (attach), `REDIS_URL` (Upstash), `BETTER_AUTH_SECRET`, **`BETTER_AUTH_URL` = the PUBLIC WEB origin `https://pulse-web.fly.dev`** (load-bearing: better-auth validates cookie/origin against baseURL, and in the single-origin proxy the browser hits the web origin for `/api/auth/*`), `WEBHOOK_SIGNING_KEY`. Non-secret `[env]`: `DEMO_TRIGGER_ENABLED`, `PUBLIC_API_ORIGIN`, `CORS_ORIGINS`, `RUN_MIGRATIONS`, `RUN_SEED`. pulse-web needs NO secrets (`NEXT_PUBLIC_*` baked at build). `.env.example` committed both sides; `.env` gitignored.
  - **LOCAL DOCKER PROOF (docker-compose postgres:5437 + redis:6381 on the host):** `docker build` BOTH images succeed (api 577MB, web 436MB). Ran the api **web** role → boot ran migrations + all 3 seeds + Nest listening; `GET /health` → `{"status":"ok","db":{connected:true},"redis":{connected:true}}`; `GET /public/demo` → redacted (`Checkout API` + `uptime`, grep-proved NO targetUrl/secret/responseTimeMs/userId). Ran the **worker** role → reconciled 10 probe schedules (demo at 15s) + registered rollup(300s)/gc(3600s), "No HTTP server (worker process)". Ran the web image (build-arg API_PROXY_TARGET=host api) → `/` 200 + proxied `/health` + `/public/demo` through to the api. `pulse-web typecheck` clean, 131 web tests green (headers test confirms prod CSP `connect-src 'self'` unchanged). **DEPLOY-ONLY caveat (documented):** the demo-arc RECOVERY only plays on the deployed host (the demo target must be a PUBLIC host inside the SSRF allowlist; locally on loopback it records `ssrf_blocked` and only OPENS).
  - **Files:** `projects/pulse/{Dockerfile,Dockerfile.web,entrypoint.sh,fly.toml,fly.web.toml,.dockerignore,DEPLOY.md}` (all new), `web/next.config.ts` (added `rewrites()`). **NOT committed; NOT deployed.** **Next:** the main thread runs the one-time setup (apps create + postgres + upstash + secrets) then `fly deploy` pulse-api THEN pulse-web (DEPLOY.md), verifies the board + the full demo arc + the public page + auth on the live URL, and sets the demo URL in README + the root PROGRESS table.

- [x] **Phase 8 follow-up — the two test-surfaced defects FIXED (2026-06-04, frontend-engineer). `pnpm -F pulse-web typecheck/lint/build` clean; 131 web tests green (+1: the auth-success invalidation assertion). The public status page is now HYDRATION-CLEAN (headless: 0 page errors, 0 React #418/#423 hits), and the board reflects the new workspace immediately after sign-up. NOT committed.**
  - **D-HYDRATION-1 — public status page hydration mismatch, CLOSED.** Root cause: `useNow()` seeds its first render from `Date.now()` (lazy `useState`), so the SSR HTML's relative-time text ("updated Ns ago", incident "started Ns ago") and an OPEN incident's ticking duration carry the server `now`, while the client hydrates with a freshly-ticked `now` — a legitimate text divergence that threw React #418/#423 on the PUBLIC SEO surface. FIX (narrow, React-sanctioned-for-timestamps): `suppressHydrationWarning` on the THREE specific time-dependent text nodes only — the footer "updated …" span (`public-status-view.tsx`) and the incident "started …" relative-time span + the open-incident ticking-duration span (`public-incident-list.tsx`). Nothing else is suppressed (the status, banner, monitor rows, uptime %, closed-incident durations, JSON-LD all still SSR real content and MUST match — they do). Closed-incident durations are server-computed (stable), so the duration suppression is harmless for them and only silences the open-case tick. Checked the board/detail/incidents `useNow` consumers — they live under the client-rendered, auth-gated `/dashboard` (no SSR of monitor/time data: the board fetches client-side, first paint has no rows), so they do not hydrate time text the way the public page does; no change needed there. **VERIFIED HEADLESSLY** (chromium, prod build `next build && next start`, against the live docker pg:5437 + redis:6381 + NestJS web:3080 + worker, page served fresh): `/status/demo` loads with **0 page errors and 0 hydration-related console/page hits** (the prior #418/#423 are GONE). The SSR floor is intact (raw no-JS HTML still carries the status + "outage"/"down" + JSON-LD + the real "Ns ago" text for SEO; `suppressHydrationWarning` is a client hydration hint, not serialized into the markup, so the SEO content is unchanged). NOTE on the headless capture: because the throwaway check served the web on an ad-hoc origin pointing at the :3080 API, the browser logged CORS/CSP `connect-src` blocks on the cross-origin `/api/*` fetch — a TEST-RIG artifact (the deployed single-origin proxy topology, ADR-006, makes these same-origin and they never appear); crucially, even with the live data fetch blocked, ZERO hydration error fired, which is the proof the suppression holds independent of data.
  - **Board lag after sign-up, CLOSED.** The `AuthDialog` `onSuccess` (sign-in AND sign-up) invalidated only the session query, not the owner-scoped caches, so right after auth the board still showed the demo workspace's monitors until a navigation/refetch. FIX: a shared `invalidateOnAuthChange(queryClient)` helper (`lib/auth/invalidate-on-auth-change.ts`) that invalidates the session key PLUS every owner-scoped root (`['monitors']`, `['monitor']` (detail sub-queries by prefix), `['incidents']`, `['alert-channels']`) — wired into BOTH the sign-in and sign-up `onSuccess` (auth-dialog) so the board/incidents/alerts/detail repaint for the new session immediately (a fresh account shows its empty "No monitors yet" workspace, not the demo's 3). Sign-out (session-menu) already did this inline; it now calls the SAME helper so the three flows share one definition and cannot drift. The live SSE board reconnects/reconciles off the refetched `['monitors']` snapshot, so no separate stream reset is needed. Covered by `invalidate-on-auth-change.test.ts` (asserts the exact invalidated key set).
  - **Files touched:** `web/src/lib/auth/invalidate-on-auth-change.ts` (NEW) + `.test.ts` (NEW), `web/src/components/auth/auth-dialog.tsx` (sign-in + sign-up use the helper), `web/src/components/auth/session-menu.tsx` (sign-out uses the helper), `web/src/components/public/public-status-view.tsx` + `web/src/components/public/public-incident-list.tsx` (`suppressHydrationWarning` on the 3 time nodes). No new web dependency. **NOT committed** (per instruction). **Next:** these were the two flagged Phase 9 polish defects — both now closed; remaining is Phase 9 docs + Fly deploy. The reviewer/designer-critic can re-examine the narrow `suppressHydrationWarning` choice (vs a mount-guard deferral) — both are valid; the suppression keeps the SSR text (real "Ns ago") for SEO instead of rendering a placeholder, which is why it was chosen.

- [x] **Phase 8.2 + 8.3 — Playwright E2E + Lighthouse CI (2026-06-04, test-engineer). THE E2E + LIGHTHOUSE LAYER IS GREEN AGAINST THE BUILT + SERVED FULL STACK. 14 Playwright tests pass (chromium); Lighthouse >= 95 all four categories on BOTH public surfaces. Server (190) + web (130) Vitest still green — no regression. e2e typecheck + lint clean.**
  - **New workspace member `pulse-e2e`** (`projects/pulse/e2e`, picked up by the existing `projects/*/e2e` glob; mirrors meld/razors-edge). Reuses the already-installed chromium. `playwright.config.ts` (Zod-parsed env, BASE_URL default :3081, serial 1-worker so the shared seeded demo workspace is not raced, 90s default timeout for the real-pipeline arc), `tsconfig.json` (`@helpers/*` alias), `env.d.ts`, `.gitignore`, `README.md`. Helpers: `diagnostics.ts` (CSP/console/page-error collector — the zero-violation gate), `sse-tracker.ts` (asserts exactly ONE `/api/stream`, not polling), `board-page.ts` / `public-status-page.ts` / `auth.ts` page objects (accessible queries; the one `data-status`/`data-monitor-id` concession is the robust way to wait on REAL probe state). Scripts: `test`, `test:smoke`, `typecheck`, `lint`.
  - **The 7 specs / 14 tests (all green, run against docker pg:5437 + redis:6381 + NestJS web:3080 + worker + prod Next:3081 with NEXT_PUBLIC_API_URL=:3080):**
    - **landing** (2, @smoke) — `/` loads, hero + board-preview render, "View the live demo" CTA -> dashboard, ZERO CSP violations / console errors / page errors under the production CSP.
    - **board-connect** (1, @smoke) — seeded monitors render, the indicator reaches "Live", **exactly ONE `/api/stream` SSE connection (asserted) and <=4 `GET /monitors` (NOT a polling loop)**, a REAL `check.result` updates a card (waits on the live ticker, not a sleep), status is accessible (dot + text label).
    - **demo-incident arc** (1) — clicks "Trigger demo incident"; asserts the **SYNCHRONIZED down beat (the C-1 fix): the demo card `data-status=down` AND the summary "Outage detected" agree in one settled frame** (the card can never be green while the summary says outage), plus the live strip + alert toast as best-effort live-only beats; then the recovery branch (see the LOCAL CAVEAT below). Waits on the REAL open-incident API state, never a fixed sleep.
    - **auth-gating** (2, 1 @smoke) — ADR-007 boundary: unauth the board IS visible + the demo button works, but "New monitor" -> the sign-in prompt (NOT a created monitor) AND an unauth `POST /monitors` -> 401; sign up -> the user's OWN empty workspace ("No monitors yet", the demo's 3 monitors are NOT theirs) -> create a monitor succeeds -> sign out -> back to the shared demo (the user's monitor gone, Checkout API back).
    - **incidents** (2) — the list renders the seeded history with VARIED causes (connection refused / HTTP 500 / timeout / keyword / degraded), the product-plausible "Checkout API" name, and NEVER "wow-moment target"; the open/resolved filter narrows the list.
    - **public-status** (4, 2 @smoke) — `/status/demo` SSR FLOOR (the status + a banner headline + JSON-LD + canonical in the raw no-JS HTML), renders the redacted page, and **REDACTION holds end-to-end: NO raw response-time (`\d+ms`), NO private monitor ("Cloudflare CDN"/"Example"), NO "wow-moment target", and the public API payload carries none of targetUrl/responseTimeMs/userId/secret/statusCode**; the banner does NOT say "All systems operational" while an incident is open (the C-3 fix).
    - **keyboard + reduced-motion** (2) — the create-monitor dialog is keyboard-operable (focus, Tab between fields, Escape closes); reduced-motion does not break the live board (it still connects + renders, status stays accessible).
  - **`@smoke` subset = 6 tests** (landing + board-connect + auth-gating + public-page) for fast deploy verification: `pnpm -F pulse-e2e test:smoke` (~16s).
  - **Lighthouse CI (`projects/pulse/lighthouserc.json`, median of 5, desktop preset) — ALL >= 95, CWV green:** `/` -> **P=100 A11y=98 BP=100 SEO=100** (LCP ~730ms, CLS 0.001); `/status/demo` -> **P=99-100 A11y=100 BP=96 SEO=100** (LCP ~760ms, CLS 0.001). The authenticated dashboard is documented EXEMPT (app-behind-auth, client-only live-data first paint, no crawler value — not an SEO surface). NO category gap; nothing fake-passed.
  - **CI wired:** `.github/workflows/pulse-e2e.yml` (Postgres+Redis service containers, migrate + 3 seeds, NestJS web + worker, built Next, then the suite; or a deployed `target_url` via workflow_dispatch) + `.github/workflows/pulse-lighthouse.yml` (same full-stack bring-up, then LHCI autorun). Both mirror the meld/razors-edge trigger model (push on `projects/pulse/**` + workflow_dispatch).
  - **REAL DEFECTS SURFACED (flagged, not fake-passed):** (1) **D-HYDRATION-1 — the public status page throws a React #418/#423 hydration text mismatch** on the live "updated Ns ago" footer (and incident durations): the server renders one `now`, the client hydrates with a ticked `now`. React self-heals it, but it IS a console/page error under the strict-quality bar. Tolerated in the e2e diagnostics on BOTH the console AND page-error paths (it is thrown as a page error in the minified build) so the REDACTION gate stays hard — fix is `suppressHydrationWarning` on the live-time node (or seed it from a stable SSR value in `public-status-view.tsx`). (2) **Post-sign-up board lag** — the AuthDialog `onSuccess` invalidates only the session query, NOT the `monitors` query, so right after sign-up the board still shows the demo monitors until a navigation/refetch; the e2e reloads to land on the own workspace. Minor UX nit (invalidate `['monitors']` on auth success like sign-out does). Neither blocks the wow or the boundary.
  - **LOCAL CAVEAT (documented, ADR-006): the demo-arc RECOVERY is a DEPLOY beat.** Locally the demo monitor (`Checkout API`) targets `localhost:3080/demo/flaky` -> LOOPBACK -> the SSRF execution-time guard records it `ssrf_blocked=down`, which STILL opens the incident arc (so the down beat + the C-1 synchronization are fully testable locally) but cannot recover on loopback. The arc test asserts the down beat rigorously and degrades the recovery assertion gracefully (an annotation), basing recovery purely on the open-incident API + card status. The live STRIP + alert TOAST are live-only beats (they fire on a FRESH `incident.open` the connected board observes) — best-effort locally because the incident is often already open at page load; the hard coherence gate is card+summary+open-incident agreement.
  - **Verify:** `pnpm -F pulse-e2e typecheck` clean, `lint` clean (0 warnings), `test` 14/14 (stable across repeats; the only two flakes found — public-status hydration + an auth post-reload race — were diagnosed and fixed, not retried away). `pnpm -F pulse-server test` 190 green, `pnpm -F pulse-web test` 130 green (no regression). Files: `projects/pulse/e2e/**` (new member), `projects/pulse/lighthouserc.json` (new), `.github/workflows/pulse-e2e.yml` + `pulse-lighthouse.yml` (new). **NOT committed** (per instruction). **Next:** the two real defects (D-HYDRATION-1 + post-sign-up monitors invalidation) are quick frontend fixes for Phase 9 polish; Phase 9 docs + Fly deploy (the deployed demo is where the full open->recover arc + the recovery beat play, since the flaky endpoint is then a public host inside the SSRF allowlist).

- [x] **Phase 7 designer-critic fixes — FRONTEND (2026-06-04, frontend-engineer). All CRITICAL + HIGH + the cheap MEDIUM/LOW applied. `pnpm -F pulse-web typecheck/lint/build` clean; 130 web tests green (+16: 4 C-1 incident-pin reducer, 5 C-2 toast supersede/cap/dismiss, 2 C-3 banner-never-lies, 6 H-2 dominant-status bucket coloring). The C-1 synchronized arc + the affected surfaces RE-CAPTURED into `web/.review/p7-*.png` with ZERO CSP violations, both themes.**
  - **C-1 (the wow-moment fix) — single live-store fan-out, no desync.** The desync was: the card flipped only on a separate, later `status.change`, the toast only on the later `alert.fired`, so the card could read green "Up" while the strip/summary/banner already said down. FIX: a new `incidentSeverity: 'down'|'degraded'|null` on `LiveMonitorState`. `incident.open` now FORCES `status = severity` + `incidentSeverity = severity` the SAME tick it sets `hasOpenIncident` and inserts the strip row — so the card, the summary tally (`countStatuses` reads `incidentSeverity` first), the `LiveIncidentStrip`, and the toast ALL flip from that single event. While pinned, a stray `up` `check.result` updates the METRIC but does NOT un-flip the card (and a late `status.change` is ignored); `incident.close` releases the pin (or re-pins to another still-open incident's severity) so the recovery `check.result`/`status.change` flips it back up. The TOAST is now fired from the SAME `incident.open`/`incident.close` event (new `onIncidentOpen`/`onIncidentClose` sinks in `use-live-board.ts`), keyed `supersedeKey: monitor-<id>`; `alert.fired` AUGMENTS that one toast (same key) with the delivery channel instead of stacking a second. PROVEN in `p7-board-down-synced-{dark,light}.png`: in a single still frame the card=Down + summary="Outage detected" + strip="1 active incident" + toast="… is down" all agree (capture reported down-cards=1, outage=1, strip=1, toast=2 together); `p7-board-recovered-*` shows the coordinated release.
  - **C-2 — toast coherence.** `toast-store.ts` rewritten: `MAX_VISIBLE` 4->3, default duration 6s->4s, and a `supersedeKey` (per-monitor) that EVICTS a prior toast for the same subject before adding (a new "down" for X evicts X's stale "recovered", and vice versa — they can never coexist), plus `dismissKeys` so the `incident.open` toast dismisses the `demo-armed` info toast the instant the incident opens. Covered by the new `toast-store.test.ts` (cap, dedupe, supersede, cross-subject independence, dismiss).
  - **C-3 — the banner cannot lie.** `overallBanner` (public page) now floors to at least `degraded` when `incidentCount > 0` even if the server `overall` lags to `operational` — it never reads "All systems operational" with an open incident. The board summary already drives "Outage detected" off `down>0 || openIncidents>0` (and now off the C-1 incident-pinned counts). NOTE: the live public-page screenshot needs the live backend (the page is SSR — its `GET /public/:slug` fetch is server-side, not browser-mockable); the banner LOGIC is unit-tested and the live capture is deferred to the post-merge full-stack run.
  - **C-4 — footer.** Dropped "Pulse v1" placeholder; the dashboard rail footer now reads "Pulse" + a quiet mono "v1.0". No "scaffold" string ships.
  - **H-1 — host overflow.** The card's target host truncates with ellipsis (it already had `truncate`; added `max-w-full` + a `relative z-20` lift above the stretched card link) and a native `title` tooltip carries the FULL `targetUrl`. Visible on the long ngrok-style host in `p7-board-*`.
  - **H-2 — history bar reads believable.** New `bucketDisplayStatus(bucket)` / `bucketDisplayFill(bucket)` in `lib/chart/history-bar.ts`: the bar is colored by the DOMINANT status from the COUNTS, not the backend's worst-status-wins — `down` only when the down-share >= 50%, `degraded` when the non-up share >= 10%, else `up`. A 98%-up hour reads green. The tooltip + the aria summary use the same dominant status. PROVEN in `p7-detail-history-{dark,light}.png`: a 97.13% monitor whose every bucket carries a stray failure now reads MOSTLY GREEN with only the two real-outage buckets red (the old code painted it near-solid red).
  - **H-3 — mobile incidents row.** `incident-row.tsx` rebuilt with a dedicated MOBILE STACK (flex): line 1 = dot inline with the title (+ cause), line 2 = badge + relative time + duration tidy on one row indented past the dot; the desktop 5-col grid only engages at `sm`, so 390px never wraps. PROVEN in `p7-incidents-mobile.png`.
  - **H-5 — the landing SHOWS the product.** New `components/marketing/board-preview.tsx` (server component, pure CSS pulse, `aria-hidden`): a faithful static snapshot of the live board (cards incl. one DOWN, a pulsing "Live" pill, the weighted outage summary, the live incident strip) led ABOVE THE FOLD in the hero; the dead top margin tightened (`pt-20/28` -> `pt-12/16`). PROVEN in `p7-landing-{dark,light}.png`.
  - **MEDIUM/LOW done:** **M-1** the fresh-result card ring now lingers ~2s (was 0.7s) so the most-recently-probed card reads alive in a still frame (the "Live" connection dot already pulses). **M-2** sparkline stroke 1.5->2px + a stronger area gradient (resting band) for contrast on the light card. **M-3** mono ligatures disabled in the webhook signing code block (`[font-variant-ligatures:none]`) so `===` does not fuse. **M-4** the 30d response-time chart: p95 demoted to a thin (1px) dashed faint wash, gridlines + p95 lightened via `color-mix`, average stays the primary line. **M-5** the board summary bar carries WEIGHT in outage/degraded — a tinted surface + left accent rule (the `ActiveIncidentBanner` register) so a problem reads heavier than "operational". **L-1** the card "ms" unit shrunk + muted (`text-xs text-fg-subtle/70`). **L-2** the rail "Status page" item now LINKS to the live public page (external arrow, no "SOON"); the "Settings — SOON" item removed entirely. No "SOON" ships.
  - **DEFERRED (with reason):** the live PUBLIC STATUS PAGE screenshot (C-3) — SSR fetch is server-side, not browser-mockable, so it needs the live NestJS backend (owned by the parallel re-seed track); banner logic is unit-tested, capture deferred to the post-merge full-stack run. Nothing else deferred.
  - **Files touched:** `lib/store/{live-store,live-store.test,toast-store,toast-store.test}.ts`, `lib/sse/use-live-board.ts`, `lib/chart/{history-bar,history-bar.test}.ts`, `lib/public-status/{public-status-view,public-status-view.test}.ts`, `components/dashboard/{status-board,board-summary,monitor-card,dashboard-nav,sparkline}.tsx`, `components/detail/{history-bar,response-time-chart}.tsx`, `components/incidents/incident-row.tsx`, `components/alerts/webhook-signing-hint.tsx`, `components/marketing/board-preview.tsx` (NEW), `app/page.tsx`, `.review/{capture-phase7,capture-phase7-detail}.mjs` (NEW). No new web dependency.

- [x] **Phase 7.3 reviewer + designer-critic fixes (2026-06-04, backend-engineer) — both must-fix-before-deploy CLOSED, the 2 nice-to-haves done, the demo seed made believable. `pnpm -F pulse-server lint/typecheck/build` clean; 190 server tests green (+22: 10 alert-channels create-time SSRF, 7 webhook-dispatcher dispatch-time SSRF, 5 Set-Cookie accumulation). LIVE-PROVEN against docker (postgres:5437 + redis:6381, fresh web process).**
  - **MUST-FIX #1 — outbound webhook SSRF (the second SSRF surface), CLOSED.** Hoisted the prober's anti-rebinding machinery into a shared `server/src/probe/ssrf-outbound.ts` (`resolveAndAssertAllowed(host)` = DNS-resolve + validate EVERY A/AAAA, fail-closed; `buildPinnedAgent(ip)` = the undici IP-pin; `guardOutboundUrl(rawUrl)` = shape-gate + resolve + pinned agent). The prober (`probe-runner.service.ts`) now REUSES these (its private `resolveAndValidate`/`buildPinnedAgent` deleted — one anti-rebinding impl). **CREATE-time:** `AlertChannelsService.create` runs `assertProbeUrlAllowed` on a webhook target -> 400 `target_not_allowed` (mirrors `monitors.service.guardTargetUrl`). **DISPATCH-time (authoritative):** `WebhookDispatcher.dispatch` calls `guardOutboundUrl(channel.target)` BEFORE the retry loop, POSTs through the PINNED agent with `maxRedirections:0`; a blocked/unresolvable/rebound target records a `failed` delivery and NEVER opens a socket (so the de-dup constraint still holds, the worker job never throws). LIVE: create at `169.254.169.254`/`10.0.0.5`/`localhost` -> 400 `target_not_allowed` (with the exact blocked-range reason); a public Slack URL -> 201. Tests: 10 create-time (metadata/loopback/RFC-1918/non-http blocked, public accepted, email NOT URL-checked) + 7 dispatch-time (literal-internal + DNS-rebinding + split-horizon fail-closed all blocked WITHOUT opening a socket; public target delivers through the pinned dispatcher).
  - **MUST-FIX #2 — Set-Cookie overwrite, CLOSED.** `auth/auth.controller.ts` `writeWebResponse` now sets the FULL `getSetCookie()` array in ONE `res.setHeader('set-cookie', string[])` call (was a loop that overwrote, keeping only the last). The `ExpressLikeResponse.setHeader` signature widened to `string | string[]`. Exported `writeWebResponse` for the unit test. 5 tests: a single cookie forwards as a one-element array; MULTIPLE cookies all survive in ONE setHeader call (the landmine if better-auth emits a session + `dont_remember`); zero cookies omits the header; non-cookie headers + body + empty-body `end()` all still correct.
  - **NICE-TO-HAVE #3 — rate-limit `POST /demo/trigger`, DONE.** `@UseGuards(new RateLimitGuard({ windowMs: 60_000, max: 10 }))` on the trigger route ONLY (`/demo/flaky` + `/demo/status` stay unlimited — the worker probes flaky every 15s, the UI polls status). Stays UNAUTHENTICATED per ADR-007 (the wow is open), just bounded so an attacker cannot keep the demo perpetually armed; `DEMO_TRIGGER_ENABLED` still gates it off entirely. LIVE: triggers #1-10 -> 201, #11/#12 -> 429.
  - **NICE-TO-HAVE #4 — `NEXT_PUBLIC_DEMO_STATUS_SLUG` documented in `web/.env.example`** (read by `web/src/lib/env.ts`, safe `demo` default; § 4 wants every read var documented). One-liner with a comment tying it to the backend `seed-public-page.ts` slug.
  - **SEED TUNING (designer-critic C-3 / H-4) — the public demo now reads believable.** (a) **Renamed the demo monitor** from "Demo flaky (wow-moment target)" to **"Checkout API"** (`scripts/seed-demo-monitor.ts`); the seed also MIGRATES any pre-rename row by the legacy name so a re-run reuses it instead of duplicating. The "(wow-moment target)" string can never reach the public page. (b) **New `scripts/seed-demo-history.ts`** — a deterministic FROZEN-NOW (env `SEED_FROZEN_NOW`, default `2026-06-01T12:00:00Z`) seed writing 30d of `check_results` at a 5-min sample (8640 checks), CLUSTERED into 5 SHORT historical outages (~70 min total downtime) so the 30d uptime reads ~99.87% (NOT the 67% flat-failure surface) and the history bar is mostly green; a mulberry32 PRNG seeded from a constant (NO `Math.random()`/`Date.now()` in generation). It writes one CLOSED incident per outage with a DISTINCT cause (connection refused / HTTP 500 / elevated-response degraded / request timeout / keyword missing), sets the monitor `current_status='up'`, and recomputes the hourly rollups so the rollup-backed 30d uptime reflects it. Idempotent (wipes seeded `< cutover` rows on re-run; never touches live forward-from-now data). LIVE `GET /public/demo` AFTER the seed: `overall=operational`, `Checkout API` up at **99.29% uptime**, **5 resolved incidents with the 5 varied causes** (no identical loop), and grep-proved NO "wow-moment target" / "Demo flaky" anywhere on the public payload; a DB-wide monitor-name grep for the legacy strings returns 0. (Note: the local stray-worker probing the loopback demo target records `ssrf_blocked=down` and opens live incidents — the documented local caveat; the SEED is what a fresh deploy renders, and the seed reads believable.) The FRONTEND history-bar dominant-status coloring fix is handled in parallel (out of this lane).
  - **Verify:** `pnpm -F pulse-server lint` (clean) / `typecheck` (clean) / `build` (clean) / `test` (190 green). Files: `server/src/probe/ssrf-outbound.ts` (new), `server/src/probe/probe-runner.service.ts` (reuse shared), `server/src/alerts/{alert-channels.service,webhook-dispatcher}.ts` (+SSRF), `server/src/auth/auth.controller.ts` (Set-Cookie), `server/src/demo/demo.controller.ts` (rate limit), `server/scripts/{seed-demo-monitor,seed-demo-history}.ts` (seed), `web/.env.example` (+slug). New tests: `server/src/alerts/{alert-channels.ssrf,webhook-dispatcher.ssrf}.test.ts`, `server/src/auth/auth-controller.set-cookie.test.ts`. **NOT committed** (per instruction). **Next:** reviewer re-checks the webhook-SSRF + Set-Cookie diffs; deploy seed must run BOTH `seed-demo-monitor.ts` and `seed-demo-history.ts` (the latter is the new believability step).

- [x] **Phase 7.2 code review (2026-06-04, reviewer) — VERDICT: YELLOW. 0 blockers, 2 must-fix-before-deploy (both LOW-MEDIUM, neither in the SSRF/auth/redaction core), a handful of nice-to-haves.** Ran `pnpm -F pulse-server lint/typecheck/test` (168 tests green, incl. the real-Postgres rollup integration test against docker :5437/:6381) and `pnpm -F pulse-web lint/typecheck/test/build` (114 tests green, prod build OK — the EPERM symlink warnings are the documented benign Windows-local standalone-trace artifact). **SSRF guard: airtight** — empirically verified octal/decimal/hex/short-form IP encodings normalize via WHATWG URL into the literal-IP denylist short-circuit, IPv4-mapped IPv6 (`::ffff:127.0.0.1`) decodes + re-checks, `0`/`[::]`/credentialed/`localhost`/`*.localhost`/non-http schemes all blocked; execution-time resolve-then-validate-EVERY-record-then-pin defeats rebinding, per-redirect-hop re-guard holds. **Auth/IDOR: airtight** — every owner-scoped query pins `user_id` in the WHERE and 404s cross-user; writes route through `requireUserId` (401 demo fallback); no client-supplied owner id trusted. **Public redaction: airtight** — shape-enforced (`PublicMonitor` has no response-time field) + `is_public` re-asserted + the single `redactForPublic` chokepoint on both live + replay SSE paths. **Webhook signing: correct** — HMAC-SHA256 over `<ts>.<rawBody>`, timestamp header for replay, insert-first de-dup on the unique constraint, secret never serialized. **MUST-FIX-BEFORE-DEPLOY (2):** (1) **The outbound alert WEBHOOK target has NO SSRF guard** (`alerts/alert-channels.ts` create validates only http(s)-URL shape; `alerts/webhook-dispatcher.ts` POSTs to it with no IP check) — an authenticated user can register a webhook at `http://169.254.169.254/...` or an internal address and the worker will POST to it on the next incident transition (a second SSRF surface the prober guards but this path bypasses; lower severity — auth-gated + response body not echoed, but a port-scan/metadata oracle via status code + timing). Run `assertProbeUrlAllowed` + resolve-then-validate on the webhook target at create-time AND dispatch-time, reusing `ssrf-guard.ts`. (2) **`auth/auth.controller.ts` `writeWebResponse` drops all but the LAST `Set-Cookie`** — `res.setHeader('set-cookie', x)` in a loop OVERWRITES (verified empirically), so if better-auth ever emits >1 cookie on a flow (e.g. rememberMe `dont_remember`), only one survives; works today because the proven flows emit a single session cookie, but it is a latent auth-correctness landmine — accumulate into an array and set once (or use `res.append`). **NICE-TO-HAVE:** `POST /demo/trigger` is unauthenticated + un-rate-limited (an attacker can keep the demo monitor perpetually armed — demo-degradation nuisance, not a hole; `DEMO_TRIGGER_ENABLED` mitigates); `web/.env.example` omits `NEXT_PUBLIC_DEMO_STATUS_SLUG` (code reads it with a safe `'demo'` default — § 4 wants every read var documented); the public rate-limit + better-auth limiter are in-memory/per-machine (already documented as a Redis swap for multi-replica). **Conventions: clean** — English-only, no emojis, no `as any`/`@ts-ignore`, sovereign tokens, `.env` gitignored + excluded from the add path, `.env.example` complete server-side, strict CSP with NO `unsafe-eval` (asserted by test; zod-jitless side-effect imported in both server-root layout + client providers + every schema module — the regression that bit twice holds). Claim accuracy: 168 server + 114 web tests match PROGRESS exactly. **Next:** `backend-engineer` fixes must-fix #1 (webhook SSRF) and #2 (Set-Cookie), then re-review the two diffs only; the rest can land as Phase 8/9 follow-ups. Neither must-fix blocks the wow moment or the demo arc.

- [x] **Frontend Phase 6 (Tasks 6.4 + 6.5 + 6.6 + 6.7) — auth UI + session header + demo gating + public status page + SEO + landing (2026-06-04, frontend-engineer). THE PRODUCT IS FEATURE-COMPLETE FOR THE PHASE 7 REVIEW. The wow stays OPEN while mutations prompt sign-in; the public status page SSRs + live-updates + carries the full SEO surface; ZERO CSP violations across every Phase 6 route, proven headlessly.** New files under `web/src/`: `lib/api/auth.ts` (the better-auth client — sign-up/in/out/get-session, all `credentials:'include'`, `humanizeAuthError`), `lib/api/public-status.ts` (the `GET /public/:slug` client + `PublicStatusError`), `lib/auth/{session-state,session-state.test}.ts` (PURE `deriveAuthState`/`gateMutation`/`isAuthRequiredError` — 11 tests), `lib/auth/use-session.ts` (the TanStack-Query session hook), `lib/auth/use-gated-action.ts` (THE demo-gating seam every write affordance routes through), `lib/schemas/{auth-form,auth-form.test}.ts` (sign-in/up Zod mirrors — 7 tests), `lib/store/auth-prompt-store.ts` (the global sign-in dialog store + `openAuthPrompt`), `lib/public-status/{public-status-view,public-status-view.test}.ts` (PURE `overallBanner`/`formatUptime`/`monitorDisplayStatus`/`openIncidentCount` — 10 tests), `lib/sse/use-public-status-live.ts` (the redacted public SSE client → refetch-on-event reconcile), `components/auth/{auth-dialog,session-menu,demo-mode-banner}.tsx`, `components/public/{public-status-view,public-monitor-row,public-incident-list}.tsx`, `app/status/[slug]/{page,opengraph-image}.tsx`, `app/{robots,sitemap}.ts`, `.review/capture-phase6.mjs`. Edited: `app/providers.tsx` (mount `<AuthDialog/>`), `app/page.tsx` (the polished landing), `app/dashboard/layout.tsx` (mount `<DemoModeBanner/>`), `components/dashboard/dashboard-top-bar.tsx` + `components/marketing/marketing-header.tsx` (mount `<SessionMenu/>`; header CTA → "Live demo"), `components/dashboard/create-monitor-dialog.tsx` + `components/alerts/create-alert-channel-dialog.tsx` + `components/detail/detail-header.tsx` + `components/alerts/alerts-view.tsx` (gate the write affordances + 401 fallback), `lib/env.ts` (+ `demoStatusSlug`). **No new web dependency** (`next/og` ships with Next 15; the auth client + the public client are plain `fetch`).
  - **Task 6.4 — Auth UI + session header.** `useSession()` reads `GET /api/auth/get-session` (TanStack Query, `credentials:'include'`, `retry:false`) and feeds the PURE `deriveAuthState` → `loading | authenticated | anonymous` (a query ERROR degrades to `anonymous` so an auth-endpoint blip never breaks the demo board). `<SessionMenu>` (in BOTH shells' headers): loading → a quiet skeleton (no "Sign in" flash); authenticated → an avatar-initial button → a dropdown with the email + "Sign out" (sign-out invalidates the session + the private workspace queries so the board falls back to the shared demo with NO redirect); anonymous → a "Sign in" button opening the dialog. `<AuthDialog>` (mounted once in providers, opened from anywhere via `openAuthPrompt`): sign-in + sign-up in one modal (RHF + zodResolver against `auth-form.ts`, the razors-edge Zod-4 resolver-cast), calls the better-auth endpoints, invalidates `sessionQueryKey` on success. Accessible (Radix focus trap + return-focus, labels, `aria-describedby` errors, `role="alert"` server error, autofocus the first field).
  - **Task 6.5 — Demo gating (ADR-007, the wow stays open).** The whole posture is ONE seam: `useGatedAction().run(action, reason)`. Authenticated → the action fires; anonymous/loading → `openAuthPrompt({reason})` and the action does NOT fire. Wired into EVERY mutation affordance: "New monitor" (board), "Add channel" (alerts), edit/pause/delete (detail header), delete-channel (alerts). The board/detail/incidents READS + the demo-incident button are NEVER gated — they stay fully usable for a demo visitor. Belt-and-suspenders: each mutation also has an `onError` that routes a slipped-through `401` (session expired mid-flow) to the SAME sign-in prompt via `isAuthRequiredError`, so the demo NEVER shows a raw `authentication_required`. The calm `<DemoModeBanner>` (dashboard, anonymous-only): "You are viewing the shared demo workspace — explore freely" + "Sign in to create your own monitors" → opens the sign-up flow. Hidden once authenticated / while loading (no flash).
  - **Task 6.6 — Public status page `/status/[slug]` (the SEO artifact).** SSR from `GET /public/:slug` (the SEO floor — the first paint has the real redacted content for the crawler; `revalidate=30`), then `usePublicStatusLive` keeps it live off the redacted public SSE stream (`/api/public/:slug/stream` — any `status.change`/`incident.*` refetches the cheap snapshot; the server stays the single source of truth for the redacted shape). `<PublicStatusView>`: the overall banner ("All systems operational" / "Degraded performance — N active incidents" / "Active outage — N…", server-derived, NOT re-derived client-side), the public monitors (`StatusDot` + 30d uptime %, mono/tabular-nums), the recent-incident timeline (severity dot + Investigating/Resolved pill + ticking/fixed duration + the safe `cause` snapshot). Redaction holds end-to-end (the shape has no field for a response time). **SEO: per-page `metadata` (title — de-doubles a "status"-ending page title — + description + canonical + OG/Twitter), an on-brand `opengraph-image.tsx` via `next/og` (the indigo + the status color + the headline + the service name, 1200x630, edge-rendered from the redacted payload — a shared link previews the CURRENT state), JSON-LD (`WebSite` + `WebPage` graph, server-generated from the redacted payload only), `app/sitemap.ts` (`/` + `/status/<demoSlug>`) + `app/robots.ts` (allow `/`, disallow `/dashboard`, point at the sitemap).** Light + dark, usable from 320px. Unknown slug → `notFound()` (404, `noindex` metadata).
  - **Task 6.7 — Landing polish.** `app/page.tsx`: a confident hero ("Uptime monitoring that proves itself"), a "View the live demo" CTA into the open dashboard + a "See an example status page" CTA into `/status/<demoSlug>`, a feature trio (live board / self-managing incidents / alerts+status page), a wow callout pointing straight at the demo-incident arc ("Trigger it yourself"), and the accessible status legend. Server component, no client data dependency (SSR/static-friendly — the Lighthouse posture). The seed/live honesty line stays. Light + dark.
  - **Verify (all green + FULL-STACK LIVE PROOF + CSP-clean).** `pnpm -F pulse-web typecheck` ✓, `lint` ✓ (0 warnings), `test` ✓ (**114 vitest**, +28: 11 session-state, 10 public-status-view, 7 auth-form), prod `next build` ✓ (11 routes incl. `/status/[slug]` SSR, `/status/[slug]/opengraph-image`, `/robots.txt`, `/sitemap.xml`). **FULL STACK (docker postgres:5437 + redis:6381, NestJS web:3080 + worker, prod web `next start -p 3081` with `NEXT_PUBLIC_API_URL/SSE_URL=:3080` baked into the CSP):** (a) UNAUTH → the board renders the seeded demo workspace, the demo button is present, `GET /monitors` 200; clicking "New monitor" → the sign-in prompt ("Sign in to create your own monitors") with the board still visible behind it (proven in `p6-new-monitor-prompt.png`); `POST /monitors` UNAUTH → 401. (b) Sign up (`200`, got a UUID id — FK reconciliation) → session cookie → get-session returns the user → authenticated `POST /monitors` → 201 on their OWN workspace (own `/monitors`=1, NOT the demo's 3) → sign-out → falls back to demo. (c) `GET /public/demo` → the redacted subset, grep-proved NO targetUrl/responseTimeMs/userId/secret/statusCode/error; the SSR `/status/demo` renders "All systems operational" + the monitor + the incident timeline; the head carries `<title>`/`description`/`canonical`/`og:*`; the OG route → `200 image/png 29KB`; `/robots.txt` + `/sitemap.xml` correct; an unknown slug → 404. (d) **ZERO `securitypolicyviolation`** across the public page (dark+light), the landing (dark+light), the dashboard demo banner, the New-monitor prompt, and the sign-in dialog (the auth POSTs + the public EventSource are CSP-clean — the zod-jitless client-graph side-effect already in place holds). Screenshots in `web/.review/p6-*.png`: `p6-public-status-{dark,light}.png`, `p6-landing-{dark,light}.png`, `p6-dashboard-demo-banner.png`, `p6-new-monitor-prompt.png` (the demo banner + board + sign-in prompt all coherent), `p6-sign-in-light.png`. **`.env` callout:** `NEXT_PUBLIC_DEMO_STATUS_SLUG` (default `demo`) added to `web/.env` intent — the landing CTA + the sitemap point at it. The seeded demo monitor + public-page mapping (`seed-demo-monitor.ts` + `seed-public-page.ts`) stay in the dev DB.
- [x] **Backend Phase 6 (Tasks 6.1 + 6.3 + ADR-007) — better-auth + the public status surface + the demo-open posture (2026-06-04, backend-engineer).** Real auth WITHOUT a login wall in front of the wow moment. New dep: `better-auth@^1.6.14` (the ONLY new runtime dep; ADR-001's committed auth lib). New migration `drizzle/0002_mixed_scalphunter.sql` (applied) — ADDITIVE only. New files under `server/src/`: `auth/auth.ts` (the better-auth instance factory + the UUID-id reconciliation + the cookie/rate-limit config), `auth/auth.service.ts` (wraps the instance + `getSession`), `auth/auth.controller.ts` (the `/api/auth/*` catch-all Express<->Web-Fetch bridge), `auth/current-owner.ts` (+ `.test.ts`, 5 tests — THE seam), `db/schema/auth.ts` (session/account/verification tables), `common/rate-limit.guard.ts` (the public-endpoint limiter), `public/public-status.derive.ts` (+ pure banner/leak helpers), `public/public-status.service.ts`, `public/public-status.controller.ts`, `public/public-status.test.ts` (7 tests — redaction + banner), `lib/schemas/public-status.ts` (the shared public read contract). Edited: `db/schema/users.ts` (the better-auth `user` model mapped onto `users`, uuid id kept), `db/schema/index.ts` + `contract.ts` + `lib/schemas/index.ts` (export the auth tables + the public contract), `config/{app-config.service,env.schema}.ts` (+ `betterAuthSecret`/`betterAuthUrl`), `app.module.ts` (`AuthModule` `@Global` first; `PublicModule` filled), `auth/auth.module.ts` (filled the seam, `@Global`), `monitors/{monitors,monitor-read}.controller.ts` + `incidents/incidents-read.controller.ts` + `alerts/alert-channels.controller.ts` + `stream/stream.controller.ts` (swap the dev-owner/`getCurrentUserId` seam for `CurrentOwnerService` — reads `resolveOwnerUserId`, writes `requireUserId`), `monitors/monitors.module.ts` (export `MonitorReadService` for the public page), `main.ts` (`trust proxy` so the rate limiter keys on the real client IP), `.env.example` (BETTER*AUTH*\* now WIRED + the cookie posture documented). DELETED `stream/current-user.ts` (superseded by `CurrentOwnerService`). Updated `incidents/incidents-read.service.test.ts` for the new controller signature.
  - **Task 6.1 — better-auth (ADR-007).** Email+password sign up / sign in / sign out / get-session under `/api/auth/*`. The `AuthController` catch-all (`@All('*splat')`) builds a Web `Request` from the Express req (re-serializing the JSON body, injecting `x-forwarded-for` from the socket so the limiter has an IP) and writes better-auth's Web `Response` back (incl. each `Set-Cookie` via `getSetCookie()`). `AuthService` builds the instance once (failing LOUDLY if the secret/URL are missing) and exposes `getSession(headers)`. The auth-endpoint brute-force rate limit is better-auth's built-in (`/sign-in/email`+`/sign-up/email` 5/min). **The FK reconciliation (the Phase-1 flag):** `users.id` stays `uuid`, `advanced.database.generateId=randomUUID` makes better-auth emit UUIDs onto the existing `users` table — migration 0002 is purely additive, NO FK repointed. Proven: a signed-up user has a uuid id the existing FKs accept.
  - **The demo-open posture (ADR-007, the crux).** `CurrentOwnerService` is the SINGLE owner-resolution seam: `resolveOwnerUserId(req)` (READS — session user else the seeded demo owner) and `requireUserId(req)` (WRITES — session user else `401 authentication_required`). Reads serve the shared demo workspace to anonymous visitors (the board / detail / incidents / dashboard SSE stream all work without login — the wow moment is not gated); writes (POST/PATCH/DELETE monitors + POST/DELETE alert-channels) 401 the demo fallback; `POST /demo/trigger` is deliberately NOT gated (safe + the wow). An authed user gets their OWN private workspace, scoped by `users.id`; they cannot touch a monitor they do not own (404). The cookie travels for the SSE `EventSource(withCredentials)` so the dashboard stream is per-user-scoped (proven: authed stream = only their monitorId; demo stream = the demo monitorIds).
  - **Cookie posture (dev vs prod).** `SameSite=Lax`+`HttpOnly`, `Secure` in prod only. DEV split-origin (web :3081 -> api :3080): the cookie travels cross-origin via the existing CORS `credentials:true` + better-auth `trustedOrigins`(=`CORS_ORIGINS`). PROD single-origin proxy (pulse-web proxies pulse-api, ADR-006): the browser sees one origin, the cookie is first-party + `Secure`, the SSE EventSource is authenticated with no special handling. `BETTER_AUTH_URL` must be the API origin (dev `http://localhost:3080`; prod the proxied API origin) — `.env.example` documents both; the local `.env` was corrected from the placeholder `:3083` to `:3080`.
  - **Task 6.3 — the public read surface.** `GET /public/:slug` -> `{ slug, title, description, overall, generatedAt, monitors:[{id,name,status,uptimePercent}], incidents:[{...severity, window, cause}] }`. REDACTED: public monitors carry only id+name+status+30d-uptime% (the shape has no response-time field, so it cannot leak); incidents carry the window+severity+cause snapshot, no check timing / alert data. Exposes ONLY monitors in the `public_status_page_monitors` join (re-asserts `is_public` — defence in depth). The 30d uptime reuses `MonitorReadService.uptime` (rollup-backed). `deriveOverallStatus` (pure, unit-tested) -> operational/degraded/outage (down dominates). Rate-limited (in-memory `RateLimitGuard`, 60/min/IP). This powers the SSR `/status/[slug]` page (SEO). The dev `seed-public-page.ts` maps the demo flaky monitor onto slug `demo`, keeping the other 2 PRIVATE so the boundary is observable.
  - **Verify (all green + the FULL posture proven live).** `pnpm -F pulse-server lint / typecheck / build / test` pass — **168 tests (+12)**: the owner seam (authed -> session user; unauth -> demo owner; requireUserId allows authed + rejects demo with `authentication_required`), the public redaction (the serialized payload carries ONLY the public subset; the `publicMonitorLeakedKeys` guard flags any private field; private monitors/incident timing absent), the banner derivation (operational/degraded/outage, down dominates, empty=operational). `pnpm -F pulse-web typecheck` passes against the new contract. **FULL-STACK LIVE PROOF (docker + web:3080 + worker):** sign-up -> uuid id + session cookie; created a monitor as the user (their `GET /monitors` = 1; the demo `GET /monitors` = the 3 demo monitors, NOT theirs; the user's SSE stream = only their monitorId, the demo SSE stream = the demo monitorIds); UNAUTH `GET /monitors`/`/incidents`/`/monitors/:id/uptime` -> 200 (demo workspace); UNAUTH `POST /monitors`/`DELETE /monitors/:id`/`POST /alert-channels` -> 401 `authentication_required`; UNAUTH `POST /demo/trigger` -> 201 ALLOWED; authed PATCH of a demo monitor -> 404 (ownership); sign-in happy path + sign-out (then the cookie falls back to demo); auth brute-force -> 429 after 5 bad sign-ins; `GET /public/demo` -> the redacted subset, grep-proved no targetUrl/responseTimeMs/userId/secret/error/statusCode + no private monitor + no test-user monitor; public rate limit -> 429 after 60/min. Test user cascade-deleted; demo workspace intact. **`.env.example` CHANGED (callout): BETTER*AUTH*\* are now WIRED + the dev-vs-prod cookie/BASE_URL posture documented; no NEW var added (the Phase-1-declared BETTER_AUTH_SECRET/BETTER_AUTH_URL are now actually read by the web process).**
- [x] **Frontend Phase 5 (Tasks 5.4 + 5.5 + 5.6 + 5.7) — incidents view + board incident indicators + alerts config + the demo-incident button, THE WOW BEAT ON DEMAND (2026-06-04, frontend-engineer).** New files under `web/src/`: `lib/api/{incidents,alert-channels,demo}.ts`, `lib/store/toast-store.ts`, `lib/schemas/alert-channel-form.ts` (+ `.test.ts`), `lib/time/duration.ts` (+ `.test.ts`), `components/ui/toaster.tsx`, `components/incidents/{incident-row,incidents-view}.tsx`, `components/alerts/{alerts-view,create-alert-channel-dialog,webhook-signing-hint}.tsx`, `components/dashboard/{demo-incident-button,live-incident-strip}.tsx`, `components/detail/{active-incident-banner,incident-history}.tsx`, `app/dashboard/incidents/page.tsx`, `app/dashboard/alerts/page.tsx`, `.review/capture-phase5-arc.mjs`. Edited: `lib/store/live-store.ts` (+ the `incidents` overlay slice + `incidentToken` + the open/close reducer cases), `lib/store/live-store.test.ts` (+ 3 incident tests + the new state fields), `components/dashboard/{dashboard-nav,dashboard-top-bar,status-board,board-summary,monitor-card?}.tsx`, `components/detail/{detail-header,monitor-detail-view}.tsx`, `app/providers.tsx` (mount the Toaster + the client-graph zod-config side-effect), `lib/sse/event-schema.ts` + `lib/schemas/monitor-form.ts` (the zod-config side-effect). **No new web dependency** (the toaster is hand-rolled on the existing Motion — NO sonner, § 15 single-library).
  - **Task 5.4 — the incidents view (`/dashboard/incidents`).** `GET /incidents` (TanStack Query) + the live SSE `incidents` overlay, merged with REST authoritative; all/open/resolved filter tabs; rows (severity dot + label, monitor link, Open/Resolved pill, relative "started", a mono duration that TICKS UP for open incidents off a real `startedAt` baseline, the cause snapshot); AnimatePresence enter/exit (reduced-motion safe); empty/loading/error states. Reconciles via a refetch on each `incidentToken` bump (an open/close). A live-ONLY incident only surfaces if it opened within `LIVE_ONLY_MAX_AGE_MS` (2 min) so a stale SSE-ring-buffer replay of an old/deleted-monitor incident is dropped (a real edge hit during the live verify — fixed).
  - **Task 5.5 — board incident indicators.** The card down-ring on `hasOpenIncident` already existed; ADDED the top-of-board `LiveIncidentStrip` (open incidents materialise the instant `incident.open` lands — the dramatic beat — and settle out on close; filtered to board monitors so a stale replay does not show), made the summary bar reflect an open incident ("Outage detected" + "N open incidents" via a board-scoped `openIncidents` count), the nav an open-incident badge, the detail header an `ActiveIncidentBanner` (severity + cause + ticking duration), and a detail "Incidents" section from `GET /monitors/:id/incidents` (same live-merge).
  - **Task 5.6 — alerts config (`/dashboard/alerts`).** List (`GET /alert-channels`), create dialog (RHF + zodResolver against `alertChannelFormSchema`, a webhook|email segmented toggle, by-type target validation mirroring the server `superRefine`, the write-only webhook signing secret), delete with confirm. The `WebhookSigningHint` documents the EXACT scheme (`X-Pulse-Timestamp` + `X-Pulse-Signature: sha256=<hex over "<ts>.<rawBody>">`, constant-time compare, replay rejection) so a receiver can verify. The email channel carries the honest amber **"Mock — no real email sent"** badge. Coverage stated plainly ("Covers all monitors" — v1 fires every enabled channel on every transition, no per-monitor routing). `toCreateAlertChannel` mapping is unit-tested (8 tests: by-type validation + secret-null-on-empty/email).
  - **Task 5.7 — the demo button (THE wow beat).** `DemoIncidentButton` on the board. Click -> `POST /demo/trigger` -> a guided RUNNING state ("Arming" -> "Incident in progress · Ns" with the fail-window countdown polled from `GET /demo/status` -> "Recovering" -> "Demo complete") with a pulsing dot + a timed progress sweep + a narration line so the ~30-45s arc reads as DELIBERATE THEATRE, not lag. Hides itself on a 404 (demo disabled). Respects reduced-motion. The incident opens/closes ORGANICALLY through the real probe cycle — the button only flips the flag.
  - **The `alert.fired` toast.** The board's already-threaded `onAlertFired` sink now resolves the monitor name (off a ref to the REST list, so the connection does not re-open) and pushes a toast — "Alert sent: <monitor> is down" (open) / "Recovered: <monitor> is back up" (close) — through the hand-rolled Motion `AnimatePresence` toaster (`useToastStore` + `<Toaster>` mounted in providers), deduped per incident+transition, auto-dismiss, reduced-motion safe. Dashboard-scope only (the wire never carries `alert.fired` to public).
  - **CSP fix (latent regression my Zod-using forms surfaced — reviewer note).** `z.config({ jitless: true })` was only imported at the SERVER root layout, so client chunks JIT-probed `new Function` under the strict no-`unsafe-eval` CSP, firing a `securitypolicyviolation` on EVERY route (incl. `/`). Fixed by importing the `@/lib/zod-config` side-effect in the CLIENT `providers.tsx` + the SSE/form schema modules so jitless is set in the client graph before any schema runs. Re-verified: ALL routes now report ZERO violations. This was pre-existing (the earlier "zero" claims tested only the monitors board); it is now genuinely zero across `/`, `/dashboard`, `/dashboard/incidents`, `/dashboard/alerts`, the create dialog, and the detail page.
  - **Verify (all green + the dramatic arc proven headlessly).** `pnpm -F pulse-web typecheck` / `lint` (0 warnings) / `test` (**86 vitest**, +21: 10 duration, 8 alert-channel-form, 3 incident-store) / prod `next build` (8 routes; `/dashboard/incidents` + `/dashboard/alerts` static-prerendered) all pass. **FULL-STACK LIVE PROOF (docker postgres:5437 + redis:6381, NestJS web:3080 + worker, prod web `next start -p 3081` with the API env baked into the CSP, the demo monitor seeded targeting a PUBLIC ngrok tunnel -> `/demo/flaky` so the SSRF guard lets the arc CLOSE locally — the documented `DEMO_FLAKY_URL`-public approach):** drove headless chromium, clicked "Trigger demo incident" -> incident OPEN detected ~28s later (REST) -> the board flipped the demo card DOWN + incident ring + HTTP 500 + red sparkline, the summary read "Outage detected / 1 open incident / 1 Down", the live incident strip materialised, the nav Incidents badge lit, the **"Alert sent" toast fired** -> the incidents view showed the OPEN incident (ticking) + resolved history, the detail showed the active-incident banner + incident history -> on recovery the **"Recovered" toast fired** + the incident closed. ONE `/api/stream` SSE connection; **ZERO `securitypolicyviolation`** across every capture (`csp: 0`). Screenshots: `web/.review/p5-board-outage.png` (THE dramatic beat — demo card down + outage summary + incident strip + alert toast all coherent), `p5-board-recovery-toast.png`, `p5-board-armed.png` (the demo-button running state), `p5-incidents-open.png` / `p5-incidents-resolved.png` (the ticking open + resolved rows), `p5-detail-incident.png` (active-incident banner + history), `p5-alerts-dark.png` / `p5-alerts-light.png` (the channels + signing hint + email-mock badge), `p5-alerts-create-email.png`, `p5-incidents-light.png`, `p5-incidents-mobile.png`. Local capture used an ngrok tunnel for `DEMO_FLAKY_URL` (the documented faithful-local approach); on the deployed public `/demo/flaky` the arc is the clean ~30s-open/~45s-close with no tunnel flap. Test alert channels + the demo incidents stay in the dev DB (they cascade-delete with the demo monitor); the throwaway capture scripts copied into the razors-edge e2e workspace were removed.
- [x] **Backend Phase 5 read surface — incidents-history endpoints + finalized fast demo monitor (2026-06-04, backend-engineer).** The READ side the Phase 5 frontend (incidents view + the detail page's incident history) consumes, plus the demo monitor + timing tuned so the wow-moment arc is fast. New files under `server/src/`: `lib/schemas/incidents-list.ts` (the shared FE/BE read contract), `incidents/incidents-read.service.ts` (+ `.test.ts`, 9 tests), `incidents/incidents-read.controller.ts`, `incidents/incidents-read.module.ts`, and `server/scripts/seed-demo-monitor.ts`. Edited: `lib/schemas/index.ts` + `contract.ts` (export the new types), `app.module.ts` (wire `IncidentsReadModule` web-side), `demo/demo.controller.ts` (finalized timing + `FAIL_WINDOW_SECONDS` 20 -> 35). **No new env var, no migration.**
  - **`GET /incidents?limit=N&status=open|resolved`** — recent incidents across ALL of the demo owner's monitors, newest-first (`incidents` INNER JOIN `monitors` scoped by `monitors.user_id`, ordered `started_at DESC`, limit 1..100 default 20). Each row: `{ id, monitorId, monitorName, monitorUrl, status, severity, startedAt, resolvedAt, durationMs, cause }`. `durationMs` is the RESOLVED duration for a closed incident, or the still-accruing `now - startedAt` for an OPEN one (so the live row counts up off a real baseline). Query validated by the shared `incidentsListQuerySchema` at the boundary. Response is `{ monitorId: null, items }` (null = cross-monitor scope).
  - **`GET /monitors/:id/incidents?limit=N&status=...`** — the SAME row shape scoped to ONE monitor (the detail page's history). The controller resolves the OWNED monitor first via `MonitorsService.getOwned` (404 on a monitor the demo user does not own — never trusts the client id), then scopes by `monitor_id` directly (rides `incidents_monitor_started_at_idx`). Response `{ monitorId: <id>, items }`.
  - **Contract + module placement.** The row-mapping math (open-vs-closed duration) is the PURE, unit-tested `toIncidentListItem`; the service is the thin DB boundary. The read surface is a SEPARATE `IncidentsReadModule` (web-side) imported by `AppModule` — deliberately NOT folded into the worker-only `IncidentsModule` (the incident ENGINE), so the web process carries no engine/alert-dispatch deps. The shared schema is re-exported via `pulse-server` (`IncidentListItem`/`IncidentsListResponse`/`IncidentsListQuery`) for the web side; `pulse-web typecheck` passes against it. **This CLOSES the "a REST incident-history read endpoint is NOT built yet" gap the Phase 5 handoff flagged.**
  - **Finalized demo monitor + timing (ADR-006).** `scripts/seed-demo-monitor.ts` seeds (idempotent by stable name) a PUBLIC demo monitor targeting `${DEMO_FLAKY_URL}` (default the app's own `/demo/flaky`) and registers its repeatable BullMQ schedule directly so a running worker probes it immediately. **Finalized timing: interval 15s, failureThreshold N=2, recoveryThreshold M=1, fail window 35s** => open ~30s after `POST /demo/trigger`, close ~45s after — inside the ~30-45s wow budget. The 15s interval is BELOW the CRUD schema's 30s floor ON PURPOSE: the demo monitor is SEEDED directly (not created through `POST /monitors`), the documented exception — the floor protects user monitors from hammering third-party targets, but the demo monitor hits our OWN endpoint so a tighter interval is safe. `demo.controller.ts`'s `FAIL_WINDOW_SECONDS` is now 35 (was 20), tuned to the seeded interval + thresholds, and its trigger response surfaces `failWindowSeconds` for the UI countdown. SSRF: `/demo/flaky` is the app's own PUBLIC origin in prod (inside the allowlist); LOCAL dev resolves to loopback (guard-blocked at probe time — the seed prints a NOTE), so set `DEMO_FLAKY_URL` to a public host for a faithful local arc.
  - **Verify (all green + LIVE end-to-end).** `pnpm -F pulse-server lint / typecheck / build / test` pass — **156 tests (+9)**: the pure mapper (closed=resolved duration, open=accruing duration, negative clamp), the service scoping (cross-monitor JOINs monitors + applies limit; single-monitor scopes by id + passes name/url through; status filter narrows the WHERE), the controller ownership 404 (getOwned NotFoundException propagates, read service never reached), and the query validation (limit bounds 1..100, optional status enum). `pnpm -F pulse-web typecheck` passes against the new contract. **FULL-STACK LIVE PROOF (docker postgres:5437 + redis:6381, both NestJS processes):** empty `GET /incidents` -> `{monitorId:null,items:[]}`; `?status=flapping` -> 400, `?limit=999` -> 400, unowned-uuid `/incidents` -> 404. Forced a real arc on a 30s/N=2/M=1 monitor (example.com, keyword-forced down -> restored): `GET /monitors/:id/incidents` returned the OPEN incident with accruing `durationMs:28269` (resolvedAt null), then after recovery the CLOSED incident with fixed `durationMs:60072` + resolvedAt; `?status=open` correctly emptied, `?status=resolved` returned it. **Measured open->close wall-clock = 60.072s** (started 23:08:32.637 -> resolved 23:09:32.709) on that 30s monitor. `GET /incidents` returned BOTH an open and a closed incident across two monitors, newest-first. **Demo monitor verified live:** seeded at 15s interval, the worker probed it at exactly ~15s cadence (23:10:06.965 / :22.005 / :37.034 / :52.063), `POST /demo/trigger` armed (`failWindowSeconds:35`), `/demo/flaky` returned 500 while armed + `/demo/status {failing:true,recoversInSeconds:34}`, and the demo monitor OPENED an incident after N=2 down checks ~30s after the first failing probe — confirming the timing math (locally the downs are `ssrf_blocked` since the target is loopback, as documented; on the deployed public host the same cadence + 35s window closes at ~45s). Test monitors cleaned up (cascade-deleted).
- [x] **Backend Phase 5 (Tasks 5.1 + 5.2 + 5.3) — incident engine + alerts + the demo-incident trigger (2026-06-04, backend-engineer).** The wow moment's server spine: real probes now open + close incidents through a pure-reducer state machine, fire a REAL signed webhook + a MOCK email, and the recruiter-facing demo trigger plays the arc on demand. New files under `server/src/`: `incidents/incident-reducer.ts` (+ `.test.ts`, 22 tests), `incidents/incident-engine.service.ts`, `alerts/alert-dispatcher.ts`, `alerts/webhook-payload.ts` (+ `.test.ts`, 7 tests), `alerts/webhook-dispatcher.ts`, `alerts/email-dispatcher.ts`, `alerts/alerts.service.ts` (+ `.test.ts`, 5 de-dup tests), `alerts/alert-channels.service.ts`, `alerts/alert-channels.controller.ts`, `alerts/alerts-dispatch.module.ts`, `demo/demo-flag.service.ts`, `demo/demo.controller.ts`, `demo/demo.module.ts`. Edited: `events/events-publisher.service.ts` (+ `publishAlertFired`, dashboard-scope only), `checks/check-recorder.service.ts` + `checks/checks.module.ts` (call the engine after recording), `incidents/incidents.module.ts` + `alerts/alerts.module.ts` (filled the seams), `config/app-config.service.ts` (+ `webhookSigningKey`), `app.module.ts` (alerts CRUD + demo, web-side), `.env.example` (documented the WIRED webhook signing scheme + demo routes). **No new migration, no new env var** (the Phase 1 `WEBHOOK_SIGNING_KEY` / `DEMO_TRIGGER_ENABLED` are now actually read).
  - **Task 5.1 — the PURE reducer (ADR-004, the centerpiece).** `reduceIncident(state, input) -> { state, effects }`, ZERO IO. State `{ consecutiveBad, consecutiveGood, worstBadSeverity, openSeverity }`. N consecutive bad (down or degraded) opens EXACTLY ONE incident (severity = worst across the run, down dominates); M consecutive up closes it; the N/M debounce IS the flap suppression; a down while a degraded incident is open ESCALATES (never opens a second, never de-escalates). 22 tests cover: N-1 does not open / Nth does / N opens exactly one not N; custom N/M; intervening-success reset; M-debounce close + relapse-resets-recovery; flap suppression (oscillation never opens; sustained-with-flaps opens once); degraded vs down + worst-across-run + escalate-once + never-de-escalate; a long noisy sequence where open never exceeds close+1 (single-open invariant); and purity (no input mutation).
  - **The engine (the IO boundary).** `IncidentEngineService.onCheckRecorded(monitor, status, checkedAt)` REHYDRATES the reducer state from Postgres (the open incident + the consecutive result tail, bounded 64 rows) so the machine survives restarts + the web/worker split — no in-memory monitor state to lose. It applies the reducer, persists rows (open insert swallows a partial-unique violation as "already open" = the invariant working under a race; close stamps resolved_at + computes duration), publishes `incident.*`, and calls `AlertsService`. It NEVER throws to the recorder (a hiccup must not fail the probe job / trigger a spurious BullMQ retry — the result is already committed).
  - **Task 5.2 — alerts (webhook REAL, email MOCK).** ONE `AlertDispatcher` interface. `WebhookDispatcher`: HMAC-SHA256 over `"<unix-ts>.<rawBody>"`, headers `X-Pulse-Signature: sha256=<hex>` + `X-Pulse-Timestamp`, generic-signed-webhook shape (version+transition+monitor+incident+timestamp), retry x3 with exponential backoff, refuses to send unsigned if no key. `EmailDispatcher`: same interface, NO SMTP — logs `MOCK EMAIL -> ...` + records a delivery with `mock:true` (documented as a demo mock, never presented as live). `AlertsService.dispatchForTransition` loads the owner's enabled channels, CLAIMS the de-dup slot FIRST (`INSERT ... ON CONFLICT DO NOTHING` on `(incident_id, channel_id, transition)`) so a re-fire is a clean skip, dispatches, records the outcome, and publishes `alert.fired` (dashboard scope only). Alert-channels CRUD (`/alert-channels`): create/list/delete, Zod-validated, secret stored but NEVER returned. (v1 fires every enabled channel on every transition — no per-monitor routing, the documented ADR-005/PLAN posture.)
  - **Task 5.3 — the demo trigger (ADR-006).** `DemoFlagService` (a single Redis key with a TTL, shared web<->worker). `GET /demo/flaky` 200 normally / 500 while the flag is set (a real probe records up/down). `POST /demo/trigger` arms the flag for 20s then the TTL auto-recovers — idempotent (re-arm refreshes the TTL, the single-open + de-dup invariants prevent double-fire). `GET /demo/status` reports failing + recoversInSeconds. All gated by `DEMO_TRIGGER_ENABLED` (404 when off). SSRF note: `/demo/flaky` is the app's OWN public origin, so the demo monitor targets `https://<host>/demo/flaky` in prod (inside the allowlist); local dev resolves to loopback (guard-blocked at probe time) so the local arc was verified by inducing failure on a real public monitor (identical pipeline) + verifying the flaky endpoint mechanism directly.
  - **Verify (all green + the FULL REAL ARC proven live).** `pnpm -F pulse-server lint / typecheck / build / test` pass (147 tests, +34). `pnpm -F pulse-web typecheck` passes (no contract change — `alert.fired`/`incident.*` were already in `events.ts`). **FULL-STACK LIVE PROOF (docker postgres:5437 + redis:6381, both NestJS processes, a throwaway HMAC-verifying webhook receiver on :4599):** demo mechanism — `/demo/flaky` 200 -> `POST /demo/trigger` -> `/demo/flaky` 500 -> idempotent re-trigger -> `/demo/status {failing:true,recoversInSeconds:20}`. Full arc on a real 30s-interval monitor (example.com, keyword-forced down): real probes recorded `down` -> after 3 fails `incident.open` row (`severity=down`, cause snapshot) + event + SIGNED webhook the receiver VERIFIED (`signatureValid:true, transition:open, severity:down`) + MOCK email (no SMTP) -> keyword restored -> 2 ups -> `incident.close` row (`duration=60096ms`) + event + signed close webhook (`signatureValid:true, transition:close, durationMs:60096`). `pulse:events`: `incident.open`/`incident.close`/`alert.fired` all `scope:dashboard:<userId>`; **0 public-scoped alert.fired**. `alert_deliveries`: open+close x (webhook sent/200, email sent/no-code MOCK) — exactly one per transition (DE-DUP held under 3+ down probes). Test data cleaned up after.
- [x] **Frontend Phase 4 (Tasks 4.1 + 4.2) — the monitor detail page + uPlot charts (2026-06-04, frontend-engineer).** The detail route the recruiter / developer (viewer 2) drills into from a board card. New dep: `uplot@^1.6.32` (the ADR-001 charting pick; ~40KB canvas, no `eval` -> CSP-clean). New files under `web/src/`: `lib/api/monitor-detail.ts` (REST client + namespaced query keys for the four read endpoints + `getMonitor`/`updateMonitor`/`deleteMonitor`), `lib/chart/series-adapter.ts` (+ `.test.ts`), `lib/chart/history-bar.ts` (+ `.test.ts`), `components/detail/{response-time-chart,window-selector,uptime-card,history-bar,recent-checks,edit-monitor-dialog,detail-header,monitor-detail-view}.tsx`, `app/dashboard/monitors/[id]/page.tsx`, `.review/capture-monitor-detail.mjs`. Edited: `lib/api/monitors.ts` (+ `updateMonitor`/`deleteMonitor` + the 204 no-body guard), `components/dashboard/monitor-card.tsx` (+ the stretched detail link).
  - **The detail surface (Task 4.1 treatment).** A back link to the board, then the header (name + live `StatusDot` + label + a "Paused" badge when paused; the target URL as an external link; interval + method + last response time). The three uptime cards each summarise one window (% mono/tabular-nums + a stacked token-colored breakdown bar with a Radix tooltip listing up/degraded/down/unknown durations + an aria summary so the split is never color-only); the active window's card is brand-ringed. Below: the response-time chart section with the window selector, the uptime-history strip, and the recent-checks list — each a bordered `bg-card` panel (the Linear/Vercel dashboard register).
  - **The uPlot IMPERATIVE wrapper (Task 4.2, the ADR/meld discipline).** `response-time-chart.tsx` is the only place uPlot lives. The instance is created ONCE in a `useEffect` (recreated only on the coarse-grained resolution/theme flip — raw 1-series vs hourly 2-series need a different uPlot `series` config, and theme needs concrete token colors since canvas cannot inherit CSS vars), data is pushed via `setData`, the canvas resizes via a `ResizeObserver` (`setSize`), and the instance is `destroy()`-ed on unmount. The HIGH-FREQUENCY live path does NOT go through React: `onReady` hands the parent a `ChartHandle.appendLive(point)` that calls `appendLivePoint` + `setData` imperatively. Theme colors are read off `getComputedStyle(documentElement)` for `--color-fg-subtle`/`--color-border`/`--color-brand`/`--color-fg-muted`. Reduced-motion is a non-issue (uPlot does no animation). `uplot/dist/uPlot.min.css` imported in the client component. The canvas is `aria-hidden` (the uptime cards + checks list carry the same data accessibly).
  - **The live 24h append (the wow continues on the detail page).** `monitor-detail-view.tsx` opens the same single `EventSource` (the global Zustand SSE store is shared with the board, so the header status flips on `status.change` and the last response time renders for free). On the 24h window it watches the store's per-monitor `pulseToken`; on a fresh `check.result` it (a) `chartHandle.appendLive({ tSec, value })` (de-dup on x, null=gap, rolling cap) and (b) prepends a synthetic `RecentCheck` row to the recent-checks list (merged above the fetched rows, de-duped by timestamp, capped 50). A windowed refetch re-baselines so live rows are not double-counted.
  - **The history bar (hand-rolled SVG, NOT uPlot).** `history-bar.tsx` renders the 90 worst-status `<rect>`s in a 0..1000 viewBox (preserveAspectRatio none), colored by `bucketFill` (the sovereign `--color-status-*` fill tokens), with a hover tooltip (`bucketTooltip`: time range via `Intl.DateTimeFormat` + status label + check counts) and a non-hovered dim. The strip carries a one-line aria summary (`historyAriaSummary`) AND a visually-hidden `<p>` mirror so it is not color-only (the AGENT_NOTES gate). A NOTE: the 30d strip reads alarmingly red on the verify fixture — worst-status-wins + the fixture's UNIFORM every-97th-check-down spread makes nearly every wide 8h bar contain a down; the 24h strip (narrower buckets) is far greener. This is the documented fixture artifact (real outages cluster); the Phase 6 frozen-`now` seed should cluster its failures.
  - **Actions wired to the real CRUD (no stubs).** Edit -> the pre-filled `EditMonitorDialog` (reuses the shared `monitorFormSchema`, PATCHes `/monitors/:id`, invalidates the list + this monitor's detail). Pause/Resume -> a direct `PATCH { isPaused }` (a REAL backend field per `monitorResponseSchema` — the brief allowed a stub, but the field exists so it is fully wired; editing the interval re-registers the BullMQ repeatable server-side). Delete -> a confirm dialog -> `DELETE /monitors/:id` (204) -> navigate back to the board. All optimistic-cache + invalidate.
  - **The two PURE data adapters are the unit-tested surface (the task's "test the adapter, not uPlot rendering").** `series-adapter.ts`: `toUplotData` maps `/series` -> `[t, value]` (24h raw, 1 y-series) or `[t, avg, p95]` (7d/30d hourly, 2 y-series), keeps null gaps, and length-normalises a drifted value array against `t`; `appendLivePoint` appends/replaces/ignores by x-order, trims to a rolling cap, never mutates input. `history-bar.ts`: `bucketFill` (status->token class), `bucketLabel`/`bucketTooltip` (Statuspage register + counts), `summarizeHistory`/`historyAriaSummary` (bars-by-status, the non-color channel). 25 tests (15 series + 10 history) — total **65 vitest** (was 40).
  - **Verify (all green + the LIVE detail proven headlessly).** `pnpm -F pulse-web typecheck` ✓, `lint` ✓ (0 warnings), `test` ✓ (65), prod `next build` ✓ (`/dashboard/monitors/[id]` 216KB First Load). **FULL-STACK LIVE PROOF (docker postgres:5437 + redis:6381, NestJS web:3080 + worker, prod web `next start -p 3081` rebuilt with `NEXT_PUBLIC_API_URL/SSE_URL=:3080`):** seeded the deterministic 30d fixture (`scripts/seed-detail-fixture.ts`, 8640 raw: 8379 up/171 degraded/90 down -> 30d uptime 97.98%) + used the live GitHub 30s monitor for the 24h append. Drove headless chromium (`@playwright/test`, run from the razors-edge e2e workspace — pulse/e2e has no package yet) against the PROD build, BOTH themes: **24h-dark/light + 30d-dark/light** all rendered canvas=1 + 90 history rects + 50 recent-check rows + **csp=0**; the **live-append** capture confirmed `appended=true`, **sseConnections=1** (genuine push, not polling), csp=0, zero page errors. Screenshots `web/.review/detail-{24h,30d}-{dark,light}.png` + `detail-live-append-dark.png`. The seeded fixture monitor stays (paused, pure history) for further review; remove with `DELETE FROM monitors WHERE name = 'detail-fixture (seeded history)'`.
- [x] **Backend Phase 4 (Tasks 4.1 + 4.2) — monitor-detail + uptime READ endpoints (2026-06-04, backend-engineer).** The read surface the Phase 4 frontend (detail page + uPlot chart + status-history strip + recent-checks list) consumes. New files under `server/src/`: `lib/schemas/monitor-detail.ts` (the shared FE/BE response contract), `monitors/uptime-math.ts` (+ `.test.ts`), `monitors/history-bucketing.ts` (+ `.test.ts`), `monitors/monitor-read.service.ts`, `monitors/monitor-read.controller.ts`, `scripts/seed-detail-fixture.ts` (a deterministic 30d-history verify helper). Edited: `lib/schemas/monitor.ts` + `monitors/monitors.serialize.ts` (Task 4.1 serialization fix), `monitors/monitors.module.ts` (+ the read controller/service), `lib/schemas/index.ts` + `contract.ts` (export the new schema). **No new env vars; no migration.**
  - **Task 4.1 — `GET /monitors` serialization fix (the Phase 3b handoff gap).** `monitorResponseSchema` now carries `currentStatus: monitorStatus.nullable()` + `lastCheckedAt: string.datetime().nullable()` (the migration-0001 columns), and `serializeMonitor` populates them (ISO string for the timestamp, raw enum/null for status). Both list AND get return them now, so a fresh load / reload paints the persisted status instantly instead of `unknown` for ~1 interval. The web `MonitorRow` already declared these — the contract is back in sync; `pulse-web typecheck` passes against it.
  - **Task 4.2 — the four detail endpoints (ADR-004).** All scoped to the demo owner via `MonitorsService.getOwned` (404 on a monitor the demo user does not own — never trusts the client id), Zod-validated query params at the boundary, mounted on a dedicated `MonitorReadController` (base `monitors`) so `:id/uptime` etc. sit alongside the CRUD `:id`.
    - `GET /monitors/:id/uptime?window=24h|7d|30d` -> `{ monitorId, window, uptimePercent (nullable), windowSeconds, observedSeconds, breakdown {up/degraded/down/unknownSeconds}, source: 'raw'|'rollup' }`. **The math (ADR-004): uptime% = (up + 0.5*degraded) / (up + degraded + down) * 100**; `degraded` counts 50% (`DEGRADED_UPTIME_WEIGHT`), `unknown` gaps (any span beyond the per-result interval cap) are EXCLUDED from numerator AND denominator. `null` when no observed data. `source` is surfaced so the UI / a reviewer can confirm the 30d view did not scan raw.
    - `GET /monitors/:id/series?window=...` -> uPlot-FRIENDLY parallel arrays. 24h: `{ resolution:'raw', t:[unix-sec], value:[ms|null] }` (one series, null = gap). 7d/30d: `{ resolution:'hourly', t, avg:[ms|null], p95:[ms|null] }` (two series from the rollup, ~168/720 points). The FE feeds `[t, value]` or `[t, avg, p95]` straight into uPlot `setData`.
    - `GET /monitors/:id/history?window=...` -> `{ bucketSeconds, buckets: [{ bucketStart, status: up|degraded|down|unknown, up/degraded/downCount }] }` — 90 bars, worst-status-wins per bucket (down>degraded>up; empty span = unknown). Raw for 24h, rollups folded into the wider bars for 7d/30d.
    - `GET /monitors/:id/checks?limit=N` -> `{ checks: [{ id, checkedAt, status, statusCode, responseTimeMs, error }] }`, latest-first, capped 1..200 (default 50).
  - **The rollup-vs-raw window strategy (the load-bearing efficiency decision).** 24h reads raw `check_results` (cheap, exact, rides the `(monitor_id, checked_at DESC)` index). 7d/30d read the hourly rollup body PLUS a raw top-up of the CURRENT partial hour (so the live number is right between 5-min rollup ticks). **Verified via EXPLAIN against a seeded 30d fixture (8640 raw rows): the 30d uptime body is an Index Scan on the rollup PK returning 719 rows; the only raw touch is the current-hour top-up, a tight index scan returning 1 row. A 30d query NEVER scans the 8640 raw rows.**
  - **The pure math is the unit-tested surface (these encode ADR-004).** `uptime-math.ts` + `history-bucketing.ts` are IO-free pure functions; the service is the thin DB boundary that fetches the right rows and hands them in. 20 new tests assert: degraded=50%, down=full, all-up=100%, the unknown-gap exclusion (a >>2\*interval gap stays 100% and is excluded from the denominator), the per-result interval cap (one result in a huge window covers only one interval), leading-unknown / partial-window, empty-window=null, the rollup path scaling counts by interval, the raw+rollup paths AGREEING for the same sequence, history worst-status precedence + correct time-bucketing + end-exclusive boundaries + multi-hour folding. **113 server tests green (was 93).**
  - **Verify (all green + live smoke):** `pnpm -F pulse-server lint / typecheck / build / test` pass (113 tests). `pnpm -F pulse-web typecheck` passes (contract back in sync). Live smoke (docker up, web process): seeded a deterministic 30d fixture (8379 up / 171 degraded / 90 down @ 300s interval) -> 30d uptime read 97.98% (matches the hand calc (8379+0.5\*171)/8640), 24h `source:raw`, 7d/30d `source:rollup`; series 24h=288 raw points / 30d=720 hourly avg+p95; history 90 bars; checks latest-first; 404 on an unowned uuid, 400 on a bad `window` / non-uuid id / over-max `limit`. Fixture cleaned up after.
- [x] **Frontend Phase 3b (Tasks 3.3 + 3.4 + 3.5) — the live status board, the WOW MOMENT (2026-06-03, frontend-engineer).** The dashboard board reacts in real time to real probes. New files under `web/src/`: `lib/sse/event-schema.ts` (+ `.test.ts`), `lib/sse/event-names.ts`, `lib/sse/use-live-board.ts`, `lib/store/live-store.ts` (+ `.test.ts`), `lib/api/monitors.ts`, `lib/schemas/monitor-form.ts`, `lib/sparkline/sparkline-path.ts` (+ `.test.ts`), `lib/time/relative-time.ts` (+ `.test.ts`), `lib/time/use-now.ts`, `components/dashboard/{sparkline,monitor-card,connection-indicator,board-summary,status-board,create-monitor-dialog}.tsx`, `components/ui/label.tsx`. Edited: `status-tokens.ts` (types-only `MonitorStatus` from `pulse-server/events`), `status-dot.tsx`, `dashboard/page.tsx`, `dashboard-top-bar.tsx`, `next.config.ts` (env-driven `connect-src`), `package.json` (+`pulse-server: workspace:*`, `@hookform/resolvers` ^5). Removed the orphaned `board-placeholder.tsx`.
  - **Task 3.3 — SSE client + live store.** Replaced the placeholder `MonitorStatus` with a TYPES-ONLY import `import type { MonitorStatus } from 'pulse-server/events'` (and `MonitorResponse`/`CreateMonitorInput` from `pulse-server`, the `contract.ts` re-export) — mirrors meld-web's `import type { App } from 'meld-server'`. Added `pulse-server: workspace:*` (types-only consumer). A `DisplayStatus = MonitorStatus | 'unknown'` adds the UI-only idle/never-checked state on top of the wire vocabulary so the contract stays the single source of truth. `use-live-board.ts` opens ONE `new EventSource(\`${env.sseUrl}/api/stream\`, { withCredentials: true })`, `addEventListener`s every named event (`check.result`/`status.change`/`incident._`/`alert.fired`/`heartbeat`— the future`incident._`/`alert.fired` are already wired so Phase 5 needs zero board changes), parses each defensively via a CHEAP web-side Zod-v4 mirror (`event-schema.ts`, pinned to the contract type via `satisfies`; the server's Zod-v3 runtime is erased), and feeds a Zustand live store. On `open`/reconnect it REFETCHES `GET /monitors` (TanStack Query invalidate) to reconcile missed events (ADR-003). The store (`live-store.ts`) is keyed by monitorId — current status, last result, a rolling 40-point response-time buffer for the sparkline, an open-incident flag, a per-monitor pulse token, and the aria-live announcement. The reducer (`reduceLiveBoard`) is PURE and unit-tested.
  - **Task 3.4 — the board UI.** `status-board.tsx`: TanStack Query `GET /monitors` for the cards, then live SSE updates; a summary bar (total + up/degraded/down tally + worst-status headline), the connection indicator (live/connecting/reconnecting), loading skeleton, error state with retry, and an empty state with a create CTA. The grid reflows 1->2->3 columns. `monitor-card.tsx`: the AA `StatusDot` (never color-alone, keeps aria-label + visible text), name, truncated host, latest response time (mono tabular-nums) + HTTP code, the hand-rolled SVG sparkline (NOT uPlot, NOT Motion — drawn directly, CSS-transitioned `<path>`, null points are gaps), and a "last checked Ns ago" ticker off one shared `useNow()` clock. Motion micro-interactions: a fresh-result ring pulse (gated on the per-monitor pulse token) and a status crossfade on a flip — both behind `useReducedMotion()`. An `aria-live="polite"` region announces status flips.
  - **Task 3.5 — create-monitor dialog.** shadcn dialog + RHF + zodResolver over a web mirror of the shared create schema (`monitor-form.ts`, pinned to `CreateMonitorInput` via `satisfies`; the web is on Zod v4 / the server Zod v3, hence the mirror — same reasoning as the SSE schema). Fields: name, URL, interval (with 30s/1m/5m presets), timeout, expected status, keyword, public toggle. POSTs `/monitors`; on success optimistically inserts the card + invalidates the list, so the card appears and goes live within an interval. Accessible (labels, `aria-describedby` errors, focus trap, Escape).
  - **CSP connect-src (dev vs prod — the load-bearing SSE detail).** `next.config.ts` now COMPUTES `connect-src` from `NEXT_PUBLIC_API_URL`/`NEXT_PUBLIC_SSE_URL`: `'self'` plus any origin that is NOT same-as-site. PROD single-origin proxy (env unset or same-origin) => exactly `connect-src 'self'` (the ADR-006 posture, unchanged). DEV / split-origin (API on `:3080`) => `connect-src 'self' http://localhost:3080` so the board's cross-origin `EventSource` actually connects. The directive is the only env-varying one and never widens to a wildcard. Header test asserts the prod default stays `'self'`-only.
  - **Verify (all green + the LIVE wow moment proven).** `pnpm -F pulse-web typecheck` ✓, `lint` ✓ (0 warnings), **40 vitest tests** ✓ (11 store-reducer: check.result updates card+sparkline / status.change flips + announces / incident open-close / malformed+heartbeat+alert no-ops / buffer cap; 6 SSE-parse: well-formed accepted, bad JSON / unknown type / mismatched payload / missing id dropped; 8 sparkline mapping; 7 relative-time; + the existing status-tokens + 5 header tests incl. the new dev-vs-prod connect-src assertion), prod `next build` ✓. **FULL-STACK LIVE PROOF (docker postgres:5437 + redis:6381, NestJS web:3080 + worker, prod web `next start -p 3081` rebuilt with `NEXT_PUBLIC_API_URL=:3080` so the dev CSP permits the cross-origin SSE):** created 4 monitors (example.com + github.com = up; google generate_204-expecting-200 + httpbingo 500 = down). Drove the board with headless Playwright chromium against the PROD build — confirmed: cards render; the connection indicator reads "Live"; exactly ONE `/api/stream` request (genuine push, NOT polling); a card's response-time metric changed live (272ms->278ms) from a fresh `check.result`; the "last checked" ticker reads "just now"/"Ns ago" and ticks; a forced `up->down` `status.change` (PATCH a never-present keyword on the GitHub monitor) flipped the card to Down with its sparkline + summary updating live; the create-monitor dialog POSTed 201 and the new card appeared and went live (status=up) within seconds; **ZERO CSP violations** under the production CSP. Screenshots in `web/.review/` (paths reported). Test monitors cleaned up; GitHub monitor restored to healthy.
- [x] **Backend Phase 3.1 (Task 3.1) — the two `@Sse()` routes + the worker->SSE Redis bridge (2026-06-03, backend-engineer).** The SUBSCRIBE side of `pulse:events` is built on the WEB process; the real-time path is now proven end-to-end. New env var `CORS_ORIGINS` added to `.env.example` (callout below). New files under `server/src/stream/`: `events-bridge.service.ts`, `public-page.service.ts`, `public-redaction.ts`, `current-user.ts`, `stream.controller.ts` (+ `stream.module.ts` filled in), and the dev/verify helper `server/scripts/seed-public-page.ts`.
  - **`EventsBridgeService` (the bridge).** `OnModuleInit` opens a DEDICATED ioredis SUBSCRIBER connection (`new Redis(redisUrl, buildRedisOptions())`, NOT the shared `REDIS_CLIENT` — a subscriber connection cannot run normal commands) and `subscribe('pulse:events')`. Each message is validated against the SHARED `sseEventSchema` at the boundary (bad JSON / unknown type / bad scope dropped, never relayed), then fanned into an in-process RxJS `Subject<SseEvent>` AND appended to a per-scope RING BUFFER (256 events) for `Last-Event-ID` replay. `ingest(raw)` is exposed (not private) so the unit test drives it without live Redis. `replayAfter(scope, lastEventId)` returns the buffered events with `id > cursor`, oldest-first, isolated per scope. Cleans up the subscriber on shutdown.
  - **`StreamController` — the two `@Sse()` routes** (base `@Controller('api')`, scoped not global so `/health` + `/monitors` keep their Phase 1/2 paths). **`GET /api/stream`** (dashboard): resolves the user via the `getCurrentUserId` seam, builds `dashboard:<userId>`, replays the ring on `Last-Event-ID` then filters live events to that exact scope (the dashboard is the private surface — it sees its own `check.result` + `alert.fired`), coalesces `check.result` per monitor in a 250ms window (`bufferTime` + per-monitor latest-wins), merges a 15s `heartbeat`. **`GET /api/public/:slug/stream`** (public, unauthenticated): resolves the slug -> `{ pageId, monitorIds }`, runs EVERY event (replay + live) through `redactForPublic` — the single chokepoint that drops `check.result`/`alert.fired` and any non-published monitor, re-scoping survivors to `public:<pageId>`. Nest sets the SSE headers itself (`text/event-stream`, `Connection: keep-alive`, `Cache-Control: no-cache/no-store`, `X-Accel-Buffering: no`); the `MessageEvent.id` we set becomes the SSE `id:` line (the resume cursor) and `type` becomes the `event:` line.
  - **The demo-user seam (`current-user.ts`, where Phase 6 auth slots in).** `getCurrentUserId(db, req)` returns the seeded demo owner (`owner@pulse.local`, the SAME id the monitors CRUD uses via `resolveOwnerUserId`), so the dashboard stream scope `dashboard:<demoUserId>` lines up EXACTLY with the `dashboard:<ownerUserId>` scope the worker already publishes (Phase 2.5). It takes the request already, so the **Phase 6 replacement is a one-function-body swap**: read the better-auth session off `req.headers` cookie, return `session.user.id`, and add a guard for the unauthenticated case. It deliberately does NOT fake security — it is a clearly-marked single-tenant demo seam. The public stream needs no user (it scopes by page). (Phase 1 monitors CRUD already assigns the demo owner, so the monitors' scopes already align — no re-wiring needed.)
  - **Public redaction (`public-redaction.ts`, the privacy boundary).** Pure `redactForPublic(event, pageId, allowedMonitorIds) -> SseEvent | null`. Allow-set = `status.change` / `incident.open` / `incident.close` ONLY (never `check.result` raw response times, never `alert.fired` owner alerting); the event's monitor must be in the page's published set. Survivors are re-scoped `dashboard:<userId>` -> `public:<pageId>` (id/ts/type/payload preserved). Pure so the "public exposes strictly less" guarantee is exhaustively unit-testable.
  - **CORS / dev-vs-prod origin (`main.ts`).** `app.enableCors({ origin: config.corsOrigins, credentials: true, allowedHeaders: [...,'Last-Event-ID'] })`. DEV is two origins (web :3081 -> api :3080) so the allowlist reflects the web origin (never `*` with credentials, and `Last-Event-ID` is allowed for cross-origin reconnect resume). PROD is the single-origin reverse-proxy posture (pulse-web proxies the API, meld precedent) so the EventSource is same-origin and this CORS allowance is effectively unused; `CORS_ORIGINS` is config-driven so prod can tighten/clear it. `credentials: true` is what lets the Phase 6 cookie-bearing `EventSource(url, { withCredentials: true })` work.
  - **Public-page seed (`scripts/seed-public-page.ts`).** Idempotent dev/verify helper: ensures the demo owner + a `public_status_pages` row with slug `demo`, and maps the owner's `is_public` monitors (or flags the oldest public if none) onto it via the join — deliberately leaving the rest PRIVATE so the redaction boundary is observable. Not the Phase 6 production seed (that is the deterministic frozen-`now` faker seed).
  - **Verify (all green + a real end-to-end smoke):** `pnpm -F pulse-server lint / typecheck / build / test` pass — **93 tests** (79 prior + 7 bridge parse/scope-filter/ring-buffer + 7 public-redaction incl. the assertion that `check.result`/`alert.fired` NEVER reach the public scope regardless of monitor set). **Live smoke (docker up, BOTH processes web+worker):** created a GOOD monitor (`https://example.com`, expect 200 -> records `up`/200, `isPublic:true`) and a FAILING monitor (`https://www.google.com/generate_204`, expect 200 -> records `down`/204, `isPublic:false`); seeded the `demo` public page (good monitor published, bad monitor kept PRIVATE). `curl -N http://localhost:3080/api/stream` streamed live `check.result` frames for BOTH monitors every 30s + `heartbeat` every 15s with `scope:dashboard:<userId>`. `curl -N http://localhost:3080/api/public/demo/stream` carried ONLY `heartbeat` — ZERO `check.result` leaked. Forcing an `up->down` transition on the PUBLIC monitor (PATCH an absent `expectedKeyword`) produced a `status.change` on the dashboard (`scope:dashboard:<userId>`) AND on the public stream re-scoped to `scope:public:<pageId>` with the source `id` preserved — proving the public path carries the redacted subset only. **`Last-Event-ID` replay:** reconnecting with `Last-Event-ID: 7` instantly replayed buffered ids 8,9,10(the status.change),11 oldest-first from the ring, then continued live (12,13) — NO id <= 7 replayed (cursor honored). SSE response headers + the cross-origin CORS preflight (reflects :3081, `Allow-Credentials: true`, `Last-Event-ID` allowed) verified. Test monitors cleaned up after.
- [x] **Backend Phase 2 (Tasks 2.1–2.5) — the prober + scheduler worker, the api-heavy core (2026-06-03, backend-engineer).** The probes now ACTUALLY run, record results, and publish the real-time events the SSE channel (Phase 3) will stream. New `undici@^6` dependency added (Node 22's internal undici is not a public import; the package gives the `Agent` + custom `connect.lookup` the anti-rebinding IP-pin needs). New migration `0001_watery_the_santerians.sql` (applied) adds `monitors.current_status` (nullable `monitor_status`; NULL = the ADR-004 `unknown`/never-checked state) + `monitors.last_checked_at`.
  - **Task 2.1 — probe runner (`src/probe/probe-runner.service.ts`).** Real HTTP(S) runner with undici `request` + `maxRedirections:0` (manual hops, up to 5, re-running the FULL SSRF guard on every hop), `AbortController` per-attempt timeout (cap 30s), 512 KB body cap (stream destroyed past the cap; only the first 512 KB searched for `expectedKeyword`), fixed `Pulse-Probe/1.0` UA, no creds/cookies. **Execution-time SSRF guard is the load-bearing step:** each hop `dns.lookup({all:true})` → `assertResolvedIpsAllowed(EVERY record)` (fail-closed if any is internal) → pin the connection to the validated IP via a per-request undici `Agent` whose `connect.lookup` returns the validated IP in the ARRAY form `cb(null,[{address,family}])` (a bare string yields `ERR_INVALID_IP_ADDRESS`) so a rebind between resolve and connect cannot slip through (Host header + TLS SNI still carry the real hostname). Classification (up/degraded/down + normalised error class) extracted to the PURE `src/probe/probe-classify.ts` so it is exhaustively unit-testable without a network. A failing target = a `down` VALUE (never throws), recorded as a SUCCESSFUL job.
  - **Task 2.2 — result recording + status derivation (`src/checks/check-recorder.service.ts`).** Exactly one `check_results` row per occurrence, then persist the monitor's derived `current_status` + `last_checked_at`, and detect a TRANSITION (a concrete-to-concrete status change — the first-ever check updates `current_status` but is NOT a `status.change`, since the SSE `from` is the concrete up/degraded/down union with no honest `from:unknown`). The full incident state machine + incident rows stay Phase 5; this is the source the engine will consume.
  - **Task 2.3 — worker process split (ADR-006).** Two entrypoints from one image: `src/main.ts` (web — HTTP CRUD + producer, NO processors) and `src/worker.ts` (`createApplicationContext`, NO HTTP — BullMQ workers + rollup/GC). `QueuesModule` is now PRODUCER-only (connection + queue vocabulary, `@Global`); the processors live in worker-only modules `ProbeWorkerModule` (probe) + `RetentionModule` (rollup/gc). `ProbeSchedulerService` split into `ProbeSchedulerModule` so BOTH roles own boot reconciliation without the web controller crossing the split. Scripts: `start:web`/`start:worker` (+ `dev:worker`). **Verified live:** both boot from the same `.env`; worker logs `WorkerModule` init + `0 active/0 orphan` reconcile + retention schedules registered (rollup 300s, gc 3600s) + the immediate rollup/gc runs, NO HTTP; web maps `/monitors` routes + `/health` 200 (db+redis), processes NO jobs.
  - **Task 2.4 — rollup + GC (`src/retention/`, ADR-005).** Rollup every 5 min: a single idempotent `INSERT ... SELECT ... ON CONFLICT (monitor_id,bucket_start) DO UPDATE` recomputing the last 3 hours' buckets from raw (recompute-from-scratch → safe under concurrency, never double-counts; up/degraded/down counts + avg/p95/min/max). GC hourly: BATCHED `DELETE ... WHERE ctid IN (SELECT ctid ... LIMIT 5000)` of raw `check_results` older than 35d (short locks) + rollups older than 400d. Both register as singleton repeatables (`upsertJobScheduler` stable ids `rollup:hourly`/`gc:retention`) in `RetentionSchedulerService.onModuleInit` — worker-only. **Postgres-js gotcha fixed:** a raw `Date` is not reliably bound as a `timestamptz` parameter on the prepared-statement path — pass ISO strings + explicit `::timestamptz` casts (applies to both services).
  - **Task 2.5 — publish to `pulse:events` (`src/events/`, ADR-003).** `EventsPublisherService` (worker-side of the bridge; `@Global` EventsModule) publishes a `check.result` envelope on every check and a `status.change` envelope on a transition, validated against the SHARED `sseEventSchema` at the EMIT boundary, JSON to the Redis `pulse:events` channel. `scope` is `dashboard:<ownerUserId>` (carries the owning user for Phase 3 routing). Fire-and-forget (a publish failure logs, never undoes the committed write — Postgres is the source of truth). Per-process monotonic `id` sequence (the authoritative ring buffer/resume is Phase 3.1).
  - **Verify (all green + a real end-to-end smoke):** `pnpm -F pulse-server lint / typecheck / build / test` pass — **79 tests** (39 existing SSRF + 22 pure classification incl. the down-records-result rule + 10 execution-time/rebinding SSRF cases + the real-HTTP integration suite [up/down/keyword/degraded/timeout/redirect/512KB-cap + redirect-per-hop-reguard-blocks-private] + the rollup idempotency integration test against real Postgres). **Live smoke (docker up, both processes):** created a monitor against `https://example.com` (records `up`/200 on the 30s interval) and one against `https://www.google.com/generate_204` expecting 200 (records `down`/`http_error`/code 204) — results accumulate every 30s, `current_status` persists, a PATCH forcing a missing keyword produced a real `up->down` transition. `redis-cli SUBSCRIBE pulse:events` showed the `check.result` + `status.change` envelopes with correct `dashboard:<userId>` scope. Manual rollup → 2 hourly buckets populated (9 up/1 down for the good monitor; avg/p95/min/max present); GC pruned a planted 40-day-old row (1 deleted, 0 remaining > 35d). **SSRF execution-time guard proven live:** a monitor targeting `http://127.0.0.1.nip.io:9999/` (a public hostname resolving to 127.0.0.1 — passes create-time) recorded `down`/`ssrf_blocked` with `status_code NULL` (NEVER connected) — the anti-rebinding pin works end-to-end. **`.env.example` UNCHANGED** (Phase 2 introduced no new env vars; the worker/web split is a start-command selector, not an env var).
- [x] **Backend Phase 1 (Tasks 1.1–1.5) — backend foundation (2026-06-03, backend-engineer).** `projects/pulse/server/` (`pulse-server`, picked up by the root `projects/*/server` glob). **Module structure** per ADR: `config` (Zod-validated env, fail-fast), `db` (Drizzle), `redis` + `queues` (BullMQ), `health`, `monitors`, `probe` (SSRF guard), plus documented SEAM modules for `checks` (Phase 2), `incidents` (5.1), `alerts` (5.2), `stream` (3.1), `auth` (6.1), `public` (6.2). **Task 1.1:** NestJS 11 (latest stable) on Node 22, TS strict extending root `tsconfig.base.json` (+ `experimentalDecorators`/`emitDecoratorMetadata`), `GET /health` → `{status,commit,ts,db,redis}` validated at the boundary against `healthResponseSchema`, binds `0.0.0.0:3080`. `.env.example` declares every Phase 1–6 ADR-006 var (DATABASE_URL, REDIS_URL, BETTER_AUTH_SECRET, BETTER_AUTH_URL, WEBHOOK_SIGNING_KEY, DEMO_TRIGGER_ENABLED, PORT, NODE_ENV) with per-var intent. **Task 1.2:** `docker-compose.yml` (postgres:17-alpine @5437 + redis:7-alpine @6381, named volumes, healthchecks, `noeviction`); full ADR-005 schema (monitors, check_results with the critical `(monitor_id, checked_at DESC)` index, check_rollups_hourly, incidents with the partial-unique-open-incident index, alert_channels, alert_deliveries with the de-dup unique, public_status_pages + join, users seam stub for better-auth); migration `0000_amused_ulik.sql` generated + applied; tolerant `pingDb()`. **Task 1.3:** `@nestjs/bullmq` wired (probe queue live + rollup/gc queues declared for Phase 2), stub `ProbeProcessor`, `pingRedis()`, graceful pool/connection drain on shutdown; **verified round-trip** (`redis:smoke` enqueues a trivial job → processor logs it; the per-monitor repeatable also fires and is consumed). **Task 1.4:** shared Zod schemas in `server/src/lib/schemas/` (monitor create/update/response, check-result, incident, alert-channel) **AND the SSE event contract `events.ts`** (`{id,type,ts,scope,payload}` envelope + the discriminated union: check.result/status.change/incident.open/incident.close/alert.fired/heartbeat) — types-only re-exported via `src/contract.ts` (+ a dedicated `pulse-server/events` export) for `pulse-web`. **Task 1.5:** monitors CRUD (create/list/get/update/delete, Zod-validated at the boundary, ownership pinned in every WHERE — never trusts client ids), the `reconcileMonitorSchedule()` seam (deterministic `probe:<monitorId>` id, remove-then-add via `upsertJobScheduler`, real boot reconciliation in `OnModuleInit` with orphan sweep — verified live), and the **SSRF guard** `server/src/probe/ssrf-guard.ts` (resolve-then-pin: scheme allowlist, credentialed-URL reject, full IPv4+IPv6 denylist incl. `169.254.169.254` metadata + IPv4-mapped-IPv6 decode + `localhost`/`*.localhost` alias block; `assertResolvedIpsAllowed` exposed for the Phase 2 prober's DNS+per-redirect-hop check). **Verify (all green):** `pnpm install` clean; `pnpm -F pulse-server lint / typecheck / build / test` pass (**39 SSRF unit tests**); docker up → migrate → `curl /health` → 200 with db+redis healthy; BullMQ round-trip processor logs the job; `POST /monitors` with a public URL → 201; `POST /monitors` targeting `localhost` / `127.0.0.1` / `169.254.169.254` / `10.0.0.5` → 400 (SSRF), `ftp://` → 400 (schema). **Phase 2 picks up:** the real probe runner (HTTP execution + execution-time SSRF via `assertResolvedIpsAllowed` + classification + retry/concurrency, 2.1), the worker process consuming `probe:<id>` jobs, the rollup (5min) + GC (hourly) jobs wired to their declared queues, and one-result-one-event publishing to `pulse:events` (2.3).
- [x] **Frontend Task 3.2 — Next 15 / React 19 / Tailwind v4 / shadcn scaffold (2026-06-03, frontend-engineer).** `projects/pulse/web/` (`pulse-web`, picked up by the root `projects/*/web` glob). Next 15 App Router + React 19 + TS strict (extends a new `projects/pulse/tsconfig.base.json` copied from root, mirroring meld/razors-edge), Tailwind v4 (`@tailwindcss/postcss`, no config.js), ESLint flat config, Vitest. `next.config.ts` carries `output: 'standalone'` + the meld Windows-standalone fix (`outputFileTracingRoot` via `fileURLToPath(new URL('../', import.meta.url))`, try/catch fallback) and the full strict security-header set (CSP **no `unsafe-eval`** + HSTS/X-Frame-Options DENY/nosniff/Referrer-Policy/frame-ancestors none/base-uri self/form-action self). `next-themes` `defaultTheme: 'system'` (`attribute="class"`), TanStack Query (live-data-tuned: generous staleTime, no window-focus refetch — freshness comes from SSE in Phase 3, not polling) + Zustand + RHF + Zod + Motion all installed. shadcn/ui new-york primitives authored: button, card, input, badge, dialog, dropdown-menu, tooltip (individual `@radix-ui/*` packages). **Sovereign clean-SaaS token set** in `globals.css` (light-canonical + intentional dark; cool-neutral foundation + indigo brand + load-bearing status semantics up/degraded/down/unknown as first-class DOT+TEXT tokens, AA-verified in both themes — NO reuse from tape/meld/razors-edge). Typography: Geist Sans (UI) + Geist Mono (metrics) via `next/font`, both OFL. App shell: a dashboard segment (left rail + top bar + board-region placeholder rendering the status tokens) and a marketing/landing placeholder (header + hero + status legend). `.env.example` declares `NEXT_PUBLIC_API_URL` + `NEXT_PUBLIC_SSE_URL` + `NEXT_PUBLIC_SITE_URL` with the v2 single-origin reverse-proxy intent (meld pattern) documented. Zod jitless side-effect wired at the layout root (CSP-clean for the runtime SSE-envelope validation, razors-edge fix). **Verify: `pnpm install` clean; `pulse-web` typecheck + lint + 7 vitest tests + prod `next build` all pass; dev + `next start` both serve `/` and `/dashboard` → 200; prod CSP string verified present with no `unsafe-eval` (recorded in AGENT_NOTES).**
- [x] Architect pass (2026-06-03): ADR-002..ADR-006 appended to `DECISIONS.md` (all accepted). ADR-002 pins the BullMQ repeatable-job-per-monitor design with stable scheduler id `probe:<monitorId>`, remove-then-add reconciliation + `OnModuleInit` boot reconciliation against the DB, worker concurrency 20 + limiter, idempotent one-result-per-probe (a down endpoint is a successful job recording `down`, BullMQ retries reserved for infra faults), and the mandatory resolve-then-pin SSRF guard (scheme allowlist + credentialed-URL reject + resolve-then-validate-then-connect against the full private/loopback/link-local/metadata/IPv4-mapped-IPv6 denylist, run at both create-validation and probe-execution). ADR-003 pins the SSE event vocabulary (`check.result` / `status.change` / `incident.open` / `incident.close` / `alert.fired` / `heartbeat`), the shared Zod envelope (`src/lib/schemas/events.ts`), two scoped `@Sse()` routes (cookie-auth dashboard + unauthenticated public exposing strictly less), the worker->SSE Redis Pub/Sub bridge over channel `pulse:events` (dedicated ioredis subscriber -> RxJS subject -> scope filter), and reconnect/heartbeat(15s)/coalescing-backpressure. ADR-004 pins the pure-reducer incident state machine (N=3 fail -> open exactly one, M=2 success -> close, degraded-vs-down escalation, N/M flap suppression, single-open-incident invariant) and hybrid uptime (24h from raw, 7d/30d from hourly rollups, degraded=50%, unknown-gap exclusion). ADR-005 ratifies the Drizzle schema + critical `(monitor_id, checked_at DESC)` index + DB-level correctness constraints (partial-unique open incident, alert-delivery de-dup unique), 35d-raw + hourly-rollup retention with a BullMQ rollup (5min) + GC (hourly) job, two small `postgres-js` pools, and the single `AlertDispatcher` interface (webhook REAL/HMAC-signed, email MOCKED). ADR-006 pins Fly topology: `pulse-api` two `[processes]` (web+worker, warm floor `min_machines_running=1`/`auto_stop=off`), separate `pulse-web` Next app proxying the API, Upstash Redis + Fly Postgres in `fra`, the owned `/demo/flaky` + Redis-flagged `POST /demo/trigger` demo mechanism, and seed-once-at-deploy / live-from-now coexistence.
- [x] Planner pass (2026-06-03): `PLAN.md` (pitch, dual audience, named wow moment, api-heavy justification — four § 10 triggers, NestJS backend justification + variance, SSE-vs-WebSocket pick, uPlot + SVG charting pick, Motion animation pick, success criteria incl. Lighthouse ≥ 95 on the public page + WCAG 2.2 AA + 320 px + SEO/OG/JSON-LD/sitemap/robots + light/dark + reduced-motion + security incl. SSRF guard + rate limiting + CSP, full surface/flow spec, data-model sketch, phased task list Phase 0–9, out-of-scope, deferred-to-architect list); `DECISIONS.md` ADR-001 (ratifies api-heavy + NestJS + BullMQ/Redis + SSE + uPlot + Motion + sovereign tokens; defers ADR-002..006); this `PROGRESS.md`; `AGENT_NOTES.md`.
- [x] Root `PROGRESS.md` composition tracker updated — Pulse added as slot 4, api-heavy, NestJS. Composition status updated: 3 api-heavy across 3 distinct backends (Elysia / Hono / NestJS), § 12 target satisfied with margin.

## In progress

- Nothing in flight. **ALL FEATURE PHASES ARE COMPLETE — the product is feature-complete for the Phase 7 review.** Backend Phase 1/2/3.1/4/5 + the Phase 5 incidents-READ surface + Backend Phase 6 (better-auth + the public read surface + the demo-open posture, ADR-007) + Frontend Task 3.2/3.3/3.4/3.5 + Frontend Phase 4 + Frontend Phase 5 + **Frontend Phase 6 (auth UI + session header + demo gating + public status page + SEO + landing, Tasks 6.4-6.7)** are ALL done. Auth is real and the demo stays open END-TO-END (an unauthenticated visitor reads the demo workspace + fires the demo incident; every write affordance prompts a tasteful sign-in; a real user gets a private workspace; the public `/status/[slug]` page SSRs the redacted surface + live-updates + carries the SEO/OG/JSON-LD/sitemap/robots surface). The remaining work is review + test + deploy:
  - **`designer-critic`** (Phase 7.1) — the LIVE BOARD + the MONITOR DETAIL + the Phase 5 incidents/alerts surfaces + **the new AUTH UI (sign-in dialog + session header + demo banner + the New-monitor sign-in prompt) + the PUBLIC STATUS PAGE + the LANDING** in light + dark + mobile against Linear / Vercel / Stripe (zero pochwał). Screenshots in `web/.review/` (`p6-*` for Phase 6).
  - **`reviewer`** (Phase 7.2) — the backend security surface (owner seam, FK reconciliation, cookie/CORS posture, public redaction, the two rate limiters) AND the new frontend surface (the PURE `deriveAuthState`/`gateMutation` gating logic, the `useGatedAction` seam covering every write affordance + the 401 onError fallback, the public-page SSR + the `next/og` route + the JSON-LD-from-redacted-payload, the SEO routes).
  - **`test-engineer`** (Phase 8) — Vitest is already strong (114 web + 168 server); Phase 8.2 E2E incl. the auth-boundary assertion the success criteria require (unauth mutation rejected, unauth read open, public page no-auth), Phase 8.3 Lighthouse >= 95 on `/status/[slug]`.
  - **Phase 9** — docs (README incl. the auth note + the seed-vs-live + mocked-email boundary) + Fly deploy (set `BETTER_AUTH_URL` to the proxied API origin, `fly secrets` for `BETTER_AUTH_SECRET`, `NEXT_PUBLIC_DEMO_STATUS_SLUG`, run `seed-public-page.ts` with the real `DEMO_FLAKY_URL`).

## Next

**Backend track (`backend-engineer`) — Phase 1 + Phase 2 DONE; Phase 3.1 (SSE) is next:**

1. ~~Task 1.1–1.5~~ **DONE (2026-06-03).** See the Done section.
2. ~~Task 2.1–2.5 (prober + worker split + rollup/GC + event publish)~~ **DONE (2026-06-03).** See the Done section. Verified live end-to-end (real probes record up/down on the interval, status transitions persist, `pulse:events` carries `check.result` + `status.change` with `dashboard:<userId>` scope, rollup populates buckets, GC prunes >35d rows, the execution-time SSRF guard blocks a nip.io rebind at probe time recording `ssrf_blocked` without connecting). 79 server tests green.
3. ~~Task 3.1 — the SSE routes + the worker->SSE Redis bridge~~ **DONE (2026-06-03).** See the Done section. Verified live end-to-end: dashboard stream carries `check.result` + `status.change` + `heartbeat` (`scope:dashboard:<userId>`); public stream carries ONLY the redacted subset (`status.change`/`incident.*`, never `check.result`/`alert.fired`, re-scoped `public:<pageId>`); `Last-Event-ID` replays the ring buffer; CORS works cross-origin with credentials.
4. ~~Phase 5 (incident engine + alerts + demo trigger)~~ **DONE (2026-06-04).** See the Done section. The reducer hooks `CheckRecorderService.record()` via `IncidentEngineService.onCheckRecorded()`; `publishIncidentOpen`/`publishIncidentClose`/`publishAlertFired` flow through the existing bridge to the dashboard (and `incident.*` to the public stream) with no Phase 3 changes. The webhook is REAL+HMAC-signed, email MOCKED, de-dup at the DB. The `/demo/flaky` + `/demo/trigger` routes are live. Full real arc proven (open->signed webhook->close).
5. ~~Phase 6 backend (better-auth + the public read surface + the demo-open posture, ADR-007)~~ **DONE (2026-06-04).** See the Done section. better-auth wired (`/api/auth/*`), the `CurrentOwnerService` seam (reads -> session-user-else-demo; writes -> session-user-else-401), the FK reconciliation (additive migration 0002, uuid kept), the dev-vs-prod cookie posture (works for SSE `EventSource`), better-auth's brute-force rate limit + the public-endpoint `RateLimitGuard`, and `GET /public/:slug` (the redacted public surface). 168 tests. Proven live end-to-end. **Backend Phase 6 is complete; the remaining Phase 6 work is the FRONTEND (auth UI + public page + SEO).**
6. **Phase 9 backend (deploy) — the remaining backend touch.** `fly secrets set BETTER_AUTH_SECRET=...`; set `BETTER_AUTH_URL` to the proxied API origin (the public web origin if the proxy rewrites `/api` -> the API); the seed-once-at-deploy runs `seed-public-page.ts` with the real `DEMO_FLAKY_URL`.

**Frontend track (`frontend-engineer`):**

1. ~~**Task 3.2 — Next scaffold**~~ **DONE (2026-06-03).**
2. ~~**Task 3.3 + 3.4 + 3.5 — the live status board (the wow moment)**~~ **DONE (2026-06-03).** See the Done section. The board reacts live to real probes, the create flow goes live, zero CSP violations, all proven headlessly. The `MonitorStatus` types-only gate is consumed; the env-driven `connect-src` is in place.
3. ~~**Task 3.4-demo / Phase 5 frontend (incidents view + board indicators + alerts config + the demo button)**~~ **DONE (2026-06-04).** See the Done section. The demo button plays the arc on demand, the `alert.fired` toast fires off the threaded `onAlertFired` sink, the live incident strip + summary + nav badge react, the incidents view + alerts page are live. Zero CSP violations, one SSE connection, all proven headlessly.
4. ~~**Phase 4 (monitor detail + uPlot charts)**~~ **DONE (2026-06-04).** See the Done section. The detail route is live (uptime cards + the imperative uPlot chart with 24h live-append + the 90-bucket SVG history bar + the recent-checks list + edit/pause/delete wired to the CRUD), proven headlessly in both themes with ONE SSE connection and ZERO CSP violations. 65 vitest tests green (25 new on the two pure data adapters).
5. **Phase 6 frontend (public status page + auth UI + demo gating + SEO) — NEXT (backend Phase 6 is LANDED).** What the frontend consumes (all LIVE):
   - **Auth UI** — `POST /api/auth/sign-up/email` (`{name,email,password}`), `POST /api/auth/sign-in/email` (`{email,password}`), `POST /api/auth/sign-out`, `GET /api/auth/get-session`. ALL with `credentials:'include'` (the session cookie). A POST must carry the browser `Origin` header (better-auth CSRF — the browser sends it automatically; a fetch from the same app does too). Sign-up/in set the `better-auth.session_token` cookie; get-session returns `{ user, session } | null`. The dashboard becomes the signed-in user's PRIVATE workspace; signed-out it shows the SHARED demo workspace (no redirect-to-login — the demo board stays viewable).
   - **Demo gating (the tasteful prompt)** — the WRITE affordances (create monitor, edit/pause/delete, create/delete alert channel) hit endpoints that return `401 { error:'authentication_required', message:'Sign in to manage monitors and alert channels.' }` when unauthenticated. Surface that as a tasteful "sign in to manage" prompt on those affordances for a demo visitor (the board / detail / incidents / the demo-trigger button all still work unauthenticated). Detect auth state via `GET /api/auth/get-session` (a TanStack Query, `credentials:'include'`).
   - **Public page** — `GET /public/:slug` -> `{ slug, title, description, overall:'operational'|'degraded'|'outage', generatedAt, monitors:[{id,name,status,uptimePercent}], incidents:[{id,monitorId,monitorName,status,severity,startedAt,resolvedAt,durationMs,cause}] }` (types via `pulse-server`: `PublicStatusPage`/`PublicMonitor`/`PublicIncident`). SSR-friendly (cheap, rollup-backed), no auth, NO raw response times / alert data / private monitors. Build `/status/[slug]` against it + the public SSE stream (`GET /api/public/:slug/stream`, already redacted) for live updates; OG image + JSON-LD + sitemap + robots + canonical, Lighthouse >= 95. The nav's "Status page" + "Settings" placeholders route here.

## Handoff

- **PHASE 5 FRONTEND CONSUMES (the endpoints + events are LIVE — `frontend-engineer` builds Task 3.4-demo / 5.3 against these):**
  - **The demo button** -> `POST /demo/trigger` (no body) -> `{ armed:true, failWindowSeconds:35, note }`. Optionally poll `GET /demo/status` -> `{ failing, recoversInSeconds }` for a "recovering in Ns" countdown. The button just flips the flag; the incident opens/closes ORGANICALLY through the real probe cycle, so the board reacts via the SSE events it already handles. **The demo monitor + timing are FINALIZED:** `scripts/seed-demo-monitor.ts` seeds a PUBLIC demo monitor at **interval 15s / N=2 / M=1**, targeting `${DEMO_FLAKY_URL}` (default `/demo/flaky`); with the 35s fail window the full arc plays in ~30-45s (open ~30s, close ~45s). The 15s interval is below the CRUD 30s floor on purpose — the demo monitor is SEEDED directly, the documented exception. Set `DEMO_FLAKY_URL=https://<pulse-api-host>/demo/flaky` at deploy; locally it defaults to loopback (SSRF-blocked at probe time, the seed prints a NOTE — point it at a public/tunneled host for a faithful local arc).
  - **The alert-fired toast** -> the board's `onAlertFired` sink is already threaded; wire a shadcn/sonner toast to the `alert.fired` event payload (`{ incidentId, monitorId, channelType, transition, deliveredAt, status }`). It is DASHBOARD-SCOPE ONLY (never public) — confirmed live.
  - **The live incident row + history** -> `incident.open` (`{ incidentId, monitorId, severity, startedAt, cause }`) and `incident.close` (`{ incidentId, monitorId, startedAt, resolvedAt, durationMs }`) flow on `/api/stream`; the store already toggles `hasOpenIncident` (the card ring). A top-of-board incident-row surface + an incident-history list (PLAN Task 5.3) is the remaining UI. **The REST read endpoints ARE now built (2026-06-04):** `GET /incidents?limit=N&status=open|resolved` (cross-monitor, for the dashboard incidents view) and `GET /monitors/:id/incidents?...` (one owned monitor, 404 on a foreign id, for the detail page's incident history). Both return `{ monitorId, items: [{ id, monitorId, monitorName, monitorUrl, status, severity, startedAt, resolvedAt, durationMs, cause }] }`, newest-first; `durationMs` is resolved-duration for a closed incident or still-accruing (`now - startedAt`) for an open one (so the live row can count up off a real number). Types via `pulse-server` (`IncidentListItem`/`IncidentsListResponse`). The board can keep the live row in sync from the SSE `incident.*` events and seed/reconcile the list from these REST reads on mount + reconnect.
  - **Alerts config view** -> `POST /alert-channels` (`{ type:'webhook'|'email', target, secret?, isEnabled? }` — the shared `createAlertChannelSchema`), `GET /alert-channels` (`{ id, type, target, isEnabled, createdAt }` — secret NEVER returned), `DELETE /alert-channels/:id` (204). The webhook `secret` is the per-channel HMAC key; document the verify scheme (`X-Pulse-Signature`/`X-Pulse-Timestamp`) in the README for anyone wiring a receiver.
- **Next subagents:** `reviewer` should focus on the Phase 5 surface: the PURE reducer (the ADR-004 invariants — single-open, N/M debounce, escalation, worst-across-run), the engine's DB rehydration + the partial-unique-violation-swallow on concurrent open, the de-dup claim-slot-first order in `AlertsService`, the HMAC signing scheme + the unsigned-refusal, the email-mock honesty, and the web/worker module split (the engine + dispatch are WORKER-side, the CRUD + demo are WEB-side, so the web never pulls `EventsPublisherService`). `designer-critic` should now review the LIVE BOARD (cards, summary bar, connection indicator, create dialog, empty/loading/error states) AND the NEW MONITOR DETAIL PAGE (header + actions, the three uptime cards + breakdown bars, the uPlot response-time chart in raw 24h + hourly 7d/30d, the 90-bucket history strip, the recent-checks list) in light + dark + mobile against Linear (alive / micro-interaction timing) + Vercel (dense-dark restraint) + Stripe (premium finish) — board screenshots `web/.review/01`–`06`, detail screenshots `web/.review/detail-{24h,30d}-{dark,light}.png` + `detail-live-append-dark.png`. KNOWN: the 30d history strip reads very red on the verify fixture (uniform synthetic failures + worst-status-wins on wide 8h bars — a fixture artifact, NOT the component; the Phase 6 seed should cluster failures). `reviewer` should additionally cover the uPlot imperative wrapper (instance lifecycle — created once / `setData` / `ResizeObserver` / `destroy`; the live append off the React render path) and the detail CRUD wiring (pause = real `isPaused` PATCH, delete = 204 + navigate). `backend-engineer` starts **Phase 5** (incident engine + alerts) — the SSE relay/redaction for `incident.*`/`alert.fired` is ALREADY wired AND the FRONTEND BOARD already consumes them (store handles incident open/close, `onAlertFired` sink threaded for a toast), so Phase 5 only needs to PUBLISH. `reviewer` should focus on the Phase 2/3.1 backend security surface AND the new frontend surface (the SSE client lifecycle / one-connection discipline, the defensive client Zod mirrors pinned via `satisfies`, the env-driven CSP `connect-src`, the create-flow double-parse fix). `frontend-engineer` picks up **Task 3.4-demo** (once Phase 5 + the demo routes exist) then **Phase 4** (monitor detail + uPlot). All subagents read `PLAN.md`, `DECISIONS.md` (ADR-001..ADR-006), `PROGRESS.md`, and `AGENT_NOTES.md` on start.
- **~~A BACKEND GAP TO CLOSE~~ CLOSED (Phase 4.1, 2026-06-04, backend-engineer).** `GET /monitors` (list AND get) now serializes `currentStatus` / `lastCheckedAt`. `monitorResponseSchema` carries `currentStatus: monitorStatus.nullable()` + `lastCheckedAt: string.datetime().nullable()`; `serializeMonitor` populates them. The board now paints the persisted status on first load / reload instead of `unknown` for ~1 interval. The web `MonitorRow` was already shaped for it; `pulse-web typecheck` confirms the contract is in sync.
- **PHASE 4 FRONTEND CONSUMES (the read endpoints are LIVE — `frontend-engineer` builds the detail page against these):** `GET /monitors/:id/uptime|series|history|checks` (all `?window=24h|7d|30d`, checks takes `?limit=N`). Shapes documented in the Phase 4 Done entry above + in `server/src/lib/schemas/monitor-detail.ts` (the shared contract — import the types via `pulse-server`). The **series** shape is uPlot-ready (`[t, value]` for 24h raw / `[t, avg, p95]` for 7d/30d hourly) — feed it straight into `setData` off the React render path; the **history** is 90 worst-status bars for the hand-rolled SVG strip; the **uptime** carries the breakdown counts behind the % for a tooltip; **checks** is the recent-activity list. The 7d/30d series/history/uptime read the rollup (cheap), 24h reads raw. A NOTE on history with synthetic-uniform seed data: worst-status-wins makes a bar `down` if ANY check in its (8h for 30d) span was down, so an evenly-spread synthetic failure rate paints many bars red — real outages cluster, so this is a fixture artifact, not a bug.
- **Cross-track dependency (CLEARED + LIVE END-TO-END):** the worker publishes `check.result`/`status.change` to `pulse:events` (scope `dashboard:<userId>`), the web process's `EventsBridgeService` (dedicated ioredis subscriber) relays into an RxJS subject + 256-event ring, and the two `@Sse()` routes stream to clients (dashboard full-fidelity by scope; public redacted by `redactForPublic`). The frontend swaps the scaffold's placeholder `MonitorStatus` union for the real type and points an `EventSource` at `/api/stream`. The web/worker split is real — they share the Redis channel, not an in-memory subject.
- **Run the FULL stack (local, to see the live board):** `docker compose -f projects/pulse/docker-compose.yml up -d`; from `projects/pulse/server/` with `.env` present: `pnpm start:web` (3080) + `pnpm start:worker`. Then for the WEB: because local dev is CROSS-ORIGIN (web :3081 -> api :3080), the CSP `connect-src` must permit the API origin — build + run the web with the API env set so it is baked into the CSP: from `projects/pulse/web/`, `NEXT_PUBLIC_API_URL=http://localhost:3080 NEXT_PUBLIC_SSE_URL=http://localhost:3080 NEXT_PUBLIC_SITE_URL=http://localhost:3081 pnpm build && (same env) pnpm start`. The CSP then reads `connect-src 'self' http://localhost:3080` and the board's `EventSource` connects. (In the deployed single-origin proxy topology the env points at the same origin, so the CSP collapses to `connect-src 'self'` — no change needed.) `pnpm dev` works too (HMR uses eval which a no-`unsafe-eval` CSP flags, but that is a dev-only artifact — the authoritative zero-violation claim is the prod build, verified). Watch the channel: `... exec redis redis-cli SUBSCRIBE pulse:events`. Streams: `curl -N http://localhost:3080/api/stream`.
