{
  "$comment": [
    "Lighthouse CI config for razors-edge (Phase 6, Task 6.3).",
    "Audits the BUILT + SERVED production surface on :3070 (next start, NOT",
    "next dev — the strict CSP forbids unsafe-eval and next dev cannot run",
    "under it). Asserts the PLAN.md / CLAUDE.md § 4 success criteria: >= 95 in",
    "all four categories on the hero (/) AND the booking flow (/book), median",
    "of 5 runs, plus the Core Web Vitals targets on the hero (LCP < 2.5s,",
    "CLS < 0.1). The desktop preset matches the recruiter audience (PLAN.md).",
    "Shared budget vocabulary mirrors .github/lighthouse-budget.json."
  ],
  "ci": {
    "collect": {
      "startServerCommand": "pnpm -F razors-edge-web start",
      "startServerReadyPattern": "Ready in|started server on|Local:",
      "startServerReadyTimeout": 60000,
      "url": ["http://localhost:3070/", "http://localhost:3070/book"],
      "numberOfRuns": 5,
      "settings": {
        "preset": "desktop"
      }
    },
    "assert": {
      "assertions": {
        "categories:performance": ["error", { "minScore": 0.95 }],
        "categories:accessibility": ["error", { "minScore": 0.95 }],
        "categories:best-practices": ["error", { "minScore": 0.95 }],
        "categories:seo": ["error", { "minScore": 0.95 }],
        "largest-contentful-paint": ["error", { "maxNumericValue": 2500 }],
        "cumulative-layout-shift": ["error", { "maxNumericValue": 0.1 }],
        "total-blocking-time": ["warn", { "maxNumericValue": 200 }]
      }
    },
    "upload": {
      "target": "temporary-public-storage"
    }
  }
}
