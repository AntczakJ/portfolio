{
  "$comment": [
    "Lighthouse CI config for atrium (Phase 6, Task 6.3).",
    "Audits the BUILT + SERVED production surface on :3080 (next start, NOT",
    "next dev — the strict CSP forbids unsafe-eval and next dev cannot run",
    "under it; the meld lesson carried in atrium AGENT_NOTES). atrium is the",
    "portfolio's front door — the most-judged page — so it asserts >= 95 in ALL",
    "FOUR categories on the landing page (/) with NO scoped exception (unlike",
    "tape's documented live-route carve-out), median of 5 runs, plus the Core",
    "Web Vitals targets: LCP < 2.5s, CLS < 0.1, and a TBT proxy for INP < 200ms.",
    "The desktop preset matches the recruiter audience (PLAN.md)."
  ],
  "ci": {
    "collect": {
      "startServerCommand": "pnpm -F atrium-web start",
      "startServerReadyPattern": "Ready in|started server on|Local:",
      "startServerReadyTimeout": 60000,
      "url": ["http://localhost:3080/"],
      "numberOfRuns": 5,
      "settings": {
        "preset": "desktop"
      }
    },
    "assert": {
      "assertions": {
        "categories:performance": ["error", { "minScore": 0.95 }],
        "categories:accessibility": ["error", { "minScore": 0.95 }],
        "categories:best-practices": ["error", { "minScore": 0.95 }],
        "categories:seo": ["error", { "minScore": 0.95 }],
        "largest-contentful-paint": ["error", { "maxNumericValue": 2500 }],
        "cumulative-layout-shift": ["error", { "maxNumericValue": 0.1 }],
        "total-blocking-time": ["error", { "maxNumericValue": 200 }]
      }
    },
    "upload": {
      "target": "temporary-public-storage"
    }
  }
}
