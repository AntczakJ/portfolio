# atrium — Progress

> Read on start. Write on end.

## State

- **Phase: CRAFT-RESIDUALS PASS — the two OPTIONAL LOW residuals (N-01, N-02) from `verify-polish.md` CLOSED (frontend-engineer, 2026-06-12).** Surgical, inside every ADR/hard constraint (single-library GSAP, no Motion; transform/opacity/clip-path/filter + transient will-change only; strict eval-free production CSP untouched; no new deps; both themes AA; reduced-motion lands on a clean composed frame; 320px no overflow; English-only). The five cleared blockers (B-01 distinct layouts / B-02 hue bathes / B-03 descent / B-04 light theme / B-05 desktop hero) did NOT regress, and the already-excellent directory/about were not touched. **RESIDUAL 1 — D-15 (was PARTIAL) the kinetic title resolve now commits to a visible clip WIPE.** A third layered real-text copy of each bay title (`data-bay-title-edge`, the word in its BRIGHT signature hue with a hue glow, aria-hidden, invisible at rest) rides the clip front across the word as the title resolves — a travelling bright-edge clip wipe LAYERED with the existing 320→600 weight/opsz settle. The ghost + final + edge now share ONE wipe axis/direction (so the eye reads a single front sweeping the word, not two competing crossfades): wall bays (left/right layouts) wipe HORIZONTALLY in their reveal direction, the centred-specimen bays wipe VERTICALLY (a curtain rising) — varies per layout off the same ordinal the layout selector uses (D-16 consistency). It still resolves to a fully legible `<h2>` and composes cleanly under reduced-motion (edge band visibility:hidden + opacity 0 at rest — verified across all six bays; nothing frozen mid-wipe). **RESIDUAL 2 — the B-03 LOW back-third residual: late scroll now produces clearly perceptible change.** Camera-Z ease stepped `power2.in → power3.in` (steeper accel — front+middle untouched/gentle, the LATE third buys clearly more visual travel); camZ reach lifted (desktop 900→1000, mobile 620→720) so the steeper curve nets MORE late motion; the pin trimmed further (desktop 1.0→0.85vh, mobile 0.8→0.7vh) so there is no soft tail; and the bay-1 hand-off second beat strengthened to OWN the back third (starts 0.56, yPercent 40→64 travel + a 1→1.08 scale-up, taller/brighter doorway) so the lit threshold visibly sweeps up into frame across the last third. Floor-pool bloom kept OPACITY-ONLY (the floor owns a CSS-3D rotateX — a GSAP scale would clobber it; the columns-warning trap avoided). **Files changed:** `web/src/app/globals.css` (the `[data-bay-title-edge]` resting-hide rule), `web/src/components/bays/project-bay.tsx` (the edge copy in the title stack), `web/src/components/bays/bays-sequence.tsx` (the shared-axis wipe + the travelling edge tween + per-layout wipe axis), `web/src/components/hero/hero-descent.tsx` (power3.in camera + camZ/pin tune + strengthened hand-off). New: `e2e/capture-craft-residuals.mjs` (the residual-frame capture driver). **VERIFIED under `pnpm -F atrium-web build` + `next start` :3080 (headless chromium, `e2e/verify-polish.mjs`):** 7 pin-spacers desktop; 0 CSP violations; 0 console/page errors; 0 non-favicon req failures; reduced-motion = 0 pins + all six bay titles resolved (visibility visible / opacity 1 / clip none) AND all six edge bands hidden (visibility hidden / opacity 0 — nothing frozen mid-wipe); 320px scrollW==clientW (no overflow). `typecheck` + `lint` clean (project + ROOT strict eslint on the four touched files, exit 0). **Vitest 54/54. Playwright 20/20** via the harness-owned flipped-GITHUB_BASE webServer build. Fresh frames (title-resolve ladder ×6 + descent back-third ×5, dark+light, + regression spot-checks of the hero/three-layout bays) in `docs/craft-residuals-shots/` (26 frames). Both residuals confirmed visually: the title wipe shows a bright hue leading edge travelling across the word frame-to-frame (dark+light); the descent back-third frames 03/05 are clearly distinct (camera rushed deeper + the hand-off doorway risen) — no longer the gentle near-identical tail. Bottom line: the two optional craft residuals are closed; nothing blocking remained and nothing was introduced.

- **Phase: POLISH PASS VERIFIED on the LIVE site (designer-critic, 2026-06-11).** Verification of the cohesion+polish pass against `critique-polish.md`, judging the DEPLOYED https://atrium-demo.fly.dev (Playwright + chromium, 1440x900 + 390x844, no-preference motion, networkidle+2.8s, full small-increment scroll; both themes; 59 frames in `docs/verify-polish-shots/`, the AFTER set in `critique-polish-after/` left untouched). **All five leads CLEARED:** B-01 six bays are now three distinct layouts (left / right-mirrored / centred-specimen) cycling deterministically off the ordinal, bound by one system; B-02/D-10 the hue BATHES each room full-frame (reaches the type band + lower area), no longer a top halo; B-03/D-13/D-14 the descent back-third reads eased/designed (columns advance+spread, floor brightens through 03->06) — no longer dead scroll (good, on the subtle side); B-04/D-12/D-21 the light theme is its own lit composition (wordmark inks heavy with a warm shaft core; the six hues read distinct as floor washes on cream; the descent is a sunlit limestone hall); B-05/D-20 the desktop hero now earns five seconds (wordmark ~60% frame, both colonnades in perspective, shaft on the letters) matching the mobile drama. **Spot-checks hold:** D-09 (meld red-brown vs razors olive-gold, separated), D-18 (wow-note promoted to a hue-ruled pull-quote), D-19 (Klim ruled hue-ticked stack), D-22 (mobile bays carry a visible hue floor wash), D-08 (bay-1 hand-off echoes the colonnade); D-15 (kinetic title is a legible weight-settle gesture — PARTIAL, still subtle vs Aristide Benoist). **Regressions: NONE.** Directory + about UNCHANGED/first-class; threshold+atrium-echo additions are CSP/console-clean (0 CSP/console/page/req errors across all six driven contexts); reduced-motion = 0 pins + all 6 titles resolved; 7 pin-spacers desktop / 1 mobile; 320px scrollW==clientW (0 overflow). Two LOW residuals only (N-01 back-third travel still gentle; N-02 title resolve short of a committed clip wipe) — optional future craft, NOT blocking. Verdict file: `docs/verify-polish.md`. Driver: `e2e/drive-verify-polish.mjs` (clone of the capture script pointed at the live URL + diagnostics). **Bottom line: the polish is a genuine improvement; nothing is blocking.**

- **Phase: POLISH PASS — the "cohesive and prettier" critique IMPLEMENTED (frontend-engineer, 2026-06-10).** Resolved the `critique-polish.md` lead-5 BLOCKERS/HIGH + the cheap-and-high-value MEDIUMs, all inside the ADRs (GSAP-only, no Motion; strict eval-free CSP; no new deps; both themes AA; reduced-motion clean; 320px no overflow). **What changed:** **B-01** the six bays are no longer one template recoloured — `project-bay.tsx` now selects one of THREE distinct room layouts (left / right-mirrored / centred-specimen) + a per-room light-source direction (top-right / top-left / overhead) DETERMINISTICALLY off the bay ordinal, so the tour is six distinctly-composed rooms bound by one system (rhythm, hue, type), not one screen ×6. **B-02 / D-10 / D-12** the hue now BATHES the room — a four-layer light-field (broad ambient hue tint + the directional source + a tall floor pool rising to ~88% behind the type + a centre lift) floods the whole bay via a new deep `--bay-*-floor` token, so the title stands IN coloured light on BOTH grounds (the light theme floors are tuned for cream — the six hues stay distinguishable, no longer one warm haze). **B-03 / D-13 / D-14** the descent back third is no longer dead scroll — the camera Z rides `power2.in` (equal scroll buys more late travel under the fixed perspective), the pin is shortened (1.0vh desktop / 0.8vh mobile), and a real SECOND BEAT (the bay-1 hand-off threshold blooming up from below — also D-08) fills the back third; the whole descent now rides eased sub-curves (`power4.out` default / `expo.out` reveals, ≈ cubic-bezier(0.16,1,0.3,1)) so it reads designed, not linear. **B-04 / D-21** the light theme is authored as its own composition — the wordmark ink deepened (0.27→0.22 L) + resolves heavier (560 in light) + a VISIBLE warm shaft core (was an almost-white amber that vanished on cream) so it reads LIT, not bleached. **B-05 / D-20** the desktop hero now earns five seconds — the wordmark clamp opened (12vw / 12.5rem ceiling so ATRIUM fills the frame), the colonnade resting opacity lifted (0.5→0.68 desktop) + deeper column contrast, a layered warm glow bringing the shaft onto the letters. **D-07 / D-17** bay vertical rhythm tightened (tagline pulled close to the title + ruled-tick bound, the orphan gap closed). **D-09** meld (deep orange, L 0.72 / C 0.15 / h 38) and razors-edge (pale gold, L 0.84 / C 0.115 / h 96) separated in lightness + chroma, not just hue angle — no longer twin muddy browns. **D-15** the kinetic title resolve is a visible gesture now (thin 320 → bold 600 weight settle + a per-bay clip wipe direction, was a near-invisible 320→440). **D-16** the reveal varies per bay (wipe direction, reveal axis y/x, stagger amount — all deterministic). **D-18** the wow-note PROMOTED to a clear hue-ruled pull-quote out-ranking the stack (was a tiny far-right label). **D-19** stack chips restyled as a Klim ruled hue-ticked spec list (were generic bordered pills). **D-22** mobile bays carry the visible hue floor wash now (the bathe layers survive at phone width). **D-23** the focus ring adopts the active bay hue inside a bay (cohesion). Directory + about UNTOUCHED (explicitly exempt — they are the bar). **Re-keyed hue contrast (WCAG 2.1 lum, both themes) — all `-text` ≥ 4.5, all base ≥ 3:** see the AGENT_NOTES "frontend-engineer — polish pass" table. **VERIFIED under `pnpm -F atrium-web build` + `next start` on :3080 (headless chromium):** 7 pin-spacers desktop; 0 CSP violations; 0 console/page errors; reduced-motion = 0 pins + all six titles resolved (visible/opacity 1/clip none); 320px zero horizontal overflow. `typecheck` / `lint` clean (web + root strict). Vitest 54/54; **Playwright 20/20** with the harness-owned flipped-GITHUB_BASE build (the 7 "failures" seen when pointing BASE_URL at a non-flipped local build are the env-baking artifact documented in the prior State entry, NOT a regression). New AFTER comparison shots (both themes, desktop + mobile 390x844) in `docs/critique-polish-after/` (56 frames) via the updated `e2e/capture-critique-polish.mjs`; the verification driver is `e2e/verify-polish.mjs`. NOTE: the designer-critic's BEFORE shots (`docs/critique-polish-shots/`) were never committed and were inadvertently overwritten by the first capture run before being moved — `critique-polish.md` is the authoritative before-record (it describes every frame by filename).
- **Phase: DEPLOYED — v1 COMPLETE + LIVE; E2E REALIGNED TO THE FLIPPED GITHUB_BASE (test-engineer, 2026-06-10).** The repo is now PUBLIC + pushed and production bakes `NEXT_PUBLIC_GITHUB_BASE=https://github.com/AntczakJ/portfolio` (Dockerfile ARG + fly.toml [env]), so `REPO_LINKS_LIVE` is true live — six live per-project repo `<a>`s (2x each slug: bay + directory row) + two repo-root profile links + JSON-LD `codeRepository`/`sameAs`. The E2E previously asserted the OLD pre-flip U2 disabled state and only stayed green because the local Playwright build defaulted to the placeholder base. FIXED: `e2e/playwright.config.ts` now OWNS the build+serve via a `webServer` block that bakes `NEXT_PUBLIC_GITHUB_BASE` into the build (the single deterministic seam; skipped when `BASE_URL`/`ATRIUM_E2E_TARGET_URL` points at an already-running/deployed prod-parity target). `links.spec` rewritten from "U2 disabled" to "six live repo links" (exact `${GITHUB_BASE}/tree/main/projects/<slug>` href, `aria-label` "<name> — GitHub repository", target/rel, bays carry the same anchors, ZERO `aria-disabled` repo controls, every `github.com` href under the single base + the profile root present). `keyboard.spec` updated (repo is now a live focusable tab stop; tabbing hits demo-then-repo per row in canonical order). `no-js.spec` updated (six live repo `<a>`s server-rendered, no disabled span). `seo.spec` updated (JSON-LD now carries each `codeRepository` + the author `sameAs`, replacing the old `not.toContain('github.com')`). Helpers: `projects.ts` exports `GITHUB_BASE`/`repoLinkName`/`repoUrl`; `landing-page.ts` swapped `repoAffordances()` → `repoLink()`/`directoryRepoLink()`. Vitest unit (54) left untouched + still green (env-agnostic; the single-seam guard NOT weakened). **Verified locally against the webServer-owned `pnpm -F atrium-web build` (with the env baked) + `next start` on :3080:** Vitest 54/54, Playwright 20/20 (was 18 — +2: a bay-repo-link test + a github.com-integrity test, mirroring the demo-link rigor), `typecheck`/`lint` clean (web + e2e). Production source/components NOT touched — they already branch on `REPO_LINKS_LIVE`.
- **Phase: DEPLOYED — v1 COMPLETE + LIVE (Phases 1 + 2 + 3 + 4 + 5 + 6 + 7 done, deployed 2026-06-10).** **Deploy (main thread, 2026-06-10):** live at https://atrium-demo.fly.dev (Fly.io region `fra`, single-Machine Next 15 standalone — the razors-edge/apex web-only pattern). Deploy surface added: `Dockerfile` (three-stage, standalone built inside the Linux image to dodge the Windows symlink-EPERM; NO `web/public` copy — atrium has no public dir), `fly.toml` (single HTTP service :3000, health check `/`, shared-cpu-1x/512MB, `fra`), `.dockerignore`, `DEPLOY.md` (runbook + the flyctl 0.4.57 first-deploy gotcha: run from the project dir, NOT `--config` + `--app`, on a zero-machine app). App is `atrium-demo` (bare `atrium` taken); `NEXT_PUBLIC_SITE_URL=https://atrium-demo.fly.dev` baked at build. Verified live: `/` → 200 + exact strict CSP (no `unsafe-eval`); `/robots.txt`, `/sitemap.xml`, `/opengraph-image` resolve + reference the canonical origin; two machines started, health checks passing. `GITHUB_BASE` NOT flipped — a remote exists (`github.com/AntczakJ/portfolio`) but the repo is PRIVATE + `main` unpushed, so repo links stay the disabled "coming soon" control (a live link would 404). README/CHANGELOG/.env.example/site-config fallback + root README/PROGRESS updated to reflect the deploy. **doc-writer Phase 7 (2026-06-09):** `README.md` (7.1 — pitch, demo status [NOT yet deployed — honest, no fake Fly URL], screenshots of the descent + bays in both themes + mobile, stack, run instructions incl. `pnpm -F atrium-web test` / `pnpm -F atrium-e2e test` / `pnpm -F atrium lighthouse`, architecture notes, key decisions, the "intended as the eventual portfolio root" note, and the `GITHUB_BASE`-flip + preview-stills + deploy v2 path) + `CHANGELOG.md` (7.2 — Keep a Changelog, `[Unreleased]` deploy/GITHUB_BASE/preview-stills + `[0.1.0]` 2026-06-09 v1 summary). Twelve curated README screenshots captured from the production build (`next build && next start` on :3080, headless Chromium) via the committed `e2e/capture-readme-screenshots.mjs` — hero dark+light, mid-descent, three resolved bays (tape/razors-edge/atlas), directory dark+light, light-theme hero+bay, mobile hero+bay; all in `docs/screenshots/`. GIF NOT assembled (the Playwright-bundled ffmpeg build lacks a PNG decoder — the apex precedent; the descent stills are the artifact, the script regenerates them). Root README updated by doc-writer (atrium row + Philosophy seven/four/three). **test-engineer Phase 6 (2026-06-09):** **test-engineer Phase 6 (2026-06-09):** Vitest unit suite (6.1 — 54 tests, the `Project` schema guard rails + the real `PROJECTS` config: six in canonical order, every demo URL the real Fly URL, every repo URL derived from `GITHUB_BASE`, the 4-api-heavy/2-web-only split + correct backend badges, and the GUARD that `github.com` appears in exactly one source file — `site-config.ts`, comments stripped); Playwright E2E (6.2 — `projects/atrium/e2e/`, 18 tests: the 12 outward affordances = 6 live demo `<a>` to the real Fly URLs + 6 U2 disabled repo spans (NOT links, zero live `github.com` href), reduced-motion = 0 pin-spacers + all titles resolved + nothing frozen, theme toggle both ways + persist + no-FOUC, full keyboard reachability with the disabled repo correctly NOT a tab stop, the no-JS directory render, and the SEO surface); Lighthouse CI (6.3 — `lighthouserc.json` + `atrium-lighthouse.yml`/`atrium-e2e.yml`, ≥ 95 ×4 with NO scoped exception + CWV). **Verified locally against `next build && next start` on :3080:** unit 54/54, E2E 18/18, Lighthouse Performance 99 · Accessibility 100 · Best Practices 96 · SEO 100 (LCP 0.9s · CLS 0.009 · TBT 10ms). `typecheck` / `lint` clean (web + e2e). No product bugs surfaced. Phase 0 architecture lock (planner + architect 2026-06-09). **frontend-engineer Phase 1 + 2 (2026-06-09):** scaffold, GSAP integration + verified smoke, sovereign tokens + six signature hues, type system, the Zod-validated project-data module. **frontend-engineer Phase 3 (2026-06-09):** app chrome + hero pinned descent (the WOW) + three-tier degradation. **frontend-engineer Phase 4 (2026-06-09):** the reusable kinetic project-bay (4.1, real-DOM two-copy title resolve via `clip-path` + `font-variation-settings`, stack ribbon + category badge + wow card + the two outward links), the SIX PINNED bays wired through the refresh-coordinator (4.2, replaced `bays-placeholder.tsx`), the elevated first-class ruled directory (4.3), the elevated about + convictions (4.4). Task 4.5 (preview stills) DEFERRED to v2 (non-blocking). **THE SIX-PIN SPIKE IS RESOLVED — all seven pins hold 60 fps; `PIN_BAYS = true` ships the full pinned tour** (desktop: 7 pin-spacers, p95 18 ms, only 3 frames > 32 ms; the fallback stays a one-line switch). `typecheck` / `lint` / `build` all clean. Verified under `next build && next start` (headless Chromium): ZERO CSP violations, ZERO page errors; full scroll holds ~60 fps through all seven pins; mobile/320px = hero pin only + bays unpinned (clean vertical stack) + ZERO horizontal overflow; reduced-motion = 0 pin-spacers, every bay title resolved + 0 reveals hidden (nothing frozen mid-transition). Phase 5 (designer-critic review) is next.
- **Category:** web-only (the portfolio landing page / lobby). Portfolio slot 7.
- **Flavour / animation / tokens (owner-confirmed, ADR-001):** web-only · GSAP (ScrollTrigger) as the single primary library (Motion only if a micro-interaction earns it — preferred end state GSAP-only) · sovereign tokens with an atrium-local six-signature-hue strategy (no § 14 sibling reuse).
- **Project-data model (ADR-001):** one typed, Zod-validated `src/data/projects.ts`; `demoUrl`s real + linked normally (internal Fly-stopped status never surfaced); `repoUrl`s derived from a single `GITHUB_BASE` placeholder constant (the repo has no git remote yet — CLAUDE.md § 10).
- **Wow moment:** a scroll-driven architectural "descent through the `ATRIUM` wordmark into a colonnade of light," handing off into six pinned, kinetically-titled project "bays," resolving into a fully-legible directory. Distinct from razors-edge (shared GSAP tool by owner's explicit call; different motif/palette/structure).
- **Dev port:** **3080** (assigned + verified no collision: razors-edge 3070, pulse 3081, apex 3090, atlas 3093, tape 3000-3002).
- **Fonts:** display = Bricolage Grotesque (variable, SIL OFL 1.1, architectural grotesque — distinct from razors-edge's serif); sans = Inter (variable, SIL OFL 1.1). Both `next/font`, self-hosted, CSP-clean.
- **Palette:** sovereign near-black warm-charcoal + volumetric-warm-light, one neutral-warm `--color-light` brand accent, six `--bay-*` signature hues (base/text/on per hue) — all contrast-verified ≥ 4.5:1 text / ≥ 3:1 UI in BOTH themes. Values + mapping + ratios in AGENT_NOTES Phase 1+2 entry.
- **Demo URL:** https://atrium-demo.fly.dev (deployed 2026-06-10, Fly web-only single-Machine; bare `atrium` name was taken).
- **Created:** 2026-06-09

## Done

- [x] Planner pass (2026-06-09): `PLAN.md` (problem, audience, wow moment, web-only justification, GSAP pick, design direction incl. the six-signature-hue strategy, project-data model + `GITHUB_BASE` strategy, site IA, success criteria with SEO/perf/a11y/reduced-motion detail, phased task list, out-of-scope), `DECISIONS.md` ADR-001 (web-only + GSAP-single-library + sovereign tokens + typed project-data/`GITHUB_BASE` placeholder), this `PROGRESS.md`, `AGENT_NOTES.md`.
- [x] **Architect pass — Phase 0 (2026-06-09):** **ADR-002** (animation posture = **single-library GSAP, NO Motion**; the GSAP/Next-15 integration contract reusing the razors-edge `loadGsap()` + `useGsapEffect` + refresh-coordinator `sort()`-before-`refresh()` shape as atrium-local modules; `matchMedia` for reduced-motion + mobile-simplified branches; transform/opacity-only + transient `will-change`; the verified CSP string, no `unsafe-eval`; free-GSAP-core-only kinetic titles via real-DOM type + CSS + transforms) and **ADR-003** (the `Project` Zod schema + `src/data/projects.ts` field set + six entries sourced verbatim from root README; `GITHUB_BASE` monorepo `tree/main/projects/<slug>` shape + the U2 disabled-repo-affordance-until-`REPO_LINKS_LIVE` presentation; the six `--bay-*` signature-hue token names + per-project mapping, values deferred to Task 2.1; the hero-descent → six-pinned-bays → directory scroll architecture + the three-tier degradation contract).

## In progress

- Nothing in active build. **v1 is COMPLETE + DEPLOYED (2026-06-10, https://atrium-demo.fly.dev).** The only remaining optional work is the `GITHUB_BASE` flip (blocked on the repo going public + pushed — see the State entry) and the v2 backlog (root-domain hosting decision, per-bay preview stills), all tracked in `CHANGELOG.md` `[Unreleased]`. **Phase 7 (doc-writer) DONE 2026-06-09** — README + CHANGELOG + screenshots; root README updated (atrium row + Philosophy counts). **Phase 6 (test-engineer) DONE 2026-06-09** — the full test suite ships green: Vitest unit (54), Playwright E2E (18), Lighthouse CI config + workflows (verified ≥ 95 ×4 locally). Details in the AGENT_NOTES "test-engineer — Phase 6" entry. **Next = Phase 7 (doc-writer):** README (pitch, stack, run, demo URL, screenshots + a GIF of the descent + bay sequence, key decisions, the GITHUB_BASE / preview-stills v2 path) + CHANGELOG init.
- **SEO surface DONE 2026-06-09 (frontend-engineer)** — full primary metadata + OG image + JSON-LD + robots/sitemap. The SEO Lighthouse category now lands at 100.
- **Phase 5.3 (reviewer) DONE 2026-06-09: verdict APPROVE, 0 blockers.** Full audit in `docs/review-5.3.md`.

## Next

1. **Deploy — DONE 2026-06-10.** Fly single-Machine Next.js standalone, web-only, at https://atrium-demo.fly.dev. `NEXT_PUBLIC_SITE_URL` baked to the real origin; the "not yet deployed" wording removed from README Demo + the root README demo cell; the standalone artifact built inside the Linux Docker stage (the Windows symlink EPERM is build-host-only). Surface: `Dockerfile`, `fly.toml`, `.dockerignore`, `DEPLOY.md`. Still OPEN: the `GITHUB_BASE` flip (blocked — repo PRIVATE + `main` unpushed) and the root-domain hosting decision.
2. **Root tracker prose — DONE 2026-06-10.** The root `C:\portfolio\PROGRESS.md` prose, table, deployment-status table, and composition status now all read atrium as v1-complete + deployed.
   - **(done, for reference) doc-writer — Phase 7:** README + CHANGELOG + 12 screenshots; details in the AGENT_NOTES "doc-writer — Phase 7" entry.
   - **(done, for reference) test-engineer — Phase 6:** Vitest unit (`src/lib/schemas/project.test.ts` + `src/data/projects.test.ts`), Playwright E2E (`projects/atrium/e2e/`: `landing`, `links`, `reduced-motion`, `theme-toggle`, `keyboard`, `no-js`, `seo`), Lighthouse CI (`lighthouserc.json` + `atrium-lighthouse.yml`/`atrium-e2e.yml`). All green.
3. **(was) reviewer — Phase 5.3:** DONE (approve). The SEO should-fix it flagged is now closed (see the "SEO surface" AGENT_NOTES entry). **Phase-5.2 surfaces to re-check:** the rebuilt CSS-3D colonnade (`hero/colonnade.tsx` — pure CSS 3D transforms, NO WebGL, `aria-hidden`, server component), the re-choreographed descent timeline (`hero/hero-descent.tsx` — continuous camera Z-travel across the full pin, `will-change` set/cleared on the colonnade stage as well as `data-descent-move`), the bay light-field layers + the demoted wow-card + the heavier bay title (`bays/project-bay.tsx`), the strengthened threshold hue wash (`atmosphere/threshold.tsx`), the new daylight LightField layers (`atmosphere/light-field.tsx`), and the new `--colonnade-*` / `--floor-*` / `--text-display-bay` tokens + re-spaced six bay hues in `globals.css`. All transform/opacity/clip-path only; no new deps; no client-runtime Zod added; build/typecheck/lint clean.
4. **(deferred) Task 4.5 — per-bay preview stills:** non-blocking v2 work. The `data-bay-media` reserved slot + the `previewImage?` schema field are the seam; source/optimise six AVIFs (`next/image`, lazy, never LCP) with provenance. The bays read complete without them — do not gate on this.

## Handoff

- **v1 COMPLETE + DEPLOYED (2026-06-10, https://atrium-demo.fly.dev).** All seven phases done plus the Fly deploy: scaffold, tokens, data, chrome, hero descent, six bays, directory, about, designer + code review, SEO surface, the full test suite (Vitest 54 + Playwright 18 + Lighthouse CI ≥ 95 ×4), the docs (README + CHANGELOG + 12 production-build screenshots), and the live single-Machine web-only Fly deploy. The only open work is the `NEXT_PUBLIC_GITHUB_BASE` flip — **blocked**: a remote exists (`github.com/AntczakJ/portfolio`) but the repo is PRIVATE and `main` is unpushed, so a live repo link would 404; do it once the repo is public + pushed (re-deploy with `--build-arg NEXT_PUBLIC_GITHUB_BASE=...`, mirror in `fly.toml [env]`). Plus the v2 backlog (root-domain hosting, preview stills). All tracked in `CHANGELOG.md` `[Unreleased]`.
- **(historical) Next subagent: `doc-writer`** — Phase 7 (README + CHANGELOG). Done 2026-06-09. The README run section documents the three test entrypoints (`pnpm -F atrium-web test`, `pnpm -F atrium-e2e test` against a built+served :3080, `pnpm -F atrium lighthouse`). Full Phase-6 detail in `AGENT_NOTES.md` under "test-engineer — Phase 6".
- **(historical) Next subagent: `reviewer`** — Phase 5.3 (code audit; the Phase-5.2 surfaces to re-check are listed under "Next" above). Phases 1 + 2 + 3 + 4 + 5.1 + 5.2 are done. The six pinned bays (`src/components/bays/{project-bay,bays-sequence,bays}.tsx`), the elevated ruled directory (`src/components/directory/directory.tsx`), the elevated about + convictions (`src/components/about/about.tsx`), the chrome, and the hero descent are all in place; build/typecheck/lint clean; the full scroll + reduced-motion + 320px verified under `next build && next start`. **The six-pin spike is closed — all seven pins hold 60 fps, `PIN_BAYS = true` ships the full pinned tour (the documented fallback stays a one-line switch).** Full Phase-4 detail (the two-copy kinetic-title technique, the 3-tier `matchMedia` branches, the ambient-`ScrollTrigger.Vars` TS gotcha, the directory/about elevation) is in `AGENT_NOTES.md` under "frontend-engineer — Phase 4".
- **Architecture (unchanged, do not re-litigate):** ADR-002 single-library GSAP (no Motion), the refresh-coordinator `sort()`-before-`refresh()` rule, the exact CSP; ADR-003 the `Project` schema + `GITHUB_BASE` shape/U2 presentation + the six `--bay-*` token names + the scroll architecture + the three-tier degradation contract.
- **Root tracker — one-line addition still needed (main thread):** the root `C:\portfolio\PROGRESS.md` composition tracker has been updated by the planner to add atrium as slot 7 (web-only, the portfolio landing page). If for any reason that edit is not present, the main thread should add the row: `| 7 | atrium | web-only (landing page) | — (none) |`.

- [x] **Designer critique complete (Phase 5.1, 2026-06-09): 2 blockers, 4 high, 14 total — file `docs/critique-5.1.md`.** Re-skin gate vs razors-edge PASSED (caveat: static hero first-frame is the weak differentiator). Must-fix tier for Phase 5.2 = D-01 (colonnade reads as flat venetian-blind stripes, not an atrium — perspective inert), D-02 (descent stalls on a static frame for its back ~40%), D-03 (light theme collapses the atrium-of-light identity to a flat document — fails the not-a-flipped-dark-site criterion), D-04 (six "differently-lit rooms" read as one dark room with coloured trim — hue is decorative trim, not lighting), D-05 (hue-temperature clustering: tape/apex/atlas all cool blue-greens), D-06 (inverted hierarchy: wow-card + lighter bay-title weight beat the bay title). Directory + about + bay copy/IA are first-class. Screenshots in `docs/critique-shots/`.
- [x] **Phase 5.2 — must-fix tier + polish pass applied (frontend-engineer, 2026-06-09).** All six must-fix defects resolved and verified under `next build && next start` (headless Chromium, dark + light + reduced-motion + 320/360px): **D-01** colonnade rebuilt as a genuine CSS-3D atrium (perspective stage + receding floor + two converging colonnades of standing columns with real gaps + a clerestory glow at the vanishing point — CSS transforms only, NO WebGL); **D-02** descent re-choreographed so a continuous camera Z-travel + a floor-light bloom run across the FULL pin (traced 0→900px stage travel, floor 0.2→1.0 through the old dead back half — no stall, the wordmark finishing early no longer leaves a static frame); CLS through the descent 0.012–0.019; **D-03** light theme re-keyed to architectural daylight (directional clerestory skylight wedge with visible falloff, limestone floor wash, sunlit columns); **D-04** each bay's hue now LIGHTS the room (a directional coloured clerestory + hue shaft + stronger hue floor per bay, plus a stronger threshold hue wash) — unmistakable on entry; **D-05** the six hues re-spaced around the wheel (meld 40 · razors 88 · pulse 150 · atlas 195 · tape 230 · apex 285 — apex pushed to indigo, atlas to teal, off tape's cyan); **D-06** bay title raised to the bold axis + a larger `--text-display-bay` size + a hue text-glow, the type column widened to 1.38/0.62, and the wow-card demoted from a filled card to a quiet hue-ruled note so the title wins the eye. Polish: reduced-motion still lands on a clean composed frame (0 pin-spacers, every title resolved); no theme FOUC (dark-canonical `:root` + `disableTransitionOnChange` unchanged); brand `:focus-visible` ring intact on every interactive element; all re-tuned hues re-verified ≥ 4.5:1 text / ≥ 3:1 UI in BOTH themes (table in AGENT_NOTES). Cheap should-fix also taken: D-11 (colonnade resting dim, further on mobile) + D-14 (an implied colonnade at rest = an atrium-specific first frame). `typecheck`/`lint`/`build` clean. New 52-prefixed verification shots in `docs/critique-shots/`.

- [x] **Designer critique (polish pass, 2026-06-10): 4 blockers, 9 high, 23 total — file `docs/critique-polish.md`.** Judged the DEPLOYED site (https://atrium-demo.fly.dev) headlessly via Playwright (1440x900 + 390x844, no-preference motion, 7 pins confirmed), both themes, all seven sections; shots in `docs/critique-polish-shots/`. Operationalises the owner's "slabo / niech bedzie bardziej spojnie i ladniej" verdict. Top-5 highest-leverage: B-01 the six bays are one template recoloured (defeats the variance thesis — the core "not cohesive" cause); B-02 the hue edge-lights/halos rather than bathing the room; B-03 the descent back third is dead scroll (the Phase-5.1 D-02 regression — perspective foreshortening front-loads the camera Z); B-04 the light theme bleaches the wordmark (~2:1) and desaturates the six-hue system into one warm haze; B-05 the desktop hero under-commits versus its own (stronger) mobile hero. Directory + about are first-class and explicitly exempt. Next = `frontend-engineer` polish pass on the lead-5, then re-shoot.
