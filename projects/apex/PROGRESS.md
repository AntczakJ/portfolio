# apex — Progress

> Read on start. Write on end.

## State

- **State (latest):** **DESIGNER-CRITIC CLOSE-OUT POLISH COMPLETE — the proper glass fix LANDED; graphite default; wheels re-tinted; fleet line-up normalised; mobile LCP trimmed; docs corrected** (frontend-engineer, 2026-06-05). Applied the full close-out list. **P0-2 (the headline win) LANDED — REAL dark glass, not the v2 fallback:** `optimize-model.mjs` now SPLITS the `body` mesh into a paint group + a dark-`apex-glass` group by classifying each triangle on the Kenney `colormap` atlas swatch its UV centroid lands on (windows = a distinct blue-grey swatch — dark on the SUV, light blue-grey on the saloons; both detected). `lumen-model.tsx` assigns the glass group its own dark `MeshPhysicalMaterial` (low roughness, slight transmission). The greenhouse NEVER takes the body paint now — EVERY paint incl. Glacier White + Voltaic Green reads as a real car with glass. Split applies to the flagship AND all four fleet bodies (verified: `apex-suv.glb` `body` mesh = `{apex-body, apex-glass}`; all four fleet GLBs split too). **P0-1 (default paint):** `defaultColorId` `col-glacier`→**`col-graphite`** in baked configurator options + hero/blur renders; default wheel `whl-turbine`→**`whl-aero`** (silver reads premium on graphite). Hero crops (`hero{,-desktop,-mobile}{,-dark}.avif`) + blur URIs re-shot in graphite. White stays selectable. **P1-C (wheels):** the raw atlas baked a tan tyre (read orange/copper) + an orange aero rim — `optimize-model.mjs` RE-TINTS each wheel atlas at author time (tyre→neutral rubber; rim→the copy finish: aero=polished silver, turbine=graphite, forged=voltaic; warm-orange swatches neutralised so none bleeds into the rim via filtering). Fleet wheels get the same tyre/warm neutralisation. **P1-A (fleet crops):** the four non-flagship renders are length-normalised (per-car `__APEX_MODEL_SCALE` ≈ 2.7 m apparent length under the fixed rig) so all five cards read as ONE line-up. **P1-D (white gallery frame):** `rear-3q` "Built to be looked at" re-shot in MIDNIGHT (was Glacier White). **P1-E (stage framing):** rig camera pulled back (~4.7→~5.1 eye distance) so the roof keeps margin on the portrait-mobile stage. **P2-A (white swatch):** inset hairline + inner shadow on the swatch chip (via box-shadow, never fights the accent ring) so a near-white chip reads. **B (mobile LCP):** mobile hero crop resized 900×1100→760×940 (~7 KB optimized AVIF); media-scoped `sizes` + `priority`+`fetchPriority="high"` unchanged. **C (docs):** README mobile-perf line corrected to the measured story (removed the "real-CPU = 100" universal overstatement); CREDITS + CHANGELOG updated; root README/PROGRESS NOT touched (their apex rows say "desktop Lighthouse ≥95" — no overstatement to fix). **D (captures):** re-shot all changed surfaces + 320/1440/2560 + a reduced-motion configurator frame into `web/scripts/.screenshots/closeout-*`; refreshed the README-embedded copies in `docs/screenshots/` (hero/configurator/gallery/mobile-tier3 now show graphite+glass). **VERIFICATION (ACTUAL):** typecheck clean · lint 0/0 · build passes (home **157 kB**; three.js split OUT of `/page`+`/reserve`) · **151/151** Vitest · `verify-csp.mjs` PASS — canvas mounted + paint swap + WHEEL swap, **0 CSP violations**, served `script-src 'self' 'unsafe-inline'` (NO `'wasm-unsafe-eval'` — confirmed still gone) · `verify-tiers.mjs` PASS · `verify-deferred-canvas.mjs` PASS (0 GLB at scrollY=0; GLB+canvas on approach) · `verify-reserve.mjs` PASS (A-06 reason + A-17 config render present, 0 CSP) · `verify-model.mjs` PASS (body = `apex-body` + `apex-glass`). **LIGHTHOUSE (median 5, :3091):** DESKTOP `/` **97**/96/96/100 (LCP 1038ms, TBT 102ms), `/reserve` **99**/96/96/69(noindex) — both clear ≥95. MOBILE synthetic preset `/` **62** (LCP 4657ms, TBT 881ms), `/reserve` **69**. MOBILE **real-CPU** (`cpuSlowdownMultiplier:1`) `/` = **100 / LCP 1035ms / TBT 31ms** — the synthetic deficit is the 4×-CPU artifact, NOT a byte/bundle problem (optimized mobile hero AVIF ~7 KB). **MOBILE LCP before→after:** synthetic LCP ~4.4s→4.66s (unchanged; it is CPU-render-delay bound, not byte-bound); the meaningful change is the LCP byte-weight (mobile hero crop 900×1100→760×940). Deployed Fly real-CPU run is authoritative (network-bound). **REDEPLOY REQUIRED** for the live site to reflect this. NO new deps; root lockfile/package.json/workspace untouched; wrote ONLY under `projects/apex/`; stayed on `main`; server stopped PORT-SCOPED (3091 PID via netstat→taskkill). **Next = orchestrator commits + redeploys; optional designer-critic re-confirm of the glass/graphite state.** PRIOR state below.
- **State (PASS B):** **MODEL-SWAP PASS B COMPLETE — the WHOLE FLEET is CC0; designer-critic N-1 CLOSED; P0-1 FULLY RESOLVED** (frontend-engineer, 2026-06-05). The four non-flagship fleet cards (Stratos, Terra, Vella, Mira) now render from the SAME live R3F rig + studio lighting + clearcoat-paint mechanism as the flagship, against four consistent CC0 **Kenney "Car Kit" v3.1** silhouettes — so all FIVE cards read as ONE studio line-up (closes **N-1**; the four were procedural-SVG jellybeans before). **FLEET MAPPING (vehicle → Kenney model → default paint):** LUMEN→`suv-luxury` (glacier, configurable, PASS A) · Stratos→`sedan-sports` (midnight) · Terra→`suv` (graphite) · Vella→`sedan` (gunmetal) · Mira→`hatchback-sports` (voltaic). **Mechanism:** `optimize-model.mjs` gained `buildFleetCar` → 4 WHOLE-car GLBs `public/models/fleet/<slug>.glb` (body texture dropped + custom clearcoat paint like the flagship; OWN bundled wheels kept; 74-98 KB). `lumen-model.tsx` gained offline-only overrides (`__APEX_BODY_URL`/`__APEX_PAINT`/`__APEX_OWN_WHEELS`, same pattern as `__APEX_YAW`) so `render-from-scene.mjs` drives the live scene per fleet body and shoots `public/renders/<slug>/hero{,-dark}.avif`. Fleet GLBs are **RENDER-ONLY** (cards are static AVIF at runtime) → dockerignored (`web/public/models/fleet`); only flagship body + 3 wheels (**~117 KB**) ship to the browser. **MOCKS RECONCILED** (`mocks:bake`): names/segments/specs/copy match the silhouettes — Stratos = 4-door performance super-saloon (4 seats, 2.8s); Mira = compact hot-hatch; Vella = executive saloon; Terra = 7-seat family SUV; determinism preserved; `vehicle.ts` schema untouched. **PASS-A CAVEATS FIXED:** default wheel `whl-aero`→**`whl-turbine`** (dark rim reads on the white body; propagates through hero/matrix/deep-link/rail) · wheel thumbs re-shot against graphite with a tight crop (3 rims read apart) · glasshouse-takes-body-paint = ONE-mesh limitation DOCUMENTED + left for v2. `generate-section-assets.mjs` → maps-only. OG regenerated. CREDITS.md + CHANGELOG.md updated. **FINAL VERIFICATION (ACTUAL):** typecheck clean · lint 0/0 · build passes (home **157 kB**; three.js split OUT of `/page` + `/reserve`) · **151/151** Vitest · **22/22** Playwright (desktop+mobile) · `verify-csp.mjs` **0 violations** (canvas+paint+wheel; served CSP `script-src 'self' 'unsafe-inline'` — **NO `'wasm-unsafe-eval'`, confirmed still gone**) · tiers PASS · deferred-canvas PASS · reserve PASS (deep-link `whl-turbine` carried) · model:verify PASS. **RUNTIME MODEL FOOTPRINT ~117 KB** (was 1.82 MB); fleet src 323 KB render-only/dockerignored. **LIGHTHOUSE MATRIX (median 5, :3091):** DESKTOP `/` **99**/96/96/100 (LCP 899ms, TBT 0ms) — was 97 PASS-A; DESKTOP `/reserve` **99**/96/96/69(noindex); MOBILE `/` **80**/96/96/100 (was ~62); MOBILE `/reserve` **86**/96/96/69 (was ~69). Mobile sub-95 = the documented Lighthouse 4×-CPU + Slow-4G synthetic artifact: at REAL CPU+net MOBILE `/` = **perf 100 / LCP 757ms / TBT 0ms**, LCP = the static hero render (never the canvas); deployed Fly real-CPU run is authoritative (ADR-002 §3). **SCREENSHOTS** re-captured (cache gotcha cleared) into `web/scripts/.screenshots/`: `fleet-desktop-{light,dark}.png`, `fleet-mobile-light.png`, `wizard-vehicle-step1-grid.png`, `wizard-vehicle-step.png`. NO new deps; root lockfile untouched; apex-only; `main`; servers stopped PORT-SCOPED (3090/3091). **Next = optional `designer-critic` confirm N-1 closed (new fleet screenshots) + (owner) run the Fly deploy — nothing else gates the public URL.** PRIOR state below.
- **State (PASS A):** **MODEL-SWAP PASS A COMPLETE — CC0 Kenney model drives the configurator + all its renders; P0-1 RESOLVED for the configurator; `'wasm-unsafe-eval'` DROPPED** (frontend-engineer, 2026-06-05). The branded Maybach placeholder is GONE from the configurator. The flagship now loads the **CC0 Kenney "Car Kit" v3.1** `suv-luxury` (an UNBADGED low-poly luxury SUV) under the fictional APEX/LUMEN naming. **Model artifacts** (`optimize-model.mjs`; raw kit gitignored under `web/scripts/.model-src/`): `apex-suv.glb` = BODY ONLY, textures dropped, **27 KB / 758 tris**; `wheel-{default,dark,racing}.glb` = the 3 wheel sets, textures KEPT, **~29-31 KB each**; total **~119 KB** (was 1.82 MB), all UNCOMPRESSED → **no meshopt/draco decoder, no WASM at load**. **Paint** = a custom `MeshPhysicalMaterial` (clearcoat, colour from the swatch) assigned to the body mesh — Kenney bakes paint as a UV region on a shared atlas, so we do NOT tint it; we own a clean material with full colour control. **Wheels** = a GENUINE GEOMETRY SWAP (the preferred path): the chosen wheel GLB instanced ×4 at the captured `WHEEL_NODES` (right side yawed π), keeping the authored atlas (tire/rim distinction). `useGLTF(url,false,false)` disables BOTH drei decoders (load-bearing: `useMeshopt=true` fires WASM, `useDraco=true` hits a CDN). **CSP TIGHTENED:** `'wasm-unsafe-eval'` REMOVED from `script-src` (no decoder anymore); `connect-src` gained `blob:` (GLTFLoader's same-origin in-memory wheel-texture fetch); `'unsafe-eval'` stays banned — **re-verified 0 CSP violations** with the live canvas + a paint swap + a WHEEL swap. **Rig re-tuned** (`rig.ts`) for the ~2.85 m model: camera `[3.0,1.85,3.4]` target `[0,0.62,0]` fov 32, orbit 3.4–7 / upper-hemisphere only; studio lighting re-tuned for flat-shaded forms (crisp key so facets catch light, neutral env, low voltaic accent graze, ContactShadows scale 6). **ALL configurator-derived stills regenerated** from the new live rig (`render-from-scene.mjs`): the light+dark colour×wheel MATRIX (Tier-1=Tier-3 same car), HERO crops (light + dark night-studio), wheel thumbs, GALLERY crops, both blur URIs (pasted into `hero-assets.ts`). **N-2** folded in (gallery establishing shots use the neutral default wheel; the accent wheel only on the dedicated wheel-detail frame). **N-3** folded in (confirmation config-render heading `scroll-mt-28 sm:scroll-mt-32` clears the sticky header). The Tier-3 confirmation render (A-17) now shows the new model. **CREDITS.md** rewritten (Kenney CC0; the BRANDED-Maybach BLOCKING flag + the "clear before public deploy" note REMOVED for the configurator; honest provenance; `.model-src` gitignored). **Verification (ACTUAL):** `typecheck` clean · `lint` "No ESLint warnings or errors" · `build` passes (home First Load **157 kB**; three.js code-split OUT of `/page` AND `/reserve` — 0 three/fiber/drei chunks in either manifest; EPERM standalone-symlink = known Windows P1-3) · **151/151** Vitest · **22/22** Playwright (desktop + mobile) · `verify-csp.mjs` **0 violations** (canvas + paint + wheel swap; served CSP `script-src 'self' 'unsafe-inline'` — NO `'unsafe-eval'`, NO `'wasm-unsafe-eval'`) · `verify-tiers.mjs` PASS (Tier-3 mobile 0 canvas + still swap; Tier-4 no-JS floor + 0 canvas) · `verify-deferred-canvas.mjs` PASS (0 GLB + 0 canvas at scrollY=0; both on approach) · `verify-reserve.mjs` PASS (A-17 render present, 0 CSP). **Honest read:** the stylized low-poly configurator reads as intentional premium product-viz (clean studio + clearcoat + contact shadow), not a toy — caveats (body is ONE mesh so the glasshouse takes the paint colour; wheel-swatch thumbs read similar; wheels low-contrast on white) flagged for the designer-critic / PASS B. NO new deps; root lockfile NOT touched by this pass; wrote ONLY under `projects/apex/`; stayed on `main`; servers stopped PORT-SCOPED (3090/3091). **Next = MODEL-SWAP PASS B = fleet cards + per-vehicle fleet renders + mocks reconciliation + final Lighthouse/screenshots.** PRIOR state below.
- **State (8.1/8.2 docs):** **Phase 8 Tasks 8.1 + 8.2 DOCS COMPLETE — README + CHANGELOG** (doc-writer, 2026-06-05). **8.1 `projects/apex/README.md`** written end-to-end (pitch + wow spine; Demo = clear "coming soon", deploy prepared, public URL gated on the P0-1 model swap, NO fake link; Screenshots embedded from a new `docs/screenshots/`; What it is; Stack — web-only stated + pinned versions; Run locally — `pnpm install` at root then `cd projects/apex && pnpm dev` on :3090 + the `test:e2e`/`test:lh`/`test:lh:mobile` production surface; Architecture notes — deferred-canvas LCP, the four-tier degradation, the mocked reservation, strict CSP + the narrow `'wasm-unsafe-eval'`, sovereign tokens; Key decisions linking ADR-001..005; Quality — 151 Vitest + 22 Playwright + the HONEST Lighthouse posture (desktop ≥95, mobile synthetic-throttle caveat + real-CPU=100 + deployed-is-authoritative, `/reserve` noindex); **Credits and honesty** — the BRANDED placeholder model surfaced honestly (manufacturer name NOT advertised as licensed; public deploy blocked until the unbadged CC0 swap), imagery = generated placeholders, nothing really booked, swap-for-real path; v1-deferred; License → root LICENSE). Structural template = `razors-edge/README.md`, adapted (no content copied). **Screenshots** copied into `projects/apex/docs/screenshots/` (the razors-edge convention) from `web/scripts/.screenshots/` and embedded: the lead wow shot is `configurator-desktop-light-voltaic-forged.png` (live colour swap + swatch controls + Reserve CTA), plus hero light/dark, configurator dark, configurator mobile Tier-3, hero mobile, the reserve dates step, the confirmation (ref APX-JZJU-RS9Q), a gallery crop, and the swap-pair stills (`configurator-desktop-light.png` + `wheel-finishes-compare.png`) referenced in text. **NO GIF** made (tooling not run; not blocking per the brief; README notes where a GIF would sit). **8.2 `projects/apex/CHANGELOG.md`** initialised (Keep a Changelog 1.1.0 + SemVer), mirroring razors-edge: one `[Unreleased]` section summarising the whole initial build under `Added` (ADR-001..005, the configurator, the reservation flow, the four-tier degradation, the 151+22 test suite, the deploy prep) + a "Not shipped in v1 (deferred)" block. Wrote ONLY under `projects/apex/` (README.md, CHANGELOG.md, docs/screenshots/\*, PROGRESS.md, AGENT_NOTES.md); did NOT touch root `README.md`/root `PROGRESS.md`/lockfile/any tape file/any other project; no server started (screenshots pre-existed); stayed on `main`, no branch/commit. **STILL OWED to the main thread: the apex row in the root `README.md` Projects table** (name · pitch · stack summary · demo = "coming soon") — deferred per the hard constraint until the concurrent tape session releases `main`. **Next = main thread adds the root-README row + (owner) the P0-1 model swap + the Fly deploy.** PRIOR state below.
- **State (8.3):** **Phase 8 Task 8.3 DEPLOY INFRA COMPLETE — ADR-005 + Dockerfile/fly.toml/.dockerignore/DEPLOY.md + the P1-3 resolution** (architect, 2026-06-05). The deploy is PREPARED (owner executes it). **ADR-005** ratifies the deploy posture mirroring razors-edge ADR-005: Fly.io single-Machine Next standalone, web-only, NO secrets, `NEXT_PUBLIC_SITE_URL` baked at build, warm floor (`min_machines_running = 1` + `auto_stop`), region `fra`, VM shared-cpu-1x / **512 MB** (the `next/image`/sharp image-optimization path — the server is light; the heavy three.js + the 1.9 MB GLB run in the BROWSER), HTTP health check on `/`. Fly app name **`apex-rentals`** (placeholder, owner-renamable). **P1-3 RESOLVED:** the standalone artifact is built INSIDE the Linux `node:22-bookworm-slim` Docker build stage (where symlink tracing works) — the Windows-host `⚠ EPERM: operation not permitted, symlink` standalone-trace failure NEVER occurs there, and the host `.next` is excluded by `.dockerignore` so a broken trace can't leak in. PROVEN by STATIC verification, NOT a docker build: the Docker CLI (29.3.1) is installed but the Docker Desktop Linux engine never became reachable in this env (polled >7 min) so NO `docker build` was run (and is NOT claimed); instead `pnpm -F apex-web build` was run (**exit 0** with the exact EPERM symlink warnings = the host failure) and the Dockerfile's COPY/CMD paths were cross-checked line-by-line against the ACTUAL `.next/standalone` output (root = `package.json` + `web/`; entry `web/server.js` does `process.chdir(__dirname)` → resolves `.next/static` + `public` as siblings; runtime `node_modules` subset inside `standalone/web/`) — all match. **`next.config.ts` UNCHANGED** (`output:'standalone'` + the one-hop `outputFileTracingRoot` → `projects/apex/` were already correct for the Docker context; no edit, no rebuild-for-config needed). **P0-1 PUBLIC-DEPLOY PRECONDITION RESTATED (BLOCKING):** the shipped GLB is a BRANDED Maybach (NOT royalty-clear) — the infra is ready and a PRIVATE/STAGING build can run, but a PUBLIC demo URL requires the unbadged CC0 GLB swap first (swap `public/models/apex-suv.glb`, re-run `model:optimize` + `renders:scene`, re-verify CSP, update `CREDITS.md`); DEPLOY.md carries this as an unmissable STOP block. **Before a PUBLIC deploy, exactly TWO owner-side items remain: (1) P0-1 model swap, (2) owner runs the Fly deploy** (`flyctl apps create apex-rentals` + `flyctl deploy --remote-only --config projects/apex/fly.toml --dockerfile projects/apex/Dockerfile projects/apex`; no Fly creds here). The deployed Fly real-CPU run is then the authoritative mobile-perf measurement (ADR-002 §3). Wrote ONLY under `projects/apex/`; no lockfile/root/tape edits; stayed on `main`. **Next = Phase 8.1/8.2 docs (doc-writer — README + CHANGELOG restate the deploy posture + the demo URL once public + the P0-1 swap path).** PRIOR state below.
- **State (Phase 7 perf):** **Phase 7 PERFORMANCE PASS COMPLETE — Lighthouse quality-gate fixed + the latent store bug fixed** (frontend-engineer, 2026-06-05). The CLAUDE.md §4 Performance gate is now cleared on the authoritative profile. **The core fix: the live R3F `<Canvas>` mount is DEFERRED off the home initial load.** `configurator-stage.tsx` no longer mounts the canvas (and therefore three.js + the 1.48 MB GLB) on hydration — it ARMS on either an `IntersectionObserver` (`rootMargin:'10%'`, fires as the configurator stage box approaches, NOT at scrollY=0 because the stage sits below the heading/copy under a full-height hero) OR user intent (`pointerenter`/`pointerdown`/`focusin` on the stage). `detectWebglTier()` still decides Tier-1-live vs Tier-3-stills, but only AFTER arming (the change is WHEN, not WHETHER); the reveal-when-ready crossfade, reduced-motion (Tier-2), no-JS (Tier-4), Tier-3 behaviors are all preserved. Until armed the pre-baked still is the surface (lazy `next/image`, never an LCP candidate). **Proven by NETWORK + DOM evidence (new `scripts/verify-deferred-canvas.mjs`, not the manifest):** at scrollY=0 there are **0 `.glb` requests + 0 `<canvas>`** in the configurator; on approach the GLB loads + the live canvas mounts (Tier-1). **Hero is the definitive LCP on BOTH profiles:** `hero-render.tsx` now gives BOTH crops `priority`+`fetchPriority="high"` with `media`-scoped `imageSizes` (`(max-width:639px) 0px,100vw` desktop / `(max-width:639px) 100vw,0px` mobile) so each device preloads exactly its LCP crop (the mobile crop was previously eager-but-unhinted → mobile LCP ~4.6s); the hero render box dropped the always-on `will-change-transform` (GSAP adds it transiently). The configurator stills are `loading="lazy"`, below the fold, never the LCP. Lighthouse trace confirms the LCP element is the hero render (`fetchpriority="high"`, "APEX Lumen SUV in Glacier White on Aero…" alt) on desktop AND mobile. **BEFORE/AFTER Lighthouse (median of 5, :3091, `next build && next start`):** DESKTOP `/` perf **56 → 97** (LCP 2.5s→1.1s, TBT 2589ms→94ms); DESKTOP `/reserve` **99 → 99**; MOBILE `/` perf **68 → ~62** median on the synthetic preset BUT **100 / LCP 1000ms / TBT 35ms with the SAME mobile screen emulation at real-device CPU** (`cpuSlowdownMultiplier:1`) — the mobile sub-95 is ENTIRELY the Lighthouse mobile preset's 4× CPU multiplier inflating React hydration long tasks (~0.9–4.9s) that delay the LCP PAINT of an already-downloaded image (LCP RENDER-delay, not load-delay; the hero image downloads in <60ms per the trace). MOBILE `/reserve` **71 → ~69** (same throttle artifact). a11y **96** + best-practices **96** ≥95 on ALL four page×profile; SEO **100** on `/` both profiles; SEO **69** on `/reserve` is the DELIBERATE `robots:noindex`. **`/reserve` SEO gate fix:** both rc files now use `assertMatrix` — `/` asserts SEO ≥95 (error), `/reserve` sets `categories:seo:"off"` (documented intentional-noindex exception); a11y/bp stay hard ≥95 errors on `/reserve`; mobile perf is a documented `warn` (surfaced, not hidden — the deployed Fly real-CPU run is authoritative per ADR-002 §3, not an `error` blocking on a synthetic-CPU artifact). **Both `test:lh` + `test:lh:mobile` now EXIT 0.** **Latent store bug FIXED (`reservation-store.ts`):** `selectVehicle` (on a vehicle change), `setRange(undefined)`, `setInsuranceTier(undefined)` used `delete next.range` on the returned partial — a NO-OP under zustand v5's shallow `set` merge (the intended clear never happened). Fixed by explicitly assigning `undefined` (`set({ range: undefined, config: undefined })` etc.); Zod's `.optional()` types `| undefined` so it's clean under `exactOptionalPropertyTypes`. `reservation-store.test.ts` updated: the two tests that DOCUMENTED the wrong behavior now ASSERT the clear actually clears (`toBeUndefined()`). **Verification (ACTUAL):** `typecheck` clean · `lint` "No ESLint warnings or errors" · **151/151** Vitest · **22/22** Playwright (deferred-mount didn't break configurator/seam/tier specs — `/#configurator` nav arms the observer; no spec edit needed) · `build` passes (home First Load **157 kB**; three.js absent from the `/page` manifest; EPERM standalone-symlink = known Windows P1-3 gotcha) · `verify-deferred-canvas.mjs` PASS (GLB+canvas off the home critical path) · served CSP UNCHANGED (`script-src 'self' 'unsafe-inline' 'wasm-unsafe-eval'`, no `'unsafe-eval'`). NO new dependencies; root lockfile NOT touched; wrote ONLY under `projects/apex/`; stayed on `main`; server stopped PORT-SCOPED (3091 PID via netstat → `taskkill //F //PID`). **Next = Phase 8 docs + deploy** (the deployed Fly real-CPU run is the authoritative mobile-perf measurement; P0-1 branded-model swap + P1-3 Linux standalone build remain the deploy-gated items). PRIOR state below.
- **State (Phase 7 tests):** **Phase 7 COMPLETE — the test suite, Tasks 7.1, 7.2, 7.3 DONE** (test-engineer, 2026-06-05). **7.1 Vitest 44 → 151 green** (added 107 critical-path tests across 5 new files + 1 extended): `availability.edge.test.ts` (27 — half-open overlap geometry at every boundary, min/max rental-length boundaries, window edges, the reason-enum PRECEDENCE, frozen-clock determinism via `__setNowForTests`, `getDisabledRanges` window-clipping/cross-vehicle/sort), `pricing.edge.test.ts` (19 — every multi-day discount boundary + longest-tier-wins, half-up minor-unit rounding, per-day vs flat add-ons + insurance, unknown-extra-id ignored, one-way fee, clamp ≥0, currency carry-over, all-zero for non-positive days), `ics.edge.test.ts` (10 — multi-day exclusive DTEND, RFC-5545 escaping incl. the lone-`\r` P2-3, 75-octet folding, config-suffix omission, demo disclosure, filename), `booking-reference.edge.test.ts` (9 — unambiguous alphabet over 200 payloads, every load-bearing field flips the ref, config presence/absence, email/licence case-normalisation), `store/wizard-machine.edge.test.ts` (22 — reachability matrix, clampStep matrix, earliestIncompleteStep to driver, extras pass-through, submit-projection config/insurance omission + null cases), `store/reservation-store.test.ts` (10 — **the forced-stale date-range reconciliation fixture, the ADR-003 NAMED GAP**, TTL expiry, step clamping P1-4, corrupt-payload tolerance, selectVehicle), `store/deep-link-seed.test.ts` (10 — valid + UNKNOWN/garbage param seeding, configureAndReserve carry-over). **7.2 Playwright E2E 22/22 green** (NEW `web/e2e/` + `playwright.config.ts`, two projects: desktop-chromium + mobile-chromium/Pixel-7=Tier-3, port **3091**, `next build && next start`, frozen-clock deterministic, ran green twice): reservation HAPPY PATH end-to-end (deep-link carry-over → keyboard date range → extras+insurance live total → driver → confirmation reference + chosen-config render + `.ics` download), reference determinism, range-persist-across-reload, configurator DOM-swatch + Tier-3 still-swap + Tier-4 no-JS floor + carry-over deep-link, keyboard date-picker (roving tabindex, Arrow nav, booked-days-disabled+announced, the **A-06 invalid-range specific reason**), theme toggle both directions + persist + dark `/reserve`, prefers-reduced-motion hero (no pin/scrub, static legible, no blocking canvas). **7.3 Lighthouse CI** (NEW `lighthouserc.json` desktop + `lighthouserc.mobile.json` mobile, median of 5, system Chrome): **SCORE MATRIX (page × category × profile)** — DESKTOP `/`: perf **56** / a11y 96 / bp 96 / seo 100 (LCP 2.5s, CLS 0.002, TBT 2589ms); DESKTOP `/reserve`: perf **99** / a11y 96 / bp 96 / seo **69** (LCP 0.9s, CLS 0.042, TBT 21ms); MOBILE `/`: perf **68** / a11y 96 / bp 96 / seo 100 (LCP 4.4s, CLS 0.000, TBT 658ms); MOBILE `/reserve`: perf **71** / a11y 96 / bp 96 / seo **69** (LCP 4.2s, CLS 0.042, TBT 564ms). **Perf < 95 + the `/reserve` SEO 69 are SURFACED with diagnosis for the frontend-engineer/architect (NOT fixed this pass — fixing is the engineer's job; see ## Next).** a11y/bp ≥95 on all; SEO 100 on `/`. Wiring: `pnpm -F apex-web test:e2e` / `test:lh` / `test:lh:mobile`. Surgical ADDITIVE install of `@playwright/test@1.60.0` + `@lhci/cli@^0.15.1` into apex-web (root lockfile +2435 insertions / **0 deletions** — no churn to tape; one observed benign side effect: the concurrent tape session's in-flight `projects/tape/e2e` importer materialised into the lockfile, additive). Wrote ONLY under `projects/apex/`; stayed on `main`; server stopped PORT-SCOPED (3091, `taskkill //F //PID`). **Next = Phase 8 docs + deploy.** PRIOR state below.
- **State (Phase 5 wizard):** **Phase 5 COMPLETE — the reservation wizard built, Tasks 5.4, 5.5, 5.6 DONE** (frontend-engineer, 2026-06-05). The centerpiece INTERACTION ships: the `/reserve` route + the five-step wizard over the EXTENDED reservation store (NOT rebuilt). **5.4 shell:** a static `/reserve` server route mounting the client `ReservationWizard` (deep-link read inside `<Suspense>`); the wizard machine is a PURE module (`lib/store/wizard-machine.ts` — step guards, reachability, `clampStep`, `earliestIncompleteStep`, the `draftToSubmit` projection) that the store delegates to; the store was EXTENDED (added per-step setters, `goToStep`/`next`/`back`, the `hydrated` SSR-safe flag, the `notice` reconciliation surface, the `confirmation` slice, and `onRehydrateStorage`) — the configurator carry-over + the fleet-card deep-link contract (`configureAndReserve`/`seedFromDeepLink`) are UNTOUCHED; the step indicator (`StepIndicator` — reachable/disabled markers, `aria-current`, track-line connector); back/next that PRESERVE state; `localStorage` persistence (key `apex:reservation-draft` v1 + 24h TTL + the date-range reconciliation → a non-blocking dismissible notice); deep-link pre-seeding (`/reserve?vehicle=<slug>&color=&wheels=`, validated against the catalog, unknown ignored); the responsive `SummaryRail` (sticky DESKTOP rail + sticky MOBILE bottom bar carrying the live total + the advance CTA); GSAP step-enter transitions (`use-step-transition.ts` — direction-aware slide+fade, reduced-motion = no travel, NO Motion per ADR-002). **5.5 steps 1-3:** vehicle pick (carried config reflected on the card, change-vehicle clears the now-irrelevant range/config); a hand-rolled keyboard-operable date-range picker (`date-range-picker.tsx` — `role=grid` roving-tabindex, Arrow/Home/End/PageUp-Down nav, Enter/Space half-open `[from,to)` selection, booked days struck-out + `aria-disabled` + announced, an `aria-live` status with SPECIFIC reasons) wired to `getDisabledRanges`/`getRangeAvailability` via a NEW TanStack Query mock layer (`lib/queries/reservation-queries.ts`, the ADR-003 keys, constant artificial latency, no `fetch`); pickup/return radio groups (different return → the flat one-way fee, surfaced in the rail); extras (multi-select checkboxes) + insurance (single-choice tier radio, incl. a "no cover" option) updating the LIVE price. **5.6 steps 4-5:** the driver form (RHF + `zodResolver` over the SHARED `driverSchema`, `aria-describedby`/`aria-invalid`/`role=alert` inline validation, licence FORMAT-only); the mocked submit (`app/reserve/actions.ts` `reserveVehicle` server action re-validating the SHARED strict `reservationSubmitSchema`, re-checking availability, recomputing `priceQuote`, returning a deterministic `ConfirmedReservation` with an `APX-XXXX-XXXX` reference from `lib/booking-reference.ts`); the hand-rolled multi-day all-day `.ics` (`lib/ics.ts` — `DTSTART/DTEND;VALUE=DATE`, exclusive DTEND on the half-open range, downloaded via a `blob:` URL); a GSAP confirmation flourish (reduced-motion safe) + honest demo copy; the persisted draft cleared on confirm. **Live price:** the `SummaryRail` recomputes the pure `priceQuote` from the store on every render (reusing `lib/format.ts`), with the TOTAL in an `aria-live` region. **Verification (ACTUAL):** `typecheck` clean · `lint` 0/0 · `build` passes (`/reserve` STATIC, **25.7 kB / 177 kB** First Load; home **157 kB**; three.js code-split OUT of BOTH `/page` AND `/reserve` — 0 three/fiber/drei chunks in either manifest, so the wizard adds NO R3F) · **38/38** Vitest (was 13; added 25: `ics` 6, `booking-reference` 4, `wizard-machine` 15) · `verify-reserve.mjs` **PASS — 0 CSP violations** across the WHOLE happy path INCLUDING the server-action submit (same-origin POST) + the `.ics` blob download, 0 console errors; deep-link lands pre-seeded on the dates step with config reflected; keyboard date picker selected a 3-day range via arrows+Enter; live total updated €946→€1,051 on extras+insurance; submit returned a deterministic confirmation (APX-JZJU-RS9Q); `.ics` download fired (`apex-apx-jzju-rs9q.ics`); range PERSISTED across reload. CSP string UNCHANGED (no edit this pass). NO new dependencies (RHF/resolvers/zod/zustand/TanStack Query already present); root lockfile change is the SAME additive-only apex registration (0 deletions — no churn to tape's resolutions); wrote ONLY under `projects/apex/`; stayed on `main`; server stopped PORT-SCOPED (3090). Screenshots in `web/scripts/.screenshots/reserve-*` (step1-vehicle-mobile, step2-dates light/dark/mobile, step3-extras, step4-driver, step5-confirmation). **Next = `designer-critic` review of the Phase-5 sections + the wizard (vs Linear micro-interactions for the wizard; Olivier Larose+Stripe for hero/gallery; Linear+Vercel+Klim for chrome/type), then `reviewer`, then Phase 7 tests.** PRIOR state below.
- **State (Phase 5 sections):** **Phase 5 marketing sections built — Tasks 5.1, 5.2, 5.3 DONE** (frontend-engineer, 2026-06-05). The home page is now the full long-form spine: hero -> configurator -> **fleet** -> **how it works / why APEX** -> **gallery** -> **locations** -> **testimonials** -> footer. **5.1:** an editorial fleet list over the seeded `FLEET` (spec highlights + daily price formatted from minor units via a new pure `lib/format.ts`, alternating image/copy rows, the track-line `Reveal` scroll-reveal on entry, a "Configurable" badge on the flagship), each "Reserve this" DEEP-LINKING pre-seeded (`/reserve?vehicle=<slug>`; the flagship adds `&color=&wheels=` default config) — reusing the EXISTING reservation-store deep-link contract (`seedFromDeepLink`), NOT rebuilt; plus the restrained Linear/Vercel "How it works" (4 steps) + "Why APEX" (4 value props) value section. **5.2:** the gallery/brand-story — a GSAP scroll-reveal sequence (masked clip-path WIPE reveal + per-frame PARALLAX + copy slide) on desktop, a vertical snap-scroll lazy stack on mobile, all `next/image` AVIF (theme-aware light/dark crops, art-directed `sizes`), reduced-motion -> static. **5.3:** locations with the STATIC-MAP treatment (generated `/maps/*` accent-pin maps, theme-aware, NO live embed; maps click-through link) + `AutoRental` JSON-LD + the testimonial/trust set + the FILLED footer credits line (3D-model + imagery provenance). The `/maps/*` 404s are RESOLVED. **NEW assets** (generated, CC0, on-brand placeholders via `scripts/generate-section-assets.mjs` = `pnpm -F apex-web assets:sections`): 4 fleet renders + 6 gallery crops + 4 static maps, light + dark. **Verification:** `typecheck` clean · `lint` 0/0 · `build` passes (home First Load **154 kB**, +2 kB for five sections; three.js still code-split OUT of `/page` — 0 three/fiber/drei chunks in the `/page` manifest) · **13/13** Vitest · `verify-csp-home.mjs` **0 CSP violations** (1 console error = the expected `/reserve` 404; CSP UNCHANGED, no edit this pass) · full-page-scroll CSP scan **0 violations** (GSAP reveals + gallery parallax + masked wipe all clean) · `verify-tiers.mjs` PASS (sections add NO R3F). NO new dependencies; root lockfile NOT touched; wrote ONLY under `projects/apex/`; stayed on `main`. **Next = Tasks 5.4-5.6 the reservation wizard.** PRIOR state below.
- **State (PASS C):** **Task 6.2 PASS C applied — the re-review P0/P1 fix pass** (frontend-engineer, 2026-06-05). The designer-critic re-review's blocking set is cleared: **P0-NEW-1** (dark hero render + dark blur theme-wired in `hero-render.tsx`, SSR-light/correct-on-client, 0 hydration warnings — the dark hero is now a real night-studio frame), **P1-NEW-2** (the visible "Scroll to configure" label removed, sr-only kept), and **P1-NEW-3** (the three wheel finishes spread far apart in value+character AND the root cause fixed — a missing `invalidate()` under `frameloop="demand"` meant material swaps did not repaint and the offline matrix captured stale frames; aero/turbine were byte-identical, now all three matrix stills + swatch thumbs are distinct). **D-07 + D-16 fully closed.** Folded in **P2-NEW-5** (roofline headroom via rig camera target/fov) and re-captured genuine **D-10 dark-focus** evidence. Verification: `typecheck`/`lint` clean, `build` passes (home First Load **152 kB**, three.js code-split out of `/page`), **13/13** Vitest, **0 CSP violations** (served CSP UNCHANGED, no CSP edit this pass), tier guarantees hold, **0 hydration mismatch**. NO new deps, root lockfile untouched, wrote ONLY under `projects/apex/`, server stopped port-scoped (3090). **Next = Phase 5 sections + reservation wizard.** PRIOR state below.
- **State (PASS B):** **Task 6.2 PASS B applied — the remaining critique-6.1 polish defects** (frontend-engineer, 2026-06-04). PASS B closes **D-08, D-10, D-11, D-12, D-13, D-14, D-16, D-17, D-19** + the two PASS-A minor flags (the bright "shard" + the hero sub-copy overlap). NOT touching the PASS-A model/scene architecture except where a defect required it (scene studio cyclorama + camera re-fit, theme blur). Highlights: **D-10** swatches now have a DISTINCT keyboard `focus-visible` treatment (a thick foreground outline offset OUTSIDE the accent selected-ring) — focus / selected / hover are all distinguishable (verified live, keyboard-tabbed, screenshot `configurator-focus-state{,-dark}.png`). **D-11/D-17** the stage is now responsive (`aspect-[5/6]` mobile portrait → `4/3` sm → `3/2` lg), the camera pulled in (`RIG_CAMERA` 6.1/2.35/6.5) so the car commands the frame, and the mobile still uses `object-cover` (no letterbox bands; the car fills). **D-12** the heading model name stays whole (`text-balance` + a `whitespace-nowrap` span around "APEX Lumen SUV"). **D-13** the wordmark is a tighter Klim-grade specimen (a 3px voltaic "track" bar + `0.08em` engineered tracking), the nav is tightened (`tracking-snug`, medium weight), and the **track-line motif is DEPLOYED**: a baseline hairline track-line under the header (faint at rest, lit when scrolled) + the section-divider TrackLine re-worked so the hairline is ALWAYS present and only the lit accent segment sweeps on scroll (legible at rest). **D-14** the "Drag to orbit" pill is edge-anchored to the bottom-right corner with a stronger surface/icon and FADES OUT on first drag/touch. **D-16** a dark `HERO_BLUR_DATA_URL_DARK` token is picked by theme (no light-blur flash on the dark stage). **D-19** the scroll cue is now an animated track-line thread with a descending accent "charge" (reduced-motion: a static accent tick). **D-08** the hero→configurator transition is art-directed via the track-line motif (the hero scroll-charge continues into a vertical track-line connector at the section top, so the scroll reads as one continuous idea — NOT a second WebGL context, per the owner default). **Shard fix:** the bright "shard" was the studio floor-reflection + backdrop edge — replaced with a seamless studio cyclorama (a large back wall + a wider curved Backdrop, both beyond the orbit frustum) and a lower-mix reflection floor; the bright triangle is GONE (verified light+dark). The faint remaining sub-wheel fragment is a model-simplification leftover inside a kept mesh (re-running `optimize-model.mjs` with extra brake/suspension hard-drop tokens produced a byte-identical 24-mesh/56368-tri GLB — the shard is NOT a droppable leaf, so it's a documented minor artifact, not regressed). **Hero sub-copy overlap fix:** the brand block is top-anchored, the H1 reduced to the hero-display step (`text-5xl`), and the car render bottom-anchored to the lower ~76% — copy + CTAs now sit fully clear of the car. **Verification:** `typecheck` clean, `lint` clean (0/0), `build` passes (home First Load **152 kB**, unchanged; three.js still code-split OUT of `/page`), **13/13** Vitest, **configurator CSP 0 violations** (CSP UNCHANGED — no PASS-B CSP edit needed; `'wasm-unsafe-eval'` intact), tier guarantees hold (Tier-3 mobile 0 canvas, Tier-4 no-JS 0 canvas + composed floor). Screenshots re-captured into `web/scripts/.screenshots/`. **Next = designer-critic re-review of the configurator + hero milestone.** PRIOR state below.
- **State (PASS A):** **Task 6.2 PASS A applied — the configurator now drives a REAL optimized GLB** (frontend-engineer, 2026-06-04). The designer-critic verdict (architecture sound, art-direction fails) is addressed for the model/scene/render/theme cluster: **D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-09, D-15 CLOSED** + the model integration. The procedural toy is replaced by the owner-provided **Mercedes-Benz Maybach GLS 600** GLB, optimized in-repo to **1.82 MB / 56k tris / exterior-only / meshopt-compressed** (`web/public/models/apex-suv.glb`); body-paint (`gls_paint.021`) + wheel-finish (`gls_kaki.104`) material swaps wired to the unchanged accessible DOM radio controls; the rig re-fit to a flattering FRONT 3/4 with upper-hemisphere-only orbit (never the underside); a grounded studio (gradient backdrop + planar reflection); theme-keyed night-studio lighting on dark; ONE signature accent; a single camera owner at the seam. The Tier-3 matrix + the hero LCP are **RE-RENDERED from the SAME rig + GLB as the live scene** (`web/scripts/render-from-scene.mjs`), so Tier-1 and Tier-3 are literally the same car and the hero is a real studio frame. **CSP/WASM resolved:** the self-hosted meshopt decoder needs `'wasm-unsafe-eval'` (narrowly WASM; the `'unsafe-eval'` JS ban is intact) — added + re-verified **0 violations**. Naming reconciled: the configurable hero is now **"APEX Lumen SUV"**. `typecheck`/`lint` clean, `build` passes (home First Load **152 kB**, three.js still code-split OUT of `/page`), **13/13** Vitest green. **PASS B (D-08, D-10..D-14, D-16..D-20) is NOT done — deferred to the next pass.** Branded-model LICENSE FLAG is BLOCKING for public deploy (CREDITS.md). PRIOR state below.
- **State:** **v1 SHIPPED + DEPLOYED (2026-06-05) — live at https://apex-rentals.fly.dev.** All phases complete: full build, designer-critic + reviewer gates (all defects cleared), 151 Vitest + 22 Playwright green, perf pass (desktop Lighthouse ≥95; mobile residual is the documented 4×-CPU-throttle artifact, real-CPU = 100), the model swap (branded Maybach placeholder → whole fleet on royalty-clear CC0 Kenney Car Kit models, so **P0-1 resolved** and the CSP tightened to `script-src 'self' 'unsafe-inline'` — no `unsafe-eval`/`wasm-unsafe-eval`), committed (`1d68105` + `0dad17a`) and pushed to `origin/main`, and deployed to Fly (single Machine, region `fra`, warm floor, app `apex-rentals`). The deploy needed two fixes: running `flyctl deploy` from within `projects/apex` (the `--config`+`--dockerfile`+context-from-root combo tripped a config-verify bug), and inlining `tsconfig.base.json` so it is self-contained in the Docker build context. Historical detail follows. — Earlier: the configurator (the wow) went LIVE in Phase 4 pass 2 (2026-06-04), Tasks **4.3 + 4.4 done** (Phase 4 complete; note the model has since been swapped from the procedural/Maybach placeholders to CC0 Kenney): the live R3F + drei configurator — a PROCEDURAL EV model (no GLB, no decoder, no WASM → CSP unchanged), live colour + wheel swaps, damped/bounded orbit, `Lightformer` studio lighting + `ContactShadows`, capped `dpr` + `frameloop="demand"` + `AdaptiveDpr`/`PerformanceMonitor`; the accessible DOM radio-group swatch controls + the debounced `aria-live` text alternative; the "Reserve this configuration" CTA writing `{vehicleId, config}` into the new reservation Zustand store (ADR-003 carry-over) + the deep-link form; AND the four-tier degradation: `detectWebglTier()` gate + runtime demotion swapping live↔pre-baked stills (Tier-3 loads NO three.js — the mobile Lighthouse path), the reveal-when-ready pose-matched crossfade (Tier-1), the reduced-motion crossfade (Tier-2, auto-orbit off), the no-JS static frame (Tier-4), AND the dark-stage matrix re-key (`matrix-dark/`). `typecheck` clean, `lint` clean, `build` passes (home First Load JS 153 kB; three.js STILL code-split OUT — verified absent from the `/page` manifest), 13/13 Vitest green, **configurator CSP smoke 0 violations** (live canvas mounts/orbits/swaps under `script-src 'self'`, no `'unsafe-eval'`/`'wasm-unsafe-eval'`), tier guarantees verified (Tier-3 mobile = 0 canvas + stills swap; Tier-4 no-JS floor complete). Earlier states: Phase 4 pass 1 (chrome + scroll-hero 4.1/4.2); Phase 2+3 (tokens/type/schemas/mocks/domain); Phase 1 (scaffold 1.1–1.4); architect Phase-0 (ADR-002/003/004); planner (`PLAN.md`, ADR-001).
- **Category:** web-only creative / marketing showcase with a genuine 3D (WebGL) configurator. Portfolio **slot 5**.
- **Structural foundation:** the **razors-edge** scaffold + patterns (web-only Next 15, sovereign tokens, frozen clock, shared Zod schemas, four-tier degradation, GSAP/Next integration via `useGSAP` + `matchMedia`, Fly single-Machine standalone deploy). The one deviation: **R3F + drei** for the configurator (ADR-002).
- **Stack flavour:** web-only (ADR-001). 3D/animation: **R3F + drei (configurator) + GSAP + ScrollTrigger (scroll choreography)**; **Motion NOT included by default**.
- **Canonical theme:** **light** default (premium-modern/EV register; deliberate contrast with razors-edge dark-canonical), intentional dark "night drive / charging-bay" register also shipped.
- **Dev port:** **3090** (set in `apex-web` `dev`/`start` scripts). 3080 was suggested but is OCCUPIED by another running process (PID 21480 at scaffold time), so 3090 was chosen instead (4010 was the other free candidate). Does not collide with tape (3000-3002 / 3055-3061), razors-edge (3070), meld, or pulse.
- **Created:** 2026-06-04
- **Demo URL:** not yet built.
- **Concurrency note:** at planning time another Claude session was actively working on `projects/tape` on the same `main` branch. This planner pass wrote ONLY under `projects/apex/` and touched no shared/root files, no lockfile, no other project.

## Done

- [x] **Frontend-engineer MODEL-SWAP PASS B — the whole fleet + mocks reconciliation + final verification (2026-06-05):**
  - **Fleet GLBs:** `optimize-model.mjs` gained `buildFleetCar` → `public/models/fleet/{stratos,terra,vella,mira}.glb` (whole car, body texture dropped + per-car clearcoat paint, OWN wheels kept; 74-98 KB). Mapping LUMEN→`suv-luxury` · Stratos→`sedan-sports` · Terra→`suv` · Vella→`sedan` · Mira→`hatchback-sports` (distinct silhouettes; all CC0 Kenney → N-1 solved by construction). `.dockerignore` excludes `web/public/models/fleet` (render-only; cards are static AVIF).
  - **Fleet renders:** `lumen-model.tsx` gained offline overrides (`__APEX_BODY_URL`/`__APEX_PAINT`/`__APEX_OWN_WHEELS`); `render-from-scene.mjs` got a FLEET pass that drives the live scene per fleet body + paint + theme → `public/renders/<slug>/hero{,-dark}.avif`. All five cards now ONE studio family (verified by screenshot, light + dark + mobile + wizard step-1 grid). `generate-section-assets.mjs` repurposed to maps-only.
  - **Mocks reconciled** (`bake-mocks.mjs` → `mocks:bake`): names/segments/specs/copy match the silhouettes (Stratos = 4-door performance super-saloon 4 seats/2.8s/290 kph; Mira = compact hot-hatch 424 km/6.4s; Vella = executive saloon; Terra = 7-seat family SUV; Lumen battery 105). Default wheel → `whl-turbine`. Determinism preserved; `vehicle.ts` untouched.
  - **PASS-A caveats:** default wheel dark turbine (contrast on white) · wheel thumbs re-shot on graphite, tight crop (3 rims read apart) · glasshouse single-mesh limitation documented + left for v2. OG regenerated. CREDITS.md (whole-fleet CC0 table + footprint note + caveat log; P0-1 RESOLVED) + CHANGELOG.md (PASS A+B, CSP tightening, de-blocked deferred list) updated.
  - **Verification (ACTUAL):** typecheck clean · lint 0/0 · build passes (home 157 kB; three.js out of `/page`+`/reserve`) · 151/151 Vitest · 22/22 Playwright · CSP **0 violations** (NO `'wasm-unsafe-eval'`) · tiers/deferred-canvas/reserve/model all PASS. Runtime model footprint **~117 KB** (was 1.82 MB). LIGHTHOUSE: DESKTOP `/` 99/96/96/100, `/reserve` 99/96/96/69; MOBILE `/` 80/96/96/100, `/reserve` 86/96/96/69 (synthetic) — REAL-CPU mobile `/` = 100/LCP 757ms; deployed run authoritative. NO new deps; root lockfile untouched; apex-only; `main`; servers stopped PORT-SCOPED. **Next = optional designer-critic confirm + owner Fly deploy.**

- [x] **Frontend-engineer MODEL-SWAP PASS A — CC0 Kenney model + configurator core + renders (2026-06-05):**
  - **Asset prep:** copied the CC0 **Kenney "Car Kit" v3.1** `suv-luxury.glb` + `wheel-{default,dark,racing}.glb` through a rewritten `web/scripts/optimize-model.mjs` → `public/models/apex-suv.glb` (BODY only, textures dropped, **27 KB / 758 tris**) + 3 wheel GLBs (textures KEPT for tire/rim, **~29-31 KB each**). **~119 KB total, UNCOMPRESSED** (was 1.82 MB Maybach). The raw kit + zip live under `web/scripts/.model-src/` and are GITIGNORED (`web/.gitignore` `scripts/.model-src/`; `git check-ignore -v` confirms).
  - **Configurator rebuilt** (`lumen-model.tsx` / `configurator-scene.tsx` / `rig.ts`): body → a custom clearcoat `MeshPhysicalMaterial` (colour from the swatch — Kenney bakes paint as a UV atlas region, so we DON'T tint it; we own a clean material); wheels → a GENUINE GEOMETRY swap (chosen wheel GLB instanced ×4 at the captured `WHEEL_NODES`, right side yawed π, keeping the authored atlas). `useGLTF(url,false,false)` disables BOTH drei decoders. Rig re-tuned for the ~2.85 m model; studio lighting re-tuned for flat-shaded forms (crisp key, neutral env, low voltaic accent). Preserved: `frameloop="demand"`+`invalidate()`, capped dpr, AdaptiveDpr/PerformanceMonitor, `detectWebglTier()`, the four tiers, the deferred mount, the reveal-when-ready crossfade, the accessible DOM swatch controls + aria-live, reduced-motion, dispose-on-unmount.
  - **CSP TIGHTENED:** `next.config.ts` drops `'wasm-unsafe-eval'` from `script-src` (no decoder anymore) + adds `blob:` to `connect-src` (GLTFLoader's in-memory wheel-texture fetch). `verify-csp.mjs` retargeted to also do a WHEEL swap → **0 violations** with `script-src 'self' 'unsafe-inline'` (no `'unsafe-eval'`/`'wasm-unsafe-eval'`).
  - **ALL configurator stills regenerated** from the new live rig (`render-from-scene.mjs`): light+dark matrix (Tier-1=Tier-3 same car), hero crops (light + dark night-studio), wheel thumbs, gallery crops, both blur URIs (→ `hero-assets.ts`). **N-2** (gallery establishing shots use the neutral default wheel; accent wheel only on the wheel-detail frame) + **N-3** (confirmation render heading `scroll-mt-28` clears the sticky header) folded in. Old Maybach-derived stills overwritten by the same paths.
  - **CREDITS.md** rewritten: Kenney CC0 provenance, the BLOCKING branded flag + "clear before deploy" note REMOVED for the configurator (P0-1 resolved here; fleet = PASS B), the CSP/WASM section updated (`'wasm-unsafe-eval'` removed), the product-renders section corrected to point at `render-from-scene.mjs` (the SVG generator is retired).
  - **Verification (ACTUAL):** typecheck clean · lint 0/0 · build passes (home 157 kB; three.js split out of `/page` + `/reserve`) · 151/151 Vitest · 22/22 Playwright (desktop+mobile) · CSP 0 violations (canvas+paint+wheel) · tiers PASS · deferred-canvas PASS · reserve PASS. Screenshots re-captured into `web/scripts/.screenshots/` (configurator desktop light/dark + swap, mobile Tier-3, hero light/dark, gallery, confirmation) after the documented cache gotcha (stop server → clear `.next/cache/images` → restart → warm both themes). NO new deps; root lockfile untouched by this pass; apex-only; `main`; servers stopped PORT-SCOPED. **Next = MODEL-SWAP PASS B (fleet renders + mocks reconciliation + final Lighthouse/screenshots).**

- [x] **Doc-writer Phase 8 Tasks 8.1 + 8.2 — README + CHANGELOG (2026-06-05):**
  - **8.1 `projects/apex/README.md`** — pitch + the wow spine; Demo "coming soon" (deploy prepared, gated on the P0-1 model swap, no fake link); Screenshots embedded from the new `docs/screenshots/`; What it is; Stack (web-only stated + pinned versions); Run locally (`pnpm install` at root → `cd projects/apex && pnpm dev` :3090 + the `test:e2e`/`test:lh`/`test:lh:mobile` production surface); Architecture notes; Key decisions (ADR-001..005); Quality (151 Vitest + 22 Playwright + the honest desktop≥95 / mobile-synthetic-throttle / real-CPU=100 / deployed-authoritative Lighthouse posture + `/reserve` noindex); Credits and honesty (branded placeholder model — NOT advertised as licensed, public deploy blocked until the unbadged CC0 swap; generated-placeholder imagery; nothing really booked; swap-for-real path); v1-deferred; License → root LICENSE. Template = `razors-edge/README.md`, adapted, no content copied.
  - **Screenshots** copied into `projects/apex/docs/screenshots/` from `web/scripts/.screenshots/` and embedded (lead = `configurator-desktop-light-voltaic-forged.png`; + hero light/dark, configurator dark, configurator mobile Tier-3, hero mobile, reserve dates step, confirmation, gallery crop, and the swap-pair stills referenced in text). NO GIF (tooling not run; not blocking; README notes where one would sit). For `reviewer`/`designer-critic`: these are current-build stills — re-capture if the configurator/hero/wizard UI changes.
  - **8.2 `projects/apex/CHANGELOG.md`** — Keep a Changelog 1.1.0 + SemVer; one `[Unreleased]` section summarising the full initial build under `Added` (ADR-001..005; the configurator; the reservation flow; the four-tier degradation; the 151+22 test suite; the deploy prep) + a "Not shipped in v1 (deferred)" block. Mirrors razors-edge's style.
  - **Scope:** wrote ONLY under `projects/apex/` (README.md, CHANGELOG.md, docs/screenshots/\*, PROGRESS.md, AGENT_NOTES.md); did NOT touch root `README.md`/root `PROGRESS.md`/lockfile/any tape file/any other project; no server started; stayed on `main`; no branch/commit. **OWED to main thread: the apex row in the root `README.md` Projects table** (deferred per the hard constraint while the concurrent tape session holds `main`). **Next = main thread adds the root-README row; (owner) P0-1 model swap + Fly deploy.**

- [x] **Architect Phase 8 Task 8.3 — deploy infrastructure + ADR-005 + the P1-3 resolution (2026-06-05):**
  - **ADR-005 authored** (`DECISIONS.md`) — Fly.io single-Machine Next standalone, web-only, no secrets; `NEXT_PUBLIC_SITE_URL` baked at build; warm floor (`min_machines_running = 1`); region `fra`; VM shared-cpu-1x / **512 MB** (the image-optimization/sharp path, the razors-edge 256→512 OOM lesson — the server is light, the heavy three.js + 1.9 MB GLB run in the browser); HTTP health check on `/`. Mirrors razors-edge ADR-005 adapted to apex. Records the **P1-3 resolution** (standalone built inside the Linux Docker stage — the Windows EPERM never occurs there) and the **P0-1 public-deploy precondition** (branded-model swap is BLOCKING for a PUBLIC URL; infra is ready + a PRIVATE/STAGING build can run now).
  - **Deploy artifacts created** (apex-only, all four mirror razors-edge): `projects/apex/Dockerfile` (3-stage: deps → Linux standalone build → slim non-root `node:22` runtime; `apex-web`; `'wasm-unsafe-eval'` CSP intact at runtime; ships `web/public/` incl. the optimized 1.9 MB GLB + the AVIF renders/gallery/maps + OG; the raw 31 MB source GLB excluded), `projects/apex/fly.toml` (app `apex-rentals` — PLACEHOLDER name, internal port 3000, warm floor, force_https, `/` health check, `NEXT_PUBLIC_SITE_URL` build-arg wiring), `projects/apex/.dockerignore` (excludes node_modules/.next/tests/scripts/docs/raw GLB), `projects/apex/DEPLOY.md` (runbook with the P0-1 STOP block at the top, exact `flyctl deploy --remote-only` command, CSP verify, rollback).
  - **`next.config.ts` UNCHANGED — confirmed correct for the Docker context.** It already carries `output:'standalone'` + the one-hop `outputFileTracingRoot` (`fileURLToPath(new URL('../', import.meta.url))` → `projects/apex/`, Windows-path-safe). No edit needed; no rebuild-for-config-change needed.
  - **P1-3 resolution — PROVEN by static verification (Docker daemon NOT reachable in this env, so NO `docker build` was run — not claimed).** Docker CLI 29.3.1 is installed but the Docker Desktop Linux engine never became reachable (polled >7 min across attempts; the `desktop-linux` + `default` npipe endpoints both unreachable). Per the task fallback: (1) ran `pnpm -F apex-web build` — **exit 0** but with the `⚠ EPERM: operation not permitted, symlink … react/next → .next/standalone/web/node_modules` warnings = the exact P1-3 Windows-host symlink failure (the host standalone trace is incomplete/unshippable). (2) Verified the Dockerfile's COPY/CMD paths match the ACTUAL build output line-by-line: standalone root = `package.json` + `web/` only (NO top-level launcher); the real entry is `.next/standalone/web/server.js` which does `process.chdir(__dirname)` + resolves `.next/static` + `public` relative to its own dir; `node_modules` (runtime subset) is inside `.next/standalone/web/`. So `COPY .next/standalone → /app/web` + `COPY .next/static → /app/web/web/.next/static` + `COPY public → /app/web/web/public` + `CMD node /app/web/web/server.js` are all correct. The Linux build stage runs the SAME `pnpm build` where the symlink succeeds → complete standalone, no EPERM. (Fixed the Dockerfile comment that wrongly described a top-level launcher.)
  - **Fly app name:** `apex-rentals` (placeholder, owner-renamable — rename updates `app =` + both `NEXT_PUBLIC_SITE_URL` in lockstep). **Machine:** shared-cpu-1x / 512 MB / `fra`. **Warm floor:** `min_machines_running = 1` + `auto_stop_machines = 'stop'`.
  - **What remains before a PUBLIC deploy:** (1) **P0-1** — swap the branded Maybach GLB for a royalty-clear unbadged CC0 model, re-run `model:optimize` + `renders:scene`, re-verify CSP, update `CREDITS.md` (owner-gated). (2) **Owner executes the Fly deploy** (`flyctl apps create` + `flyctl deploy --remote-only ...` per DEPLOY.md; no Fly credentials here). The infra is ready; only these two owner-side items gate the public URL. The deployed Fly real-CPU run is then the authoritative mobile-perf measurement (ADR-002 §3).
  - **Verification (ACTUAL):** `pnpm -F apex-web build` exit 0 (EPERM symlink warnings = the documented Windows P1-3, resolved by the Linux Docker build); standalone paths cross-checked against the Dockerfile COPY/CMD; Docker build NOT run (daemon unreachable — explicitly not claimed). Wrote ONLY under `projects/apex/` (DECISIONS.md, Dockerfile, fly.toml, .dockerignore, DEPLOY.md, PROGRESS.md, AGENT_NOTES.md); NO tape/other-project/root files touched; NO lockfile change; stayed on `main`; no port-scoped process left running (the build is one-shot). **Next = Phase 8.1/8.2 docs (doc-writer: README + CHANGELOG restate deploy posture + the P0-1 swap path).**

- [x] **Frontend-engineer Phase 7 performance pass — Lighthouse gate + the latent store bug (2026-06-05):**
  - **Core fix — deferred WebGL canvas mount (`configurator-stage.tsx`).** The live `<Canvas>` (three.js chunks + the 1.48 MB GLB) NO LONGER mounts on the home initial load. The stage arms on `IntersectionObserver` (`rootMargin:'10%'`, observes the stage box which sits below the section heading/copy under a full-height hero, so it does NOT fire at scrollY=0) OR on user intent (`pointerenter`/`pointerdown`/`focusin`). `detectWebglTier()` still gates Tier-1-live vs Tier-3-stills AFTER arming. Reveal-when-ready crossfade + reduced-motion + Tier-3/Tier-4 + no-JS floors preserved. Stills are `loading="lazy"` (never an LCP candidate). New `scripts/verify-deferred-canvas.mjs` proves it by network+DOM: 0 `.glb` + 0 `<canvas>` at scrollY=0; both on approach.
  - **Hero is the LCP on both profiles (`hero-render.tsx`).** Both crops now `priority`+`fetchPriority="high"` with `media`-scoped `imageSizes` so each device preloads exactly its crop (the mobile crop was previously eager-but-unhinted). Dropped the always-on `will-change-transform` on the hero render box (GSAP sets it transiently). Verified the Lighthouse LCP element is the hero render on desktop + mobile; the mobile hero image downloads in <60ms.
  - **`/reserve` SEO gate (intentional noindex).** Both `lighthouserc.json` + `lighthouserc.mobile.json` switched to `assertMatrix`: `/` asserts SEO ≥95 (error); `/reserve` sets `categories:seo:"off"` with a documented rationale (the deliberate `robots:noindex` caps SEO at 69; a11y/bp stay hard ≥95 errors). Mobile perf is a documented `warn` (the deployed real-CPU run is authoritative).
  - **Latent store bug (`reservation-store.ts`).** `selectVehicle`/`setRange(undefined)`/`setInsuranceTier(undefined)` used `delete` on the returned partial — a no-op under zustand v5's shallow merge. Now explicitly assign `undefined`. `reservation-store.test.ts` updated to ASSERT the clear clears (was documenting the wrong behavior).
  - **BEFORE → AFTER Lighthouse (median of 5):** DESKTOP `/` perf **56 → 97** (LCP 2.5s→1.1s, TBT 2589ms→94ms); DESKTOP `/reserve` 99→99; MOBILE `/` perf **68 → ~62** synthetic / **100 at real-device CPU** (LCP 1000ms, TBT 35ms — the synthetic 4× CPU multiplier is the entire sub-95 cause: LCP render-delay on an already-loaded image); MOBILE `/reserve` 71→~69. a11y 96 / bp 96 ≥95 on all; SEO 100 on `/` both profiles. Both `test:lh` + `test:lh:mobile` EXIT 0.
  - **Verification (ACTUAL):** `typecheck` clean · `lint` 0/0 · **151/151** Vitest · **22/22** Playwright · `build` passes (home 157 kB; three.js out of `/page`) · `verify-deferred-canvas.mjs` PASS · served CSP UNCHANGED. NO new deps; root lockfile untouched; apex-only; `main`; server stopped PORT-SCOPED (3091). **Next = Phase 8 docs + deploy.**

- [x] **Test-engineer Phase 7 — the test suite, Tasks 7.1 / 7.2 / 7.3 (2026-06-05):**
  - **7.1 Vitest 44 → 151 green.** New: `src/lib/availability.edge.test.ts` (27), `src/lib/pricing.edge.test.ts` (19), `src/lib/ics.edge.test.ts` (10), `src/lib/booking-reference.edge.test.ts` (9), `src/lib/store/wizard-machine.edge.test.ts` (22), `src/lib/store/reservation-store.test.ts` (10 — the forced-stale reconciliation fixture + TTL + clamp + corrupt-payload), `src/lib/store/deep-link-seed.test.ts` (10). All deterministic on the frozen clock. Did NOT duplicate the existing 44.
  - **7.2 Playwright E2E 22/22 green** (`web/e2e/*.spec.ts` + `playwright.config.ts`, port 3091, `next build && next start`, desktop + mobile/Pixel-7 projects): `reservation-happy-path.spec.ts` (3), `date-picker-keyboard.spec.ts` (5 incl. A-06), `configurator.spec.ts` (5 — DOM swatch + Tier-3 still-swap + Tier-4 no-JS + carry-over, runs on both projects), `theme-and-motion.spec.ts` (4 — theme both ways + persist + reduced-motion hero). Ran green twice (no flake after stabilising the date-picker async-focus polling + the availability-query-latency wait + label-click swatch interaction).
  - **7.3 Lighthouse CI** (`lighthouserc.json` desktop, `lighthouserc.mobile.json` mobile, median of 5). FULL MATRIX in ## State. **Surfaced (not fixed): perf < 95 on `/` (desktop 56 — live GLB loads on Tier-1; mobile 68 — throttled LCP/bootup) and SEO 69 on `/reserve` (deliberate `noindex`); a11y 96 + bp 96 ≥95 on all, SEO 100 on `/`.** Diagnosis + the engineer/architect actions are in ## Next.
  - **Findings raised (see AGENT_NOTES Phase-7 entry):** (1) Lighthouse perf gate — desktop canvas + the 1.48 MB GLB; mobile Tier-3 gate is EMULATION-SENSITIVE (fires with explicit `screenEmulation` width 412, NOT with UA-only). (2) `selectVehicle`/`setRange(undefined)`/`setInsuranceTier(undefined)` do NOT clear the key under zustand v5's shallow `set` merge (`delete` on the returned partial is a no-op) — the intended clear is unmet at the store level (masked by the per-vehicle availability re-check); the fix is an explicit `set({ range: undefined })`. (3) mobile `/` LCP element is the LAZY configurator still, not the `priority` hero render.
  - **Verification (ACTUAL):** `typecheck` clean · `lint` "No ESLint warnings or errors" · **151/151** Vitest · **22/22** Playwright (×2 runs) · Lighthouse 5-run medians captured per page/category/profile. Surgical ADDITIVE install (`@playwright/test@1.60.0` + `@lhci/cli@^0.15.1` → apex-web devDeps); root lockfile +2435/−0 (no tape resolution churn; the `projects/tape/e2e` importer line is the concurrent tape session's own in-flight additive entry, materialised by the install — NOT a tape edit by this pass). `test:e2e`/`test:lh`/`test:lh:mobile` scripts wired. Wrote ONLY under `projects/apex/`; stayed on `main`; the 3091 server stopped PORT-SCOPED. **Next = Phase 8 docs + deploy.**

- [x] **Frontend-engineer Review-6.3 PASS 2 — the WIZARD interactions + CODE defects (2026-06-05):** the SECOND consolidated review-6.3 fix pass. **Closed A-06, A-07, A-08, A-09, A-15, A-16, A-17, A-20, A-21 (designer) + P1-1, P1-2, P1-4, P2-1, P2-2, P2-3, P2-4 (reviewer).** Did NOT do P0-1 (branded-model swap — owner-gated) or P1-3 (Linux/CI standalone build — architect Phase-8 deploy ADR); both remain the only known deploy-gated items.
  - **A-06 (P1, the make-or-break) — date-picker silent rejection FIXED.** `date-range-picker.tsx`: extracted a pure `validateCandidateRange()` returning a SPECIFIC reason (`conflict | too-short | too-long | after-window`, conflict checked first) instead of a boolean; `handleSelect` sets a `rejection` state on each failure branch and `rejectionMessage()` surfaces an explanatory line on the EXISTING `aria-live="polite"` status region ("Those dates overlap a day that is already booked — restarting from your new pick-up day.", etc.) — sighted + SR users now learn why a range didn't take. VERIFIED headless (booked-crossing pick surfaces the conflict reason, not the default).
  - **A-07 (P1) — two-month desktop + disabled-Continue hint.** TWO months side-by-side on `lg+` (each its own `role="grid"`; one roving tabindex spans both via `focusDay`), one month + forward chevron on mobile. The disabled desktop Continue carries an announced hint (`aria-describedby="wizard-continue-hint"` → "Pick your dates to continue.") in `reservation-wizard.tsx`.
  - **A-08 (P1) — confirmation wording.** "Total paid on collection" → "Estimated total (due on collection · demo)" so the number no longer fights "no card was charged".
  - **A-09 (P1) — step-1 heading focus.** Heading focus lifted out of `StepShell`/`StepConfirmation` into the wizard, gated on a step CHANGE only (a `didMountRef` skips initial mount); heading-appropriate accent-underline focus style, not a field outline box.
  - **A-15 (P2) — picker polish + contrast.** Hover/keyboard-focus PREVIEW band between the pending start + the hovered day; struck booked-day text lifted `text-fg-subtle/50`→`text-fg-subtle` (≥4.5:1 both themes); in-range accent-ink on accent-soft = 4.56:1 light / 9.24:1 dark.
  - **A-16 (P2) — insurance ladder.** Shared `InsuranceCard` gives ALL tiers (incl. "No extra cover / Included") ONE selection idiom (accent border + wash + top-right accent check, matching the add-ons) + a quiet "Recommended" pill on `plus`.
  - **A-17 (P2) — close the configurator spine.** Confirmation shows a small theme-aware studio render of the chosen colour × wheel (`getThemedRenderStill` + `ThemedImage`, reusing the matrix stills); non-configurable vehicles omit it.
  - **A-20 (P2) — summary-rail perf.** `useReservationStore((s)=>s)` → a `useShallow` selector of only the price-relevant fields (no re-render on driver keystrokes / `savedAt` bumps); `draftRentalDays`/`draftIsOneWay` widened to `Pick<>` slices.
  - **A-21 (P2) — reduced-motion evidence.** `reserve-step5-confirmation-rm.png` + `reserve-step2-dates-rm.png` via a `reducedMotion:'reduce'` context.
  - **P1-1 (P1) — OG image.** `scripts/generate-og-image.mjs` (`pnpm -F apex-web og:image`) → `public/og/opengraph.png` (1200×630 branded card from the flagship night-studio render); wired into `layout.tsx` `openGraph.images` + `twitter.images`. Same-origin (`img-src 'self'`), verified in served HTML + 200.
  - **P1-2 (P1) — CREDITS.md.** Rewrote the stale "CSP / WASM outcome" section to match the SHIPPED meshopt GLB + self-hosted WASM decoder (→ `'wasm-unsafe-eval'` IS required; actual served CSP transcribed) + a location header (lives at `projects/apex/CREDITS.md`, paths relative to `web/`).
  - **P1-4 (P1) — clamp ALL rehydrated steps.** `reservation-store.ts merge` now runs `clampStep(draft.step, …)` for every step (was confirmation-only).
  - **P2-1 (P2) — dispose cloned GPU resources.** `lumen-model.tsx` cleanup disposes per-instance cloned body/rim materials + clone-unique geometry on unmount (never the shared cached original).
  - **P2-2 (P2) — JSON-LD escaping.** `business-jsonld.tsx` `.replace(/</g,'\\u003c')`.
  - **P2-3 (P2) — `.ics` lone CR.** `ics.ts escapeText` normalises `\r\n | \r | \n` → `\n`.
  - **P2-4 (P2) — ARIA gridcell.** Each day button wrapped in a `role="gridcell"` div carrying `aria-selected`; roving tabindex + keyboard nav preserved.
  - **Date-picker focus retention (incidental):** a selection re-render dropped DOM focus to `<body>` (root cause: `onFocus → setPreviewDay` cascade). Fixed: preview now from `moveFocus`+`onPointerEnter` only, + a `useLayoutEffect` re-asserting focus to `focusDay` when it falls out of the grid (gated by `keyboardActiveRef`, released on grid blur). Keyboard select→arrow→select commits a multi-day range with focus intact.
  - **Tests:** added `date-range-picker.test.ts` (6, `validateCandidateRange` branch precedence) → **44/44** (was 38).
  - **Verification (ACTUAL):** `typecheck` clean · `lint` 0/0 · `build` passes (`/reserve` static **26.9 kB / 178 kB**; home **157 kB**; three.js code-split OUT of BOTH `/page` + `/reserve`; EPERM standalone-symlink = known Windows P1-3 gotcha) · **44/44** Vitest · `verify:reserve` **PASS — 0 CSP / 0 console errors** incl. server-action submit + `.ics` blob, **A-06 invalid-date proof PASSES**, A-17 render present, keyboard 3-day range, live total €946→€1,051, confirmation APX-JZJU-RS9Q, range persists · `verify-csp.mjs` (live configurator) **0 violations** (P2-1 dispose didn't break the canvas) · `verify-tiers.mjs` PASS · `verify-csp-home.mjs` **0 violations** (OG + JSON-LD clean). Served CSP UNCHANGED. **NO new deps**; root `pnpm-lock.yaml` shows ONLY the additive-only apex importer block (705 insertions, **0 deletions** — no tape churn). apex-only; `main`; server stopped PORT-SCOPED (3090). Screenshots `web/scripts/.screenshots/reserve-*`. **Next = `designer-critic` re-review of PASS-2 (wizard vs Linear micro-interactions), then Phase 7 `test-engineer` (full Vitest + Playwright incl. the forced-stale reconciliation fixture). Open before deploy: P0-1 (branded-model swap, owner-gated) + P1-3 (Linux standalone build, architect Phase-8).**

- [x] **Frontend-engineer Review-6.3 PASS 1 — the MARKETING-SECTIONS cluster (2026-06-05):** the FIRST consolidated review-6.3 fix pass. Closed **A-01, A-02, A-03, A-04, A-05, A-10, A-11, A-13, A-14, A-18, A-19** (the marketing-sections set). The wizard + code defects (A-06..A-09, A-15..A-17, A-20, A-21, the P-series) are left for PASS 2 — untouched.
  - **A-01 (P0) gallery — real GLB studio crops.** The gallery no longer ships abstract glyphs. `render-from-scene.mjs` now renders FOUR art-directed studio crops of the REAL flagship GLB from the SAME live rig (a new `setYaw()` rotates the model under the fixed rig camera; the gallery loop captures `hero-3q` front 3/4 / `wheel-detail` / `profile` / `rear-3q`, light+dark) → `/gallery/{hero-3q,wheel-detail,profile,rear-3q}{,-dark}.avif`. They read as genuine product photography of the configurable car. The masked-wipe + parallax mechanics are unchanged; the frame copy + heading were re-written to the studio register. Old glyph AVIFs (open-road/charging/coast/city-night/track-line) deleted.
  - **A-02 (P0) fleet — one studio family.** `generate-section-assets.mjs`'s `fleetSvg` rewritten to a 3/4-front STUDIO composition (seamless cyclorama sweep, key-light vignette, contact shadow, floor reflection, glasshouse, specular sheen) shared by all four non-flagship cars, distinguished by SEGMENT/PROPORTION (sedan/suv/coupe/compact) on a cohesive premium metallic palette — NO flat 2-tone, NO green underline, NO watermark. The flagship keeps the real GLB frame; all five read as one line-up.
  - **A-03 (P1)** the literal "PLACEHOLDER" watermark text removed from every generated render (the watermark `<text>` was in `fleetSvg`/the now-deleted gallery SVGs).
  - **A-11 (P1)** `FleetSection` restructured: the configurable flagship is a LARGER, distinct featured block (bigger GLB frame + "Configurable flagship" badge + richer copy + Configure/Reserve), the other four in a consistent 2-col card family below — the hierarchy now reads "this is the one you configure".
  - **A-10 (P1)** `--width-content` is now `clamp(1280px, 60vw, 1440px)` (rests at 1280 to ~2133px, grows to 1440 on ultra-wide) so the centred spine breathes at 2560 instead of being stranded left-of-centre; the hero render cap bumped to `min(92vw,1440px)`. Verified at 1440 + 2560.
  - **A-04 (P1)** the footer credits line no longer leaks build status — now "3D & imagery: APEX studio renders. Type set in Inter & Space Grotesk. Full provenance in CREDITS.md." The build/license-flag notes live ONLY in CREDITS.md.
  - **A-05 (P1)** testimonial roles re-baked — each of the 6 authors now has a DISTINCT role (assigned by index over an expanded pool, no random sampling → no more triplicate "Photographer on assignment"). Determinism preserved.
  - **A-13 (P2)** the why-APEX value props (and the gallery frame copy) softened from load-bearing EV claims (no fuel / instant torque / included charging / tail-pipe-free) to "premium" where the ICE flagship render visibly contradicts them.
  - **A-14 (P2)** the dark static maps re-keyed (lifted slate base `#161d27`, blocks stepped up, road `#566476`) so the street/landmass contrast reads as a map on dark instead of near-black-on-near-black.
  - **A-19 (P2)** the how-it-works/why-APEX section given subtle micro-interactions (a per-card top accent hairline that draws on hover; the value cards lift + the icon scales on hover) on top of the existing `Reveal` scroll stagger — reduced-motion safe.
  - **A-18 (P2)** the gallery parallax is now per-aspect (each frame carries a `parallax` drift range; the inner-image cover scale is computed to cover `2*range`) so the drift reads on every frame instead of barely registering at a fixed 8%.
  - **Asset constraint honoured:** only ONE GLB exists, so the two P0s are solved by leaning on the REAL model's studio renders (gallery + flagship) + a consistent procedural family (the four non-flagship). True photography / more models documented as a later swap in CREDITS.md.
  - **Verification (ACTUAL):** `typecheck` clean · `lint` "No ESLint warnings or errors" · `build` passes (home First Load **157 kB**, +3 kB over 154 for the richer fleet section; three.js absent from the `/page` manifest — sections add NO R3F; the EPERM standalone-symlink warnings are the known Windows gotcha) · **38/38** Vitest · `capture-sections.mjs` full-page-scroll CSP scan **PASS — 0 CSP violations** (GSAP reveals + gallery parallax + masked wipe clean under the production CSP; the served CSP was UNCHANGED — no edit this pass; the 3 console 400s in the FIRST run were stale-server-snapshot 404s on freshly-rendered gallery files, GONE after a server restart). NO new dependencies; wrote ONLY under `projects/apex/`; stayed on `main`; server stopped PORT-SCOPED (3090). Re-captured affected sections (fleet/gallery/how-it-works/locations/footer) desktop light+dark + mobile + a 2560px ultra-wide shot into `web/scripts/.screenshots/` (dark via `colorScheme:'dark'` on the Playwright context). **Next = PASS 2 (the wizard + code fixes).**

- [x] **Frontend-engineer Phase 5 reservation wizard — Tasks 5.4, 5.5, 5.6 (2026-06-05):** the `/reserve` route + the five-step wizard, the centerpiece interaction. Phase 5 is COMPLETE.
  - **5.4 — Shell + machine + persistence + deep-link + summary rail.** `app/reserve/page.tsx` (static server route, `robots:noindex`, metadata) mounts `components/reserve/reservation-wizard.tsx` (the client orchestrator; deep-link read in `<Suspense>`). The wizard MACHINE is pure: `lib/store/wizard-machine.ts` (`WIZARD_STEPS`, `stepIndex`/`next`/`previousStep`, the per-step completion guards, `canAdvance`, `canReachStep`, `clampStep`, `earliestIncompleteStep`, `draftRentalDays`/`draftIsOneWay`, `draftToSubmit`). The store was EXTENDED (`lib/store/reservation-store.ts`) — added `hydrated`, `notice`, `confirmation`, the per-step setters (`selectVehicle` clears a now-stale range/config, `setRange`, `setPickup/ReturnLocation`, `toggleExtra`, `setInsuranceTier`, `setDriver`), navigation (`goToStep`/`next`/`back` delegating to the machine), `dismissNotice`, `setConfirmation`, `setHydrated`, and `onRehydrateStorage` setting `hydrated` + the reconciliation `notice` (the merge now flags `range-unavailable`/`expired` instead of silently dropping). The configurator carry-over (`configureAndReserve`) + the fleet/configurator deep-link (`seedFromDeepLink`) are UNCHANGED. `StepIndicator`, the `SummaryRail` (desktop sticky rail + mobile sticky bottom bar, live `priceQuote`, `aria-live` total, reuses `lib/format.ts`), GSAP step transition (`use-step-transition.ts`, direction-aware, reduced-motion safe, NO Motion).
  - **5.5 — Steps 1-3.** `step-vehicle.tsx` (radiogroup over the TanStack-Query fleet, carried config shown, change-vehicle), `date-range-picker.tsx` (hand-rolled accessible `role=grid` keyboard widget — Arrow/Home/End/PageUp-Down roving focus, Enter/Space half-open selection, booked days struck-out+disabled+announced, `aria-live` status with specific reasons; reads `useDisabledRanges`), `step-dates-locations.tsx` (the picker + pickup/return radio groups, the availability verdict from `useRangeAvailability`, the one-way notice), `step-extras.tsx` (add-on checkboxes + insurance tier radios incl. "no cover", live price). The TanStack Query mock layer is `lib/queries/reservation-queries.ts` (the ADR-003 keys, constant latency, no `fetch`).
  - **5.6 — Steps 4-5.** `step-driver.tsx` (RHF + `zodResolver` over the shared `driverSchema`, accessible inline errors, format-only licence, a stable form id so the mobile bar can `requestSubmit`), `step-confirmation.tsx` (the deterministic confirmation + `APX-XXXX-XXXX` reference, the `.ics` "Add to calendar", GSAP success flourish, honest demo copy). The submit surface is `app/reserve/actions.ts` (`reserveVehicle` — `'use server'`, re-validates `reservationSubmitSchema`, re-checks availability, recomputes `priceQuote`, deterministic `ConfirmedReservation`). `lib/booking-reference.ts` (FNV-1a → `APX-XXXX-XXXX`, unambiguous alphabet). `lib/ics.ts` (hand-rolled multi-day all-day VEVENT, exclusive `DTEND;VALUE=DATE`, `blob:` download). Draft cleared on confirm.
  - **Tests added:** `lib/ics.test.ts` (6), `lib/booking-reference.test.ts` (4), `lib/store/wizard-machine.test.ts` (15) → 38/38 total. (Phase 7 owns the forced-stale reconciliation fixture + the exhaustive E2E.)
  - **Verification (ACTUAL):** `typecheck` clean · `lint` 0/0 · `build` passes (`/reserve` static 25.7 kB / 177 kB; home 157 kB; three.js code-split out of both `/page` and `/reserve`) · 38/38 Vitest · `pnpm -F apex-web verify:reserve` **PASS, 0 CSP violations** across the full happy path incl. the server-action submit + the `.ics` blob download, 0 console errors; deep-link seeds, keyboard date picker works, live price updates, deterministic confirmation + reference, `.ics` downloads, range persists across reload. NO new deps; lockfile additive-only (no tape churn); apex-only; `main`; server stopped port-scoped (3090). Screenshots `web/scripts/.screenshots/reserve-*`.

- [x] **Frontend-engineer Task 6.2 PASS C — re-review P0/P1 fix pass (2026-06-05):** the designer-critic re-review verdict (milestone acceptable for Phase 5 EXCEPT one P0 regression + two P1s) is cleared. **P0-NEW-1 CLOSED** (dark hero render + dark blur now theme-wired), **P1-NEW-2 CLOSED** (scroll text label removed, sr-only only), **P1-NEW-3 CLOSED** (three wheel finishes now visibly distinct — AND the root cause fixed: a missing `invalidate()` on swatch change under `frameloop="demand"`). **D-07 + D-16 FULLY CLOSED** (the residual half — the dark hero LCP — is now a real night-studio frame). Also folded in **P2-NEW-5** (roofline headroom) and the **D-10 dark-focus evidence re-capture**.
  - **P0-NEW-1 (dark hero):** `hero-render.tsx` is now a `'use client'` theme-aware component using the SAME `html.dark` MutationObserver pattern as `configurator-stage.tsx`. It switches `src` (`hero-{desktop,mobile}-dark.avif`) + `blurDataURL` (`HERO_BLUR_DATA_URL_DARK`) by theme. SSR default = LIGHT (light-canonical) so the no-JS/SSR floor renders a sensible default; corrected post-mount on the client. The desktop crop is still the LCP (`priority` + `fetchPriority="high"`), CLS-safe (same reserved box), and **NO hydration mismatch** (server + first client render both emit light; the swap is a post-mount state update — verified headless, 0 hydration warnings on both default and stored-dark loads). The dark hero now renders a genuine night-studio SUV frame (evidence `hero-desktop-dark.png`), not a bright studio render on a dark page.
  - **P1-NEW-2 (scroll label):** `hero-section.tsx` — the visible "Scroll to configure" `<span>` is removed; an `sr-only` "Scroll down to configure your car" instruction is kept for assistive tech. The animated track-line scroll-charge thread is unchanged. The low-contrast-over-the-car label on mobile/no-JS is gone (evidence `hero-desktop-light.png`, `hero-mobile.png`).
  - **P1-NEW-3 (wheel finishes):** `rig.ts` `WHEEL_FINISHES` spread far apart in value AND character — Aero `#eef2f6` (bright polished machined silver, roughness 0.08), Turbine `#23272d` (true dark graphite, roughness 0.46), Forged `#1f9e78` (clearly voltaic-tinted brushed green-cyan, roughness 0.3). **ROOT-CAUSE FIX:** `lumen-model.tsx` now calls `invalidate()` after every paint/finish material edit — under `frameloop="demand"` a material edit alone did NOT repaint, so swaps "did nothing" until an unrelated render fired AND the offline matrix captured stale frames (aero/turbine were byte-identical). With the fix the three matrix stills + three swatch thumbnails are all distinct (re-rendered via `render-from-scene.mjs`; thumb crop region re-tuned to the lower-CENTRE front wheel). Evidence `wheel-finishes-compare.png` (bright / dark / voltaic, unmistakable).
  - **P2-NEW-5 (roofline clip):** `rig.ts` `RIG_CAMERA` target raised `0.92 → 1.02` + fov `30 → 31` so the tall SUV roofline carries vertical headroom and never tucks under the sticky header at any scroll offset (one-rig change → applies to live canvas AND stills).
  - **D-10 dark-focus evidence:** `capture-focus-state.mjs` fixed — the earlier byte-identical dark capture was next-themes' storage listener reverting a directly-set `dark` class; now the theme is persisted to `localStorage` + reloaded before capture, with an explicit dark-class wait. `configurator-focus-state-dark.png` is a genuine dark capture (the voltaic swatch shows SELECTED accent ring + KEYBOARD-FOCUS foreground outline on the dark surface).
  - **Verification (actual):** `typecheck` clean · `lint` 0/0 · `build` passes (home First Load **152 kB**, unchanged; three.js absent from `/page` manifest — code-split holds) · **13/13** Vitest · `verify-csp.mjs` **0 CSP violations** (served CSP UNCHANGED — `'wasm-unsafe-eval'` intact, `'unsafe-eval'` banned; NO CSP change this pass; the 1 console error is the harmless `/reserve` 404, route lands Phase 5) · `verify-tiers.mjs` PASS (Tier-3 mobile 0 canvas + still swap; Tier-4 no-JS heading+still+reserve+0 canvas) · **0 hydration mismatch** warnings (theme-aware hero). Re-captured screenshots in `web/scripts/.screenshots/`: `hero-desktop-dark.png` (night-studio), `hero-desktop-light.png` + `hero-mobile.png` (label gone), `wheel-finishes-compare.png` + `wheels/{aero,turbine,forged}.avif` (three distinct), `configurator-focus-state-dark.png` (genuine dark focus). NO new dependencies; root lockfile NOT touched; wrote ONLY under `projects/apex/`; stayed on `main`; server stopped PORT-SCOPED (3090 only).

- [x] **Frontend-engineer Task 6.2 PASS B — remaining critique-6.1 polish defects (2026-06-04):**
  - **D-10 (a11y):** `configurator-controls.tsx` — both swatch groups dropped `focus-within:ring-ring` on the label and now drive the visible chip via `peer` on the (sr-only) radio: `peer-focus-visible:outline-[3px] outline-offset-[5px] outline-[var(--color-foreground)]` — a DISTINCT keyboard-focus outline OUTSIDE the accent selected-ring, so focus / selected / hover are all visually distinct. Verified live (keyboard tab), evidence `configurator-focus-state{,-dark}.png`.
  - **D-11 / D-17 (responsive):** `configurator-section.tsx` stage box now `aspect-[5/6] sm:aspect-[4/3] lg:aspect-[3/2]`; `rig.ts` `RIG_CAMERA` pulled in (`[6.1,2.35,6.5]`, target `[0,0.92,-0.1]`) so the car commands the frame; the Tier-3 still uses `object-cover object-center sm:object-contain` so the portrait mobile box fills (no letterbox bands), `object-contain` from `sm` up.
  - **D-12 (responsive):** the heading is `text-balance` with the model name wrapped in `whitespace-nowrap` so "APEX Lumen SUV" never orphans.
  - **D-13 (chrome/type + track-line):** `wordmark.tsx` is a tighter Klim-grade specimen (a 3px voltaic "track" bar + `0.08em` engineered tracking, the trailing letter pulled flush); `site-header.tsx` nav tightened (`tracking-snug`, `font-medium`) and the generic 1px border replaced by a **track-line baseline hairline** (`--gradient-track`, faint at rest → lit when scrolled); `track-line.tsx` re-worked so the hairline is ALWAYS present (legible at rest) and only the lit accent segment sweeps on scroll.
  - **D-14 (configurator):** the "Drag to orbit" pill in `configurator-stage.tsx` is edge-anchored to the bottom-right corner (stronger `bg-surface/85` + border + `Move3d` icon, reads in dark) and fades out on the first drag/touch (`onPointerDown` → `interacted`).
  - **D-16 (themes):** `hero-assets.ts` adds `HERO_BLUR_DATA_URL_DARK` (re-rendered by `render-from-scene.mjs`, now emits `hero-blur-dark.txt`); the stage picks the blur by theme — no light-blur flash on the dark stage.
  - **D-19 (hero):** the scroll cue is an animated track-line thread with a descending accent "charge" (`apex-scroll-charge` keyframe in `globals.css`); reduced-motion → a static accent tick (keyframe neutralised + `motion-reduce` sizing).
  - **D-08 (seam):** KEPT the S3 framing (no 2nd WebGL context, per owner default) but ART-DIRECTED the transition — the hero's descending scroll-charge continues into a vertical track-line connector at the configurator section top (`configurator-section.tsx`), so the scroll reads as one continuous "one car, brought closer" idea.
  - **Shard fix (PASS-A minor):** the bright "shard" was the studio floor reflection + Backdrop edge — `configurator-scene.tsx` now uses a seamless studio cyclorama (a large back-wall plane + a wider/further Backdrop, both beyond the orbit frustum) and a lower-mix reflection floor; the bright triangle is GONE (light + dark). The faint sub-wheel fragment is a model-simplification leftover inside a kept mesh — re-running `optimize-model.mjs` (with added brake/suspension hard-drop tokens) yields a byte-identical 24-mesh / 56368-tri GLB, so it is NOT a droppable leaf; documented as a minor artifact, the model is NOT regressed.
  - **Hero sub-copy overlap fix (PASS-A minor):** `hero-section.tsx` brand block top-anchored (`mt-[max(7rem,16vh)]`, `items-start`), H1 reduced to the hero-display step (`text-5xl`), `hero-render.tsx` car render bottom-anchored to the lower ~76% (`object-bottom`) — copy + CTAs sit fully clear of the car.
  - **Verification:** `typecheck`/`lint` clean, `build` passes (home First Load **152 kB**, unchanged; three.js code-split holds), **13/13** Vitest, **configurator CSP 0 violations** (CSP string UNCHANGED — no PASS-B CSP edit; `'wasm-unsafe-eval'` intact, canvas mounts + orbit + swatch swap all clean), **tier guarantees hold** (Tier-3 mobile 0 canvas + still swap; Tier-4 no-JS heading + still + reserve link + 0 canvas), reduced-motion + no-JS hero floors verified. New capture scripts: `capture-focus-state.mjs`, `capture-header.mjs`. NO new dependencies; root lockfile NOT touched in PASS B. Wrote ONLY under `projects/apex/`.
  - **OUT OF SCOPE (left for Phase 7 test-engineer, per the task):** D-18 (SR aria-live verify with a real screen reader), D-20 (full 320/1440/2560 breakpoint range + a reduced-motion configurator frame). The branded-model LICENSE FLAG remains BLOCKING for public deploy (CREDITS.md).

- [x] **Frontend-engineer Task 6.2 PASS A — real GLB + scene/render/theme cluster (2026-06-04):**
  - **Model (D-01):** owner-provided Maybach GLS 600 GLB optimized via `web/scripts/optimize-model.mjs` (gltf-transform + meshoptimizer) from 31 MB/474k tris -> **`web/public/models/apex-suv.glb` 1.82 MB / 56k tris / 24 meshes / 0 textures, meshopt-compressed**. Raw GLB NOT committed. New `lumen-model.tsx` loads it via drei `useGLTF` + maps BODY PAINT (`gls_paint.021`) to colour swatches and WHEEL FINISH (`gls_kaki.104`) to wheel swatches (single rim geometry -> finish swap; documented limitation). Inspect/verify helpers: `scripts/inspect-model.mjs`, `analyze-model.mjs`, `verify-model.mjs`. a11y radio-group + aria-live contract UNCHANGED.
  - **One rig, three outputs (D-02, D-04):** `web/scripts/render-from-scene.mjs` drives the live R3F scene headless and re-renders the light+dark colour×wheel matrix + hero crops + wheel thumbs + blur from the SAME rig + GLB -> Tier-1 and Tier-3 are the same car; the hero LCP is a real studio frame. Old SVG `generate-hero-renders.mjs` RETIRED (hard-exits).
  - **Scene/framing/theme:** `rig.ts` re-fit (FRONT 3/4, upper-hemisphere-only orbit — never the underside, D-03), `configurator-scene.tsx` adds a grounded studio (backdrop sweep + planar reflection, D-05), theme-keyed night-studio lighting/exposure on dark (D-06/D-07, `theme` threaded through `configurator-canvas.tsx` from the stage), ONE signature accent (D-09), and a SINGLE camera owner at the seam (`IntroCamera` drives until p===1, then enables `OrbitControls`; D-15).
  - **Naming reconciliation:** `bake-mocks.mjs` re-baked — configurable hero is "APEX Lumen SUV" (`tier: 'suv'`); EV copy softened to premium on that one vehicle; matrix-coverage refinement still passes.
  - **CSP/WASM (RESOLVED):** added `'wasm-unsafe-eval'` (narrowly WASM; `'unsafe-eval'` ban intact) for the self-hosted meshopt decoder; re-verified **0 CSP violations** with the live GLB under `next build && next start`.
  - **Verification:** `typecheck` clean, `lint` clean, `build` passes (home First Load **152 kB**; three.js chunks ABSENT from `/page` manifest — code-split holds), **13/13** Vitest, **configurator CSP smoke 0 violations**. Tooling devDeps added ADDITIVELY (`@gltf-transform/*`, `meshoptimizer`); lockfile +insertions/0 deletions; concurrent tape session unaffected.
  - **DEFERRED to PASS B (NOT done):** D-08 (seam morph), D-10 (focus vs selected ring), D-11/D-12/D-17 (mobile/responsive configurator), D-13 (chrome/type), D-14 ("Drag to orbit" placement), D-16 (dark blurDataURL), D-18 (SR aria-live verify), D-19 (scroll cue), D-20 (full-range screenshots).

- [x] Frontend-engineer Phase 4 pass 2 — the live configurator (the wow) + four-tier degradation (Tasks 4.3, 4.4) (2026-06-04):
  - **Task 4.3 — Live R3F configurator.** `web/src/components/configurator/`: `configurator-section.tsx` (SERVER component — heading/copy + the reserved `[data-configurator-stage]` box + the Tier-4 default-still floor + the `<noscript>` reserve link), `configurator-stage.tsx` (the client four-tier display gate + the reveal-when-ready crossfade + theme-aware still resolution via a `documentElement.class` MutationObserver), `configurator-canvas.tsx` (the `next/dynamic ssr:false` + Suspense code-split boundary — the ONLY place the scene is imported), `configurator-scene.tsx` (the R3F `<Canvas>` — capped `dpr`, `frameloop="demand"`, `AdaptiveDpr`/`AdaptiveEvents`/`PerformanceMonitor`-with-`onFallback`-demotion, `Lightformer`-based studio `Environment` (NO CDN HDRI), `ContactShadows`, damped/bounded `OrbitControls`, the `SeamBridge` (reads seam-progress + reports `setSeamReady`) + `IntroCamera` (eases the intro from seam-progress, one-directional GSAP→R3F read), `configurator-controls.tsx` (the accessible DOM radio-group colour + wheel swatches + the debounced `aria-live` text alternative + the "Reserve this configuration" carry-over CTA — identical DOM across all tiers). `lumen-model.tsx` is the PROCEDURAL EV (extruded fastback body, paint-matched fender shells, tinted glasshouse, voltaic rocker/light-bar accents, four wheels with SWAPPING rims). `web/src/lib/r3f/rig.ts` is the shared camera/lighting/paint rig ("one rig, three outputs"). Stores: `web/src/lib/store/reservation-store.ts` (the ADR-003 Zustand draft store — persist key `apex:reservation-draft` v1 + 24h TTL + the date-range reconciliation `merge`; `configureAndReserve`/`seedFromDeepLink`) + `configurator-store.ts` (the transient previewed `{colorId, wheelId}`).
  - **Task 4.4 — Four-tier degradation + seam + dark stage.** `detectWebglTier()` gates the live canvas (Tier-1/2 only); Tier-3 (mobile/no-WebGL/data-saver) + the runtime `PerformanceMonitor` demotion show the pre-baked AVIF stills and load NO three.js; the reveal-when-ready crossfade is gated on `seam.ready === 'ready'`; reduced motion → crossfade + no auto-orbit (seam-progress pinned to 1 by the hero); no-JS → the server-rendered Tier-4 floor. `generate-hero-renders.mjs` extended to render BOTH the light stage AND the **dark "night drive" stage re-key** (`matrix-dark/` + `hero-dark*.avif`) — resolves the Task-4.2 dark-stage-owed finding; `getThemedRenderStill()` picks the register.
  - **Wiring.** Home page now renders `<HeroSection/>` + `<ConfiguratorSection/>`. The Task-4.2 placeholder (`configurator-placeholder.tsx`) + the dead scaffold smokes (`components/scaffold/*`, `mocks/scaffold-ping.ts`, `lib/queries/scaffold-queries.ts`) removed (the R3F pattern was lifted into the real scene first).
  - **Verification.** `typecheck` clean, `lint` clean (0/0), `build` passes (home 36.5 kB / **153 kB** First Load; three.js chunks 326+370+144 kB + the drei/scene chunk 83 kB all ABSENT from the `/page` manifest — code-split holds), 13/13 Vitest green. **Configurator CSP smoke** (`scripts/verify-csp.mjs`, retargeted to the live canvas, headless chromium under `next build && next start`): served CSP unchanged (`script-src 'self' 'unsafe-inline'`, **no `'unsafe-eval'`/`'wasm-unsafe-eval'`**), **0 CSP violations** with the live three.js canvas mounted + orbited + a material swap. **Tier guarantees** (`scripts/verify-tiers.mjs`): Tier-3 mobile = 0 `<canvas>` + stills swap on swatch change; Tier-4 no-JS = heading + still + reserve link + 0 canvas. Screenshots in `scripts/.screenshots/` (configurator-desktop-light/-dark, the voltaic+forged live swap, mobile Tier-3 light + dark). No new runtime deps; lockfile untouched.

- [x] Frontend-engineer Phase 4 pass 1 — Chrome + scroll-hero (Tasks 4.1, 4.2) (2026-06-04):
  - **Task 4.1 — App chrome.** `web/src/components/chrome/`: `site-header.tsx` (fixed, transparent-over-hero → translucent-surface-after-hero via an IntersectionObserver sentinel, NOT a GSAP scrub; desktop anchor nav with `aria-current` active-section tracking; accessible mobile menu via **radix-ui `Dialog`** — focus trap + Escape + focus return + `VisuallyHidden` description; brand theme toggle; "Reserve" CTA → `/reserve`), `wordmark.tsx` (Space Grotesk lockup + voltaic accent dot — accent as a SOLID surface, never accent-text on light), `theme-toggle.tsx` (CSS crossfade), `brand-icons.tsx` (apex's OWN hand-rolled IG/FB/TikTok/X glyphs — lucide v1.16 dropped brand icons, § 14 do-not-share so NOT razors-edge's), `track-line.tsx` (the recurring `--gradient-track` motif divider — GSAP scaleX scroll-reveal + reduced-motion resting state + a `static` behaviour for the footer), `site-footer.tsx` (brand block + nav + locations/support/social columns + final CTA + legal/**credits-line slot** for the 3D-model/photography provenance). Header mounted in `layout.tsx` (appears/sticks after the hero per IA). Mobile drawer transitions are plain CSS `data-state` (no tailwindcss-animate dep) added to `globals.css`. `site-nav.ts` is the nav source of truth. Mobile-first from 320 px.
  - **Task 4.2 — Scroll-hero (ADR-004).** `web/src/components/hero/`: `hero-section.tsx` (the pinned client section — one reserved box: static render layer + the **reserved SEAM MOUNT slot** + the brand DOM floor; GSAP `matchMedia` Tier-1 scrubbed RESTRAINED intro that writes seam-progress via `setSeamProgress`, Tier-2 reduced-motion static composed frame, Tier-4 no-JS composed frame; a legibility scrim so the wordmark reads over the render; CLS-safe 100svh pin), `hero-render.tsx` (the static AVIF as the LCP — `next/image` `priority` desktop crop + eager mobile crop, art-directed `object-[center_78%]`, blur placeholder), `hero-assets.ts` (inlined blur data URI). The seam contract lives in `web/src/lib/r3f/seam.ts` (one-directional GSAP→R3F value passing + a `ready` signal — the writer is wired now; Task 4.4 wires the canvas reader). `web/src/components/configurator/configurator-placeholder.tsx` prepares the `#configurator` seam target (reserved stage + the Tier-4 default-config static still + "Reserve this configuration" deep-link) — mounts NO R3F (Task 4.3/4.4).
  - **Assets.** PLACEHOLDER on-brand studio renders generated by `web/scripts/generate-hero-renders.mjs` (`pnpm -F apex-web renders:placeholder`) → `public/renders/lumen-gt/` (hero crops + the 12-still colour×wheel matrix + wheel thumbs + blur). CC0, generated (no external imagery, no IP exposure), clearly marked. Provenance + swap-for-real path in **`CREDITS.md`** (new) + AGENT_NOTES.
  - **Verification:** `typecheck` clean, `lint` clean, `build` passes (home 31 kB / 146 kB First Load; three.js absent from the `/page` manifest — verified), 13/13 Vitest green. **CSP smoke** (`web/scripts/verify-csp-home.mjs`, headless chromium under `next build && next start`): **0 CSP violations**, hero static render is the LCP `<img>` (not a canvas), **0 `<canvas>`** on home (no R3F mounted), GSAP hero pin engages, mobile menu opens. **Reduced-motion** + **no-JS** floors verified (composed legible hero, H1 + render + Reserve link present, 0 canvas). Screenshots in `web/scripts/.screenshots/` (desktop light/dark, mobile, mobile-menu, reduced-motion, no-JS). No new runtime deps; lockfile untouched (radix-ui + sharp were already present).
- [x] Frontend-engineer Phase 2 + Phase 3 pass — Design system + domain foundation (2026-06-04):
  - **Task 2.1** — Sovereign premium-modern/EV tokens in `web/src/app/globals.css`, REPLACING the Phase-1 placeholders wholesale. Cool near-white light grounds (`#f4f6f8`/`#ffffff`), near-black cool ink (`#10151c`), ONE EV accent (voltaic green-cyan `#18c08a`, with a darkened `accent-ink #0a7a5e` for accent-coloured TEXT so it clears 4.5:1, and `accent-contrast` ink for ON the vivid fill), hairlines, the **track-line motif** tokens (`--color-track`, `--color-track-glow`, `--gradient-track`), the full type scale, spacing/radius/motion tokens. The **intentional dark "night drive / charging-bay" register** (a re-key, NOT an inversion: deep cool charcoal `#0a0e14`, surfaces LIFTED, accent re-keyed brighter to `#2ee6a6`, accent-tinted glow shadow instead of black drop). Contrast verified ≥ 4.5:1 in BOTH themes (figures in AGENT_NOTES Task 2.1). NO token value reused from tape/meld/razors-edge/pulse.
  - **Task 2.2** — Type system in `web/src/app/layout.tsx`: **Inter** (variable sans, OFL-1.1, `--font-apex-sans`, `axes:['opsz']`) for UI/body + **Space Grotesk** (variable display, OFL-1.1, `--font-apex-display`) for the APEX wordmark/heads, both self-hosted via `next/font/google` (no runtime Google request, CSP-clean). Variables bound on `<html>`; type scale + `font-variation-settings` hooks (`.font-display`, `--weight-*`) in `globals.css`. `themeColor` updated to the new grounds.
  - **Task 3.1** — Eleven shared Zod schemas in `web/src/lib/schemas/` (`common`, `vehicle`, `configurator-option`, `location`, `extra`, `vehicle-booking`, `availability`, `price-quote`, `driver`, `testimonial`, `shop`, `reservation-draft`) + a barrel. Modelled to ADR-003: `Vehicle.configurable`, `ConfiguratorOption.renderMatrix` with a superRefine that guarantees every colour×wheel still exists, `Extra` as a discriminated union (insurance rows carry `tier`), `ReservationDraft` with the SEPARATE `insuranceTier` field + `extras[]` array + `config` + `savedAt`, the strict `reservationSubmitSchema` projection, and `ConfirmedReservation`. zod-config imported side-effect-first in every schema file + the barrel (CSP).
  - **Task 3.2** — Seeded mock data. faker baked to a STATIC `web/src/mocks/seed-data.ts` by `web/scripts/bake-mocks.mjs` (run via `pnpm -F apex-web mocks:bake`) — faker runs at AUTHORING time only, never on a CSP-governed runtime path (ADR-002 §5). Hand-curated fleet (5 EVs, one configurable LUMEN GT), configurator colours×wheels + the render-matrix mapping, 4 locations, extras + 3 insurance tiers, shop; faker-driven seeded per-vehicle bookings (15) + testimonials (6, assembled from curated English pools, no Latin lorem). `web/src/mocks/*.ts` accessors parse the seed through the schemas (validating the bake). Frozen clock in `web/src/lib/clock.ts` (now = 2026-06-15, 60-day window, min/max rental days, UTC date helpers, test-only override).
  - **Task 3.3** — Pure `getRangeAvailability` + `getDisabledRanges` in `web/src/lib/availability.ts` (half-open `[from,to)` overlap, window/length/past guards, returns conflicts + a specific reason; no React, frozen-clock only).
  - **Task 3.4** — Pure `priceQuote` in `web/src/lib/pricing.ts` (base + per-day/flat extras + per-day insurance tier + one-way fee − multi-day discount; `ONE_WAY_FEE_MINOR`, `MULTI_DAY_DISCOUNT_TIERS`; total clamped ≥ 0; no React).
  - **Verification:** `pnpm -F apex-web typecheck` clean, `lint` clean (0 warnings/errors), `build` passes (First Load JS 117 kB unchanged; the two Windows EPERM standalone-symlink warnings are the known Phase-1 gotcha, not a failure), sanity Vitest `availability.test.ts` (7) + `pricing.test.ts` (6) = 13 green. faker confirmed NOT imported on any `src/` runtime path. No new dependencies; lockfile untouched this pass.
- [x] Frontend-engineer Phase 1 pass — Scaffold (2026-06-04):
  - **Task 1.1** — Next 15 (15.5.18) + React 19 (19.2.6) + TS strict + Tailwind v4 + shadcn-ready scaffold under `projects/apex/`: umbrella `apex` package + workspace member `apex-web`, project `tsconfig.base.json` extending the repo-root base, `web/tsconfig.json` extending the project base; ESLint flat config (`next/core-web-vitals` + `next/typescript`) + Prettier/Tailwind class-order inherited from root; `next-themes` with **light as default** (`defaultTheme="light"`, class strategy, `enableSystem`); the full security-header set + strict CSP in `next.config.ts` (razors-edge's verified string + the WebGL additions `worker-src 'self' blob:` / `child-src 'self' blob:` / `img-src 'self' data: blob:`; the `'unsafe-eval'` ban holds); `output: 'standalone'` + `outputFileTracingRoot` pinned to the apex project root via the `fileURLToPath` Windows-safe form. Dev port **3090** (3080 was occupied).
  - **Task 1.2** — `Providers` (`app/providers.tsx`, single low `'use client'` boundary): TanStack Query (`QueryClientProvider`, mock-tuned defaults) + `next-themes`. zod (`z.config({ jitless: true })`) and react-hook-form/Zod are installed and CSP-safe; Zustand installed (store lands Phase 5). A trivial `useScaffoldPing()` query round-trips the in-memory stub mock source — verified `success` in the headless run.
  - **Task 1.3** — GSAP + ScrollTrigger via apex's OWN `src/lib/gsap/*` (`register.ts` lazy `loadGsap()` code-split, `use-gsap-effect.ts`, `refresh-coordinator.ts` — the inherited razors-edge pattern, not the files). The `GsapSmoke` ScrollTrigger proved registration + cleanup + `gsap.matchMedia()` reduced-motion branch + CSP-clean execution under `next build && next start` (state `entered`, 0 violations).
  - **Task 1.4** — R3F (`@react-three/fiber` 9.6.1) + drei (`@react-three/drei` 10.7.7) + three.js (0.180.0): `next/dynamic ssr:false` smoke Canvas (`webgl-smoke.tsx` → `webgl-smoke-scene.tsx`) with a trivial box + `OrbitControls`, capped `dpr={[1,2]}`, `frameloop="demand"`, inside Suspense, gated by `detectWebglTier()` (the real ADR-002 §4 capability heuristic — KEEPER code). **Verified code-split:** the three.js chunks (333+378+146 kB) are absent from the `/page` initial manifest; home First Load JS is 117 kB. **Verified CSP-clean:** chromium resolved `tier-1`, the live R3F canvas mounted + rendered, 0 CSP violations, 0 console/page errors. No `'wasm-unsafe-eval'` needed for the smoke (no draco/meshopt decoder yet — that is Phase 4; re-verify then).
- [x] Architect Phase-0 pass (2026-06-04): **ADR-002** (animation/3D posture ratified — R3F + GSAP two-library, **Motion excluded by default** with an ADR-only escape hatch; the hard inside-canvas/outside-canvas boundary; R3F-with-Next-15 integration — `next/dynamic ssr:false` Canvas + Suspense + drei `<Loader>` + low `'use client'` + `frameloop="demand"` + capped `dpr` + `AdaptiveDpr`/`PerformanceMonitor`; the WebGL performance contract + model budget; the concrete capability/tiering heuristic `detectWebglTier()`; the strict-CSP posture carrying razors-edge's verified string + the WebGL additions). **ADR-003** (reservation-mock architecture — Zustand wizard + configurator store, `localStorage` persist key `apex:reservation-draft` v1 + 24h TTL + the date-**range** reconciliation rule; insurance = a **separate single-choice field** (I-A); different pickup/return with a **flat one-way surcharge** (R-A); the pure `getRangeAvailability` + `getDisabledRanges` + `priceQuote` over a frozen `now`; TanStack Query keys; the `reserveVehicle` server action + shared Zod + the multi-day `.ics`). **ADR-004** (hero→configurator seam — static AVIF render is the LCP, **reveal-when-ready pose-matched crossfade** into the live canvas, **one-directional GSAP→R3F value coupling**; the pre-baked render matrix from the **shared camera/lighting rig**; the accessible DOM radio-group swatch controls + `aria-live` text alternative; the **four-tier degradation** with reduced-motion resolved as user-driven-interactive, auto-motion-off).
- [x] Planner pass (2026-06-04): `PLAN.md` (pitch, audience, the reconciled wow spine — configurator centrepiece + scroll-hero lead-in + gallery connective tissue, four-tier WebGL degradation, web-only justification, R3F+GSAP/no-Motion animation pick, the WebGL performance strategy, sovereign light-canonical design direction, full section/flow spec, reservation-flow spec, mock-data shape, success criteria, phased task list Phases 0–8, out-of-scope), `DECISIONS.md` ADR-001 (web-only + R3F+GSAP + sovereign light-canonical tokens + reservation-mock + the portfolio-composition flag), `PROGRESS.md`, `AGENT_NOTES.md`.

## In progress

- Nothing in flight. **Phase 7 (test-engineer) — Tasks 7.1, 7.2, 7.3 COMPLETE.** Vitest 44 → 151 green, Playwright 22/22 green (deterministic, frozen clock), Lighthouse matrix captured (desktop + mobile × `/` + `/reserve`). Phase 5 + Task 6.2 PASS A/B/C + Review-6.3 PASS 1/2 remain green. **Next = Phase 8 (docs + deploy).**

## Next

**Phase 8 — docs + deploy.** 8.1 `README.md` (pitch, stack, run, demo URL, configurator + reservation GIFs, key decisions, 3D-model/photography provenance, swap-for-real path) · 8.2 `CHANGELOG.md` · 8.3 deploy infra (architect ADR-005-equivalent — Fly single-Machine Next standalone; the P1-3 Linux/CI standalone build that the Windows EPERM symlink gotcha blocks locally must be built in the Linux Docker/CI context).

**BLOCKERS for Phase 8 / public deploy (carried, NOT introduced by Phase 7):**

- **Lighthouse Performance < 95 (the gate the task surfaced — for the frontend-engineer/architect, NOT a test bug).** Desktop `/` perf **56** and mobile `/` perf **68** / `/reserve` **71** are below the ≥95 success criterion. Diagnosis: (a) DESKTOP `/` — `detectWebglTier` correctly returns Tier-1 on the capable Lighthouse desktop profile, so the live R3F scene + the **1.48 MB meshopt GLB** load and run, driving TBT to ~2.6 s and LCP to ~2.5 s; total page weight 2.24 MB. This is by design for desktop (the canvas is the wow) but it means the **desktop hero route cannot hit perf ≥ 95 while the live scene loads on approach** — the engineer must decide whether to defer the canvas harder (load only on explicit user intent / further off the LCP) or accept a sub-95 desktop-perf with the canvas as the justified bundle (Stripe/Vercel "WebGL earns its weight" posture — but the success criterion says ≥95). (b) MOBILE — with the lighthouserc.mobile.json `screenEmulation` (412 px, mobile UA) the Tier-3 gate DOES fire (canvas NOT loaded, page weight 475 KiB, NO three.js chunk) yet perf is still 68/71 because under 4× CPU + slow-4G throttle the LCP is ~4.2-4.4 s and JS bootup ~2.2 s; the mobile `/` LCP element resolves to the LAZY configurator still (`loading="lazy"`), NOT the `priority` hero render — a real LCP-element finding for the engineer. **NOTE (gate is emulation-sensitive):** a Pixel-7-UA-only mobile run WITHOUT explicit `screenEmulation` width did NOT route to Tier-3 (canvas loaded, perf 45) — the `(pointer: coarse) AND innerWidth < 768` heuristic depends on the emulation setting both signals at first paint. The deployed Fly run must be re-measured (the ADR-002 §3 "measured, not asserted" + the standing PerformanceMonitor-threshold-tuning follow-up); on real mid-tier hardware the gate behaves as verify-tiers proves.
- a11y / best-practices / SEO ARE essentially at target: a11y **96**, best-practices **96** on BOTH routes/profiles (≥95 ✓). SEO is **100** on `/` but **69** on `/reserve` — because `/reserve` ships `robots: noindex` (deliberate; it is a focused sub-experience, not an indexable landing page), which Lighthouse's "page is blocked from indexing" SEO audit penalises. This is intentional, not a defect; if the ≥95-SEO bar must hold on `/reserve` literally, drop the noindex or exclude `/reserve` from the SEO assertion (a config choice for the engineer/architect).
- **P0-1** (branded-model swap — owner-gated) + **P1-3** (Linux standalone build — architect Phase-8 deploy ADR) remain the standing pre-deploy items.

**Closed by Phase 7:** the forced-stale date-range reconciliation fixture (the ADR-003 named gap — now unit-tested), the submit-projection edge cases, the deep-link valid/garbage seeding, D-20 (320/desktop responsive exercised via the mobile + desktop Playwright projects). **Still OPEN:** D-18 (SR aria-live verify with a REAL screen reader — Playwright asserts the aria-live regions + roles exist, but a human/AT pass is still owed).

## Handoff

- **Next subagent (implement):** `frontend-engineer` (Review-6.3 PASS 2 — the wizard A-06..A-09/A-15..A-17/A-20/A-21 + the FE code defects) and `backend-engineer` where a server defect applies. Read the **Review-6.3 PASS 1** entry in `AGENT_NOTES.md` (the new gallery/fleet asset approach + the `setYaw` harness + the stale-server-snapshot gotcha) before re-rendering anything. After PASS 2 → `designer-critic` re-review.
- **Designer-critic (after PASS 2) — Phase 6.1 — the Phase-5 sections + the reservation wizard.** Read this file + the **Phase 5 reservation wizard** entry in `AGENT_NOTES.md` (the wizard machine shape, the date-picker a11y model, the `.ics` approach, the live-price wiring), the **Phase 5 sections** entry, AND the **Task 6.2 PASS C** entry in `AGENT_NOTES.md` (the demand-frameloop `invalidate()` gotcha — IMPORTANT for any future scene-state change; the theme-aware hero pattern; the wheel-finish spread) + the **PASS B / PASS A** entries (focus treatment, responsive stage, track-line, cyclorama shard fix, hero recomposition; model provenance + optimization, the material/wheel mapping, the CSP/WASM `'wasm-unsafe-eval'` outcome, the single-rim limitation) AND the Phase-4 pass-2 entry (tier wiring, seam, store shape).
- **Model tooling (Task 6.2):** `pnpm -F apex-web model:optimize` (re-run after a GLB swap), `model:inspect <path>`, `model:verify`, `renders:scene <port>` (re-render the matrix+hero from the live rig — run against a running `next start`). The raw source GLB stays in the owner's Downloads (never committed). The optimized artifact is `web/public/models/apex-suv.glb`.
- **The reservation store (`web/src/lib/store/reservation-store.ts`) — now EXTENDED by Task 5.4** (the configurator carry-over + the fleet/configurator deep-link were preserved untouched): `useReservationStore` — Zustand + `persist` (key `apex:reservation-draft`, v1, 24h TTL via `savedAt`). Shape = the ADR-003 `ReservationDraft` + `hydrated`/`notice`/`confirmation`. Actions: `setDraft`, `configureAndReserve`, `seedFromDeepLink`, `reset` (preserved) + NEW `selectVehicle`/`setRange`/`setPickupLocation`/`setReturnLocation`/`toggleExtra`/`setInsuranceTier`/`setDriver`/`goToStep`/`next`/`back`/`dismissNotice`/`setConfirmation`/`setHydrated`. Navigation + guards DELEGATE to the pure `web/src/lib/store/wizard-machine.ts` (the single home for the rules — `canAdvance`, `canReachStep`, `clampStep`, `earliestIncompleteStep`, `draftToSubmit`, etc.; unit-tested). The `merge` now flags a `notice` (`range-unavailable`/`expired`) on reconciliation instead of silently dropping; `onRehydrateStorage` sets `hydrated` (the wizard renders an SSR-safe skeleton until then — no hydration mismatch). The transient previewed config is still in `configurator-store.ts`.
- **The wizard components** (`web/src/components/reserve/`): `reservation-wizard.tsx` (orchestrator — Suspense'd deep-link read, the panel `aria-live` region, GSAP step transition, the mobile sticky bar, the reconcile notice, the submit), `step-indicator.tsx`, `summary-rail.tsx` (live `priceQuote`, reuses `lib/format.ts`), `date-range-picker.tsx` (the hand-rolled accessible `role=grid` keyboard widget — READ THIS before changing date logic), `step-vehicle.tsx`/`step-dates-locations.tsx`/`step-extras.tsx`/`step-driver.tsx`/`step-confirmation.tsx`, `step-shell.tsx` (heading focus-on-mount), `use-step-transition.ts` (GSAP, no Motion). The TanStack Query mock layer is `web/src/lib/queries/reservation-queries.ts` (`src/lib/queries/` was previously empty). The submit is `web/src/app/reserve/actions.ts` (`reserveVehicle`). `web/src/lib/booking-reference.ts` (`APX-XXXX-XXXX`) + `web/src/lib/ics.ts` (multi-day all-day `.ics`) are pure + tested. Re-verify the wizard with `pnpm -F apex-web verify:reserve` (run against a running `next start`).
- **Domain-foundation API for Phase 5** (all under `web/src/`): `lib/schemas/` (barrel `@/lib/schemas`), `lib/clock.ts` (frozen `now`), `lib/availability.ts` (`getRangeAvailability`, `getDisabledRanges`), `lib/pricing.ts` (`priceQuote`, `ONE_WAY_FEE_MINOR`), `mocks/` (`FLEET`, `HERO_VEHICLE`, `CONFIGURATOR_OPTIONS`, `DEFAULT_CONFIG`, `getRenderStill`, `getThemedRenderStill`, `getBookingsForVehicle`, `LOCATIONS`, `EXTRAS`, `ADD_ONS`, `INSURANCE_TIERS`, `BOOKINGS`, `TESTIMONIALS`, `SHOP`). Re-bake mocks with `pnpm -F apex-web mocks:bake` if the seed/curated data changes.
- **NEW Phase-5 shared utilities the wizard should REUSE** (under `web/src/`): `lib/format.ts` (`formatCurrency`/`formatDailyPrice`, fixed `en-GB` locale — the wizard price summary MUST reuse this), `lib/use-theme-class.ts` (`useThemeClass()` — the `html.dark` observer, SSR-light default), `components/sections/reveal.tsx` (`Reveal` GSAP scroll-reveal), `components/sections/themed-image.tsx` (`ThemedImage` theme-aware AVIF). The marketing sections live in `components/sections/*` (`fleet-section`, `how-it-works-section`, `gallery-section`, `locations-section`, `testimonials-section`, `business-jsonld`).
- **NEW Phase-5 asset script:** `pnpm -F apex-web assets:sections` (`scripts/generate-section-assets.mjs`) generates the fleet renders + gallery crops + static maps (light + dark, CC0 placeholders). `scripts/capture-sections.mjs` (light + full-page CSP scan) + `scripts/capture-sections-dm.mjs` (dark + mobile; **dark capture requires `colorScheme:'dark'` on the Playwright context — next-themes `enableSystem` follows the emulated scheme on first paint; a stored `theme` alone is ignored. Lesson recorded.**).
- **Scaffold stubs status:** REMOVED — the scaffold smokes (`components/scaffold/*`, `mocks/scaffold-ping.ts`, `lib/queries/scaffold-queries.ts`) + the Task-4.2 `configurator-placeholder.tsx` are gone (the R3F pattern was lifted into `configurator-scene.tsx`). `src/lib/r3f/detect-webgl-tier.ts` is KEEPER (the ADR-002 §4 gate). `src/lib/queries/` is now empty (Phase 5 fills it with the TanStack Query mock layer).
- **Verification scripts:** `scripts/verify-csp.mjs` (asserts the live configurator canvas + 0 CSP violations under the production CSP incl. `'wasm-unsafe-eval'`; run against a running `next start`), `scripts/verify-tiers.mjs` (Tier-3-no-three.js + Tier-4-no-JS guarantees), `scripts/capture-configurator.mjs` (configurator screenshots), `scripts/capture-hero.mjs` (hero + reduced-motion + no-JS), `scripts/capture-focus-state.mjs` (NEW PASS-B — the D-10 swatch focus/selected/hover evidence), `scripts/capture-header.mjs` (NEW PASS-B — header rest/scrolled + wordmark crops for D-13; screenshots use `animations:'disabled'` because the scroll-charge keyframe otherwise stalls Playwright's stability wait), `scripts/render-from-scene.mjs` (re-renders the matrix+hero+BOTH theme blurs from the live rig+GLB — now emits `hero-blur.txt` AND `hero-blur-dark.txt`), `scripts/inspect-model.mjs`/`analyze-model.mjs`/`verify-model.mjs` (GLB graph + meshopt round-trip; `inspect-model` can read the RAW source but NOT the committed meshopt-compressed GLB without the decoder registered). `scripts/generate-hero-renders.mjs` is RETIRED.
- **PASS-B workflow gotcha (Windows):** the offline `render-from-scene.mjs` drives the LIVE running build, so after editing the scene you MUST rebuild (`pnpm build`) + restart (`pnpm start`) BEFORE re-rendering, or the stills reflect the stale build. Stop the server by PORT (`netstat -ano | grep ':3090' | grep LISTENING` → `taskkill //F //PID`), not a blanket `taskkill //IM node.exe` (that would also kill the concurrent tape session's node).
