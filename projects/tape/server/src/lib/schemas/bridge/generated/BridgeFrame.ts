// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { CellClose } from "./CellClose";
import type { CellDelta } from "./CellDelta";
import type { ControlCommand } from "./ControlCommand";
import type { SnapshotPayload } from "./SnapshotPayload";
import type { TickFrame } from "./TickFrame";
import type { WorkerReady } from "./WorkerReady";
import type { WorkerUnavailable } from "./WorkerUnavailable";

/**
 * Discriminated union of every frame that crosses the bridge in either
 * direction. Tagged on `kind` so a single `rmp_serde::from_slice::<BridgeFrame>`
 * decodes any frame the receiver might see; the consumer matches on the
 * variant and dispatches.
 *
 * The serde tag is `kind` (not `type`) to match the Elysia-side WS
 * envelope vocabulary (`{ topic, kind, payload }`) — one mental model
 * across the project per ADR-006.
 *
 * The variant names mirror the inbound / outbound payload types one-to-
 * one. `Tick` and `Control` carry the inbound payload; `WorkerReady`,
 * `WorkerUnavailable`, `CellDelta`, `CellClose`, and `Snapshot` carry
 * the outbound payloads.
 */
export type BridgeFrame = { "kind": "tick", "payload": TickFrame } | { "kind": "control", "payload": ControlCommand } | { "kind": "worker_ready", "payload": WorkerReady } | { "kind": "worker_unavailable", "payload": WorkerUnavailable } | { "kind": "cell.delta", "payload": CellDelta } | { "kind": "cell.close", "payload": CellClose } | { "kind": "snapshot", "payload": SnapshotPayload };
