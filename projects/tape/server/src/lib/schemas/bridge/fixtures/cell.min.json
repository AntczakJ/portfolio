{
  "_doc": "Bridge conformance fixture — minimal CellSnapshot (single trade in the cell). The companion .msgpack file is the byte-level oracle: both sides (Rust rmp-serde + Bun msgpackr with useRecords:false) MUST produce and accept these exact bytes. Hand-edit this JSON to change the logical value, then regenerate the .msgpack via `cargo run --bin gen-fixtures` from projects/tape/worker/. NEVER hand-edit the .msgpack file. See ADR-003 § Consequences and projects/tape/server/README.md `Bridge conformance fixtures`.",
  "schema": "CellSnapshot",
  "value": {
    "ts_ms": "1",
    "symbol": "BTC",
    "price_bucket": "1",
    "bid_volume": 0.5,
    "ask_volume": 0.5,
    "trades": 1
  },
  "bigintFields": ["ts_ms", "price_bucket"]
}
