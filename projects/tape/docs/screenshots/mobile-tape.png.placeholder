Placeholder — replace with the captured PNG at this exact path: `projects/tape/docs/screenshots/mobile-tape.png`. Capture from `pnpm -F tape-web dev` with DevTools device emulation at 360 px (iPhone 12 / Pixel 5) so the layout collapses to the tape-only fallback per PLAN.md success criterion. See ../CAPTURE.md for the exact steps. Delete this `.placeholder` file once the real PNG is committed.
